# Usernames live in the database; one-time New Creator rename done; app change #849 awaiting device review

_2026-09-13 01:28 UTC · branch `usernames-sql`_

**Unique usernames are live in the database, and the app side is open for device review.** Design `docs/usernames-design.md` (#841), decided in #844 (all nine recommendations, checked against industry standards).

## Done and verified — not taken from a receipt

- **`backend/usernames.sql` pasted by the owner 2026-09-13 01:07 UTC.** Every `makers` row has a `platform` and a `handle`, unique as a pair. Checked from outside: 417 rows, 355 pinned creators matching their `Platform @handle` names exactly, 34 studios on `atlas.*`, 28 accounts (12 named from their display name, 16 `user.xxxxxx`). Every display name was byte-identical to the day before. Anon is refused `handle_available` (401). **`check-catalog-contract.py` PASS**, so places, priceTier and isPrivate are all still served. ⚠️ That checker needs `SSL_CERT_FILE=/etc/ssl/cert.pem` on the local Mac, or it exits 2 (COULD NOT VERIFY), which is not a pass.
- **[#847](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/847) merged** (`7e826d0c`). All 389 `Tours.json` makers carry both keys, cross-checked by id against the live database with 0 mismatches. `make-link-pin.py` emits them, the seed writes them without ever blanking a value, and `check-catalog-keys.py` requires them.
- **One-time rename, on explicit owner instruction (2026-09-13):** *"for the users whose names are 'New Creator', change them to the '@username' for now. we'll only do this this one time."* Pasted 01:26 UTC, recorded in `backend/rename_new_creators_260913.sql`. Verified by PostgREST counts: 0 rows named "New Creator", 16 accounts named `@user.*`, 28 accounts in total. 🔴 **This is the only account rename on record and is not a precedent.** New nameless signups still arrive as "New Creator", deliberately.

## Open

- **[#849](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/849): app code, waiting on owner OK plus a device check.** It adds `@handle` under names, a username field with a live availability line in Edit Profile, handle search, and a one-time prompt before first publish. The profile upsert never sends the handle, and a test guards that. `test_sim`: 660 passed / 0 failed, then 11/11 `UsernameTests` after the rename tweak (no line repeating a name that already is the username). **No TestFlight build dispatched; the owner has not asked for one.**
- **Account self-delete: owner decided 2026-09-13, "yes, users can delete their own account."** The rule becomes: **we** never delete anyone, **a person** may delete **their own**. Not built. It needs a short design: `makers.user_id` is `on delete set null` and `tours.maker_id` is `on delete restrict`, so what happens to a deleting person's tours, maker earnings and handle has to be decided first.
- **Not built:** `make-link-pin.py` warning when a new pin's handle matches an existing Dozent account (design Decision 5).
