# #875 merged on owner device OK (build 159); private-friend branch deleted

_2026-09-13 21:42 UTC · branch `board-875-merged`_

The owner installed TestFlight **build 159** and confirmed it works: a private creator's followers and following screens now say the account is private instead of "No followers yet."

- **#875 merged** on the owner's device OK, with all CI green on `55329921`, the commit build 159 was made from.
- **`claude/private-friend-account-33psch` deleted.** Both halves are on `main` now: the SQL came in #873 and the app wording in #875. Before deleting, I checked that every line that branch added to the Swift files exists on `main`. The only thing it carried beyond that was a `backend/test-migrations.sh` fixture, which was left out on purpose: CI does not run that script, and the fixture conflicts with the `catalog_snapshot` fixture added since.
- `review-875-private-followers` cleared.

⚠️ A tip for the next session on this Mac: don't run `test_sim` locally. On 2026-09-13 one run crash-looped `NavigationDepthPairingTests` on the iOS 26.5 simulator, producing 40 "quit unexpectedly" popups for the owner. The same suite passes on GitHub CI. A fix is queued as a separate task.
