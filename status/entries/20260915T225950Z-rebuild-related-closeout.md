# Auto-rebuild of "More like this" merged (#929). publish-catalog.yml now regenerates suggestions on every content merge — but first checks scripts/related-text.digest, a sha256 of exactly what the model reads, and skips in under a second when no embedded text changed. Verified: the digest is byte-identical across #930's 170 changed coordinate lines, and the committed digest matches main's catalogue so the first run correctly skips. 39 generator self-tests green. 🔴 STILL UNEXERCISED: the commit-and-push path. It needs a real content merge on a real runner — WATCH THE NEXT CITY LAUNCH OR PIN BATCH for a 'chore(catalog): rebuild More like this suggestions [skip ci]' commit on main, and check the new entries have neighbours. If it is absent, the automation did not work.

_2026-09-15 22:59 UTC · branch `rebuild-related-closeout`_
