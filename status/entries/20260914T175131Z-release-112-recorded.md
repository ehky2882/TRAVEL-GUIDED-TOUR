# Dozent 1.1.2 (160) released on the App Store

_2026-09-14 17:51 UTC · branch `release-112-recorded`_

**Dozent 1.1.2 (build 160) is RELEASED.** Owner reported Apple approved it and they pressed Release.

Machine-verified via the App Store Connect API: version `1.1.2` state **READY_FOR_SALE**,
no phased release (it went to everyone at once, not over 7 days).

⚠️ At the moment of checking, the public keyless lookup
(`itunes.apple.com/lookup`) still returned **1.1.1 / 2026-09-01** — that feed lags ASC
by hours. Re-run it before quoting the public version; do not read the lag as a failed release.

What the public now has that 1.1.1 lacked: in-app account deletion (Guideline 5.1.1(v)),
usernames, the rebuilt filter row, the architect and subject tags already in the
catalogue, the breathing splash, the private-follower-list message, the pinned-post
load failure message, the bottom-module fix (#728), and Continue listening removed.

Clears `release-112-when-approved`. `MARKETING_VERSION` on `main` is still 1.1.2, so the
next build on that train will be refused (90186 `Invalid Pre-Release Train`) until it is bumped.

⚠️ **`status.py --clear-owner` only PRINTS `git rm`; it does not delete.** #880 ran it and committed with `submit-160-to-apple.md` still present, so that item stayed on the board after the submission. Both it and `release-112-when-approved` are removed here with a real `git rm`. After clearing, check `git status` shows `D`.
