# #949 MERGED (0838cb6e) - semantic search. Model live at release search-model-v1 (41.9MB, verified). Dispatched publish-catalog.yml on main to bootstrap the search index rather than waiting for the next content merge: the two-digest gate sees no published digest, so it treats the index as owed. Watch for search/embeddings.bin + search/embeddings.digest on gh-pages. Next: TestFlight build once the index is live.

_2026-09-16 17:48 UTC · branch `main`_
