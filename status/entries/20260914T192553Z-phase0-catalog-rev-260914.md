# Phase 0 opened as PR #904 (needs the Supabase paste first)

_2026-09-14 19:25 UTC · branch `phase0-catalog-rev-260914`_

`claude/phase0-catalog-rev-260914` → **PR #904**. Backend only; no app code, no
payload change, no release.

🔴 **Do not merge before the owner has pasted `backend/catalog_rev.sql`** into the
Supabase SQL Editor — `status/owner/apply-catalog-rev-sql.md`. Merging first is
survivable but leaves one gap: until the migration is applied, reordering a
walk's stops fails the content seed. The generated seed warns with a `raise
notice` rather than refusing, so content keeps flowing either way.

⚠️ **`backend/` is in NEITHER list in CLAUDE.md § Merging PRs** — not in
auto-merge, not in the owner-OK-required list. Treated as "when in doubt, ask",
which is also forced here by the paste step. Worth settling that boundary
explicitly at some point.

Proven against a throwaway Postgres carrying the real schema and the real
catalogue: an identical re-seed went from rewriting **3,745 tours / 424 makers /
303 places / 4,117 stops** to writing **zero rows**, while still detecting a
title change, a stop coordinate change, and a stop inserted mid-walk.
`backend/test-seed-idempotence.sh` was confirmed to FAIL with the guard removed.
