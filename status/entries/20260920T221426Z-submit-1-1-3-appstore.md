# 1.1.3 (176) submitted to Apple — WAITING_FOR_REVIEW, manual release, phased ON

_2026-09-20 22:14 UTC · branch `submit-1-1-3-appstore`_

Submitted through the App Store Connect API with the existing, owner-approved
TestFlight binary. **No new build was produced** — `fastlane release` and
`release.yml` both call `build_ipa`, which would have submitted an untested
binary instead of 176, so neither was used.

| | |
|---|---|
| Version | **1.1.3** (ASC id `62bfdb31-ac50-45a0-b03f-b1dcf8235a7a`) |
| Build | **176**, uploaded 2026-09-19, VALID, from `a0671e39` on `claude/map-clustering-screen-space` |
| State | `WAITING_FOR_REVIEW`, submitted **2026-09-20 22:13 UTC** |
| Release | **MANUAL** — it will not go live on approval; the owner presses the button |
| Phased release | **ON** (`INACTIVE` until release, then 7 days) |
| What's New | the 1,086 characters of `fastlane/metadata/en-US/release_notes.txt`, byte-identical (#1028) |

**Build 176 is current.** `git diff a0671e39 HEAD` over `*.swift`, `*.pbxproj`,
`Info.plist` and `Assets.xcassets` is empty — everything merged since is content,
scripts and docs, and content reaches phones over the air.

**Screenshots were not touched.** All 10 APP_IPHONE_67 images carried forward
from 1.1.2 on version creation, with **matching `sourceFileChecksum` on every
one** — the same files Apple approved and the store shows today. Nothing was
uploaded; `fastlane/screenshots` is empty and gitignored, and a lane that
"uploaded" them would have deleted the live set. Copies were pulled down
beforehand as insurance and were not needed.

**No in-app purchases were attached.** Eleven tier products sit in
`READY_TO_SUBMIT` and three in `APPROVED`; none is new or changed for 1.1.3, and
the App Review notes already carried forward say so in as many words — *"none is
attached to a published tour yet, so they are not part of this submission."*
1.1.2 shipped the same way.

Export compliance: build 176 reports `usesNonExemptEncryption: false`, from
`ITSAppUsesNonExemptEncryption=NO`. IDFA: `usesIdfa` null, as on 1.1.2.

**Owner still owes, after Apple approves:** press *Release this version*, then
bump `MARKETING_VERSION` 1.1.3 → 1.1.4 (a released train closes; the next
TestFlight upload is refused with 90186 until it moves).
