# #912 merged; catalog_since.sql owed as an owner paste

_2026-09-15 13:55 UTC · branch `board-912-merged`_

**#912 MERGED** (squash `c46a31b`) — `get_catalog_since(rev)` is on `main`.

🔴 **It is NOT live.** `backend/catalog_since.sql` must be pasted into the Supabase SQL Editor by
hand; the RPC returns **404** in production until then. Logged as
`status/owner/apply-catalog-since-sql.md` with the exact steps — CLAUDE.md Rule 10: *a merged PR
that still needs an owner paste is not finished.*

⚠️ **Do not run `check-catalog-contract.py` before the paste** — Rule 11 wants it *after* SQL
touching `get_catalog`, and it pulls the full **2.4 MB**, which is this project's actual problem.

**[#914](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/914) (the client half) stays open** —
app code, owner simulator/TestFlight review. All four checks green on head `114dbb1`.

⚠️ **What there is to actually review on #914, stated honestly:** *nothing visual.* It touches only
`Data/` — no views, no UI. And **the delta path is dormant until the SQL above is applied**, so
before that it can only behave exactly as today and any on-device test passes trivially. The useful
check is a smoke test **after** the paste: app launches, catalogue appears, content still refreshes.
The path-based review gate in CLAUDE.md § Merging PRs was written to catch *visible* regressions;
the real risk here is quieter — a broken loader showing a stale or empty catalogue — which CI's 24
tests cover in logic but not on a device against the real backend.
