# Branch audit: 12 safe to delete, 3 must be kept, one applied migration missing from the repo

_2026-09-13 19:31 UTC · branch `board-checkin-260913`_

Every branch checked against **GitHub's compare API** (server-side, full history),
because this checkout is a **shallow clone** and its own topology numbers are
worthless — see the correction in the check-in entry.

⚠️ **This session cannot delete branches.** `git push origin --delete` returns
**HTTP 403** from the proxy, then prints `Everything up-to-date` and **exits 0** —
a failed delete reads exactly like a successful one. Caught only because that is
the wrong thing for a delete to print. **Verify by re-listing, never by exit code.**

## ✅ Safe to delete — 12

**Nothing unique (0 ahead):** `amazing-rubin-pkub81` · `clever-wozniak-mmyxa6` ·
`tour-links-26dmsx` · `img-encoding-test` (0 ahead of **gh-pages**).

**Closed PRs, content preserved elsewhere:** `project-tracking-dashboard-1kggmu`
(#843 → #845) · `status-account-delete-decided` (#848) · `status-delete-decision`
(#850, wrong premise).

**Work landed by another route — verified on `main`:**
| branch | evidence |
|---|---|
| `stripe-questions-fjhdo3` | `docs/stripe-review.md` is on `main` |
| `web-landing-site-preserve` | `PurchaseService.swift` + `EntitlementStore.swift` on `main`; 66 paid tours live |
| `link-fullscreen-probe` | `LinkEmbedFullscreenTests.swift` on `main`, 56 fullscreen refs in `LinkEmbedView`; its unique commits are `TEMP-PROBE` |
| `dreamy-wozniak-tags-260612` | `tag-taxonomy.md` + `seed_tags.py` on `main`, plus a **v2** |
| `seed-hang-note` | edits a `STATUS.md` section that **no longer exists** |

## 🔴 Keep — 3

| branch | why |
|---|---|
| `amsterdam-handoff-preserve-hlhyp8` | the **only** copy of 30 staged Atlanta scripts |
| `transcript-square-floor` | **NOT on `main`** (0 matches for `transcriptMinHeight`). An unlanded fix answering the owner directly, 2026-08-21: *"for transcript box, you can also just make square to be safe."* |
| `private-friend-account-33psch` | 🔴 **`backend/private_friends_social.sql` is NOT in the repo, and the branch's own commit says it was APPLIED to the live database.** A migration running in production with no record on `main` — a rebuild would miss it. |

## ⚖️ Two the owner should rule on

`paris-scripts-260622` and `london-batch3-scripts-260616` stage draft scripts for
cities that **did ship** (Paris 50 tours, London 100, *After the Fire: Wren's City*
live with 6 stops). By design drafts never reach `main`, so these branches are the
**only copy of the source scripts**. Deleting loses provenance, not function.
