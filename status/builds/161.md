# Build 161 — Phase 2 delta client on #914 — first build that can use get_catalog_since

_2026-09-15 14:19 UTC_

**Dispatched 2026-09-15 14:18 UTC** from `claude/catalog-delta-client` @ `114dbb1`, the head of
[#914](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/914). Actions run
[34980743305](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/actions/runs/34980743305).
Build number = `github.run_number` = **161** — re-derived from the run list, not predicted.

**The first build that can actually use the delta path.** The server half
(`get_catalog_since`) went live earlier the same day, so every build before this one would have
fallen back to the full download regardless of what it contained.

🔴 **The test is ONE NUMBER, not a look at the UI.** #914 changes only `Data/` — no screen, no
visible behaviour. Settings → About → **Tours must read 3,901** (matching the live published row
count, verified from PostgREST at 14:1x UTC) and must **stay** 3,901 across four or five
force-quit-and-relaunch cycles.

**Why that is the real test:** the first launch has no cursor and does an ordinary full download.
Every launch after uses the merge path. A merge bug drifts the number — **climbing** if rows
duplicate (the id-case hazard: pin ids are UPPERCASE in `Tours.json`, lowercase from Postgres),
**falling** if rows are lost. A steady 3,901 means the merge reproduces exactly what a full
download would have.

Build notes were attached to the `notes` workflow input, so they auto-populate TestFlight's
"What to Test" field.

⚠️ **An earlier reading of 3,901 did NOT test this.** The owner checked before any build existed
from this branch — the newest TestFlight build was run 160, off `main`, from 2026-09-13. That
reading confirmed the catalogue is healthy and gave a clean baseline; it exercised the old path.
