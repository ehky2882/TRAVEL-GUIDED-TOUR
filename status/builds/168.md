# Build 168 — Spelling correction on queries (#973) — and a fixture bug that looked like a code bug

_2026-09-18 20:42 UTC_

_2026-09-17 01:26 UTC · run [35170542492](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/actions/runs/35170542492) · branch `claude/search-spelling-pxuxdh` @ `f2041b37` · success_

**⚠️ Recorded retroactively on 2026-09-18.**

**Spelling correction on search queries** (#973) — a typo now finds the thing anyway.

⚠️ **The build that shipped with a fixture bug, not a code bug.** One assertion failed and the fault
was the **test corpus**: *"Brutalist"* appeared in a single title, so it fell below
`minimumTargetCount` and was not an eligible correction target at all. The live catalogue carries it
many times over — which is why the Python run against real data had corrected `brutalst` correctly
all along.

**The fix added a test that guards the corpus itself:** every word the other assertions expect a typo
to land on must resolve back from a one-edit typo. Without it the next case added fails for a reason
that has nothing to do with the code, and the obvious response is to look in the wrong place.
