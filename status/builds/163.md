# Build 163 — 1.1.3 (163) from main — first build with both the delta merge and More like this

_2026-09-15 17:20 UTC_

Dispatched from **`main` @ `ec734bf`** — 1.1.3 (163). **The first build carrying BOTH app features**;
nothing has been on a device with the two together.

| | |
|---|---|
| [#914](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/914) | Phase 2 client-side delta merge — *was* in build 161 |
| [#915](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/915) | **"More like this"** — related tours on `TourDetailView` — **was NOT** |

Build notes attached to the workflow's `notes` input, so they reach TestFlight's "What to Test".

**What the owner can actually see:** only #915. #914 is **invisible by design** — same content,
fetched far more cheaply — so the only user-facing check for it is that the catalogue count stays
right (3,901) across cold launches, and specifically that it never goes **up**.

⚠️ **Re-derived, not predicted:** the run is **163**, not 162. The previous build was 161, so the
obvious guess was wrong — a CI run between them consumed the number. CLAUDE.md rule 10's
"read a build number back from the Actions run list" earns its place here.

⚠️ `MARKETING_VERSION` on the app target is 1.1.3 and the train is open, so no 90186 expected.
Note the `1.0` that a naive `grep -m1 MARKETING_VERSION project.pbxproj` returns is the **Tests**
target — match on `PRODUCT_BUNDLE_IDENTIFIER` before believing a version.
