# Usernames: decided, now needs building (one SQL paste, later)

_opened 2026-09-13 · clear with `git rm status/owner/usernames-build.md`_

✅ **DECIDED 2026-09-12 — all nine recommendations**, on your condition that they
match industry standards. Each was checked in `docs/usernames-design.md`
§ Industry check: seven match named precedent (Instagram, TikTok, X, YouTube,
GitHub); two are brand/process choices with no standard to match.

**Nothing is owed by you right now.** Next step is `backend/usernames.sql`, which
is **not yet written** — when it exists it becomes one paste, then a content PR,
then an app change. This item is here so the thread is not dropped, not because
it is waiting on you today.
