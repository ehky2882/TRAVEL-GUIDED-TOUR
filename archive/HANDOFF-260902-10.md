# HANDOFF 2026-09-02 (session 139) — nineteen Hong Kong + Macau link pins, a what3words in New York, and two pins beside a pin

Branch `claude/more-tours-a1xhw4`, cut clean off `main`, opened as
[#722](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/722) on owner instruction. Content
only — no Swift, no SQL, no build. **Sent in two waves: sixteen links, then five more.**

## What shipped

**Sixteen owner-sent links; 14 wired, 2 structurally unpinnable.**
`linkPins` **905 → 919** · `makers` **297 → 299** · `tours` and `places` **byte-identical**.
gh-pages `0786a135` (exactly 14 additions).

Two creators, both new maker rows, matching the owner's own headings:
**Instagram `@history_withjim`** (12) and **`@hongkongheritage`** (2).

## 🔴 An Instagram STORY HIGHLIGHT can never be a link pin

Two of the sixteen were `instagram.com/s/<token>?story_media_id=…`. **Three independent
blocks, all structural:**

1. **`LinkSource.embedURL` (`Models/Tour.swift`) matches only `p`, `reel`, `tv`** — a `/s/`
   path returns `nil`, so the app can build **no player**. Verified in the Swift itself, not
   just the tool's mirror. Same class as a TikTok `/photo/` carousel.
2. **No `display_url`** → no hero, and a pin with no hero cannot exist.
3. **No caption blob** → no subject, no description.

⚠️ **They are NOT blocked and NOT dead.** The page returns a healthy **476 kB** titled
*"Hong Kong 🇭🇰 • Highlights by @kieranbrowntravel"* with `accounts/login` count **0** — it is
simply not a shape the app embeds. **Do not re-try them on "the link opens on my phone."**
Their two Plus Codes (`75J3+FJ Central`, `76M6+MX Quarry Bay`) are **orphaned**; the fix is a
reel or permanent post from that creator, and the creator is a **third account** not otherwise
in this batch.

## ✅ what3words is now a usable coordinate source — and it must be anchor-gated

Every one of the twelve `@history_withjim` captions carries a `///word.word.word` address.
**`https://mapapi.what3words.com/api/convert-to-coordinates?words=X.Y.Z` returns 200 with no
API key.** It is **gated behind published anchors before any answer is trusted** — the
resolver raises unless `filled.count.soap` → 51.520847, -0.195521 and `index.home.raft` →
51.521251, -0.203586 to 1e-5. Same discipline as the Plus Code decoder.

🔴 **It disagreed with the owner's Plus Codes twice, in OPPOSITE directions — each source was
wrong exactly once:**

| Site | Plus Code | what3words | OSM | Winner |
|---|---|---|---|---|
| Race Course Fire Memorial | 5 m | 5 m | names it exactly | three-way |
| Pinewood Battery | 6 m | 70 m | names it exactly | three-way |
| Blue House | — | **1 m** | names it exactly | w3w |
| Cape Collinson **rock carving** | **830 m off** | **4 m** | `黑角頭石刻` | **w3w** |
| Cape Collinson **Battery** | **25 m** | **New York** | battery ruin | **Plus Code** |

⚠️ The Battery caption *also* carries an explicit DMS grid ref (`22°15′3.78″N, 114°15′13.91″E`)
which is **299 m off** — so that one caption contradicts itself twice and the owner's code won.

## Coordinates

**9 of 14 named exactly by OSM at their final point**: Kadoorie Farm, Pinewood Battery,
Race Course Fire Memorial, Osborn Memorial, Jardine's Lookout AOP, Blue House, Barker Road,
黑角頭石刻, Cape Collinson Battery Searchlight.

Documented shapes for the rest, all accepted:
- **The Kowloon Memorial Tower** reverse-geocodes to `九龍醫院 Kowloon Hospital, Ma Tau Wai` —
  the nearest addressed node, and **Ma Tau Wai is the district the owner's own Plus Code names**.
  OSM names no memorial tower there. **The batch's weakest coordinate, stated not hidden.**
- **Ruins of St Paul's** → `大三巴斜巷 Calçada de S. Paulo`, the street named for it.
- **Fort Monte** → `澳門博物館 Museu de Macau`, which is built into the fort (inner-POI case).
- **Blake Garden** → `西街 Sai Street, 太平山 Tai Ping Shan`, exactly the district its caption names.
- **HK Railway Museum** → `人和里 Yan Wo Lane, 大埔墟 Tai Po Market`, the right district.

⚠️ **`countrycodes=hk` returns nothing** (OSM files Hong Kong under `cn`) — every query was
bounded to a viewbox. ⚠️ **Nominatim 429'd repeatedly**; every errored request was **excluded
and produced no verdict**, then re-run with backoff. A request that never completed must not be
able to answer.

## Heroes

**All 14 opened and read against their captions — zero wrong subjects.** Several confirm
themselves in frame: the Antiquities Authority plague plaque at Blake Garden, the Grand Lisboa
behind Fort Monte, the carved grooves at Cape Collinson, the Blue House's cobalt balconies.

**Three hand re-crops** through a mirror of the tool's own pipeline (importing its `trim_bars`,
same blur/dim/quality, **same filenames**, so `Tours.json` is untouched) — the vertical `--focus`
gap, **fifteenth batch running**:
- **Pinewood Battery vfocus 0.22** — the only one where the subject's NAME was lost: its
  location card reads *"WWII Anti Air Battery"* and never says "Pinewood".
- **Jardine's Lookout** and **Ruins of St Paul's** at **0.58** — both free, recovering a clipped
  card line and bringing more of the harbour / the St Paul's facade into frame.

⚠️ **Weak hero, flagged not fixed: `The Four Columns at Kadoorie Farm`** — the frame is a
**historical B&W photograph of the demolished building the columns came from**, not the columns
at the farm where the pin sits. Correct topic, wrong place shown (the Charging Bull / foundry
shape). It is also the batch's lowest-resolution source at **510×900**.

## Sensitivity

The **Race Course Fire Memorial** caption states a death toll. The derived caption truncates at
140 chars, **well before the figure**, so **no mortality number appears in any title, caption or
description this catalogue authors**; the creator's words stay verbatim in `longDescription`
(the Eastland / Harbour Bridge convention). The **Osborn Memorial** copy likewise states the
Victoria Cross factually with no figure.

## Verification

- Validator mirror: vocabulary from **both** Swift files (**385 tags**), self-test **19/19**,
  then **0 errors, 0 warnings** across 1,552 tours + 919 pins + 107 places.
- 🔴 **18 faults injected against THIS BATCH'S OWN rows, 15/18 caught, control clean.**
  **The 3 misses are real blind spots in the mirror, not in the data**: *centroid disagreeing
  with stop 0*, *empty title*, *duplicate maker id*. The Swift validator in CI checks all three;
  all three were **verified directly instead** (0 centroid drift across the 14, 0 empty titles
  catalogue-wide, 299 maker ids all unique). **Worth closing in the mirror.**
  ⚠️ A first harness attempt reported 12 false misses — the mirror **draws its control from the
  live catalogue and refuses to run when that file is dirty**, so mutating it on disk makes the
  mirror exit 2 rather than report. Inject in memory via its own `check()`.
- `make-link-pin.py --selftest` **71/71** — ⚠️ **with Pillow installed first**; a fresh container
  has none and reports 62/62, which reads as a pass and is not one.
- `decode-plus-code.py --selftest` clean (3 published anchors, floor-encode, grid refusal,
  2,000 round-trips, 18 short-code recoveries).
- **0** duplicate tour/stop/maker ids, **0** already-pinned sourceURLs, **0** byte-duplicate
  heroes, **0** perceptual pairs nominated at Hamming ≤ 45.
- **0** filename collisions against **6,756** gh-pages `images/` paths, the listing **asserted
  to hold >1,000 first**. ⚠️ The bare-slug check was clean — the handle suffix was not
  load-bearing this batch.
- **File count asserted equal to pin count: 14 = 14**, no avatars (Instagram exposes none).
- `Tours.json` **byte-stable under a Python re-dump before editing**; diff **665 insertions /
  0 deletions**, asserted purely additive (`tours` and `places` byte-identical, existing makers
  and pins unchanged as prefixes).
- `seed_from_toursjson.py` clean at **299 / 2,471 / 2,843 / 107**; **0 `images//`** in the
  catalogue *or* the generated SQL.
- `check-place-candidates.py` **EXACT still none and exits 0 on BOTH sides** — the clean-exit
  state is preserved and **no coincident group was manufactured**. **NEAR 35 → 36**, the single
  addition being the two Railway Museum pins **0.46 m** apart. ⚠️ Exit code read **directly, not
  through a pipe**.
- gh-pages `0786a135`: remote head **re-read in the same command as the push**, tree diff
  **exactly 14 additions, 0 deletions, nothing outside `images/`**.

## ⚠️ Flagged for the owner

1. **A place candidate: the Hong Kong Railway Museum.** `@love.in.hongkong` already has a pin
   titled exactly `Hong Kong Railway Museum`; this batch's is **0.46 m** away because **both
   derive from OSM's own museum node** — convergence, not manufacturing. **Neither was nudged**,
   in either direction. To avoid two identically-titled pins on one point, this one ships as
   **`The Trains at the Hong Kong Railway Museum`**, which is what its caption and its
   locomotive hero are actually about (the Tribune Tower precedent). A place is the real fix and
   it is the owner's call.
2. **The two orphaned Plus Codes** from the unpinnable story links.
3. **The weak Kadoorie Farm hero** (above).
4. **10 of 14 will not play inline** — the documented licensed-music gate. ⚠️ The tool's reason
   strings are the known cosmetic mis-capture (`licensed music (7K followers)`); **the verdict
   comes from the absent `video_url`, never the track name.**

⚠️ **`main` moved mid-session** (#719, two places + an Atlas tour moved onto the right street,
places 105 → 107). The catalogue edit was applied to **`main`'s file** and every check re-run.


---

## FOLLOW-UP, same session — five more links, and two pins that land beside a pin of the same subject

**Owner: *"add a few more"* + five Instagram reels.** All five alive (256–261 kB, `contextJSON`
present), **0 overlap** with the 361 shortcodes already indexed. **`linkPins` 919 → 924 · `makers`
299 → 303** (four creators; `@amandastrang_` posts twice and correctly produces **one** maker row,
so **5 files cover 5 pins** — no avatars, Instagram exposes none). `tours` and `places`
byte-identical. gh-pages `b65f18d6` (exactly 5 additions). **No what3words in any of these five** —
four carry Plus Codes, one carries a street address.

- **🔴 TWO LAND BESIDE AN EXISTING PIN OF THE SAME SUBJECT BY A DIFFERENT CREATOR, AND NEITHER
  COORDINATE WAS NUDGED IN EITHER DIRECTION.** This is the Cube House / Vizcaya shape (two creators,
  one site — legitimate), not the duplicate-Cheung-Hing shape (one creator, one venue, twice — which
  the owner has pulled twice). Both ship under their own subject title, the Railway Museum precedent
  from the first wave:
  - **`In's Point` already exists** (`@shivanidukhandee`, **37 m**) *and its caption is also about
    LEGO* — "An affordable LEGO Paradise in Hong Kong?? 📍In's Point, Yau Ma Tei". So this is the
    same venue **and the same story**, told by a second creator. `@bricksbyrowan`'s is specifically
    about the **rare and sealed sets**, so it ships as **`The Rare LEGO Sets at In's Point`**.
    ✅ Its Plus Code `857C+88` reverse-geocodes to OSM's **`Ins point`, `shop/toys`, 530 Nathan
    Road** — named exactly — and the hero carries the shop's own sign **twice**.
  - **`Villain Hitting Under the Canal Road Flyover` already exists** (`@love.in.hongkong`,
    **26 m**). `@hongkongd_`'s post is about the **打小人口訣 incantation on 驚蟄**, the busiest day
    of the year for it, so it ships as **`The Villain-Hitting Incantation at Canal Road`** and
    **matches the existing pin's tag set exactly** (`Bridge, Faith, History, Hidden Gem`) so the pair
    shares shelves. Its Plus Code reverse-geocodes to **`堅拿道東 Canal Road East`**, the road under
    the flyover where the stalls are.
  - **🔴 `check-place-candidates.py` REPORTS THE In's POINT PAIR AND CANNOT SEE THE VILLAIN-HITTING
    ONE.** Its NEAR tier matches on **title containment**, and the hyphen in `Villain-Hitting` breaks
    the token match against `Villain Hitting` — the documented Grand Central / Textile Conservation
    Lab blind spot. **Flagged by hand: it is a genuine place candidate at 26 m.**

- **⚠️ ONE CAPTION NAMES NO PLACE AT ALL AND THE HERO NAMES IT.** `@scentsandserums`' caption is
  *"a friend of mine took me here… the perfect place to spend an afternoon"* — no venue, no district.
  The frame burns in **"come with me to the pedder arcade, hong kong"** over a barrel-vaulted arcade
  with globe pendants and timber shopfronts. The Plus Code `75J4+PR` lands **11 m from OSM's 畢打行
  Pedder Building** on a *forward* geocode; ⚠️ **its reverse returns `華人行 China Building`, 29
  Queen's Road Central** — the nearest addressed node, not an error. Ships as **`The Pedder Arcade`**,
  the creator's own words, asserting nothing more.

- **⚠️ ONE IS A POP-UP AND IS TITLED FOR THE VENUE, SO IT CANNOT GO STALE.** `@amandastrang_`'s post
  is the **Bar Leone × Louis Vuitton** collaboration at the Lee Gardens store — a temporary bar
  inside a permanent shop. Titled **`Louis Vuitton Lee Gardens`** (the Balloon Museum / Grace
  Cathedral AURA rule); the coordinate reverse-geocodes to **`利園一期 Lee Garden One`, 33 Sunning
  Road** by name, and the hero carries the **LOUIS VUITTON** sign with the LEONE mark above it.
  ⚠️ **Stated mismatch: it is categorised `foodAndDrink` while titled for a fashion store** — the
  post is entirely a bar visit, and the catalogue has no shopping category.

- **🔴 THE FIFTH NEEDED THREE GEOCODING TRAPS DEFEATED IN A ROW.** `I Love Cake`, a baking-supply
  shop the caption addresses as `G/F, 201 Wan Chai Rd, Wan Chai`. (1) **OSM has an `I Love Cake`
  node — in Yau Ma Tei, 4 km away**, a different branch: the Komeda / Chagee / Lin Heung
  wrong-branch trap, and taking it would have been a confident 4 km error. (2) **`201 Wan Chai Road`
  returns only the ROAD**, twice, 111 m apart — the Warehaus house-number-interpolation trap.
  (3) **Walking the street by reverse-geocode drifted onto Hennessy Road** — Wan Chai Road bends and
  a straight-line walk leaves it. What settled it was a **structured** Nominatim query
  (`street=201 Wan Chai Road`), which returns **`偉希大廈 Wai Hay Mansion` addressed `201-203, 灣仔道
  Wan Chai Road`** — the exact house-number range the caption names — and reverse-verifies to the
  same at zoom 18. The M&S Schmalberg / Chiap Thong enclosing-building case.

- **⚠️ TWO HAND RE-CROPS — the vertical `--focus` gap, SIXTEENTH batch running.** Every source is
  9:16, so the square is width-limited and `--focus` does nothing vertically. Re-rendered through a
  mirror of the tool's own pipeline (importing its `trim_bars`, same blur/dim/quality, **same
  filenames**, so `Tours.json` is untouched): **Louis Vuitton Lee Gardens at 0.05**, where the
  centred square had cropped the **LOUIS VUITTON sign out of the frame entirely**, leaving a woman
  with a glass and nothing identifying the venue; and **the villain-hitting pin at 0.08**, recovering
  its own **`打小人口訣`** title card, of which only the strapline had survived. ⚠️ The other three
  were **left alone** — In's Point keeps both signs, the Pedder Arcade keeps all three lines, and
  I Love Cake destroyed no text.

- **⚠️ TWO WEAK-ISH HEROES, FLAGGED NOT FIXED.** **In's Point's is the shop's SIGN**, correct for the
  venue and carrying none of the LEGO the post is about. **I Love Cake's is creator-forward** — she
  fills the doorway — though the shelves and a KitchenAid box read behind her, which is what
  independently confirms the category. A link pin re-hosts only the thumbnail, so no other frame
  exists.

- **⚠️ The villain-hitting caption discloses its own AI audio** (`情歌版打小人歌曲由AI生成` — the
  love-song version of the incantation is AI-generated). That is the creator's disclosure about
  **their soundtrack**, not about the photograph, and it reaches nothing this catalogue authors; the
  hero is an ordinary frame of the ritual. **Not the Mercedes-Benz Stadium case.**

- **⚠️ One will not play inline** (`@scentsandserums`) — the documented licensed-music gate.

- **Verification.** Mirror **self-tested 19/19**, then **0 errors, 0 warnings** across 1,552 tours +
  **924** pins + 107 places. **18 faults injected against THESE FIVE rows: 15/18 caught, control
  clean** — the 3 misses are the **same documented mirror blind spots** as the first wave (centroid
  vs stop 0, empty title, duplicate maker id), each **verified directly instead**: 0 centroid drift,
  0 empty titles catalogue-wide, **303 maker ids, 303 unique**. `make-link-pin.py --selftest`
  **71/71** (with Pillow). **0** duplicate tour/stop/maker ids, **0** already-pinned sourceURLs,
  **0** byte-duplicate heroes, **0** perceptual pairs nominated at Hamming ≤ 45, **0** filename
  collisions against **6,771** gh-pages `images/` paths — the listing **asserted to hold >1,000
  first** — and the **bare-slug check was clean too**, so the handle suffix was not load-bearing
  here. **File count asserted equal to pin count.** `Tours.json` **byte-stable under a Python
  re-dump before editing**; diff **260 insertions / 0 deletions**, asserted purely additive.
  `seed_from_toursjson.py` clean at **303 / 2,476 / 2,848 / 107**; **0 `images//`** in the catalogue
  *or* the SQL. **`check-place-candidates.py` reports 0 EXACT and exits 0 on BOTH sides** — the
  clean-exit state is preserved and no coincident group was manufactured — **NEAR 35 → 37**, the two
  additions being the Railway Museum and In's Point pairs and nothing removed; ⚠️ exit code read
  **directly, not through a pipe**. gh-pages `b65f18d6`: remote head **re-read in the same command as
  the push**, tree diff **exactly 5 additions, 0 deletions, nothing outside `images/`**.
  ⚠️ **`main` moved mid-session** (#720, a docs commit) and collided **add/add on this handoff's own
  filename**, so it renumbered **`-8` → `-9`**; `Tours.json` was untouched by that merge, and
  `STATUS.md` was resolved by taking **main's** DONE row for `pull_pins_260902.sql` over this
  branch's stale OWED one. ⚠️ **Nothing compiled — CI on
  [#722](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/722) is the authoritative validator.**
