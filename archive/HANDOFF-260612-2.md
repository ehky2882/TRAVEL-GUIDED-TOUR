# HANDOFF — 2026-06-12 (session 35, local build cut)

## TL;DR

**TestFlight 1.0 (41) cut, uploaded, and live.** Carries **PR #193 (London
batch 2 — 18 Bloomsbury/South Bank tours)** + the #195 docs sync — everything
on `main` since build 40 (`0cf79b3`). Content + images only, no app-code
change. **London 40 → 58 — now the #2 city ahead of Porto. Catalog 225 → 243
tours / 4 makers** (100 NYC + 58 LDN + 54 OPO + 31 LIS).

## What happened

1. Build bumped **40 → 41 direct-to-main** (`7cf3590`) — app-target
   `CURRENT_PROJECT_VERSION` lines only; test target stays 1;
   `MARKETING_VERSION` stays 1.0.
2. `xcodebuild archive` clean at `/tmp/Atlas-20260612-0832.xcarchive`
   (~1–2 min — warm DerivedData). Embedded version verified `1.0 (41)`.
   **No validation 90474** — `UIRequiresFullScreen=YES` from build 34 held.
3. Owner uploaded via Organizer → **1.0 (41) live 2026-06-12**.

## Worktree pattern (important precedent)

The primary checkout at `/Users/EY/TRAVEL-GUIDED-TOUR` was mid-flight on
`claude/home-drawer-rails` (**PR #194 — category rails — intentionally NOT
merged or included in this build**; owner reviewing separately, with
uncommitted local changes). The bump + archive ran in a throwaway worktree
(`git worktree add /tmp/build41 main`), removed after the archive. The rails
branch and its working files were never touched. Use this pattern whenever
the main checkout isn't on `main`.

## Observations / follow-ups

- During the session, the rails checkout's modified-file set grew from
  `RailCarousel.swift` alone to also include `HomeDrawerContent.swift` — not
  from this session; presumably the owner/another session editing live.
- **CLAUDE.md sync: done** (`0eea118`) — but only after the owner explicitly
  said "update claude.md now". Lesson: **this environment's permission
  classifier blocks unprompted CLAUDE.md edits**; the automation-rule-#5
  end-of-session sync needs an explicit owner go-ahead (or a settings
  permission rule) before touching CLAUDE.md. ROADMAP/HANDOFF/archive-README
  edits go through fine.
- Standing offer: 5-min App Store Connect API key setup would let Claude run
  the upload step too (`docs/testflight.md` § Full upload automation).
- Stale merged branches still to delete via GitHub UI:
  `claude/dreamy-wozniak-nM6a4`, `claude/dreamy-wozniak-nM6a4-batch2`.
- Carried code follow-ups unchanged (on-device M-qa, design pass,
  `placemark` deprecation, Player drag/volume device pass).
