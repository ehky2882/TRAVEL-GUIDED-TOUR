# HANDOFF 2026-09-04 — seventeen @archimarathon link pins across six countries

Branch `claude/tour-links-7ova6v`, cut clean off `main`, **rebuilt on `main` TWICE** —
[#730](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/730) and then
[#731](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/731) merged mid-session.
⚠️ **This file was written as `HANDOFF-260904.md` and renumbered to `-2` on an add/add collision** —
session 142's follow-up claimed that name in #731. **Check `archive/` for the next free suffix
immediately before writing, and expect to renumber if `main` moves.**
**linkPins 1,151 → 1,168 · `tours`, `makers` and `places` byte-identical · 0 new maker rows.**
Content only — no Swift, no SQL, no build. **Opened as
[#733](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/733) on owner instruction.**

## What arrived

Seventeen links: **16 TikTok short links + 1 Instagram reel**, five carrying Plus Codes
(four Shibuya, one Atsugi). Every one resolves to **`@archimarathon`** — the same creator as
sessions 141 and 142, on two platforms.

## Liveness, dedupe

- **All 17 alive.** 16 TikTok oEmbeds returned thumbnails and the author handle; the Instagram
  embed came back at **262,755 bytes with `contextJSON` present**, against **three already-live
  catalogue pins fetched in the same pass with the same UA at 254–258 KB**.
- **0 already pinned, 0 in-batch duplicates**, re-checked against `main` *after* #730 landed.
- **Both maker rows already existed and the uuid5 scheme reproduced their ids field-for-field**
  (`TikTok @archimarathon` `04A9A86D…`, `Instagram @archimarathon` `3311846C…`), so this adds
  **0 maker rows**. The regenerated avatar hashed **byte-identically to the served file** and was
  **excluded rather than overwritten** (the `@urbanistariel` case, twelfth time): 18 generated,
  **17 uploaded**.

## 🔴 Two subjects were already pinned, and one would have overwritten a live hero

- **`Serlachius Museum Bridge` is already a pin — by `@archimarathon` themselves.** A bare subject
  slug would have produced `serlachius-museum-bridge-archimarathon_hero.webp`, which **is the live
  file**: the handle suffix cannot disambiguate two posts by one creator about one subject (the
  Rosewood Mayakoba / Apthorp / Kowloon Hum case). ⚠️ **It is NOT a repost** — the two heroes score
  **hamming 508 / pixdiff 69.2**, where identical pictures score under 1. Ships under its own
  subject slug as **`Crossing the Serlachius Museum Bridge`**.
- **KAIT** already carries `KAIT Plaza` by `Instagram @avant.arte` at **0.0 m** (convergence — both
  derive from the same Plus Code / OSM node, nothing was nudged), and #730 then added
  `KAIT Plaza and Workshop` at **79.3 m**. Mine is the third marker at that site.

⚠️ **Two pre-existing ORPHANS were found on gh-pages** — `kait-plaza-workshop-archimarathon_hero.webp`
and `serlachius-art-sauna-archimarathon_hero.webp` were uploaded by an earlier session and are
referenced by nothing in the catalogue. Not this session's, left alone per convention.

## Coordinates

- **Five Plus Codes decoded** with `scripts/decode-plus-code.py`, which **self-tests on every
  invocation** (3 published anchors + floor-encode + grid refusal + 2,000 round-trips + 18
  short-code recoveries). Short codes recovered against **structured city geocodes**.
- **Every coordinate reverse-verified at zoom 18.** Named exactly by OSM: `東三丁目公衆便所`,
  `神宮通公園` (both `amenity/toilets`), `代々木第一体育館`, `神奈川工科大学`, plus forward hits
  naming `Casa de Chá da Boa Nova`, `Piscina das Marés`, `Marrickville Library`,
  `Temppeliaukion kirkko`, `High Court of Australia`, `Embassy of Finland`,
  `Kyungdong Presbyterian Church`, `大阪府立狭山池博物館`, `ヒューマックスパビリオン`.
- 🔴 **The BP Bridge geocode returned the WRONG BRIDGE first** — `35th St Pedestrian Bridge` in
  Bronzeville, **4 km south of Millennium Park**. Overpass was unreachable (both mirrors, as
  documented) and reverse-geocoding along Columbus Drive returned only benches and waste baskets.
  🎉 **A bounded search on the bare token `BP` returns `BP Bridge`, `man_made/bridge`, named
  exactly** at `41.8824679, -87.6210680`, between Jay Pritzker Pavilion and Maggie Daley Park.
  **Re-querying was the whole fix, again.**
- ⚠️ **`Sayamaike Museum Osaka Japan` returned ZERO hits**; the Japanese name `狭山池博物館` found it
  immediately, in **大阪狭山市 (Osakasayama)** — a different municipality, so both pins ship
  `city: "Osakasayama"` (the Aït Benhaddou / Campinas convention).

## 🔴 The hero audit settled a post whose caption named no place

**All 17 heroes opened and read against their captions — zero wrong subjects.**

- 🎉 **#5's caption names no building at all** (*"day 2 part 4 … went exploring architecture in
  Tokyo"*). Its frame burns in **"The Humax Pavilion by Shin Takamatsu"**, and the owner's Plus Code
  lands **4 m from OSM's `ヒューマックスパビリオン`**. The picture settled what the metadata could not.
- **#17 names itself in Korean** — `경동교회` is legible on the entrance pier, independently
  confirming the coordinate.
- **Four re-crops**, the vertical `--focus` gap for the seventeenth batch running (every source is
  9:16, so the square is width-limited and `--focus` does nothing vertically). Re-rendered through a
  mirror of the tool's own pipeline — importing its `trim_bars`, same blur/dim/quality, **same
  filenames**, so `Tours.json` is untouched:
  - **#4 at 0.30** — the centred crop had cut **"Leça Swimming Pool"**, the subject's own name.
  - **#5 at 0.60** — recovers **"…by Shin Takamatsu"** while keeping the building's dome in frame.
  - **#9 at 0.00** — ⚠️ **stated trade-off**: recovers **Prada Aoyama's diamond-grid crystal**, which
    the centred crop had pushed almost out of frame, at the cost of the burned-in
    *"and here we have the Prada Epicentre"* caption. The building is what makes the pin
    recognisable on a map; the caption survives verbatim in `longDescription`.
  - **#14 at 0.15** — the centred crop had cut the presenter's head off **and** pushed the High Court
    building to the top edge.
  - ⚠️ **Straplines were deliberately LEFT** — *"some more crazy looking buildings"*,
    *"I found this straggler"*, *"it's actually pretty good"* are topic hooks, not the subject's name
    (the California Academy rule).

## ⚠️ Four weak heroes, flagged not fixed

- **#11 Yoyogi National Gymnasium is a pure talking head** — the creator's face fills the frame with
  a generic Tokyo street behind, and nothing of the gymnasium or Meiji Jingu is visible. **The
  weakest in the batch.**
- 🔴 **#6's hero is a building while its caption and its coordinate are both about a toilet.** The
  caption is *"exploring some of the architect designed toilets in Tokyo"*, the owner's Plus Code
  reverse-geocodes onto `東三丁目公衆便所` (`amenity/toilets`) in Ebisu, and the frame shows a concrete
  building with a jagged glass facade under the burned-in line *"some more crazy looking buildings"*.
  The reel is a compilation covering both. Titled for the coordinate; **the mismatch is real.**
- **#7** is two presenters filling the frame, with the Jingu-dori toilet's vertical louvres barely
  readable behind them.
- **#1** shows a **display model in a case** rather than the museum, which is correct for its caption
  (*"he's a model explaining what's going on here"*) but reads as an interior exhibit on a map.

A link pin re-hosts only the thumbnail, so no other frame exists for any of them — the choice is
keep or pull.

## Architect tags

**In the vocabulary and used by name**, each **alongside** `Designed by a Master`: `Tadao Ando` ×2,
`Álvaro Siza` ×2, `Herzog & de Meuron`, `Frank Gehry`, `MX_SI`.

⚠️ **`Junya Ishigami` was absent when this batch was wired and became available when
[#731](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/731) merged mid-flight (vocabulary 383 →
419)** — KAIT gained the name on the rebase rather than shipping the generic tag alone.
**Verified absent and still shipping the generic tag only:** `BVN Architecture`, `EMTB`,
`Hirvonen-Huttunen`, `MGT Architects`, `Kim Swoo-geun`, `Shin Takamatsu`.

⚠️ **Two pins carry NO master tag at all, deliberately.** **`Yoyogi National Gymnasium`** and
**`Temppeliaukio Church`** name no architect and do not make authorship the point (the Jules Dalou
rule). **`Kenzō Tange` IS in the vocabulary** — with a macron, which a naive grep misses — and is
still not tagged, because that caption never names him. **Do not "finish the job."**
⚠️ By contrast **#6 and #7 DO carry the generic tag**: *"architect designed toilets"* makes
authorship the point without naming anyone.

## Place candidates — flagged, NOT created

⚠️ **The clean-exit state was already broken by #730, not by this batch** — the merged base reads
**11 EXACT / 50 NEAR** (#731 having resolved the Barcelona Pavilion group). This batch takes it to **13 / 52**, and the diff proves exactly what it adds:

- **NEW: KAIT** — `KAIT Plaza` (@avant.arte) + `KAIT Workshop and Plaza` (mine), coincident, with
  #730's `KAIT Plaza and Workshop` 79 m away. **Three markers for one site.**
- **NEW: Sayamaike Museum** — my #1 and #2, two different reels from consecutive parts of one
  walkthrough (the Kowloon Hum precedent: two different reels are two pins).
- 🔴 **Serlachius goes 2 → 3 members** — `Serlachius Museum Bridge` + `The Art Sauna at Serlachius`
  (both #730's) + mine, **exactly `TourSetMap.maxStacked = 3` with no headroom**, and all three are
  one creator, so they stack on that creator's own maker page. **This is the case a place exists to
  fix.**
- **NEAR added: exactly two** — `Prada Aoyama` (28.8 m from the Atlas tour) and the KAIT pair.
  **Nothing removed**, so no coincident group was manufactured and nothing was nudged apart.

⚠️ **Four same-subject pairs are INVISIBLE to `check-place-candidates.py`** because its NEAR tier
matches on **title containment** and these titles share no distinctive word — flagged by hand:
**Boa Nova Tea House ↔ `Casa de Chá da Boa Nova` (52 m)**, **Leça Swimming Pool ↔
`Piscina das Marés` (31.5 m)**, **Yoyogi National Gymnasium ↔ `Yoyogi National Stadium` (67.8 m)**,
and **BP Bridge ↔ `Jay Pritzker Pavilion` (38.9 m)** — the last being a genuinely different subject
and correctly separate.

## Verification

- `make-link-pin.py --selftest` **71/71** ⚠️ **with Pillow installed first** — a bare container
  reports 62/62, which reads as a pass and is not one. `decode-plus-code.py --selftest` clean.
- Mirror **self-tested 22/22 with a clean control**, then **0 errors, 0 warnings across 1,552 tours
  + 1,168 pins + 114 places**, **exit code read directly, not through a pipe**.
  ⚠️ **The first reading of that exit code was through `| tail`, which reports `tail`'s status** —
  the session-90 `PIPESTATUS` trap, caught and redone.
- ⚠️ **22 faults injected against THIS batch's own 17 pins — 18 caught, control clean before and
  after.** The four misses are the documented **mirror** blind spots (centroid drift, empty title,
  negative duration, stop order) and were **asserted directly on the 17 instead**, along with
  one-stop-each, `kind == link`, `manual` trigger, empty `audioURL`, https heroes, no `images//`,
  city+country set, ≥1 Place type and ≥1 Theme, and **17 distinct heroes for 17 pins**.
  ⚠️ The harness counts **errors AND warnings** — "no Place type" and "no Theme" are warnings, and a
  harness counting only errors reports false misses (the session-141 bug).
- **0** duplicate tour/stop ids, **0** collisions with live ids, **0** already-pinned sourceURLs,
  **0** byte-duplicate heroes, closest perceptual pair **179 hamming / 31.3 pixdiff**.
- **0 filename collisions against 6,970 gh-pages `images/` paths**, the listing **asserted to hold
  >1,000 first**.
- `Tours.json` **byte-stable under a Python re-dump before editing** (asserted on both bases); diff
  **797 insertions / 0 deletions**, asserted purely additive with `tours`, `makers` and `places`
  byte-identical and the existing pins unchanged as a prefix.
- `seed_from_toursjson.py` clean at **305 / 2,720 / 3,092 / 114**; **0 `images//`** in the catalogue
  *or* the SQL.
- gh-pages `0b15aa4`: remote head **re-read in the same command as the push**, push status read
  through **`PIPESTATUS`**, tree diff **exactly 17 additions, 0 deletions, nothing outside
  `images/`**; the deploy read **`in_progress`, never `cancelled`** against the Actions API.
- ⚠️ **Nothing compiled — no PR is open, so CI has not run.**

## ⚠️ `main` moved TWICE mid-session

[#730](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/730) merged **104 more `@archimarathon`
pins** while this batch was being wired — the same creator — and
[#731](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/731) then took the architect vocabulary
**383 → 419**, which is what made `Junya Ishigami` available to KAIT.

⚠️ **The second rebase silently discarded the docs commit.** `git reset --hard origin/main` takes the
working tree with it and says nothing; the doc edits were recovered only because every replacement
asserted its anchor was unique and reported **0 matches** rather than no-opping quietly. **`git
checkout <pushed-sha> -- <files>` is the recovery.** The edit was **redone the documented way:
reset onto `main` and re-run the idempotent assembler**, never hand-resolve a JSON conflict, with
**every check re-run** and overlap re-verified (**0** on sourceURL, ids and hero URLs).

---

## Follow-up, same session — the Serlachius place + six architects

Owner: **"Make the place. Add the architect's."** Both applied on this branch, which ⚠️ **turns
[#733](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/733) into a CODE PR** (`Models/Tag.swift`
+ `scripts/validate-tours.swift`) wanting an owner OK and a simulator look; CI is the compile check.

### The place — places 114 → 115

`Serlachius Museum Gösta`, id `62bbabf6-5646-5b4b-8c3d-0fdfb093d2ff`
(`uuid5(NAMESPACE_URL, "atlas-place:m-ntt:serlachius-museum-g-sta")`).

Members, all `TikTok @archimarathon`, all `manual`, all already on the anchor:

| member | source |
|---|---|
| `Serlachius Museum Bridge` | #730 |
| `The Art Sauna at Serlachius` | #730 |
| `Crossing the Serlachius Museum Bridge` | this batch |

- **Nothing moved.** `tours`, `makers`, `linkPins` byte-identical; the 114 existing places unchanged
  as a prefix; diff **30 insertions / 0 deletions**. The build asserts every member is `manual` and
  already coincident *before* it writes, so it cannot silently relocate a pin or disturb a geofence.
- **Anchor confirmed both ways.** Reverse at zoom 18 → `Serlachius Kartano`, `tourism=museum`,
  **47 Joenniementie, Mänttä**. Bounded forward on the bare token `Serlachius` → **node
  1228315466 at 0.0 m**. ⚠️ **Four unbounded phrasings returned nothing at all** — the bounded
  viewbox is what found it, the same lever the BP Bridge needed earlier in this session.
- **Swept 1500 m** rather than trusting the checker's group: **nothing else is in range**, so unlike
  Alwyn Court or PMQ there is no deliberate exclusion to record.
- **Hero borrowed, structurally.** Every member is a link pin with an empty gallery and there is no
  Atlas tour at the site, so no third photograph of it exists in the catalogue. All three candidates
  were rendered and looked at; the bridge-across-the-lake frame wins on the establishing-shot
  criterion. ⚠️ **Stated trade-off:** the Art Sauna's board-formed concrete entrance is the finer
  picture and reads as nowhere in particular. One line swaps it. Borrowed-hero count **re-derived:
  41 of 115** (69 third photograph, 5 none).
- **Naming.** `Serlachius Museum Gösta` matches no member title; "Gösta" is what distinguishes this
  lakeside campus from the Serlachius Gustaf museum in town. Address editorial, corroborated by the
  reverse geocode rather than taken from it.

### The architects — vocabulary 419 → 425

Added to **both** files, alphabetically, as one batch:
`BVN Architecture` · `EMTB` · `Hirvonen-Huttunen` · `Kim Swoo-geun` · `MGT Architects` ·
`Shin Takamatsu`.

Seven entries gained a name:

| entry | name(s) | why |
|---|---|---|
| Marrickville Library | `BVN Architecture` | caption |
| High Court of Australia | `EMTB` | caption |
| Embassy of Finland in Canberra | `Hirvonen-Huttunen`, `MGT Architects` | caption names both |
| Kyungdong Presbyterian Church | `Kim Swoo-geun` | caption |
| Humax Pavilion Shibuya | `Shin Takamatsu` | **the frame**, not the caption — the Ewha precedent |
| **Ark Building \| アークビル** | `Shin Takamatsu` | **catalogue-wide sweep**; also gained the shelf tag |
| **Pharaoh \| ファラオ** | `Shin Takamatsu` | **catalogue-wide sweep**; also gained the shelf tag |

- 🔴 **The two Kyoto tours are the find.** Both state authorship in their own text and carried
  **neither an architect tag nor `Designed by a Master`** — naming an architect while sitting off the
  shelf built for exactly those entries. That is the #493 mirror-image defect, closed two cities from
  where this batch was working.
- ⚠️ **`EMTB` and `MGT` are the false-positive risk** and were word-bounded and read: 1 hit each,
  both genuine, **0 false positives across the whole sweep**. That is unusual and only because these
  six names are unlike ordinary English — contrast `Public Architecture`, which matched eight tours.
- ⚠️ **Humax is a judgement, one line to reverse.** Its caption names nobody; its frame reads *"The
  Humax Pavilion by Shin Takamatsu"*. The frame is the entry's own source and is how the subject was
  identified at all — the Ewha precedent, not a stretch of the Jules Dalou rule. **The hero was
  re-opened and re-read before tagging**, rather than trusting the earlier note in this session.
- 🔴 **`Kenzō Tange` stays untagged** on Yoyogi and **`Temppeliaukio Church` still carries no master
  tag** — neither caption names an architect. **Do not "finish the job."**
- 🔴 **`Designed by a Master` kept on every one.** After the change: **708 entries name an architect,
  0 missing the shelf tag, 0 of 425 names unused.**

### Verification of the follow-up

- Both vocabularies **parsed out of the Swift** and asserted **identical at 425, 0 duplicates on
  either side**; brace/bracket/paren balance checked on both with **string literals stripped**.
- Near-duplicates on **normalised token sets**: **0 collisions, 0 exact matches**.
- Mirror **22/22 with a clean control**, then **0 errors, 0 warnings across 1,552 tours + 1,168 pins
  + 115 places** at **475 tags**.
- ⚠️ **24 faults injected against THIS place and the SEVEN retagged entries — 23 caught, control
  clean before and after.** 🔴 **The one miss was a rule I invented**: the Swift validator has **no
  duplicate-tag check** (`Set(t.tags)` collapses repeats silently), so a repeated tag is not a fault
  at all — the session-142 mirror-invents-a-rule class, caught on myself. Asserted **directly on the
  seven** instead.
- 🎉 **`check-place-candidates.py` 13 EXACT → 12, NEAR unchanged at 52**, and the **report diff**
  proves it fell by exactly the group resolved and gained nothing.
- Place-id scheme **reverse-verified against 112 of 114** existing places (the two misses are the
  documented legacy uppercase ids). ⚠️ **The trailing-hyphen question was settled by `Chichén Itzá`**,
  whose city slug `chich-n-itz` matches only the **stripped** variant; minted **lowercase**, which is
  108 of 114.
- `Tours.json` **byte-stable under a Python re-dump before AND after** each edit; seed clean at
  **305 / 2,720 / 3,092 / 115**; **0 `images//`** in the catalogue *or* the SQL.
- ⚠️ **Nothing compiled locally** — no Swift toolchain in a Linux web session.
