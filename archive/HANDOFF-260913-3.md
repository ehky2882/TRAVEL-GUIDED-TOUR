# Handoff: 2026-09-13 (3) — branch cleanup, the applied migration recovered, and the private-account wording shipped

## What happened

Acted on the branch audit in #872. Every claim was re-verified with GitHub's compare API, because the shared checkout is a shallow clone.

| PR | What |
|---|---|
| **#873** | `backend/private_friends_social.sql` brought onto `main`, byte-identical to the branch copy (blob `b4817462`). It had been **applied in production** on 2026-09-01/09 but existed only on an unmerged branch. Checked live: `rpc/can_see_social` → HTTP 200. |
| **#875** | The unlanded app half of that fix: a private creator's followers/following screen now says "This account is private…" instead of "No followers yet." Ported unchanged from `4a383905`. TestFlight **build 159**; the owner confirmed on device, then it was merged. |
| #876, #877 | Board entries, build 159, and owner items opened and cleared. |

## Branches

**Deleted: 16**, each confirmed by re-listing, where it returns 404, not by exit code. All deletes used `DELETE /git/refs/heads/…`.
- The audit's 12: `amazing-rubin-pkub81`, `clever-wozniak-mmyxa6`, `tour-links-26dmsx`, `img-encoding-test`, `project-tracking-dashboard-1kggmu`, `status-account-delete-decided`, `status-delete-decision`, `stripe-questions-fjhdo3`, `web-landing-site-preserve`, `link-fullscreen-probe`, `dreamy-wozniak-tags-260612`, `seed-hang-note`.
- `transcript-square-floor`: the owner said it was already implemented. It is: #558 makes the box fill the step. The branch only changed the shape of the pre-measurement first frame.
- `private-friend-account-33psch`, after both its halves were on `main`, plus the PR branches `recover-private-friends-sql`, `private-account-followers-copy`, `board-branch-decisions` and `board-875-merged`.

**Kept, on the owner's word:** `paris-scripts-260622` and `london-batch3-scripts-260616`, the only copy of the source scripts. Also `amsterdam-handoff-preserve-hlhyp8`, which holds the 30 Atlanta scripts waiting on audio.

**Untouched and never audited:** `more-tours-a1xhw4` (+1: "Close both place-hero questions…").

Two corrections to the audit, both checked before deleting:
- `link-fullscreen-probe` had two **non**-probe commits. They are on `main` in `LinkEmbedView.swift`.
- `web-landing-site-preserve` also carried the `•••`-menu paywall-bypass fix and the per-account pending-purchase queue. Both are on `main`.

## ⚠️ Traps hit this session

- **A rejected `test_sim` call still ran.** Its xcodebuild process kept going for 30+ minutes. `NavigationDepthPairingTests.test_nestedDetails_stayInDetailFormUntilTheLastPop` aborts on the local iOS 26.5 simulator (`AtlasNavigationState` deinit → `swift_task_deinitOnExecutorImpl` → pointer freed that was not allocated), and xcodebuild relaunched it about every 20s. That meant **40 "quit unexpectedly" popups for the owner.** The same suite passes on GitHub CI. Fix: kill the process and check `~/Library/Logs/DiagnosticReports/`. **Don't run local `test_sim` on this Mac; use CI.** A fix task was offered as a chip.
- `status.py --clear-owner` **prints** the `git rm` command; it does not run it. #876 initially shipped without the deletion.
- `gh pr merge --match-head-commit` rejects a short SHA.
- `gh pr merge` exits 1 when `main` is checked out in another worktree, but the merge still happens on GitHub. Read the PR state.
- In macOS bash 3.2, an apostrophe inside `$(cat <<'EOF' … EOF)` breaks parsing. Use `--body-file`.

## Left out on purpose

`backend/test-migrations.sh`'s social fixture from the old branch. CI does not run that script, and the fixture conflicts with the `catalog_snapshot` fixture added since. If anyone wants the three-viewer assertion back, it has to be re-fitted to the current base schema.
