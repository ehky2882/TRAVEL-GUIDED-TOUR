# HANDOFF — 2026-07-02 (session 52, web/PM — content)

## Headline

**Kyoto launched — 30 tours + a 9th maker, Atlas Studio KYO.** [PR #309](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/309) (`af02819`, squash, auto-merged on CI green). Owner-supplied audio + images (no image pipeline). **Catalog 479 → 509 tours / 8 → 9 makers / 561 stops; Kyoto = 30.** Validator PASS; confirmed live on both the Supabase RPC and the gh-pages mirror.

## What shipped

- **New maker Atlas Studio KYO** — `50b53af5-68ac-5e6e-8185-ae367326632d`, 🇯🇵, bio "Atlas Studio's Kyoto bureau — audio tours of the old capital's temples, gardens, machiya, teahouses, and design landmarks." Deterministic uuid5 (`atlas-maker:kyo` under `NAMESPACE_URL` — the same scheme as Tokyo; tours/stops `atlas-{tour,stop}:kyo:<slug>`; all 61 new ids collision-checked against the full catalog).
- **30 single-stop, geofenced tours** (30 m radius, priceUSD 0, `kind` single), bilingual `English | 日本語` titles on both tour + stop, mirroring the TYO/HKG schema. `transcriptText` = the verbatim clean narration from each `_clean.txt` (outer whitespace + production header line `… / Atlas Audio Tour` + trailing `*~NNN words*` footer trimmed). Durations read via `afinfo`; `totalDurationSeconds` = stop duration.
- **Assets-first:** 30 mp3 + 111 webp staged to gh-pages (`62ea953`) under the lowercase-hyphen slug convention (`<slug>.mp3`, `<slug>_hero.webp`, `<slug>_2.webp`… — hero = image `01`, gallery from `02→_2`); all webp, none needed double-extension cleanup. Sample published URLs 200.
- **Categories:** sacredSites 6 (Kinkaku-ji, Ginkaku-ji, Fushimi Inari, Byōdo-in, Enkōji, Giōji) · foodAndDrink 10 (cafés, tea, kaiseki, kawadoko, cocktail bar) · architecture 5 (A-POC, Ark, Face House, La Collina, Le Labo) · visualArt 4 (KYOCERA, Asahi villa, Garden of Fine Arts, Heisei Chishinkan) · culturalHeritage 4 (Gion, Kyukyodo, lisn, APFR) · natureAndParks 1 (Arashiyama bamboo).

## Flags for the owner (source data)

- **🚩 La Collina Ōmi-Hachiman** (`35.16193, 136.08880`) is in **Ōmi-Hachiman, Shiga Prefecture**, ~40 km from Kyoto (a different prefecture). Included under the KYO maker with `city` = **Ōmi-Hachiman** — owner to decide whether it belongs in the Kyoto set.
- Other out-of-Kyoto-city (but Kyoto-Prefecture) tours set to their locality: **Byōdo-in → Uji**, **d-matcha → Wazuka**, **Asahi Ōyamazaki villa → Ōyamazaki**, **both Kibune → Kibune**. All 30 coords sanity-checked inside Kansai; no other outliers; no folder missing coordinates.
- **Supplied Japanese for 7 folders** that lacked it (brief listed 6): A-POC ABLE ISSEY MIYAKE KYOTO · APFR → エーピーエフアール京都 · アークビル (Ark Building) · ブレンド京都 (Blend Kyoto) · フェイスハウス (Face House) · d:matcha Kyoto · **ハリオカフェ (HARIO CAFE — not on the brief's list; it too had no JP).**

## Count note — Kyoto is the 9th city, not the 8th

The brief anticipated Kyoto as the 8th city (469 → 499 / 8 makers). While it was in flight, **Toronto ([PR #306](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/306), `323a95c`) landed as the 8th maker (Atlas Studio YYZ 🇨🇦, +10 tours, 469 → 479)**. The Kyoto batch was built on a worktree off the moved `origin/main` and rebased additively, so the real result is **479 → 509 / 8 → 9 makers**; nothing dropped (diff was +1408 lines, all insertions).

## Confirmed-live

After merge the publish-catalog + Supabase auto-seed workflows ran. Polled both until: **Supabase `get_catalog`** served 509 tours / Kyoto 30 (~1 min); **gh-pages mirror** served 9 makers / 509 / Kyoto 30 (~7 min CDN lag). **Gotcha logged (memory `reference-live-poll-maker-count`):** the Supabase RPC reports **more** makers than `Tours.json` (upsert-only accumulation — it showed 10) — so assert the confirmed-live poll on **tour counts + the new maker's own tour count**, not the total maker count. The gh-pages mirror is the exact file, so its maker count matches.

## Process notes

- Worked entirely in a dedicated worktree (`/tmp/kyoto` off `origin/main`), removed when done — the primary checkout was mid-flight on a parallel session's `claude/create-tour-260702` (maker-authoring Swift work), left untouched.
- The known `gh pr merge --delete-branch` snag recurred (`'main' is already used by worktree` — the primary had `main` checked out by then); the squash landed server-side regardless, and the remote branch was deleted with `git push origin --delete`.
- This docs-sync PR also backfilled the **Toronto** content block + the catalog counts (`CLAUDE.md` Key facts had drifted to 469/7, missing both YYZ and KYO).
