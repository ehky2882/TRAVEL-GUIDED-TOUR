# HANDOFF 2026-08-31 (9) — Dozent 1.1.1 submitted: build 139, ten new screenshots, and the metadata that had to be rewritten around it

**Session type:** App Store Connect (browser + API). **No Swift, no `Tours.json`, no build.**
One docs/metadata PR ([#684](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/684), squash `5cfe5c2c`).

**Outcome: version 1.1.1 is `WAITING_FOR_REVIEW`**, submitted **2026-09-01 02:17 UTC** by the owner,
in **ONE submission (`4130a508`) carrying 12 items — the version plus all 11 pending IAP tiers.**
`releaseType` is **MANUAL**, so approval does not publish; the owner presses Release.

---

## What shipped in the listing

| Field | Value |
|---|---|
| Version / build | 1.1.1 / **139** (137 and 138 superseded) |
| What's New | 1,314 chars |
| Description | 3,362 chars (was 2,837) |
| Promotional text | 144 chars (**was empty**) |
| Screenshots | **10**, iPhone 6.9" `APP_IPHONE_67`, all `COMPLETE` |
| Gates | TRAVEL · 12+ (Brazil 14) · 2026 Dozent · third-party content · privacy URL live |

---

## 🔴 THE THREE DURABLE FACTS

### 1. Adding a version for review LOCKS ITS SCREENSHOTS. It does not lock its text.

Deleting a screenshot on a version in `READY_FOR_REVIEW` returns:

    409 STATE_ERROR — "Can't Delete Screenshot while Ready For Review appScreenshots"

**Nothing in the DOM says so** — the file inputs are not `disabled`, the fields are not `readOnly`,
and the only visible hint is that the thumbnails render dimmed, which they also do on a perfectly
editable version. So the browser gives you no signal until Apple refuses.

**The order that avoids this: do screenshots BEFORE `Add for Review`.** If you are already past that
point, the sequence is remove the version from the submission → replace → re-add. It costs nothing
but time, and the submission survives it.

**⚠️ Text metadata is NOT locked in that state.** `description` and `promotionalText` both PATCHed
`200` while the version sat in `READY_FOR_REVIEW`, so there is no need to pull a version out of a
submission to fix copy. Only assets.

### 2. `promotionalText` DOES NOT INHERIT between versions — and it fails silently.

Version 1.1.1 was created from 1.1. The description carried over. The screenshots carried over.
**`promotionalText` came back `None`**, while the live 1.1 page had one.

Submitting as it stood would have **blanked the promotional line on the live store page** with no
warning anywhere. Nobody reads that field; it would not have been noticed.

**Check `promotionalText` on every new version record.** It is also the ONE field Apple lets you edit
on a live app **with no submission**, which makes it the right home for anything timely — it now
carries the catalogue size and can be bumped the day a city lands.

### 3. The standing "nine IAP tiers are missing review screenshots" note is WRONG on both count and cause.

Measured this session across all 14 tiers:

- **`images = 0` on every single one**, including the three Apple has already **APPROVED**
  (0.99, 1.99, 12.99). A review screenshot is therefore **not** what gates these products.
- Only **one** tier was `MISSING_METADATA` — `tour.tier.999` — and the difference from its healthy
  neighbours was that it had **no `inAppPurchaseAvailability` record** (404 on that relationship,
  where 8.99 returns one). Its localization, price schedule and review note were identical.

⚠️ **Availability was created to match the other thirteen** (all 13 shared exactly one configuration:
`availableInNewTerritories: true`, 175 territories) **and the state did not clear.** The owner then
added an image in the UI and it flipped to `READY_TO_SUBMIT`. **The image, not the availability, is
what unblocked it** — the availability gap was real and needed fixing regardless, but do not record
it as the cause. What actually gates an IAP here is still not fully characterised.

---

## Other things worth keeping

- **The IAP ladder is complete.** 14 tiers: 3 approved (0.99, 1.99, 12.99), **11 submitted**
  (2.99–9.99, 14.99, 17.99, 19.99). Every price point is now approved or in review.
- **Bundling was correct and is now moot.** Apple appears to allow one submission in flight, so
  splitting the version from the tiers buys a second sequential review cycle for nothing. The old
  hard constraint (*the first non-consumable must accompany a version*) no longer binds — three
  tiers are already approved.
- **⚠️ `gh pr merge --delete-branch` still fails from a worktree** ("'main' is already used by
  worktree") — merge without the flag; GitHub's `delete_branch_on_merge` removes it anyway.
- **⚠️ Raw coordinate clicks in the browser tool are unreliable here** — the screenshot frame is
  scaled against the real viewport, so two clicks landed nowhere (silently). **Use `ref` from
  `find`/`read_page`.** A long `type` into an ASC textarea also timed out mid-stream
  (`Input.dispatchKeyEvent` 30 s) while the text *did* land — so **verify by checksum, not by the
  tool's success line.**
- **⚠️ ASC relationships return links, not values.** `primaryCategory` read as "NOT SET" until
  queried directly; it is **TRAVEL** on both app records. Nearly a false alarm.
- **The API beats the UI for screenshots.** Deletion is a hover-reveal control and upload is
  drag-drop — neither exposes a DOM handle. `POST /v1/appScreenshots` → PUT the upload operations →
  PATCH `uploaded:true` + `sourceFileChecksum` is deterministic and verifiable.

---

## Verification

- **Screenshots:** 10 uploaded, **every md5 matched the local file**, 10 distinct names, 10 distinct
  hashes, all `COMPLETE` — the duplicate failure of 2026-08-17 is not present.
- **1.1 (live) was never at risk** — it holds its own screenshot set (`d1092393…`) with entirely
  different asset ids from 1.1.1's (`6738b383…`).
- The delete script **stopped at the first 409 without deleting anything**; all six originals were
  re-listed intact afterwards.
- Description and promotional text on `main` are **byte-identical to what Apple holds** (sha256).
- `whatsNew` re-checked at 1,314 after each PATCH — only one attribute was ever sent, so the
  documented atomic-PATCH trap could not bite.

## ⚠️ Two judgements the owner should be able to revisit

1. **Screenshot order is filename order** (`App Store Preview-01` → `-10`). Verified as byte-correct
   and correctly ordered; **nobody has judged whether that is the right narrative order.**
2. **Pinned posts are described as "playing without leaving the app."** True of the large majority;
   a minority of Instagram pins sit behind the licensed-music rights gate and open Instagram. The
   sentence does not say "all".
