# HANDOFF — 2026-07-25 (session 71: paid tours Phase 2 — the money backend)

## What shipped

**V2 Step 6 Phase 2: the backend for paid tours, written but not yet applied.** No app code touched; the free catalog is unaffected (`price_tier` is NULL everywhere). Design: `docs/paid-tours-design.md`. Phase 1 (the 10 IAP tier products in ASC) landed session 69.

Three new files + three edits:

| File | What |
|---|---|
| `backend/paid_tours.sql` | new — the whole money layer, idempotent, one paste |
| `backend/functions/record-purchase/index.ts` | new — the `purchases` write path |
| `backend/functions/appstore-notifications/index.ts` | new — Apple V2 notifications (refunds) |
| `backend/README.md` | new § "Paid tours" — the owner runbook |
| `backend/seed_from_toursjson.py` | comment only — codifies why `price_tier` is omitted |
| `docs/paid-tours-design.md`, `CLAUDE.md` | Phase 2 status + the deviations below |

## What's in the SQL

- **`tours.price_tier`** — int cents, **NULL = free**. CHECK-constrained to the 10 Phase-1 tiers (99, 199, 299, 399, 499, 699, 899, 999, 1499, 1999). The ASC product id is derivable: `tour.tier.` + zero-padded cents.
- **`purchases`** — one row per Apple sale = the buyer's entitlement **and** the maker's ledger entry. `apple_transaction_id` **UNIQUE** (StoreKit-history replay after a dead spot is a no-op). Stores the tier actually paid, so later price changes don't rewrite history. `apple_environment` keeps Sandbox rows out of earnings. `user_id` is `on delete set null` and `tour_id`/`maker_id` are `on delete restrict` — a sold tour can be taken down but never hard-deleted, and account deletion never destroys a maker's earnings history.
- **`payouts`** + `purchases.payout_id` — Phase 5 stamps rows as paid.
- **`maker_payout_accounts`** — the Stripe Connect account id.
- **`maker_net_cents(tier)`** — `0.85 × 0.80` = 68% of sticker (Apple 15% Small Business → Atlas 20% of the remainder). Single place to change if a rate moves; payout rows store computed amounts so history is stable.
- **`maker_earnings`** view (`security_invoker`) — units, gross, earned, accrued-unpaid, paid-out. Production + non-refunded only. Phase 4's Earnings screen reads this.
- **`get_catalog()`** — full rebuild, identical except a new `'priceTier'` key. Old app builds ignore it.

**RLS:** `purchases` is **read-only to every client** — buyer sees own rows, maker sees sales of their tours (`owns_maker`), admin sees all. All writes come from Edge Functions via the service role. `payouts` read = paid maker + admin, write = admin. `maker_payout_accounts` = owning maker + admin, read and write.

## Two deliberate deviations from the design doc (don't "fix" them)

1. **`maker_payout_accounts` table, not `makers.stripe_account_id`.** `makers` carries a **public-read** RLS policy (`makers_public_read ... using (true)`), so a Stripe account id column there would be readable by every anonymous client. The separate table is owner + admin only.
2. **Neither Edge Function trusts its caller's payload.** Both extract *only* a transaction id from the incoming JWS, then fetch the authoritative record from **Apple's App Store Server API** (production host, falling back to sandbox) under our signed ES256 JWT, and act on that. So a forged POST can neither mint an entitlement nor fake a refund. The unverified local decode is used for id extraction only — never as a recorded fact. This is why the App Store Connect **In-App Purchase API key** is required.

## Owner's 3 steps to make it live (hand-hold — dashboard work)

Full runbook: `backend/README.md` § "Paid tours".

1. **SQL Editor** → paste all of `backend/paid_tours.sql` → Run → *"Success. No rows returned."* Verify: `select count(*) from public.purchases;` → 0, and `select get_catalog() -> 'tours' -> 0 ? 'priceTier';` → true.
2. **App Store Connect** → Users and Access → Integrations → In-App Purchase → generate an API key. **The `.p8` downloads once.** Note the Key ID + Issuer ID.
3. **Supabase → Edge Functions** → deploy both: `record-purchase` with **Verify JWT ON**, `appstore-notifications` with **Verify JWT OFF** (Apple sends no Supabase JWT). Set 4 shared secrets: `APPSTORE_IAP_KEY` (full `.p8` contents), `APPSTORE_IAP_KEY_ID`, `APPSTORE_ISSUER_ID`, `APPSTORE_BUNDLE_ID`. Then ASC → the app → App Information → **App Store Server Notifications** → set **both** Production and Sandbox URLs to `https://<project>.supabase.co/functions/v1/appstore-notifications`, Version 2.

## Notes / gotchas

- **`price_tier` must never enter `seed_from_toursjson.py`.** Price is maker-set in the DB, not in `Tours.json`; the seeder's tour upsert omits it from the column list *and* the `DO UPDATE`, so a content re-seed preserves prices and new tours default to free. A comment in the file says so — verified by regenerating the seed (914 tours, 0 occurrences of `price_tier`).
- **No Postgres in this session**, so the SQL wasn't executed — only structurally checked (balanced parens/dollar-quotes; the odd single-quote counts are all apostrophes inside comments). First real execution is the owner's step 1; if anything errors, fix and re-run — the file is idempotent.
- **Pricing a tour before Phase 4's maker UI:** `update public.tours set price_tier = 299 where id = '<uuid>';` — `null` puts it back to free.
- Sandbox purchases record with `apple_environment = 'Sandbox'` and are excluded from `maker_earnings`, so Phase 3/6 testing can't pollute real earnings.
- Widening the tier menu later = create the ASC product first, then extend the CHECK constraint in `paid_tours.sql` (it's the only place the list is enforced; the functions accept any `tour.tier.<digits>` product Apple confirms).

## NEXT — Phase 3 (buyer side in the app, StoreKit 2, 1–2 sessions)

Price badge on paid tours · Buy button → StoreKit `purchase()` → POST to `record-purchase` · locked/unlocked playback gating · entitlement check on launch/sign-in · transaction-history replay for missed recordings. Ships the usual way (web session → PR + `ci.yml` → `testflight.yml` → owner sandbox-tests with fake money). Then Phase 4 (maker tier picker + Stripe onboarding + Earnings), Phase 5 (monthly payout run), Phase 6 (dress rehearsal → go live, where the IAP products finally get their review screenshots and ship with an app version).
