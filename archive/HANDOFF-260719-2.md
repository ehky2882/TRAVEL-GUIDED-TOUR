# HANDOFF — 2026-07-19 (session 60, code + docs, web)

Bridges to the next session. Durable rules live in `CLAUDE.md` / `ROADMAP.md`; this is the mid-flight state.

## What shipped this session

### 1. Home map/animation performance fix — TestFlight 1.1 (10) — merged

**Trigger:** owner asked "my map and app animations are not as smooth and seems a little slow. Am I imagining things?" — no, it was real, and scaling with catalog growth.

**Diagnosis (all in `Features/Home/`):**
- `HomeMapSection.cluster` built + diffed a SwiftUI annotation for **every** stop in the whole catalog (~969), not just near the viewport. MapKit culls off-screen annotations from *drawing*, but SwiftUI still builds/diffs the full set each recompute.
- `HomeRailsViewModel`'s 13 curated shelves each distance-sorted the full **790**-tour catalog on every camera settle, and `Tour.distance(from:)` allocates a `CLLocation` + runs a geodesic calc **per comparison** → ~100k allocations per pan-release (the settle-time hitch).
- `HomeDrawerContent.toursInViewCount` computed twice per header render.

**Fix ([PR #397](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/397) → `main`, squash `51c628a`) — behavior-preserving; pin drop shadow KEPT (explicit owner call):**
- `HomeMapSection.cluster` — viewport-cull: drop markers outside the visible region **expanded by one full viewport of margin on every side** (`cullMarginViewports = 1.0`) before bucketing, so a pin is present well before it pans on-screen (no pop-in under normal panning). Absolute-grid bucketing unchanged → cluster IDs stay stable (pans update in place). Auto-disabled above `cullDisableSpan = 30°`.
- `HomeRailsViewModel.sortedByDistance` — decorate-sort-undecorate: compute each tour's distance once. `nearYouRail`/`inViewRail` routed through it. Output order identical (existing `HomeRailsViewModelTests` guard it).
- `HomeDrawerContent.countHeader` — compute the count once.

**Verification:** CI green (iOS Simulator build + unit tests + `Validate Tours.json`). **TestFlight 1.1 (10)** built+signed+uploaded via `testflight.yml` run on the branch (run_number 10). **Owner device-confirmed** the map is smoother and saw no edge-pin pop-in, then said "merge it." Squash-merged.

**Why this holds up:** these paths now scale with **what's on screen**, not the whole catalog — adding more cities won't re-introduce the slowdown. Deliberately did NOT debounce the rail recompute (would delay the drawer-list update slightly — a visible effect the owner didn't want). That's the next lever if the drawer ever feels laggy at much larger scale.

**Owed:** `claude/map-animation-performance-lw9nli` is merged; the web-session git proxy blocks branch deletion → **delete it in the GitHub UI** (or it gets swept as a stale merged `claude/*` branch).

### 2. Catalog docs resync — 509→790 / 9→14 makers / 561→969 stops

The docs had drifted **badly** — CLAUDE.md Key facts still read 509/9/561 while the live catalog (bundled `Tours.json` == gh-pages mirror, both verified) is **790 tours / 14 makers / 969 stops**. This is what made Claude quote the wrong count to the owner early in the session. Five undocumented bureaus had launched: **Bangkok (BKK 57), Paris (PAR 50), Seoul (SEL 43), Los Angeles (LAX 42), Naoshima (NAO 15)**.

Corrected in CLAUDE.md Key facts + added Current State entries; added a session-60 ROADMAP status. Historical Current State / ROADMAP status snapshots left as-is (dated).

**Accurate catalog now (verify against `Resources/Tours.json`, don't trust older doc numbers):**
- **790 tours / 14 makers / 969 stops.** Per maker: NYC 100 · LDN 100 · KYO 71 · LIS 66 · TYO 63 · BKK 57 · OPO 54 · HKG 52 · PAR 50 · SEL 43 · YYZ 42 · LAX 42 · SFO 35 · NAO 15.
- **760 single-stop + 30 multi-stop.** Multi-stop by maker: LDN 5, PAR 5, SFO 4, YYZ 4, LAX 4, SEL 3, NYC 2, NAO 2, BKK 1.
- **Bilingual titles** (`English | native`): TYO/KYO/NAO `日本語`, HKG `中文`, SEL `한국어`, BKK `ไทย`.

## Notes for next session
- The catalog is remote-loaded (Supabase `get_catalog` primary, gh-pages mirror fallback). It grew via parallel content sessions; **always re-count from `Resources/Tours.json` before quoting numbers.**
- Session 59's Journeys + Group Listen (TestFlight 1.1 (7)/(8)) are the prior code milestones — see `HANDOFF-260719.md`.
- No app-code follow-ups owed from this session beyond the branch deletion.
