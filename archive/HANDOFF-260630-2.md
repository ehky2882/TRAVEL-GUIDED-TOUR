# HANDOFF — session 47 (2026-06-30, code — V2 Step 3 completed: Google + sync + fixes)

**Session-end snapshot.** V2 **Step 3 (accounts & auth) is complete and
shipped in TestFlight 1.0 (56).** This session took the auth foundation
(email, from PR #262/build 51) through **Apple, Google, full cross-device
sync, and two follow-up fixes.** All merged to `main`; owner uploaded each
build via Organizer.

*(Note: the same-day `HANDOFF-260630.md` is a **different, parallel** web/PM
session — the Tokyo content launch. This one is the code arc.)*

## Build arc (all live except 56 pending owner device retest)

| Build | PR | What |
|---|---|---|
| 51 | #262 | Email sign-in + `supabase-swift` foundation (prior session) |
| 52 | #274 | **Sign in with Apple** — device-confirmed |
| 53 | #277 | **Sign in with Google** |
| 54 | #279 | **Library + saved-makers sync** |
| 55 | #283 | **Logout privacy clear** |
| 56 | #285 + #287 | **Recents-recording fix** + **recently-viewed sync** |

## Sign in with Apple (#274, build 52)

Native `SignInWithAppleButton` → `AuthService.signInWithApple(idToken:nonce:)`
→ `supabase.auth.signInWithIdToken(.apple)`, SHA256-hashed nonce (raw to
Supabase) for replay protection. Added the `com.apple.developer.applesignin`
entitlement (`TRAVEL GUIDED TOUR.entitlements`; `CODE_SIGN_ENTITLEMENTS` on
both app-target configs, set via `mod-pbxproj`). **Owner setup:** App ID
capability enabled + Supabase Apple provider with the bundle id in Client IDs.
**Build-cut change: archive now needs `-allowProvisioningUpdates`** (the new
capability must regenerate the auto profile — first archive without it failed
on a stale profile). Device-confirmed by owner.

## Sign in with Google (#277, build 53)

supabase-swift's OAuth **web** flow — `signInWithOAuth(provider: .google,
redirectTo: dozent://login-callback)` via `ASWebAuthenticationSession`. **No
Google SDK.** Registered the `dozent` URL scheme in `Info.plist`
(`CFBundleURLTypes`) + `SupabaseConfig.oauthRedirectURL`. **Owner setup
(hand-held, done):** Google Cloud → new "Google Auth Platform" UI (Branding +
Audience = the old consent screen; **Clients** = the OAuth client) → **Web
application** client with redirect `https://apkcihljybvuyuzpbnqd.supabase.co/
auth/v1/callback` → client id + secret into Supabase Google provider +
`dozent://login-callback` in **Authentication → URL Configuration → Redirect
URLs**. Verified `/auth/v1/settings` → `google:true`.

## Sync (#279 library+makers, #287 recently-viewed, #283 logout-clear)

**`Data/SyncService.swift`** (`@MainActor @Observable`). Model: on sign-in
**pull → merge (union) → push**; while signed in **write-through** each local
change, debounced 2s, as a full-state replace (upsert + `delete-not-in`) via
supabase-swift PostgREST. Anonymous = on-device only, unchanged.

- Stores (`LibraryStore`, `SavedMakersStore`, `RecentlyViewedStore`) each got
  an `onChange` write-through hook + `applyMerged(_:)` (sets merged state
  without re-firing the hook). Wired in the App entry's `.task` (captures the
  live instances).
- **Merge is pure + unit-tested** (`SyncServiceMergeTests`, 7 cases): union;
  saved/completed survive from either side; `listenedSeconds` max, `lastListened`
  later; **`downloadedAt` stays device-local**; recently-viewed keeps the
  latest `viewedAt`.
- **Logout clear (#283):** sign-out `applyMerged([])` on all three stores —
  **local-only, no remote delete** (restored on next sign-in). Anonymous users
  never fire it (nil→nil isn't an observable change).
- **`RecentlyViewedStore` reshaped (#287):** now stores
  `RecentlyViewedEntry{tourId, viewedAt}` (the table needs a timestamp).
  **Same UserDefaults key** — old `[String]` is read via `stringArray` and
  migrated; new format via `data`/JSON; the two are distinguished by value
  type, so migration is clean AND the existing store tests' key cleanup still
  isolates state. `tourIds` is now a computed accessor (Home rail unchanged).
- Tables (already applied via `accounts.sql`): `user_library`,
  `user_saved_makers`, `user_recently_viewed`; RLS own-row (`auth.uid()`).

## Recents-recording fix (#285, build 56)

**Bug:** the Library "Recents" list stopped updating for tours played via the
**mini-player**. **Root cause:** progress was recorded **only inside the
full-screen `PlayerView`** (its `.paused`/`.ended`/`.onDisappear`), but
"Start Tour" plays through the mini-player and does NOT auto-open the full
player — so listening without expanding never recorded anything. **Not
sync-related** (affects anonymous too; only *saving* was fine).

**Fix:** moved recording into **`AudioPlayerService`** (owns state +
`currentSourceId` = the tour + `currentTime`): new `onProgressCheckpoint`
hook fires on **pause / end / stop / interruption** regardless of UI, wired at
app start to `LibraryStore.updateProgress`. Removed the redundant
`writeProgress` from `PlayerView` (kept its end-of-playback handling). Bonus:
`.ended` passes `completed:true` → finished tours are now marked completed
(the old code always wrote `completed:false`).

## Verification

- `test_sim` **120/120** at session end (113 + 6 sync-library + 1 recently-viewed).
- Each build archived with `-allowProvisioningUpdates`; binaries greped for
  version / `UIRequiresFullScreen` / applesignin / `dozent` scheme / supabase
  host / no `TEMP_LOCAL_DEMO`.
- **Owner device-confirmed:** sign in/out; saved tours/makers/recents wipe on
  sign-out + reappear on sign-in.
- **Pending owner device retest (build 56):** the Recents-updating fix (play
  via mini-player → pause → Recents shows) + recently-viewed cross-device.

## Process notes / gotchas

- **Sim can't verify the network/auth paths:** email-confirm is ON (can't
  click the link in-sim), Apple/Google are device features, and audio
  playback + the secondary-window tab bar don't automate reliably
  (XcodeBuildMCP tap/touch are elementRef-only; the tab buttons + the detail
  play control aren't a11y-exposed). So auth/sync are device-verified; merge
  logic is unit-tested; the rest is compile + reasoned.
- **Stacked-PR + squash snag:** #287 (recently-viewed) was stacked on #285;
  merging #285 with `--delete-branch` auto-**closed** #286 (base branch gone).
  Recovery: `git rebase --onto main HEAD~1` the recently-viewed commit onto
  main (dropped the now-merged #285 commit, applied clean), force-push, open a
  fresh PR (#287). Lesson: don't `--delete-branch` a base that another PR
  stacks on; or rebase children first.
- **`-allowProvisioningUpdates` is now standard** for every archive of this app
  (since the Apple capability).
- Owner **can't see the Simulator window** on their Mac (some local display/
  Space issue) — irrelevant: the device stays booted and screenshots read the
  framebuffer headlessly; and all meaningful review here is on-device anyway.

## Next

- **"Report a concern" → `reports` table** (small; the last Step-3 tail item).
- Optional **"Sign in to sync" Library nudge** — owner's friction-free
  sign-up idea (they decided NOT to gate saving).
- **Step 4 — maker authoring UI** (`Features/Maker/`), the next major arc.
- **Cleanup:** delete throwaway `auth.users` test rows in the Supabase dashboard.

## Addendum — "Report a concern" + build 57 (same session, later)

Shipped **"Report a concern"** (PR #290) and cut **TestFlight 1.0 (57)** (build
bump #292; also carries the parallel-session sync flush fix #291). Live 2026-06-30.

- **App:** the ••• menu item (tour / player / maker) now presents
  `Features/Report/ReportSheet.swift` → inserts into `reports` via
  `Data/ReportsService.swift`. **All three `mailto:` links (with the hardcoded
  owner email) removed** — the address no longer ships in the client.
- **Two bugs found + fixed during sim/live testing:**
  1. **Insert RLS-blocked (42501)** — the default insert does a
     `return=representation` read-back, but `reports` is admin-read-only, so the
     SELECT RLS failed the whole insert. Fix: `insert(row, returning: .minimal)`.
     Curl-verified HTTP 201. (Also: the live DB was missing the `reports` insert
     grant/policy from `accounts.sql` — owner re-applied it in the SQL Editor.)
  2. **Crash on open** — `No Observable object of type AuthService found`: the
     tour-detail + player are UIKit-hosted layers that don't propagate the SwiftUI
     environment, so `ReportSheet`'s required `@Environment(AuthService.self)`
     crashed. Fix: made it optional (`AuthService?`); absent → anonymous report.
- **Diagnosis tricks that worked:** `set local role anon; insert …; reset role;`
  in the SQL Editor to test the policy directly (bypassing the key/PostgREST
  layer); `simctl launch --console-pty` + a temp `showingReport = true` to
  headlessly capture the SwiftUI fatalError (the ••• menu + toolbar aren't
  a11y-exposed to XcodeBuildMCP, so the menu can't be script-driven).
- **Pending (owner):** email notifications — deploy `notify-moderation` + Resend
  key + `MODERATION_EMAIL=edward.yung@gmail.com` + a `reports` INSERT webhook
  (owner said they'll do Resend next). Owner declined a new email service at
  first; options discussed (dashboard-only / Discord-Slack webhook / Resend).
  Until then, reports are reviewed in the Supabase Table Editor / `moderation_queue`.
- **Report-table cleanup:** owner deleted the pipeline test rows via the SQL Editor
  (`delete from public.reports where details ilike '%pipeline test%' …`) — the app
  key can't delete (admin-only), which is the correct posture.
