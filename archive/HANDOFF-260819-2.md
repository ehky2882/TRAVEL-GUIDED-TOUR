# HANDOFF 2026-08-19 (2) — the website wears the app's clothes

Session 98. **[PR #539](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/539)**
(squash `f24bf879`), branch `claude/site-app-typography`, **merged and LIVE on
`dozent.world`**. Website only — 8 files, none of them Swift. No build, no SQL,
no catalogue change. The iOS app, `Tours.json` and the gh-pages CDN are untouched.

## What the owner asked for, in order

1. *"can we reconcile so that its aesthetics are more similar to my app? for
   example, use all the same fonts"*
2. *"make everything caption text"* → then *"except dozent logo/title"*
3. *"it doesnt look like all fonts are the same size. also, flatten all font
   color and unbold"*
4. *"for now, just put everything in dark mode"*
5. *"the footer should be on all pages. but i dont want it to list my full name"*
6. *"the current homepage should go to a 'About Dozent' page/tab. The homepage
   should have a logo and wordmark, exactly like my load page, and then below
   that it should say 'Coming Soon'"*
7. *"the links at the bottom should all be one row"*
8. *"the links/footer should not be scrollable and should always reside at the
   window's bottom … also make Dozent wordmark Title Case"*
9. *"it jumps around"* + a sliver below the footer + *"a thin white line at the
   top of the footer"*
10. spacing between footer and body, then *"more on the bottom"*

## The shape it landed in

    /                  the splash — pulsing brass mark, wordmark, COMING SOON
    /about/            what used to be the home page
    /privacy/          /terms/  /acceptable-use/
    /atlas.css         one stylesheet, linked by all five

**`site/atlas.css` is a port of `Theme/Atlas*.swift`** and every token names its
Swift counterpart in a comment. Nothing enforces the link — **if a value changes
in Swift, change it here in the same session.**

- **Everything is caption**: SF Mono 13px, **one size, one weight, one colour**.
  Hierarchy is carried by case, tracking and spacing alone.
- **The wordmark is the one exemption**, in face and colour: New York serif,
  15px, tracked 2, Title Case — identical to the splash's, brass on the prose
  pages and white on the splash.
- **Dark only, for now.** `prefers-color-scheme` is not answered at all; a
  visitor on a light system still gets black. The file says how to put the
  adaptive palette back — every rule reads tokens, so it is a `:root` change.
- **`/` is a port of `SplashView.swift`**, verified against it: 44px circle in
  `rgb(139,117,53)`, `splash-pulse 0.8s ease-in-out alternate`, wordmark
  `ui-serif 15px / 2px / white`. Pulse suppressed under Reduce Motion.

## Five things that cost real debugging — worth not rediscovering

1. **🔴 A heading does NOT inherit `font-size` from `body`.** Removing `h2`'s
   explicit size on the assumption it would inherit left it at the browser
   default `1.5em` — **19.5px against everything else's 13px**, which is what
   the owner spotted as "not all the same size". Fixed at the root with a
   `body, body *` reset declaring size, weight and colour, so no browser default
   can leak in when a new tag appears. **That reset is load-bearing; leave it.**
2. **🔴 The footer jumped between pages for TWO reasons, and the second is
   invisible.** Each page omitted its own link, so the row's labels and width
   differed everywhere — fixed by making the footer markup **byte-identical on
   all five pages**. But it still moved **7px**, because the splash fits its
   window and has no scrollbar while every policy page does, shifting the
   centred column. `html { overflow-y: scroll; scrollbar-gutter: stable }` is
   what holds it still. **Undoing either brings the jump back.**
3. **⚠️ `position: sticky` cannot make a footer sit at the window bottom on a
   page shorter than the window** — it only ever shifts an element *up* to keep
   it visible, never down. `/about/` above ~1060px tall proved it. The footer is
   therefore `fixed`, and its height has to be reserved by hand: that is
   `--footer-h`, with a larger value under 500px where the link row wraps.
   **Add or rename a footer link and BOTH the token and the breakpoint need
   re-measuring** — shortening two labels moved the wrap point 570px → 500px and
   left the phone reserve 12px too small, which would have hidden the last line
   of every policy behind the bar at 375px.
4. **🔴 A pinned bar must be `fixed`, never `sticky`, once `html` is a scroll
   container — and lesson 2's gutter fix makes it one.** A sticky bar sticks
   to *that scrollport*, and on iOS the scrollport does not track the visible
   top as the address bar collapses and expands, so the bar drifts and page
   text shows past it. Reported twice from a phone. The tell: the **`fixed`
   footer was fine on the same page while the `sticky` masthead was not**.
   Both bars are `fixed` now, each reserving its height through a token.
   **⚠️ A `box-shadow` backstop does NOT fix this** — tried first, measured
   perfectly, and the owner still saw the sliver, because the shadow is
   painted relative to the element and drifts with it. Kept on both bars as
   belt-and-braces against sub-pixel rounding, but it is not the fix.
   **⚠️ Nothing on a desktop engine reproduces this**, and `xcode-select` is
   not pointed at Xcode on this Mac so the Simulator would not start
   (`sudo xcode-select -s /Applications/Xcode.app/Contents/Developer`, needs
   the owner's password). Owner confirmation on a real phone was the only
   proof — say plainly which parts are measured and which are unverified.
5. **⚠️ The browser pane cached `atlas.css` across edits and screenshots came
   back stale, off-centre, and eventually all black.** A measurement that "did
   not move when it should have" was the only tell. **Verify layout by reading
   the DOM (`getBoundingClientRect`, `getComputedStyle`), not by looking at the
   pane**, and append a cache-buster to the stylesheet href when measuring in an
   iframe. Every claim in the PR was measured this way — 30 checks across 5
   pages × 6 window sizes.

## Owner decisions recorded

- **The footer stays on the splash**, despite *"That's it"* — it is what the
  previous instruction asked for, and without it the policies are reachable only
  by typing their URLs while **Stripe's platform review cites the acceptable-use
  policy and Apple's reviewer follows the privacy link**.
- **"COMING SOON" is rendered uppercase** though the owner typed it title case,
  because every other label on the site is uppercase. One word to change.
- **The footer attribution reads "Dozent is operated by AHWY/EHKY. © 2026."**

## ⚠️ Still open

- ~~The owner's full name in the body of `/terms/` and `/privacy/`~~ **CLOSED
  2026-08-19** — owner: *"replace all mentions of Edward Ho Kiu Yung with
  AHWY/EHKY. I'll take the chance."* Both now read *"operated by AHWY/EHKY, a
  sole proprietor"*; the name appears nowhere on the site. **The concern was
  accepted knowingly, not resolved:** those sentences are what Apple's reviewer
  and Stripe's platform review read, and initials identify the operator more
  weakly than a legal name. The LLC decision is still the real fix.
- The policy pages are long legal prose at 13px monospace. The owner has not
  said whether that reads acceptably; exempting prose on those two pages only,
  keeping every heading and label in caption, is a few lines if it does not.
