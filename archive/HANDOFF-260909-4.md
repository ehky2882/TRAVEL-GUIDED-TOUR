# Handoff — 2026-09-09 (session 152)

**One job: a batch of new tour links.** The owner sent 62 TikTok links from
`@urbanistariel` with Plus Codes or coordinates beside most of them. 61 shipped as
[#777](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/777); one is held back and needs an
owner decision.

**`linkPins` 1,489 → 1,550.** `tours`, `makers` and `places` byte-identical — the creator's
maker row already existed (149 pins live) and was reused, not re-minted.

| Cluster | Pins |
|---|---|
| Greece | 28 — Athens 14 · Mystras 2 · Ancient Messene 2 · Mykonos 2 · Filiatra 2 · Chania · Sparti · Nafplio · Megara · Pyrgos Dirou · Gytheio |
| New York area | 13 — Manhattan 8 · Brooklyn/Coney Island 3 · Hoboken · Weehawken |
| France | 9 — Paris 8 · Mont-Saint-Michel |
| Puerto Rico | 5 — San Juan 4 · Loíza |
| Germany | 5 — Munich 3 · Schwangau · Ettal |
| Scotland | 1 — Edinburgh |

Greece is the headline: the catalogue had 7 Greek pins before this and now has 35.

---

## 🔴 The find: a short Plus Code recovers against its reference, and the reference was wrong

Two links arrived labelled **`Lower Town, Greece`**. Read as **Monemvasia** — a real, well-known
Peloponnesian lower town — both codes recovered to **37.074, 23.367: eighty-eight kilometres out,
in the Myrtoan Sea.**

Both captions say **Mystras** outright, the Byzantine city beside Sparta. Recovered against a
Sparta reference, both land on the site, 1 km apart, exactly where the captions describe.

**Nothing downstream would have caught this.** `validate-tours-mirror.py` passes, CI compiles,
every URL 200s, and the pin sits in the sea and never fires. It was caught only by reading each
recovered coordinate back against what its own post was about — which is now the habit, and is
written into `docs/lessons.md` § 4.

The general shape: **a vague locality (`Lower Town`, `Old Town`, `Centro`) is not a place name,
it is an unresolved reference**, and a short Plus Code has no absolute position to fall back on.

All 43 supplied-coordinate pins and the 18 I geocoded from captions were read back the same way.
Every other one agrees to within metres of its named subject — the Cyclone, Tom's at 112th &
Broadway, the Hamilton–Burr duel site, the Dimitrios wreck at Valtaki, the Real Mary King's Close,
the Choragic Monument of Lysicrates.

## ⚠️ A hero filename already existed on gh-pages, unreferenced

`castillo-san-felipe-del-morro-urbanistariel_hero.webp` was already on `gh-pages` with **different
bytes** and **zero references in the catalogue** — an orphan from some earlier run (the shallow
fetch could not date it). Published as `castillo-san-felipe-del-morro-2-urbanistariel_hero.webp`
rather than overwriting a live URL. Cost: nothing. The rule it respects is the one that matters —
a phone that has downloaded a tour reads its photographs off its own disk and would never see a
correction made in place.

## ⚠️ A check that exited 0 having fetched nothing

`check-image-duplicates.py --pins` was run immediately after pushing the heroes and printed
**`OK — no suspicious duplicates`** — while 404ing on all 61 new files, because GitHub Pages had
not deployed yet. It says so in its own WARN lines, and the verdict line does not.

**Re-run after the deploy landed: 1,543 images fetched, 0 failures, no duplicates**
(stamp `2026-09-09T12:16:21Z`). The shared-URL half — which needs no network — read 0 errors on
both runs. This is `CLAUDE.md` § Reading a check's result happening live; the counts are what
distinguish the two runs, not the verdict.

## Titles are the subject, not the caption

`make-link-pin.py --batch` ignores `--title` and `--slug` (single-`--url` runs only), so a batch
here would have put *"🧐 Why go to Paris when you can go to Southern Greece? This is th…"* on the
map. Each pin was minted individually instead — 61 runs, output redirected per-pin to files so no
pin JSON entered the conversation.

Two titles clashed with pins already live **from the same creator** and were disambiguated rather
than shipped as twins: **The Parthenon: The Missing Roof** and **Central Park: How It Was
Designed**. (A third clash, `Central Park`, is `@archiwhisperer`'s and was left alone.)

An earlier validate run raised **27 tag-facet warnings** — pins missing a Place-type or Theme tag.
All fixed; `LGBTQ+` was added to **Stonewall**, **Julius'** and **Eve's Hangout**, where it was
plainly owed and simply had not been reached for.

---

## 🟡 Owed: one link needs the owner

**`https://www.tiktok.com/@urbanistariel/video/7255575696582446378`** — *"Where have you seen a
building that looks like The Parthenon in Athens, Greece? They're everywhere!"* It arrived with no
location **and it has none**: the video is about neoclassical buildings worldwide, not a place. It
also overlaps in subject with the **The Parthenon** pin already live from 8 September.

Either name a coordinate for it or drop it. Everything else in the batch is wired.

## Checks

- `validate-tours-mirror.py` — **0 errors, 0 warnings** across 1,552 tours + 1,550 pins +
  140 places; selftest **32/32**, control clean.
- All 61 ids reproduce from the documented `uuid5` scheme; no pin, stop or maker id collides
  with anything live; no hero URL collides with a live one.
- `check-image-duplicates.py --pins` — clean on the re-run (see above).
- **No sourceURL overlap with the open #749** (54 pins, another session's, conflicted and 1,390
  pins behind) — checked link by link against that branch's catalogue. The two batches are disjoint,
  but both touch `Tours.json`, so whichever merges second rebuilds.

## Counts, re-derived on this branch

**1,552 tours · 1,550 pins · 353 makers · 1,924 tour stops (3,474 incl. pins) · 140 places ·
442 cities · 63 countries.** `CLAUDE.md` § Key facts updated in the same commit — it had read
1,489 pins and 427 cities. ⚠️ Re-derive before quoting; `main` moves under this constantly.

## Environment note

**Pillow is not installed in a fresh web-session container**, and `make-link-pin.py` correctly
refuses to write an uncropped hero without it — 61 loud failures and 61 zero-byte placeholder
files, rather than a batch of letterboxed images. `pip3 install Pillow` first. Two further pins
failed on a transient `SSL_ERROR_SYSCALL` to TikTok and succeeded on retry.
