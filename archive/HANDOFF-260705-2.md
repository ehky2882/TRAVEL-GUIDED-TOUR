# HANDOFF — 2026-07-05 (session 57, part 2): Tag Phase 2 + Dozent identity → TestFlight 1.0 (72)

## What shipped this session
Two code/asset PRs merged to `main` and cut into **TestFlight 1.0 (72)** (bump [#354](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/354)):

1. **[PR #352](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/352) — Tag Phase 2 (the visible switch).** Built this session, turn-by-turn sim review with the owner. Turns the Phase-1 tag vocabulary into browsable Home UI. See the CLAUDE.md "Current State" block for the full feature list. Key files:
   - `Models/Tag.swift` (new) — vocabulary value type; **single edit point** for the curated shelf order (`Tag.curatedShelves`) + filter-chip set (`Tag.filterChips`) + the D6 matcher + D5 `derivePrimary`.
   - `Features/Home/HomeRailsViewModel.swift` — curated shelves + near/far location-rail swap (§1.5) + `filteredResults()`.
   - `Features/Home/TagFilterChipRow.swift` (renamed from `CategoryChipRow`) — multi-select filter chips + "Walks".
   - `Features/Home/HomeDrawerContent.swift` — shelves-vs-flat-results, `FilterResultCard` (full-width 4:3 hero, **195pt** = rail height).
   - `Features/Home/HomeView.swift` / `HomeSharedState.swift` — `selectedTags: Set<String>` + `walksOnly` replace `selectedCategory`.
   - `scripts/validate-tours.swift` — enforces the vocabulary (unknown = error; missing Place type/Theme = warning).
   - Tests: `TagTests.swift` (new) + rewritten `HomeRailsViewModelTests.swift`; `TestFixtures.makeTour` gained a `tags:` param. **159/159**.
2. **[PR #353](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/353) — Dozent identity** (parallel session): brass app icon (replaces the placeholder green sphere) + rename to **Dozent** (`CFBundleDisplayName`).

## Review decisions locked (owner, at the sim)
- Filtered results = **full-width visual cards, not shelves-while-filtered** (shelves repeat a tour across shelves + collapse under an AND).
- Card hero **195pt** (= rail height) — ~2 per screen, slight crop, one card size app-wide. (The taller uncropped 4:3 was rejected.)
- **Continue-listening filter-scoped** (out of the results view — mini-player covers resume).

## Build 72 cut
- Bump 71→72 via short-lived PR #354 (admin-merged, metadata-only); archived from a `/tmp/build72` worktree off `origin/main` with `-allowProvisioningUpdates` → `/tmp/Dozent-20260705-b72.xcarchive`.
- **Binary-verified:** `1.0 (72)`, `CFBundleDisplayName=Dozent`, `UIRequiresFullScreen=true`, mic key, `applesignin`+`associated-domains`, AppIcon baked in, Supabase host compiled in, **no `TEMP_LOCAL_DEMO`**.
- **Owner uploading via Organizer** (Distribute App → Upload).

## NEXT
- **15 tag-backfill tours** — the validator now warns on tours with no Place type / Theme tag (cable cars, tunnels, miradouros, trams). Content-only, ships over-the-air (no build). Cheap win now that tags are visible.
- **App-icon / GPS field-test** were the other two next-work candidates discussed (icon now done via #353; GPS field-test needs the owner walking a multi-stop tour — AMNH Four Facades).
- **Tag Phase 3** (drop `primaryCategory`/`TourCategory` + migrate the ~12 category-reading consumer surfaces to `primaryTag`) — LATER separate session; let the tag UI settle first.

## Gotchas re-confirmed
- XcodeBuildMCP session defaults reset across turns — re-run `session_set_defaults` (worktree project + iPhone 17 Pro / iOS 26.5) before each build; the persisted default reverts to Desktop / iPhone 16 Pro.
- `gh pr merge` prints `fatal: 'main' is already used by worktree` during its local-checkout step but the squash lands server-side — verify with `gh pr view --json state`.
- Archive from a **clean worktree off `origin/main`** + grep the binary before upload (the poisoned-build-47 lesson).
