# HANDOFF — 2026-08-31 (session 128) — 105 link pins from one NYC historian

**Branch:** `claude/link-tours-upload-qw2l2a` (restarted clean off `origin/main` at `308ad23`
— the previous PR on this branch name, #674, is merged, so this is a fresh change and not
stacked on merged history). **Commit `4f7a881c`, pushed. NO PR OPENED** — this session's
harness forbids opening one unasked.

**Content only.** No Swift, no SQL, no build, nothing for the owner to run.

**Catalogue: linkPins 378 → 483. Tours (1,552), makers (208) and places (56) unchanged.**

---

## What arrived

107 pasted links → **105 distinct posts**, all TikToks by **`@hereinnyc`**, a New York history
creator. Two exact repeats: `7534841980715584799` pasted twice with different `web_id`
parameters (TikTok query params are stripped by `normalize_url`, so both canonicalise
identically), and `7381954476334206251` pasted twice with two Venice coordinates 2 m apart.
One post carried two coordinates 0.9 m apart; the first was taken.

**64 posts carried a coordinate, 41 did not.**

**✅ ALL 105 WERE ALIVE AND PINNABLE** — no dead posts, no `/photo/` carousels, and **no
`5009` CDN refusals** (the failure class the previous batch hit on 13 of 95 thumbnails).
105/105 oEmbed payloads complete, 105/105 thumbnails downloaded first try.

**This is the largest link-pin batch to date, against a previous record of 95.**

---

## The creator burns a title card into every frame, and that is what made 105 tractable

Every one of the 105 thumbnails carries the post's title in a black card over the picture.
That turned the open-every-hero audit from a guessing game into reading, and it **settled four
subjects the captions alone would not have**:

- **#3 Big Apple Corner** — the caption only says "it has to do with horse racing"; the frame
  carries a **`W 54 St`** sign, which confirms the supplied coordinate at Broadway and 54th.
- **#46 The Pearline Ghost Sign** — the frame includes the **`2 Avenue & East 72 Street`**
  subway entrance sign, confirming the coordinate exactly.
- **#56 Jericho Cider Mill** — the bag in shot prints **`213 ROUTE 106 JERICHO, NY 11753`**,
  which is the address the geocode then resolved.
- **#32 The Schinasi Mansion** — the caption names no building; the reverse-geocode gives
  **351 Riverside Drive** and the frame shows a free-standing white marble Beaux-Arts mansion,
  which is exactly what stands at that address. Two independent confirmations.

**Zero wrong subjects across all 105.**

---

## Coordinates

**All 64 supplied coordinates were reverse-geocoded at zoom 18.** Several land on a neighbour
and are still right — the documented pattern:

- **#1 Breakfast at Tiffany's** reverse-geocodes to *164 East 72nd Street*, but a forward
  geocode of **169 East 71st Street**, the actual Holly Golightly brownstone, sits **26 m**
  away. Well inside noise; the supplied point is the opposite pavement.
- **#78 The Hayes Theater** reverse-geocodes to the *Broadhurst Theatre* across 44th Street.
- **#47 The Red Room** reverse-geocodes to *2 Wall Street*; the Red Room is in **One** Wall
  Street, immediately adjacent, and the owner's own note on that link read "Red Room Bar".

**The 41 without coordinates** were identified from caption + frame, then forward-geocoded and
reverse-verified. **Two needed a bounded viewbox**, the documented fix:

- **🔴 `1 Cherry Street` unbounded matched Johnson City, Broome County — 250 km upstate.**
  Bounded to Manhattan it resolves onto Cherry Street; the Samuel Osgood House site is beneath
  the Brooklyn Bridge approach, which is where #91 sits.
- **The 91st Street Garden** returned nothing by name at all; probing the Riverside Park
  promenade found **`Riverside Walk`** at `40.7933, -73.9803`.

### The one coordinate finding worth carrying

**#9 and #61 are two different Park Avenue subjects, and the supplied coordinate for #9
reverse-geocodes to #61's subject.** #61 is explicitly *"Inside the Park Avenue Tunnel"* and
its frame is a tunnel; #9 is *"Restoring Park Avenue"* and its frame is a **planted mall with
benches and paths**. The owner's #9 coordinate resolves to "Park Avenue Tunnel" because that
is the nearest named feature. Resolution: **#9 keeps the owner's coordinate**, and **#61 was
placed at the tunnel's south portal** (`40.7472, -73.98075`, reverse-verified to *9 Park
Avenue, Murray Hill*), putting ~190 m between two pins that would otherwise have sat 23 m
apart under confusingly similar names. ⚠️ **#9's hashtag says `#uppereastside` while its
coordinate is Murray Hill — flagged, not overridden.**

---

## Stack cap: checked, and clear

`TourSetMap.maxStacked` is **3** on the maker page, and this batch is essentially one creator,
so coincident pins matter. **The deepest coincident group in the batch is 2**:

| pins | distance | site |
|---|---|---|
| #39 · #89 | 0.0 m | Gracie Mansion |
| #11 · #14 | 2.0 m | Madison Square Garden |
| #27 · #75 | 9.0 m | Riverside Church |
| #24 · #54 | 9.4 m | the Merchant's House, East 4th Street |

**Nothing is unreachable.** Two pins on one building is the Westin Bonaventure precedent — the
owner sent both links, so both ship with distinct slugs.

---

## Flagged, not resolved

- **🔴 #15 and #83 are two frames of the SAME vintage subway carriage.** Perceptual Hamming
  **6 of 256** with a 32×32 thumbnail diff of **12.2** — every other pair in the batch sits at
  32+ / 30+, the tonal false-positive band. Opened side by side: same carriage, same camera
  position, differing only in the burned-in title card. **The creator reused the footage.**
  Consequence: **#15 is pinned at Yankee Stadium and its hero shows a Transit Museum
  carriage** (its caption is that the song was written on the subway). Not byte-identical, so
  the byte check passes; a link pin re-hosts only the thumbnail, so **no other frame exists**.
- **⚠️ #92 is a paid advertisement** — a Coca-Cola partnership, marked **`AD`** in the frame
  and opening `AD |` in its own caption. It is still a real location post (Marilyn Monroe
  photographs at Bowling Green) and ships, but pinning an ad is the owner's call.
- **⚠️ #35 is the one building this batch could not name.** "Would you move into this Gilded
  Age mansion? #upperwestside", a Beaux-Arts mansion near Riverside Drive and 86th–87th. The
  Isaac L. Rice Mansion was checked and is **172 m** away, so it is not that. It ships under
  the creator's own frame title, **"A Gilded Age Mansion for Sale"**, asserting no attribution
  — the Banksy precedent.
- **⚠️ Weak heroes, all the creator's own frames:** #7 (a bar interior for The Slide), #11 and
  #80 (talking heads), #12 (a sepia portrait), #38 / #60 / #99 (food close-ups).

---

## Seven hand re-crops — the vertical `--focus` gap, eleventh batch running

**Every source is 9:16**, so the square crop is width-limited and `--focus` does nothing at
all. Seven title cards were sliced and were re-rendered through a **mirror of the tool's own
pipeline** (importing `make-link-pin.trim_bars` so the two cannot drift), at the same
filenames, with a vertical focus the tool has no lever for:

`#13` 0.74 · `#14` 0.72 · `#21` 0.72 · `#24` 0.74 · `#37` 0.74 · `#51` 0.74 · `#56` 0.74

**⚠️ A programmatic clipping detector was written and then discarded as untrustworthy** — it
measured "a dark card touches the bottom edge", which is not the same as "text is cut". It
**missed #13** (whose card sits over a bright statue) and flagged ~22 heroes whose padding was
cut but whose text was entirely legible. All 105 outputs were eyeballed instead.

---

## The handle suffix earned its keep once

**`images/empire-state-building_hero.webp` is the live Atlas NYC tour's own hero.** A bare
slug would have written over it, and since #567 a phone that has downloaded that tour reads
its photographs off its own disk and would never see the correction. The bare-slug sweep found
**1 of 105** would have collided.

**The maker row already existed** (`5657EA40-DA53-552F-823A-D040F31FF526`) and the uuid5 scheme
over `atlas-maker:tiktok:@hereinnyc` reproduced it exactly, so it merged rather than
duplicating. **The regenerated avatar was byte-identical to the live file** (sha256
`e6478f7c…`) and was **excluded rather than overwritten** — the `@urbanistariel` case, seventh
time. **106 generated, 105 uploaded.**

---

## Tags and architects

Every pin carries ≥1 Place type and ≥1 Theme. **Exactly one architect tag in the whole batch:
`Giles Gilbert Scott`** on #16, whose caption explicitly says *"We have one person to thank for
the iconic red phone booths in London…Sir Giles Gilbert Scott"* — authorship, named in the
source. It carries **`Designed by a Master` alongside**, never replacing.

**Deliberately NOT tagged, under the caption-driven (Jules Dalou) rule** — all are in the
vocabulary, and none is named by its post: **Christopher Wren** (#18 Painted Hall),
**McKim, Mead & White** (#44 / #94 / #95 Penn Station), **Calvert Vaux** (#97 Bow Bridge).

**Verified absent from the vocabulary:** Raymond Hood (Daily News Building), Warren & Wetmore
and Reed & Stem (Grand Central), Ralph Adams Cram and Heins & LaFarge (St John the Divine),
Shreve, Lamb & Harmon (Empire State), Hildreth Meière (the Red Room mosaic), Frederick Clarke
Withers (Jefferson Market). Adding them is a `Models/Tag.swift` **code** change, kept out of a
content batch.

---

## Place candidates — flagged, none created

`check-place-candidates.py` goes **10 EXACT / 30 NEAR → 11 EXACT / 41 NEAR**.

**The one new EXACT group is this batch's own Gracie Mansion pair (#39 · #89)** — two posts
about the same building on the same geocoded node. That is honest rather than manufactured:
they genuinely are one site, and it is a real candidate. **The pins were deliberately not
nudged apart to dodge the checker.**

The **+11 NEAR** are new pins beside existing Atlas tours of the same subject — Empire State
Building (92 m), Madison Square Garden (19 m, 20 m), Yankee Stadium (31 m), Riverside Church
(9 m, 253 m, 256 m), Times Square (100 m, 256 m), Wall Street / the Red Room (108 m). ⚠️ The
Yankee Stadium pair sits beside the **existing Yankee Stadium place**, which already has 6
members; #15 would be a seventh.

---

## Verification

- **Validator mirror** — vocabulary parsed from **both** `Models/Tag.swift` **and**
  `scripts/validate-tours.swift`, refusing to run if they disagree or either parse is empty
  (they agree at **385 tags across 5 facets**) — **self-tested against 42 injected fault
  classes, 42/42 caught**, then **0 errors, 2 warnings** across 1,552 tours + 483 pins + 56
  places. **Both warnings are pre-existing**: the same mirror against `origin/main` reports the
  identical pair (VIA 57 West's transcript gap, Bedrock Caverns' deliberate null
  `walkingDistanceMeters`).
- `make-link-pin.py --selftest` **71/71** (62/62 without Pillow, which a fresh container lacks
  — install it before reading that as a pass).
- **0** duplicate tour/stop/maker ids in the batch or against the catalogue; **0** already-pinned
  sourceURLs; **0** byte-duplicate heroes.
- **gh-pages `f94e16fa`**: the remote head was **re-read in the same command as the push**, and
  the tree diff is **exactly 105 additions, 0 deletions, nothing outside `images/`**. The Pages
  deploy read **`in_progress`, never `cancelled`**.
- `Tours.json` **byte-stable under a Python re-dump before editing**; diff **4,725 insertions /
  0 deletions**.
- `seed_from_toursjson.py` regenerates cleanly at **208 makers / 2,035 tours / 2,407 stops /
  56 places**, exercising its own `validate_places`.
- **⚠️ Nothing compiled and CI has not run: no PR is open.**
