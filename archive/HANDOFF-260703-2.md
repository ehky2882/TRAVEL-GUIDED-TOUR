# HANDOFF — 2026-07-03 (session 53, code)

Profile/maker **polish batches A + B**, then TestFlight **1.0 (64)**. The owner pivoted from pricing to "polishing this new profile page and the maker portion" and gave 11 notes; I grouped them into four batches (A visual · B delete/recover/avatar · C links+custom-icon · D social) and shipped A + B this session.

## What shipped

### Batch A — visual polish ([PR #319](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/319), `a219bc7`)
- Website → **inline blue `Link`** under the bio (host label = URL host minus `www.`); removed the boxed `websiteLink` row.
- Display name **keeps user casing** (dropped `.textCase(.uppercase)` on the header) + a **40-char limit** (`ProfileEditorView.nameLimit` + `.onChange` truncation). Owner: all-caps was just their own preference.
- **Public** empty state = a single grey placeholder box in the first grid tile / list row (`toursGrid` + `emptyPlaceholderRow`); own-profile keeps the "+" add-a-tour affordance. (Owner corrected an earlier dashed-CTA-box attempt: only the *public* page shows the empty box.)
- Library **Saved** = individual tours first, saved makers below (`savedContent` reorder).

### Batch B — delete / recover / avatar ([PR #320](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/320), `d096d41`)
- **Delete a tour:** `MakerTourService.deleteTour(_:)` → `client.from("tours").delete().eq("id", …)` + drop from `myTours`. UI: a destructive **Delete tour** button + `.confirmationDialog` ("This can't be undone.") at the bottom of `TourAuthoringView`, dismisses to the profile on success. RLS allows owner delete on non-published statuses.
- **Forgot password?** `AuthService.resetPassword(email:)` → `supabase.auth.resetPasswordForEmail(_, redirectTo: SupabaseConfig.oauthRedirectURL)`; a "Forgot password?" button on `SignInView` (sign-in mode only), prefilled from the email field, reuses the confirmation-state UI.
- **Saved-list avatar:** `LibraryView.makerRow` renders `maker.avatarURL` via AsyncImage → `.clipShape(Circle())` before the `person.crop.circle.fill` fallback.

`test_sim` **140/140** on both. Both touch `LibraryView` but different funcs (`savedContent` in A, `makerRow` in B) — merged clean in order A then B.

## Build 64
- Bump #321 (63→64, two app-target `CURRENT_PROJECT_VERSION` lines). Admin-merged (metadata-only; app code already cleared full CI on #319/#320).
- Archived from a **clean `main` checkout** → `/tmp/Atlas-20260703-b64.xcarchive`, `-allowProvisioningUpdates`.
- Binary verified: `1.0 (64)`, `UIRequiresFullScreen=true`, `NSMicrophoneUsageDescription` present, `applesignin` + `associated-domains` entitlements, Supabase host `apkcihljybvuyuzpbnqd.supabase.co` compiled in, **no `TEMP_LOCAL_DEMO`**.
- Owner uploads via Organizer → Distribute App.

## Gotcha (re-)logged
For sim UI verification I temporarily forced signed-in state (`if true` in `ProfileView` + a `debugSeed()` on `MakerTourService` + `.me` default tab in `BottomModuleWindow`) so the own-profile/editor could be driven without a real session (sim can't hold one). **All three reverted + grep-verified clean before commit and before archive** — the clean-checkout discipline (`reference-archive-clean-checkout`) covers temp review hacks too.

## NEXT
- **Batch C:** up to **3 profile links** (small `makers` schema add — hand-hold the SQL) + the real **custom profile icon** (initials + bg color, OR photo upload → a shared `MakerAvatarView` reused in profile header + Library + mini-player; fully closes the saved-list icon gap).
- **Batch D (design-first):** the social layer — follow/followers with counts (mirror saved-maker), public vs private accounts, friend requests (auto vs manual accept). New `follows` table + RLS + owner decisions.
- Deferred: admin **Publish** (`select publish_tour('<id>')` works today in SQL); Phase 2 multi-stop authoring.
- Owner cleanup owed: throwaway `auth.users` test rows; the test tour `11111111-1111-1111-1111-111111111111` if still present.
