# HANDOFF 2026-09-01 (session 136) — Twenty-eight Chicago link pins

**Owner sent 34 Instagram reel links under the heading "Chicago links 260901".**

Branch `claude/tour-links-26dmsx`, cut clean off `main` at `eaa7cea` (0 ahead, 0 behind at start).
**linkPins 712 → 740 · makers 247 → 262 · tours unchanged at 1,552 · places unchanged at 88** (merged figures).

⚠️ **The branch was cut when the catalogue held 565 pins.** [#698](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/698) (25 Toronto), [#699](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/699) (106 from `@hereinnyc`) and
[#700](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/700) (16 Hong Kong) landed **147** pins underneath it — #700 arriving after the PR was already open, so the
catalogue edit was **redone the documented way — take `main`'s file and re-run the idempotent
assembler, never hand-resolve a JSON conflict** — and every check re-run. **0 sourceURL overlap,
0 id collisions** with either batch. 🔴 **#699 also created maker rows for two of my creators
(`@bestaddress`, `@mvvvc_`), so new maker rows went 17 → 15** — a naive JSON resolve would have
emitted `duplicate maker id` twice.
Content only — no Swift, no SQL, no build. Commit `3e939350`, pushed. **No PR opened** (this
session's harness forbids opening one unasked).

⚠️ **Filed as `-10`, and it took three tries.** `-7` went to the Toronto batch, `-8` was already
taken, and `-9` was claimed by the Hong Kong session (#700) while this PR was open — **three add/add
collisions on one afternoon**, with four link-pin sessions running in parallel. Check `archive/`
immediately before writing, and expect to renumber more than once if merges keep landing.

---

## 1. What came in and what shipped

| | count |
|---|---|
| links sent | 34 |
| distinct posts | **33** (#19 and #20 are byte-identical URLs) |
| already in the catalogue | **1** — #30, `River City` by `@abby.architecture`, live on `main` |
| blocked by Instagram | **4** — #4, #10, #12, #24 |
| **wired** | **28** |

**Every one of the 28 is Chicago-area**, from **20 creators**. 26 ship `city: "Chicago"`; two
deliberately do not — **Hyatt Regency O'Hare → `Rosemont`** (its own burned-in card reads
`📍 Rosemont, Illinois`) and the **Bahá'í House of Worship → `Wilmette`** (its card says
*"right in Chicago suburbs"*). The Gaylord Palms / Aït Benhaddou convention.

Categories: 15 architecture · 6 history · 4 visualArt · 1 each foodAndDrink / culturalHeritage /
sacredSites.

---

## 2. 🔴 Four posts Instagram will not serve — blocked, not gone

**#4 `DX38JvggEfB` · #10 `DVMtwCSElBA` · #12 `DU6KtupEq2w` · #24 `DV9JhvTDytK`**

Their embeds return a **~218 KB shell with `contextJSON: null`** — no handle, no thumbnail, no
caption — against **~263 KB with content** for a readable post. This is exactly the signature
the Toronto session (`claude/tours-links-upload-h6t2cs`) documented for six `@juni.toronto` reels.

**Confirmed with live controls in the same pass, same user-agent** — without that a transient
failure reads as a restriction:

```
DX38JvggEfB  bytes=219899  ctxnull=1  STILL-BLOCKED
DVMtwCSElBA  bytes=217713  ctxnull=1  STILL-BLOCKED
DU6KtupEq2w  bytes=217709  ctxnull=1  STILL-BLOCKED
DV9JhvTDytK  bytes=217714  ctxnull=1  STILL-BLOCKED
DcgYMskhyD5  bytes=262497  ctxnull=0  READABLE     <- control, already in the catalogue
DY4zhbWxFfh  bytes=264548  ctxnull=0  READABLE     <- control, wired this session
```

🔁 **A restriction is often temporary, so these may become pinnable with no work from us.**
Re-run the one-field test before re-deriving anything:

```bash
UA="Dozent/1.0 (link-pin tool; +https://dozent.world)"
for sc in DX38JvggEfB DVMtwCSElBA DU6KtupEq2w DV9JhvTDytK; do
  n=$(curl -sSL --max-time 25 -A "$UA" "https://www.instagram.com/reel/$sc/embed" \
      | grep -c 'contextJSON":null')
  echo "$sc  $([ "$n" = 0 ] && echo READABLE || echo still-blocked)"; sleep 3
done
```

⚠️ **Always fetch a known-live control in the same pass.** ⚠️ **Do NOT wire them on "the link
opens on my phone"** — the owner is signed in; a logged-out Atlas user gets a blank card and no hero.

---

## 3. Three subjects the captions never named

All three were closed from **the picture** plus the published record — the documented method.

- **#14 `@nic__white`** — caption is *"Breathtaking and significant!"* and names nothing. The frame
  shows an **Egyptian Revival doorway numbered 3052** with lotus capitals and a cavetto cornice.
  That is the **Egyptian Lacquer Manufacturing Company Building, 3052 W Carroll Ave** (1926,
  Lockwood Greene). ✅ **The reverse-geocode returns `3052, West Carroll Avenue` — the door number
  in the photograph and the address node agree independently.**
- **#13 `@2passports1dream`** — *"It's called Mojo and it's basically inside a bank."* Mojo has four
  Chicago sites; the one **inside a bank** is **190 South LaSalle, the U.S. Bank Building**. ✅ And
  the frame settles it beyond the caption: those curved tiered steps with brass rails are the
  **amphitheatre Norman Kelley added to that lobby in 2022**. The picture and the caption confirm
  each other from different directions.
- **#2 `@seeingcoolplaces`** — *"the oldest 'L' station in Chicago, built in 1892"*, hashtags
  `#greenline #uofchicago`. The frame is a restored brick head house with **a projecting bay under
  a half-cone roof** — the Queen Anne detail the landmark record names for the **1892 Garfield
  station house**, *"the oldest station facility on the Chicago 'L'"*.
  ⚠️ **Titled `The 1892 Garfield Station House`, not "the oldest L station"** — `35th–Bronzeville–IIT`
  is the oldest *continuously operating* station, so "oldest" is contested and stays the creator's
  claim in `longDescription` rather than something this catalogue asserts. The Banksy convention.

---

## 4. Coordinates — all 28 forward-geocoded, then reverse-verified at zoom 18

**16 are named exactly by OSM**, among them `Former Garfield Station`, `Obama Presidential Center`,
`860 Lake Shore Drive`, `St. Regis Chicago`, `Museum of Contemporary Art`, `Lake Point Tower`,
`Carl Street Studios`, `Francis J. Dewes House`, `Bahá'í House of Worship for North America`,
`S.R. Crown Hall`, `Glessner House Museum`, `Railway Exchange Building` and — pleasingly —
**`Money Museum`**, which is precisely what the Federal Reserve post is about.

**Seven resolve to the right address with no name** (Tribune Tower, Marina City, the Hyatt,
875 N Michigan, 3052 W Carroll, 150 N Michigan, 659 W Wrightwood).

⚠️ **Seven subjects failed their first query and SIX resolved on a re-query** — the documented rule.
`Obama Presidential Center`, `860 North Lake Shore Drive`, `9300 Bryn Mawr Avenue, Rosemont`,
`DuSable Museum of African American History`, `Glessner House Museum`, `Railway Exchange Building`
and `Wrightwood 659` all came back immediately under a different phrasing.

**Four reverse-geocode onto something else and are all correct** — the documented inner-POI case:
`Michael J. Petro` (a tenant at 53 W Jackson = the Monadnock), `Bain & Company` (a tenant at
190 S LaSalle), `Van Buren Street Station RTA Information Board` (which **independently confirms**
the `METRA` sign legible in the Guimard entrance's own frame), and `Money Museum` inside the Fed.

**Two needed care:**
- **Garfield Park Gold Dome Field House** reverses onto `Central Park Drive Bikeway`. OSM does not
  name the building; the point is its **`100 N Central Park Avenue` address node, type `house`** —
  a real address point, not a road centroid. ⚠️ **Checked against the Conservatory (41.8868, -87.7186),
  which is ~580 m north — a different building, and the two were not confused.**
- **The Damen Silos** reverses onto `West 29th Street`. OSM *does* name the site —
  **`Damen Silos Demolition`**, which is also a quiet corroboration of the caption's
  *"one step closer to being demolished"*.

⚠️ **Overpass was unreachable from this container — all three mirrors** (`overpass-api.de`
rate-limited, kumi.systems and private.coffee both `Internal Server Error`). Nominatim was fine.
Documented behaviour; budget for it.

---

## 5. 🔴 The handle suffix prevented two live-hero overwrites

`images/monadnock-building_hero.webp` and `images/marina-city_hero.webp` are **live Atlas Studio ORD
tour heroes**. A bare slug would have written over both — and since #567 a phone that has downloaded
either tour reads its photographs off its own disk and would never have seen the correction.

With the suffix: **0 collisions against 6,528 existing `images/` paths.** ⚠️ The check asserts the
tree returned **>1,000** images before trusting a clean result — the session-123 failure, where the
trees API returned nothing and every target "looked free".

---

## 6. Heroes — all 28 opened and read against their captions

**Zero wrong subjects.** Many name themselves in frame: *DAMEN SILOS Chicago, IL* ·
*OLDEST "L" STATION* · *Obama Presidential Center / Tod Williams Billie Tsien Architects* ·
*875 N Michigan* · *the Chicago Tribune Tower* · *Check Out this Brutalist Hotel… 📍Rosemont, Illinois* ·
*Two German Baroque Mansions in Chicago* · *Chicago / National Landmark by Mies* · *The Glessner House* ·
*THE CORN COB* · the DuSable's own banners, and **`3052`** on the Egyptian Lacquer door.

**Three hand re-crops — the vertical `--focus` gap, thirteenth batch running.** Every source is 9:16,
so the square is width-limited and **`--focus` does nothing at all**. Re-rendered through a mirror of
the tool's own pipeline (importing `trim_bars`, same blur/pad, same filenames, so `Tours.json` is
untouched):

| pin | vfocus | what it recovers |
|---|---|---|
| **Lake Point Tower** | **0.00** | the burned-in *"…THIS IS LAKE POINT TOWER"* was **gone entirely**; at 0.00 the whole caption returns and the tower dominates |
| **Mojo Coffee at 190 South LaSalle** | 0.15 | *"Chicago Builds Coffee Shops Differently"* was clipped; the amphitheatre also reads better |
| **The Santa Fe Building** | 0.15 | *"THIS BUILDING IS ONE"* returns and the arched entrance comes into frame |

⚠️ **Quincy Station was deliberately LEFT ALONE.** Four focus values were rendered and **none
recovers anything** — the source is a selfie under the L at every crop, with no burned-in title and
no better view of the station. **Nothing was destroyed by the crop, so there is nothing to recover**
— the distinction the owner drew on 2026-08-27: recovering what the crop destroyed is fidelity,
removing what the creator put there is editorialising.

### ⚠️ Five weak heroes, flagged not resolved

A link pin re-hosts only the thumbnail, so **no other frame exists** for any of these.

1. **The Crain Communications Building** — the sharpest mismatch. The frame is a technician on the
   roof beside lighting gear; **the diamond silhouette the post is entirely about is not in it.**
   No crop helps.
2. **Quincy Station** — a face fills the frame; the 1897 station is behind it.
3. **Carl Street Studios** — interior only, so it does not read as a building on a map.
4. **Mojo Coffee** — two presenters dominate even after the re-crop.
5. **Wrightwood 659** — a person centred in Ando's stairwell.

---

## 7. Editorial calls, each one line to reverse

- **🔴 `The Artifacts in Tribune Tower`, not `Tribune Tower`.** `@about_buildings` already has a pin
  titled `Tribune Tower` **on the identical coordinate** — both were geocoded to OSM's own node, so
  this is convergence, not manufacturing. Two identically-titled pins on one point read as a
  duplicate, so mine takes the subject its post is actually about (150 embedded artifacts from the
  Forbidden City, the Berlin Wall, Notre Dame). The slug and hero filename are unchanged.
- **`Wrightwood 659`, not the Chryssa show** — a limited-engagement exhibit would go stale; the
  venue does not. The Grace Cathedral *AURA* precedent.
- **Neither the Damen Silos' demolition nor the Paris Métro entrance's removal is asserted anywhere
  this catalogue authors.** Both claims are the creators' and stay verbatim in `longDescription`.
- **The Monadnock caption misspells it "Monadnok"** and the frame carries the misspelling. **The pin
  is titled correctly and the caption is kept verbatim**, so the error stays the creator's — the
  Schweizer convention.

---

## 8. Architect tags — caption-driven, and the omissions are deliberate

**Tagged by name:** `Mies van der Rohe` (860–880 Lake Shore Drive **and** S. R. Crown Hall — both
captions name him outright), `John Portman` (the Hyatt caption names him), `Norman Foster`
(*"Проект Нормана Фостера в Чикаго"*). All three carry **`Designed by a Master` alongside**, never
replacing it — `Tag.matches` performs no implication and the curated shelf is keyed on that literal
string.

**Generic tag only:** Obama Presidential Center (its own frame credits **Tod Williams Billie Tsien**,
absent from the vocabulary) and Carl Street Studios (**Edgar Miller**, likewise absent).

**🔴 Deliberately NOT tagged, and a future session will be tempted to "finish the job":**

- **`Bertrand Goldberg` on Marina City** — in the vocabulary, and it is his building. **The caption
  names no architect.** The Jules Dalou rule.
- **`Jeanne Gang` on St. Regis Chicago** — same.
- **`Tadao Ando` on Wrightwood 659** — same; that post is about the Chryssa exhibit.
- **`Daniel Burnham` on the Santa Fe Building** — the caption says only *"this was home to Daniel
  Burnham's office"*. He **did** design it, but **a mention is not authorship** and the source does
  not make the claim. The Sullivan rule, at its most tempting. ⚠️ **The most defensible one-line
  addition in this batch if the owner wants it.**

**⚠️ The most conspicuous absence this batch surfaces is `Georgia Louise Harris Brown`** — the
860–880 Lake Shore Drive caption names her as the structural engineer on the team and *"the second
black American to hold an architecture license in the US"*. Adding her is a `Models/Tag.swift`
**code** change, deliberately kept out of a content batch.

---

## 9. Playability — 4 of 28 will not play inline

`plays_inline: False` on **Tribune Tower** (*The Great Mystery*), **MCA Chicago**, **The Egyptian
Lacquer Building** and **The Paris Métro Entrance** (*La vie en rose*) — the documented licensed-music
gate, where Instagram withholds `video_url`. They still pin; they open Instagram on tap.

⚠️ **Two of the four reason strings are wrong while the verdicts are right** — they read
*"licensed music (77K followers)"* and *"(4K followers)"*, the documented `instagram_playability`
regex mis-capture. **The verdict comes from the absent `video_url`, not the track name.** Cosmetic,
one line in that function.

---

## 10. Places — one genuine candidate, not created

`check-place-candidates.py` goes **0 EXACT / 21 NEAR → 1 EXACT / 24 NEAR** (exit 1).

**The one EXACT group is the two Tribune Tower pins** at `41.890584, -87.6232046`. Honest rather
than manufactured — **neither pin was nudged apart to dodge the checker**, and both landed there
independently from OSM's own node.

⚠️ **It is a three-member candidate, not two**: the Atlas tour **`The Wrigley Building & Tribune
Tower`** sits 142 m away. **And it carries the documented naming problem** — that tour covers *two*
subjects, so a place named for the tower only half-covers its own member. This is exactly the
Tribune Tower / Petit Palais case CLAUDE.md already records as flagged-and-not-built. **Owner's call.**

New NEAR pairs, all pins beside an Atlas tour of the same subject and all correct where they are:
**Monadnock 85 m · Tribune Tower 142 m ×2 · Obama Presidential Center 214 m · Marina City 486 m.**

---

## 11. Verification

- **Validator mirror**, vocabulary parsed from **both** `Models/Tag.swift` **and**
  `scripts/validate-tours.swift`, **refusing to run if they disagree or either parse is empty**
  (they agree at **385 tags**). ⚠️ **That guard fired on the first run** — my enum parser returned
  nothing because the type is `StopTriggerMode`, not `TriggerMode`. It also initially parsed a stray
  word out of a doc comment into the `TourKind` domain, which would have made the kind check too
  permissive; tightened to strip comments first.
- **Self-tested against 22 injected fault classes aimed at THIS batch's own pins — 22/22 caught,
  control clean.** A suite that predates the rows you just added is not evidence about them.
- **0 errors, 0 warnings** across **1,552 tours + 740 pins + 88 places** (re-run after each merge).
- **0** duplicate tour/stop/maker ids · **0** already-pinned sourceURLs (checked against `main`
  **and both parallel link-pin branches**, `tour-links-yg5yw2` and `tours-links-upload-h6t2cs`) ·
  **0** byte-duplicate heroes · closest perceptual pair **29.7** (identical pictures score under 1,
  and that pair is a Brutalist atrium against green ironwork — the tonal false positive).
- **The uuid5 scheme reproduced all three existing maker ids exactly** — `@abby.architecture`,
  `@dan__asher`, `@urbanistariel` — so those merged rather than duplicating. 17 new rows.
  **All 20 ship `avatarURL: null` by design**; Instagram's embed exposes no avatar.
- `Tours.json` **byte-stable under a Python re-dump before editing**; diff **1,401 insertions /
  0 deletions** against the merged base — `tours` and `places` untouched.
- gh-pages `10ec78bf`: remote head **re-read in the same command as the push**, tree diff **exactly
  28 additions, 0 deletions, nothing outside `images/`**; commit re-confirmed **an ancestor of head**
  afterwards.
- `seed_from_toursjson.py` clean at **262 makers / 2,292 tours / 2,664 stops / 88 places** (post-merge).
- `make-link-pin.py --selftest` **71/71** — ⚠️ **with Pillow**, installed first; a fresh container
  has none and reports 62/62, which reads as a pass and is not one.

**⚠️ Nothing compiled and CI has not run — no PR is open.** The Swift validator is the authority;
the mirror is the only check this session could run.

---

## 12. Owed / open

1. ✅ **RESOLVED — the Pages deploy landed after ~30 minutes of 404s** (documented; this repo has
   taken 40). **All 28 live URLs hash-verified against the uploaded blobs: 28 ok, 0 mismatch,
   0 non-200.** `check-image-duplicates.py --pins` **`OK`** over 588 images, shared-URL half
   **0 errors / 208 documented reuses**. A 404 during a slow deploy is not a failed upload.
2. **No PR opened** — this session's harness forbids it unasked.
3. **The Tribune Tower place candidate** (§10) and **the five weak heroes** (§6) are owner decisions.
4. **`Daniel Burnham` on the Santa Fe Building** and **`Georgia Louise Harris Brown` in the
   vocabulary** (§8) — both one-line, both deliberately not taken.
