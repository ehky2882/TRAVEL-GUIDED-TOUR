# HANDOFF — 2026-06-12 (session 37, web/PM — Lisbon batch 2 + build 43)

## TL;DR

**15 Lisbon tours shipped (PR #198) and TestFlight 1.0 (43) cut, uploaded,
and live.** LIS **31 → 46** — Lisbon batch 2: funiculars, the big museums,
Pink Street. **Catalog 243 → 258 tours / 4 makers / 267 stops**
(100 NYC + 58 LDN + 54 OPO + 46 LIS). All audio + images owner-supplied
(`/Users/EY/Downloads/260612_PORTUGAL/`), no sourcing pipeline run.

## What happened

1. **gh-pages:** audio `a7d532c` (15 MP3s, slug-named), images `4116b8e`
   (48 webp, `<Base>_hero/_2…` naming). All 63 referenced URLs
   live-checked 200.
2. **PR #198** — 15 tours appended to `Tours.json` via an idempotent
   assembler (`/tmp/add_lisbon15.py`); validator clean (4 makers /
   258 tours / 267 stops); CI fully green; squash-merged `f01a76e`,
   branch deleted (worked from this env — no proxy block).
3. **PR #199** — bump **42 → 43** via the short-lived-PR pattern
   (app-target `CURRENT_PROJECT_VERSION` lines only; test target stays 1;
   `MARKETING_VERSION` stays 1.0); admin-merged `30db8fa`.
4. `xcodebuild archive` clean at `/tmp/Atlas-20260612-1923-b43.xcarchive`;
   embedded version verified `1.0 (43)`; no validation 90474. Owner
   uploaded via Organizer. **TestFlight 1.0 (43) is live.**

## Tours added (all Lisbon, single-stop, manual trigger, 30 m, free)

Glória Funicular, Igreja de São Roque, Amoreiras 360º Panoramic View,
Ascensor da Bica, Church of São Vicente de Fora, Livraria Bertrand,
MUDE — Museu do Design e da Moda, Museu Nacional do Azulejo,
MAC/CCB — Museum of Contemporary Art, Museum of the Orient,
National Museum of Ancient Art, National Pantheon,
Palácio Nacional da Ajuda, Pink Street, Tropical Botanical Garden.

## Quirks handled / lessons

- **São Roque's drop folder had no title** (named only by coordinates);
  identified by the files inside (`Igreja de São Roque.*`).
- **Pink Street ships hero-only** (`additionalImageURLs: []`) — owner
  supplied a single image; that's fine.
- Two transcripts (Museum of the Orient, National Pantheon) carried
  **CSV-export quote artifacts** (`""Namban""`, line wrapped in quotes) —
  unescaped before embedding as `transcriptText`.
- **Parallel-session checkout flip:** mid-task another session merged
  PR #197 and switched this checkout from the batch branch back to
  `main`, so the content commit landed on local `main`. Recovery:
  `git branch -f <batch-branch> <commit>` + force-push the branch +
  `git reset --hard origin/main` — never force-push main. When sessions
  run in parallel, **re-verify the current branch immediately before
  committing**, not just at branch creation.
- gh-pages worktree at `/tmp/ghpages` had been cleaned; recreated with
  `git worktree add /tmp/ghpages gh-pages`.

## Follow-ups

- CLAUDE.md "Current State" + catalog counts not yet synced (classifier
  blocks unprompted CLAUDE.md edits — needs explicit owner go-ahead).
- Standing offer: 5-min App Store Connect API key setup for upload
  automation (`docs/testflight.md` § Full upload automation) — owner
  deferred again this session.
- Carried code follow-ups unchanged (on-device QA, compass ⌥-drag hand
  check, `placemark` deprecation, Player drag/volume device pass).
