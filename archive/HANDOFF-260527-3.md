# Atlas — Handoff Notes (2026-05-27, session 10)

**Discovery-only session.** Started the tour-detail-sheet retool
pass briefed by the user. No code or content touched. Build, sims,
and `main` are unchanged from the end of session 9 (`HANDOFF-260527-2.md`).

---

## Where we are

The brief: comprehensive retooling pass on the **tour detail
sheet** — the slide-up modal that opens when a user taps a tour
from the home drawer, placecard, library, search results, etc.
Two-phase plan: **Phase 1 = discovery + scope (AskUserQuestion
groups A–G); Phase 2 = implementation**, only after owner
approves a written plan.

Phase 1 reached the very first question (Group A, share button +
shape). Owner dismissed the AskUserQuestion before answering and
called it for the night. Per working-style memory: dismiss = pause,
not pick-a-default. Session ended cleanly with nothing modified.

## What was done before the pause

1. **Session-start ritual** — `git fetch`, `gh pr list`, latest
   `HANDOFF-260527-2.md` read. Tree clean on `main`, no open PRs.
2. **Survey of `TourDetailView.swift`** — full read-through.
   Current post-PR #79 layout:
   - Nav bar: X close (top-leading) + inline title
   - Hero image (single OR paging `TabView` carousel — tiny default
     `.page` dots; no separate gallery affordance)
   - Title + chip row (category, "Walking tour" for multi-stop)
   - Maker row (person icon + name + chevron → MakerView push)
   - Meta row (clock/duration; + walking distance + stop count for
     multi-stop)
   - Long description (always full inline; no Read More collapse)
   - Stops section ("Location" header for single-stop, "Stops" for
     multi); numbered rows with title/caption/duration + dividers
   - **Inline button row** (the post-PR-#79 "first draft"): Start
     Tour (filled prominent, terracotta, flex width) | bookmark
     (bordered square) | download (bordered square). All h=36.
   - Bottom inset = `AtlasBottomModule.height()` so last line
     clears the mini-player + tab bar.
3. **Build + launch on iPhone 17 Pro sim (iOS 26.5)** —
   `build_run_sim` clean, NYC location set, dark mode, opened the
   Braga Municipal Stadium detail sheet (gallery-rich tour, 4 hero
   images) as a reference capture. Screenshots stashed at
   `/tmp/atlas-detail-DARK-gallery-top.jpg` and
   `/tmp/atlas-detail-DARK-gallery-actionrow.jpg`.
4. **Inventory + Group A question prepared.** Asked: (a) where to
   place the missing *SHARE BUTTON* (now / separate PR / defer);
   (b) what shape the share affordance should take (square icon /
   pill / nav-bar trailing slot). Owner dismissed both.

## How to resume

Re-read this handoff + the original brief in the user's prior
message. Re-open the sim (build is still in DerivedData; no rebuild
needed if Xcode and the workspace are untouched) — or just rebuild
fresh. Then re-pose **Group A, one question at a time** rather
than two stacked. The owner's working-style memory is
explicit about one-step-at-a-time for multi-step flows; the
two-stacked-questions probably contributed to the dismiss. Suggest
starting with just Q1 (where the share button goes), and only
asking about shape after a yes is given.

Suggested ordering for the remaining groups (B–G) once A is
unblocked, per the brief:

- **B** — information hierarchy (what's above the fold; hero
  treatment; metadata chip row?)
- **C** — description + stops body (collapse? show-stops-when-1?)
- **D** — gallery surfacing (huge for FAUP / Expo'98 / Boa Nova,
  all of which have 6–17 additional images)
- **E** — maker section (dedicated section? more-by-maker link?)
- **F** — sheet behavior (detents? drag-to-dismiss anywhere?)
  Includes whether to fold chrome-shade-mismatch into this PR.
- **G** — visual chrome (typography, padding, spacing concerns;
  reaffirm buttons-identical-everywhere rule from
  `feedback-atlas-module-design.md`)

Out of scope reminders to carry forward:

- **Don't** touch the bottom module / mini-player / tab bar / drawer.
- **Don't** change the slide-up presentation animation unless
  owner approves it in Group F.
- **Don't** add a third-party dependency. Use
  `UIActivityViewController` for share.
- **Don't** bump the build number.
- **Don't** fold the chrome shade mismatch in unless approved.

## State of `main`

| Thing | Value |
|---|---|
| Branch | `main` (clean) |
| HEAD | `646eb44` (PR #79) |
| Open PRs | none |
| Open issues | chrome shade mismatch (subtle, dark mode) — known, deferred |
| Tours | 53; makers 3; stops 57 |
| TestFlight build | 1.0 (12) — owner has not flagged completion of the M-qa pass yet |
| Sim setup | iPhone 17 Pro (F89A0D35…), iOS 26.5, dark mode, NYC location (40.758, -73.9855) |

Nothing to commit. Session ends with `git status` clean (modulo
the already-untracked `.DS_Store` + xcuserdata).
