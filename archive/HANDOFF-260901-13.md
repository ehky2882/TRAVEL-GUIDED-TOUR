# HANDOFF 2026-09-01 (session 137, follow-up) — three Hong Kong places

**Owner: *"make all 3 listed places"*** — the three NEAR pairs flagged when #700 was reported live.

Branch `claude/tour-links-9ynig6`, restarted clean off `main` at `c868a964` (its previous PR, #700,
is merged, so this is a fresh change and not stacked on merged history). **Places 97 → 100.**

⚠️ **`main` moved THREE TIMES mid-session, and took this handoff's filename twice.**
[#701](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/701) landed 28 Chicago link pins
(**pins 712 → 740, makers 247 → 262**); [#702](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/702)
and [#703](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/703) added seven places (88 → 95); then
[#707](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/707) added Cube House and Tribune Tower
(95 → 97) and **drove `check-place-candidates.py`'s EXACT tier to zero**. Resolved every time the
documented way: **take `main`'s file and re-run the idempotent assembler**, never hand-resolve a JSON
conflict — **the diff came out unchanged at 57 insertions / 12 deletions on all four bases** — and
every check below was re-run. This handoff renumbered **`-10` → `-11` → `-13`**. **0 overlap** with any
of them: my three tours and three pins are all still present and unclaimed, my three place ids collide
with nothing, and `tours`/`makers` are byte-identical to the current `main`.

Content only — no Swift, no SQL, no gh-pages push, no build.

---

## 1. What was built

| place | members | pin moved | address |
|---|---|---:|---|
| **PMQ** | Atlas tour + `@love.in.hongkong` pin | 45.2 m | 35 Aberdeen Street, Central, Hong Kong |
| **Jamia Mosque** | Atlas tour + `@love.in.hongkong` pin | 27.8 m | 30 Shelley Street, Mid-Levels, Hong Kong |
| **Sai Wan Swimming Shed** | Atlas tour + `@love.in.hongkong` pin | 30.0 m | Victoria Road, Mount Davis, Hong Kong |

All six members are `manual` at radius 30 — **nothing is geofenced**, so moving a pin costs nothing
and no audio changes where it fires. The build asserts that on every member before it writes.

---

## 2. 🔴 The pin moved and the tour did not — three times, for three different reasons

The standing rule is "the pin moves, never the tour", but here neither side is geofenced, so the
usual argument does not apply. Each anchor was decided on its own evidence:

- **PMQ** — the tour reverse-geocodes to **`PMQ Courtyard & Marketplace, 35 鴨巴甸街 Aberdeen Street`**,
  PMQ's own named amenity carrying its house number, and its transcript opens *"You're standing in
  the central courtyard"*. The pin lands on **`Glimpse PMQ`**, a venue inside the complex — the
  documented inner-POI case.
- **Jamia Mosque** — the tour reverse-geocodes to the **Central–Mid-Levels Escalator**, which reads
  wrong until you read the tour: *"The mosque sits off Shelley Street, set back behind a green
  archway and a flight of stairs."* That is a **deliberate vantage** (the Grand Central / Petersen
  precedent), and the escalator runs up Shelley Street. The pin sits 28 m away on OSM's own mosque
  node.
- **Sai Wan Swimming Shed** — **both sides reverse-geocode to `西環鐘聲泳棚 Sai Wan Swimming Shed` by
  name**, so there is nothing to choose between them on accuracy; take the authored one.

---

## 3. ⚠️ OSM carries Jamia Mosque TWICE, and only one node has the address

A forward search returns two `place_of_worship` nodes, 37 m and 38 m from the tour:

```
些利街清真寺 Jamia Mosque, 30, 些利街 Shelley Street, SoHo, Mid-Levels
回教清真禮拜總堂 Jamia Mosque, 摩羅廟街 Mosque Street, SoHo, Mid-Levels
```

They are the same building filed under two of its names. **The Shelley Street node is the one with
a house number**, it matches the tour's own text, and it matches the tour's Chinese title
**些利街清真寺** — which literally *is* "Shelley Street Mosque". Address: **30 Shelley Street**.

---

## 4. ⚠️ PMQ's district was VERIFIED, not recalled — and OSM disagrees with the venue

OSM files PMQ under **SoHo / Sheung Wan**; PMQ's own site publishes **Central**:

```
S614, Block A, PMQ, 35 Aberdeen Street, Central, Hong Kong     — pmq.org.hk/contact-us
```

Addresses are **editorial, corroborated by geocoding rather than taken from it** (the La Pedrera
rule), so the published form ships. The house number and street come from the geocode and are
uncontested; only the district differed, and it was settled by fetching the venue's own page rather
than by recollection.

⚠️ **Sai Wan has no house number in OSM at all** and none was invented — it ships
`Victoria Road, Mount Davis, Hong Kong`, the Operaparken precedent.

---

## 5. ✅ Every hero is a third photograph, and the build refuses to write otherwise

All three are promoted from the Atlas tour's own gallery — **already uploaded, already verified,
nothing sourced** — and the assembler asserts `hero != tour.hero`, `hero != pin.hero`, and
`hero ∈ tour.additionalImageURLs` before it writes. **Borrowed-hero count re-derived, not carried
forward: 26 of 100** (69 take a third photograph, 5 carry none) — **`main` alone reads 26 of 97, so
these three add ZERO borrowed heroes.**

**All four candidates were rendered and looked at**, never chosen by filename:

- **PMQ** → `PMQ_2.webp`, the courtyard entrance under the Qube glass link with the stone retaining
  wall and the mural. ⚠️ **The only option** — that tour's gallery holds exactly one image (the Wave
  Hill case).
- **Jamia Mosque** → `Jamia_Mosque_2.webp`, the full mint-green flank with the onion dome, the
  pointed-arch windows and the tower blocks behind. ⚠️ **`_3` is the prayer hall interior** — mihrab,
  red carpet, stained glass, genuinely fine — and was rejected on the establishing-shot criterion.
- **Sai Wan Swimming Shed** → `Sai_Wan_Swimming_Shed_2.webp`, the timber pier running out to the
  water with the lifebuoy, swimmers, and Green Island behind. Also the only option.

---

## 6. ⚠️ The copy asserts only what the members carry — and one creator claim is deliberately dropped

**The Sai Wan pin's caption calls it "the famous Instagram pier in Kennedy Town."** The shed is at
**Mount Davis**, on Victoria Road, which is what both reverse-geocodes say. The creator's words stay
verbatim in their own `longDescription`; **nothing this catalogue authors repeats the Kennedy Town
claim** (the Schweizer / #9 Park Avenue convention).

Otherwise the descriptions describe the **site**, not either tour, and every fact is one a member
states: PMQ's Central School → Queen's College → Police Married Quarters → *Three* film set →
4,000-objection petition → 2014 reopening, and the Underground Interpretation Area's excavated
granite foundations; Jamia's 1850 land grant, the 1915 Elias building, the only minaret on Hong Kong
Island, the lascars and the Lascar Temple name, Ma Chik Ting's pulpit, monument status in 2022;
Sai Wan's Chung Sing shed, the 1911 North Point original, the 1970s die-off, Wan Hung-fai's 1988
application, and the dawn-swimmers / sunset-photographers split.

---

## 7. Verification

- **Validator mirror** — vocabulary from **both** `Models/Tag.swift` and the Swift validator, plus the
  enum domains, refusing to run on disagreement or a short parse (**385 tags**) — **self-tested 40/40**,
  then **0 errors, 2 warnings** across 1,552 tours + 740 pins + 100 places. **Both warnings are
  pre-existing**: the same mirror against `origin/main` reports the identical pair.
- ⚠️ **Seventeen place-layer faults were injected against the NEW places specifically — 17/17 caught,
  control clean** — because a suite that predates the layer you just changed is not evidence about it.
  Among them: the place's own latitude and longitude drifting off its members, the **tour** member
  nudged 55 m, the **pin** member nudged 55 m, a member moved out of town, a member claimed by two new
  places, a member claimed by a new *and* an existing place, an id colliding with an existing place,
  and the hero repeated in its own gallery.
- **Diff shape asserted, not eyeballed:** `tours` and `makers` **byte-identical** to `origin/main`, the
  97 existing places unchanged as a prefix, **exactly 3 link pins changed in exactly 4 coordinate
  fields each**, all three `manual`. **57 insertions / 12 deletions.**
- **Place ids** `uuid5(NAMESPACE_URL, "atlas-place:hong-kong:<name-slug>")`, the scheme
  **reverse-verified against 95 of the 97 existing places** (the two misses are the documented legacy
  uppercase ids, Green-Wood and Oedo); **0 collisions**.
- **`check-place-candidates.py` reports EXACT *none* and exits 0 on BOTH sides, NEAR 36 → 33** — falling by exactly the
  three pairs resolved and gaining nothing, so **no coincident group was manufactured**. ⚠️ Exit code
  read **directly, not through a pipe**; #707 had just driven EXACT to zero, so this change **preserves the clean-exit state** rather than breaking it.
- `seed_from_toursjson.py` clean at **262 / 2,292 / 2,664 / 100**, carrying the places, so this reaches
  Supabase on merge with **no owner SQL**.
- `Tours.json` **byte-stable under a Python re-dump before editing** — the assembler asserts it and
  refuses otherwise — and again after.
- All three hero URLs live **200** and **hash-verified against the local files**, not merely 200-checked.
  **0 `images//`** in the catalogue.
- ⚠️ **Nothing compiled — a Linux web session has no Swift toolchain, so CI is the authoritative validator.**

---

## 8. What is left

Nothing owed on these three. Still open from #700 and **not closed**: the four weak heroes (#10 The
Pokfulam Farm is the likeliest pull), and `Coldefy & Associés` absent from the tag vocabulary, which
is a `Models/Tag.swift` code change for a future architect PR.
