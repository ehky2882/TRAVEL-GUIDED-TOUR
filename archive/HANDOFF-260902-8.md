# HANDOFF 2026-09-02 (session 138 — content, Hong Kong closeout)

Merged as [#719](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/719), squash `6769b890`.
**Places 105 → 107; tours, pins and makers all unchanged.** Content + docs only — no Swift, no
gh-pages push, no build. Verified live on the RPC after the merge, not on GitHub's success line.

## What the owner asked, in order

1. Ten notes by tour id / dozent.world URL — five renames, seven coordinate moves as Plus Codes, two
   removals. Shipped as [#716](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/716).
2. *"there are 2 'lan fong yuen' tours. they should be the same spot. so 1. one of them is wrong (or
   both) and 2. it should be made into a place"*
3. *"make hideout a place"* · *"dont worry about min fong hong"* · *"haidilao. make carnarvon road"*
4. ***"TAKE OUT THESE TOURS. CANT FIND A RELIABLE ADDRESS FOR THESE SO WE'RE GOING TO OMIT"*** —
   Lau Kee Noodle, Matchbox Cafe, Knockbox Cafe, Dozy Cafe, Hara Station, Honolulu Coffee Shop,
   Takimoto, Vission Bakery.

## 🔴 The removal turned out to require no removal — and checking is the whole lesson

**All eight were deferred at authoring time and never wired.** Verified before answering: all eight
shortcodes are absent from every `sourceURL` across `tours` and `linkPins`. So unlike the two
removals in #716 this **owes no SQL** — a post that never reached `Tours.json` never reached
Postgres either.

**Two catalogue entries share a name with that list and are NOT it.** A name sweep hits both, and
deleting either would have been the expensive mistake:

- **`Lau Kee Aberdeen Boat Noodle | 流記艇仔粉`** is an **Atlas Studio HKG narrated tour**, not a link
  pin — real audio, its own account of Lam Lau's licensed floating kitchen at Aberdeen Pier Six since
  1981 — and its coordinate reverse-geocodes to **Aberdeen Praya Road**, the promenade its own script
  describes.
- **`Cookie Vission, Tai Hang`** (`DW1KgzhEReK`) is a **different post** from the deferred
  `Vission Bakery` one (`DA3HyZFyuQv`, the matcha mochi danish with no district).

⚠️ **This was checked rather than acted on because the same deferred list called Haidilao unshipped
minutes earlier when it was already live.** A tracker is not evidence about the catalogue — search
`Tours.json` before removing anything one names.

`drafts/hk-shivanidukhandee/ADDRESSES-NEEDED.txt` is now a **closed record** carrying all of this, so
a future session cannot read the file as unfinished work.

## 🔴 A count I got wrong, and how it surfaced

The closeout first reported **106 shipped of 118 pinnable**, swept from
`drafts/hk-shivanidukhandee/analysis.json`. **That file is not batch-scoped** — it holds 120 records,
two by other creators entirely (`DcLpQ7yOoBU` `@sato_stays`, a Tokyo hotel; `DcTW0yzsEok`
`@poche_space`, the Gilder Center in New York), both already live under their own maker rows — so
both figures were two too high. **Caught because 106 disagreed with the 104 pins actually on the
`@shivanidukhandee` maker row.**

Final, derived from the maker row and reconciling exactly (104 + 12 not-live = 116):

> **104 shipped · 11 dropped · 1 deferred · 2 blocked, of 116 pinnable.**

**Count a creator's batch from the maker row, never from the analysis file** — and when two
derivations disagree, the disagreement is the finding.

## The two places

**Lan Fong Yuen** (tour + pin) and **The Hideout, Mui Wo** (two pins, already coincident).

🔴 **The Atlas Lan Fong Yuen tour was on the wrong street and the pin was right.** The pin sits on
OSM node/3493470992 (`蘭芳園 Lan Fong Yuen`, 2 結志街 Gage Street, `wikidata=Q15921803`) to **0.0 m**;
the tour was **94 m away** on `8 擺花街 Lyndhurst Terrace`, while **its own script opens "the corner of
Gage Street and Cochrane Street."** ⚠️ Both members are `manual`, so *"the pin moves, never the
tour"* did not apply on trigger grounds — **the tour moved because the evidence says it was wrong.**

⚠️ **Both sites were swept at 400 m rather than built from the checker's pair.** Lan Fong Yuen sits on
a crowded Central block — **Beyrouth Bistro is 33 m away, closer than the tour was** — with Haeundae
Galbi 95 m, Francis West 109 m, Tai Kwun 114 m, Stone Slab Street 122 m, all correctly excluded.

🔴 **Both heroes are borrowed and that is structural — do not go sourcing replacements.** The Atlas
tour ships an empty gallery and both Hideout entries are link pins with none, so no third photograph
of either site exists in the catalogue. Borrowed-hero count **re-derived: 33 of 107**.

## 🔴 Haidilao was never deferred — it was already pinned, and I said otherwise

It shipped in the 58-coordinate batch as *Haidilao Hot Pot, Tsim Sha Tsui*. **Caught because the
newly generated pin's uuid5 collided with a live id** — that check doing its job; the duplicate and
its hero were discarded rather than uploaded. Now exactly on the **Carnarvon Road** node,
reverse-verified by name, and **titled for the road rather than the district** because OSM carries two
branches 83 m apart. ⚠️ The Granville one is `海底撈火鍋・宴聚`, a different sub-brand.

## Verification

- Mirror **self-tested 19/19, control clean**, then **0 errors, 0 warnings** across 1,552 tours + 905
  pins + 107 places. ⚠️ **22 place-layer faults injected against the two NEW places — 22/22 caught.**
- `check-place-candidates.py` **0 EXACT, exits 0**; NEAR 36 → 35. The place backlog is empty.
- `Tours.json` byte-stable under a re-dump before each edit; every moved entry asserted `manual`
  first; every member asserted on its place's exact coordinate; **each hero asserted to BE one of the
  members' own**; every untouched entry asserted to differ in no field at all.
- Seed clean at **297 / 2,457 / 2,829 / 107**; **0 `images//`**.
- **The squash was checked against `main` for real content** (the #629 empty-commit lesson): 140
  insertions across 4 files, both places present, Haidilao renamed.
- **Live RPC after the merge**: 1,553 tours · 907 pins · **107 places** · 311 makers, `priceTier` on
  all 1,553 with 66 priced, `isPrivate` on all 311, `country` on 1,552, `videoRole` on 1,553, **0 link
  pins wrongly inside `tours`**. ⚠️ **Two polls during seeding read `places 0`** — transient, not the
  session-99 dropped-key failure; the full key check above is what settled it, and is worth running
  rather than assuming whenever a poll reads zero.

## ✅ The removal SQL has been run — nothing is owed

**`backend/pull_pins_260902.sql` was run by the owner the same day**, and was **verified against the
live RPC rather than the SQL Editor's success line**: `linkPins` **907 → 905** and
`@shivanidukhandee` **106 → 104**, both now matching the catalogue exactly; `Xiang Bo Bo` gone.

🔴 **`Cheung Hing Coffee Shop` is STILL SERVED and that is CORRECT — a future session will read the
survivor as a failed deletion.** The file deleted the *duplicate* (`c4071953…`, the Pineapple Bun
Hunt post) and kept the surviving twin `d3bb855c…`, confirmed present. **Exactly one remains where
there were two, which is what the owner asked for.** ⚠️ **`Cheung Hing Tea Hong` is a DIFFERENT venue
2 km away** and was untouched.

Session-99 dropped-key check clean on the same payload: `priceTier` on all 1,553 with 66 priced,
`isPrivate` on all 311 makers, `country` 1,552, `videoRole` 1,553, **0 link pins wrongly inside
`tours`**, places 107 with both new ones served.

⚠️ **makers 311 vs the catalogue's 297 is the documented upsert-only accumulation, not a defect** —
every signup auto-creates a maker row. **Assert on link-pin counts, never maker totals.**

## Also open

- **2 blocked posts** (`C6vyhv4NhMu`, `DW_uE17B0dW`) — `contextJSON: null`; recheck in ~a month.
- **1 deferred**: `DMz6jSxy0u8`, a second Kowloon Hum Spicy Hotpot post. The venue already carries a
  pin from `DBYnXEky1Cb`, so wiring it would put two pins for one venue under one creator — the shape
  the owner has ruled on twice. One line ships it.
