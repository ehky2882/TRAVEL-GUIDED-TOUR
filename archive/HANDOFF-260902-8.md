# HANDOFF — 2026-09-02 (session 139) — thirty-two Hong Kong link pins

**Branch:** `claude/tour-links-vcn3xc` · commit `87038c28` · **NO PR OPENED** (this session's
harness forbids opening one unasked). gh-pages `2baba65`.

**Catalogue:** linkPins **905 → 937** · makers **297 → 299** · tours **unchanged at 1,552** ·
places **unchanged at 105**. Content only — no Swift, no SQL, no build.

---

## What arrived

33 Instagram reels: four loose at the top (three carrying Hong Kong Plus Codes), then 29 under a
heading reading `Insidehongkong`. **33 distinct shortcodes, 0 in-batch duplicates, 0 already
pinned** against 881 indexed shortcodes.

**32 ship, 1 is blocked.**

| creator | pins | maker row |
|---|---:|---|
| Instagram `@insidehongkong` | 28 | **new** (`7F24E46E-504A-5E0E-B2CA-03672189C73A`) |
| Instagram `@notbadgalriri__` | 3 | **existing — uuid5 reproduced its id exactly, so it merged** |
| Instagram `@broadcastingwatches` | 1 | **new** (`72357B98-4BD0-5E2B-9131-FE8C2E5DAAA5`) |

⚠️ The owner's heading was accurate for *its own group* — all 29 posts under `Insidehongkong` are
that account. The four above it are one `@broadcastingwatches` and three `@notbadgalriri__`. Still
worth the session-135 rule: **read the payload, not the heading a link sits under.**

All three ship `avatarURL: null` by design (the Instagram embed exposes none), so **32 files cover
32 pins** — and the file count was asserted equal to the pin count after generating, which is the
check that catches two pins slugging identically and one hero silently overwriting the other.

---

## 🔴 A SUPPLIED PLUS CODE CONTRADICTS ITS OWN POST, AND THE CAPTION WINS

`75M2+9X Central, Hong Kong` was pasted beside `DTNJFerk9Ye`. It decodes to
`22.2834375, 114.1524375`, which reverse-geocodes to **20 Aberdeen Street, SoHo, Central**.

That post's caption gives an explicit full address:

> `📍EONIQ / 🗺️G02,03 Mill6, The Mills, 45 Pak Tin Par St, Tsuen Wan`

**Tsuen Wan is ~13 km away, in the New Territories.** Three things back the caption:

1. OSM names **南豐紗廠 The Mills** exactly, at `22.3749699, 114.1101881`.
2. That point **reverse-geocodes to house number 45, Pak Tin Par Street** — the caption's own number.
3. The hero is a display case of vintage-style watches, consistent with EONIQ (a build-your-own
   watch workshop), and nothing about SoHo.

**The pin ships on the caption.** The stray code most likely belongs to a reel that was not in the
batch — note it lands 90 m from `DTK5kAFk6Dx`'s code, in the same SoHo block. **Flagged for the
owner rather than guessed at.** This is the session-138 Nocturnal Stationery case exactly.

**The other five codes all check out**, and two are confirmed twice over:

| code | decodes to | corroboration |
|---|---|---|
| `858F+C2` Mong Kok | Kwong Wa Street, Mong Kok | hero is a watch-shop arcade with `錶匠 WATCH MAKER` / `A ONE WATCHES` in frame |
| `75J2+WX` Central | 34B Staunton Street, SoHo | Chez Trente's published address is **6 Chung Wo Lane**, 90 m away — and the caption deliberately withholds it, so the code was the only signal |
| `85G7+QR` Sham Shui Po | 112-116 Ki Lung Street | caption gives `Tai Nan St 191-193`, one street over; the shipped point reverse-verifies onto **191-193 Tai Nan Street** exactly |
| `75G9+PF` Wan Chai | **樂滿大廈 Lok Moon Mansion, 29-31 Queen's Road East** | Soft Thunder Bakery |
| `85G8+7P` Prince Edward | **英來大廈 Ying Loy Mansion, 19-25 Yu Chau Street** | Heartwarming's published address is **Shop B, G/F, 23 Yu Chau Street** — the exact range |

`scripts/decode-plus-code.py` self-tested first: 3 published anchors + floor-encode + grid refusal
+ 2,000 round-trips + 18 short-code recoveries across four hemispheres.

---

## 🔴 ONE POST IS BLOCKED, PROVEN AGAINST THREE LIVE CONTROLS

`C3r_kz1v9Ct` returns **220,176 bytes with `contextJSON: null`**, against **260,914–268,939** for the
other 32. **Three already-live catalogue pins were fetched in the same pass with the same UA**
(`Dozent/1.0`) and all came back healthy at ~264 KB with a full context.

🔁 **Blocked now, not gone.** ⚠️ Do NOT re-wire it on "the link opens on my phone" — the owner is
signed in; a logged-out Atlas user gets a blank card and no hero.

---

## Coordinates

**Every one of the 32 reverse-verifies at zoom 18 onto its caption's or its published address's own
house number.** Highlights:

- **Named exactly by OSM:** `The Opposites` (49 Hollywood Road), `Mum's not home`, `H Queen's`
  (80 Queen's Road Central, for LPM), `利園三期 Lee Garden Three` (Tiffany Blue Box Café),
  `南豐紗廠 The Mills` (EONIQ).
- **Exact house numbers:** Savon 191-193 Tai Nan · Social Goods 43-49 Wellington · Il Presidente 13
  Duddell · Chouchou 60 Johnston · Lazy Suzy 21 Staunton · Alma 51-53 Peel · Dieci 29-31 Gough ·
  Roru Baru 100 Queen's Road East · O'rm 8 On Wo Lane · Omnia 33 Staunton · Crushed Wines 6-8
  Second · Pineapple Bakery 87 Wing Lok · Louise 35 Aberdeen · Mama Tiger 12-14 Kau U Fong ·
  Trattoria Felino 1-7 Ship Street.
- **Inner-tenant / nearest-addressed-node cases, all correct:** Roru Baru reverses to *La Crêperie
  Wan Chai* at the same number 100; Madame Fu to *Dragonfly* at **10 Hollywood Road**, which is Tai
  Kwun's own address; Bakehouse to *Monopoly Dreams* at **118 Peak Road**, inside Peak Galleria; Red
  Sugar to *Red Chicken* at **38 Hung Luen Road**, the Kerry Hotel's own address; Louise to *Glimpse
  PMQ*, the neighbour at 35 Aberdeen Street.

**⚠️ WEBSEARCH WORKS FROM THIS CONTAINER AND IT UNBLOCKED HALF THE BATCH.** Session 137 recorded
headless web search as blocked; it is not, and twelve venues that OSM does not map were closed from
their own published addresses: Lazy Suzy (21 Staunton), Alma & (51 Peel), Roru Baru (100 Queen's Rd
E), Omnia (33 Staunton), Mama Tiger Noodles (12 Kau U Fong), Jean-Pierre (9 Bridges St), O'rm (8 On
Wo Lane), The Ice Bath Club (38 Belcher's St), Tiffany Blue Box Café (Lee Garden Three), Chez Trente
(6 Chung Wo Lane), Heartwarming (23 Yu Chau St). **Try it before deferring a venue.**

**⚠️ Two street-walk cases, resolved by reading the road AND the number back** (the Crosby Street
method):
- **The Ice Bath Club, 38 Belcher's Street** — no house-number node. Walking the street found
  **35A at `22.28360, 114.13030`**, the immediate neighbour; the pin sits there.
- **Jean-Pierre, 9 Bridges Street** — no node either, and the street centroid reverse-geocoded to
  **no. 68**, ~100 m west. Bridges Street's numbers *decrease* eastward: **Bridges Street Market is
  no. 2** at `114.15093` and **Yardbird no. 33** at `114.15075`, which bracket no. 9. The pin sits
  at `22.28331, 114.15080`. ⚠️ **This is the batch's weakest coordinate, stated rather than hidden.**

**⚠️ `countrycodes=hk` returns nothing — OSM files Hong Kong under `cn`** — so every forward query
was bounded to a viewbox. Nominatim 429s under load; the passes were paced at 2 s with backoff.

---

## Heroes

**All 32 opened and read against their captions — ZERO wrong subjects.** Nine name themselves in
frame: `RORUBARU`, `SOFT THUNDER`, `the ice bath club`, `little bao` (on the chef's shirt),
`Trattoria Felino / Modern Trattoria`, the Mama Tiger neon (a tiger's head over a noodle bowl),
`PRIMO POSTO`, the Tiffany-blue ceiling installation, and `錶匠 WATCH MAKER`. Two more are confirmed
by an independent detail: **Bakehouse's frame is pastries on a terrace over Victoria Harbour from
the Peak**, and **Mum's Not Home's is the hand-drawn menu board its caption describes**.

**⚠️ THREE HAND RE-CROPS — the vertical `--focus` gap, FIFTEENTH batch running.** Every source is
9:16, so the square crop is width-limited and `--focus` does nothing vertically. Re-rendered through
a mirror of the tool's own pipeline (`/tmp/hkbatch/recrop.py` — imports `make-link-pin.trim_bars`,
same blur/dim/WEBP quality, **same filenames**, so `Tours.json` is untouched):

| pin | vfocus | recovered |
|---|---:|---|
| The Vintage Watch Arcades of Mong Kok | 0.62 | `BEST VINTAGE WATCH AREA IN HONG KONG` |
| Tiffany Blue Box Café | 0.28 | `Breakfast at Tiffany's is now real in Hong Kong` |
| Mama Tiger Noodles | 0.26 | the whole neon tiger sign |

**⚠️ Straplines clipped elsewhere were deliberately LEFT** — `Hong Kong's hottest new opening`,
`Underrated restaurant series`, `New Viral Bakery` are topic hooks, not the subject's name (the
California Academy rule).

**⚠️ FLAGGED, NOT FIXED — weak heroes.** **Heartwarming is the weakest**: the creator walking a
street, no view of the shop or its sesame desserts. **Lazy Suzy, Dieci and La Petite Maison** are
creator-forward with little venue identity. A link pin re-hosts only the thumbnail, so no other
frame exists.

**⚠️ 27 of 32 will not play inline** — the documented licensed-music gate (`video_url` absent from
the embed payload). Poster + `OPEN IN INSTAGRAM` is the correct outcome.

---

## Tags and categories

29 `foodAndDrink`, 2 `hiddenGems` (EONIQ, Savon Workshop, the watch arcades, the Ice Bath Club — 4
in total), 1 `musicAndPerformance` (Chez Trente, a jazz club and art space). Every pin carries ≥1
Place type and ≥1 Theme. **The Ice Bath Club takes `Commerce` as its Theme** — a wellness club has
no closer honest fit in the 17-term theme facet. **No architect tag anywhere** — not one caption
names an architect.

---

## Verification

- **Validator mirror** (`scripts/validate-tours-mirror.py`) — vocabulary from both Swift files,
  **385 tags**, plus the enum domains. Its own selftest **19/19 with a clean control**, then
  **0 errors, 0 warnings across 1,552 tours + 937 pins + 105 places**.
- **⚠️ 18 fault classes injected against THIS batch's own rows — 16 caught, control clean.** The two
  misses are **gaps in the mirror, not in the data**: it checks neither an empty title nor centroid
  drift. Both are asserted directly on the 32 new pins instead (plus `kind=link`, exactly one stop,
  `manual` trigger, HK city/country, no `//`).
  **⚠️ My first fault harness reported 16 false MISSES** — the mirror **exits rc=2 with
  `control DIRTY` / `SELFTEST FAILED — verdict not trustworthy`** when the catalogue it is checking
  carries an injected fault, and my regex only looked for the `N errors, N warnings` line. **Read
  the exit code, not the summary line.** That is the session-126 "the miss was the TEST's own bug"
  lesson again.
- `make-link-pin.py --selftest` **71/71** (⚠️ with Pillow installed first; a bare container reports
  62/62, which reads as a pass and is not one).
- **0** duplicate tour/stop/maker ids · **0** already-pinned sourceURLs · **0** byte-duplicate heroes
  · closest perceptual pair **35.9** (identical pictures score under 1) · **0** orphan `makerId`s.
  ⚠️ The catalogue's 2 duplicate `sourceURL` groups are **both the documented deliberate cases**
  (`@malata.antwerp` five Italian markets from one video; the Bedi Makky / Charging Bull pair) and
  **neither is this batch's**.
- **0 filename collisions** against **6,724** gh-pages `images/` paths, the listing **asserted to
  hold >1,000 first**. ⚠️ The **bare-slug check was clean too** — the handle suffix was not
  load-bearing here, it simply was not called on.
  ⚠️ **`curl` to `api.github.com` is blocked in this session** (`GitHub access is not enabled`), so
  the tree came from a `--filter=blob:none --no-checkout` clone of gh-pages instead.
- `Tours.json` **byte-stable under a Python re-dump at `indent=2` before editing** (the assembler
  asserts it and refuses otherwise); diff **1,443 insertions / 0 deletions**, asserted purely
  additive — `tours` and `places` byte-identical, existing makers and pins unchanged as a prefix,
  top-level keys unchanged.
- `seed_from_toursjson.py` clean at **299 / 2,489 / 2,861 / 105**; **0 `images//`** in the catalogue
  *or* the generated SQL.
- `check-place-candidates.py` **EXACT unchanged at 1, NEAR 36 → 37** — the one added pair is
  **Bakehouse 129 m from the Atlas `Victoria Peak | 太平山頂` tour**, honest and expected. **No
  coincident group was manufactured and none was nudged apart to dodge the checker.** ⚠️ Exit code
  read **directly, not through a pipe**. The 1 EXACT is the pre-existing Hideout, Mui Wo pair.
- **gh-pages `2baba65`**: remote head **re-read in the same command as the push**, tree diff
  **exactly 32 additions, 0 deletions, nothing outside `images/`**, none of the 32 target paths
  pre-existing. Deploy run 817 read **`in_progress`, never `cancelled`**; my commit re-confirmed an
  **ancestor of head** afterwards. **All 32 live URLs then hash-verified against the uploaded bytes
  — 32 ok, 0 mismatch, 0 non-200.**
- 🔴 **`check-image-duplicates.py --pins` was run AFTER the deploy** (the session-135 false-pass
  lesson): **`OK — no suspicious duplicates`** over **932 images with 0 × 404**; shared-URL half
  **0 errors / 208 documented reuses**, identical to baseline, so this batch adds no shared URL.
  ⚠️ 932 images for 937 pins is exactly the documented `@malata.antwerp` five-pins-one-file case.
- ⚠️ **Nothing compiled — CI is the authoritative validator, and no PR is open.**

**⚠️ In-batch proximity, checked not assumed:** closest pairs are **Primo Posto ↔ Little Bao 13.8 m**
(7-9 vs 1-3 Shin Hing Street — genuinely two addresses on one short stepped lane), Dieci ↔ O'rm
17.8 m, Chouchou ↔ Trattoria Felino 30.0 m. All building-scale, which
`MapClustering.needsDisambiguation`'s backstop handles. **No coincident pairs.**

---

## Owner decisions outstanding

1. **The stray Plus Code `75M2+9X`** — it belongs to no post in this batch. Was it meant for one?
2. **Heartwarming's hero** — the batch's weakest; keep or pull.
3. **Jean-Pierre's coordinate** — bracketed between Bridges Street nos. 2 and 33, not pinned to no. 9.
4. **No PR opened.** Say the word and one goes up.
