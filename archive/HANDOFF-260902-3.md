# Handoff — 2026-09-02 (session 137, follow-up 2)

**Three more Hong Kong link pins, recovered from the deferred list with no outside help.**
Follow-up to `HANDOFF-260902-2.md`, which shipped 46 of 116 and deferred the rest for want of a
street address. Content only — no Swift, no SQL, no build.

**linkPins 786 → 789 · makers unchanged at 263 · tours and places byte-identical.**

---

## What happened

The 46-pin batch merged as [#708](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/708)
(squash `a86ab103`) and was **verified live on the Supabase RPC — 786 pins, 46 new, 0 wrongly
inside `tours`**, with `priceTier` on all 1,553 (66 priced), `isPrivate` on every maker and
`country` on 1,552. ⚠️ **The gh-pages CDN served the old 740 for roughly ten minutes afterwards
while its committed blob already held 786** — the documented lag; the committed blob, never the
served URL, is what says a publish landed.

Then, **before asking anyone for addresses**, the deferred captions were swept for address shapes.

## 🔴 The deferred list was searched before asking for help — do this first on any batch

A regex sweep for `\d+ …Road/Street`, `G/F`, `\d+/F` and `號` across every unshipped caption
returned **exactly four hits**, three of them Hong Kong:

| Post | Venue | Address the caption itself states |
|---|---|---|
| `DBYnXEky1Cb` | Kowloon Hum Hot Pot | 469-471 Nathan Road, Yau Ma Tei |
| `DGAsg8HyIdt` | 雞蛋仔屋 Egg Waffle House | G/F Chiap Thong Building, To Kwa Wan |
| `DbQLTEhRSt1` | Haeundae Galbi | 2-8 Wellington Street, Central |

(The fourth is a Tokyo hotel — out of scope.)

All three had been deferred because their **forward geocode was wrong or absent** — 雞蛋仔屋 was one
of the seven the reverse check caught, landing in **Tai Po**. The address was in the caption the
whole time.

## Coordinates — each reverse-verified at zoom 18 onto the building the caption names

- **Kowloon Hum** → `新光商業大廈 Sunbeam Commercial Building, 469-471, 彌敦道 Nathan Road, 油麻地
  Yau Ma Tei` — forward **and** reverse, at the **exact house range** the caption gives.
- **雞蛋仔屋** → forward names `捷通大廈 Chiap Thong Building, 土瓜灣道 To Kwa Wan Road` **by name**.
  ⚠️ **Its reverse lands on `中信置業 K&T Realty` on the same street** — the documented
  nearest-addressed-node case, not a bad point.
- **Haeundae Galbi** → `威靈頓廣場 Wellington Place, 2-8, 威靈頓街 Wellington Street, 中環 Central` —
  forward **and** reverse, exact range match.

## Heroes — all three opened and read against their captions

Zero wrong subjects.

- **Kowloon Hum names itself in frame** — the burned-in card reads *"With Mahjong & Karaoke rooms?
  (九龍坎麻辣火鍋)"*, confirming the venue **independently of the caption**.
- **Egg Waffle House** shows the heart-shaped waffles the caption is entirely about.
- ⚠️ **Haeundae Galbi's frame is largely the creator rather than the room** — flagged, not fixed. A
  link pin re-hosts only the thumbnail, so no other frame exists. Its top line is clipped, but what
  is lost is a hook, not the subject's name (the California Academy rule), so it was left alone.
- ⚠️ **One will not play inline** — the documented licensed-music gate. Poster +
  `OPEN IN INSTAGRAM` is the correct outcome, not a defect.

## Titles

`雞蛋仔屋 Egg Waffle House` carries both forms: 雞蛋仔 is *egg waffle* and 屋 is *house*, so the
English half is a direct rendering rather than an invented trading name. Consistent with the
batch's existing `Pak Mei Snack 百味食品 …`.

## Verification

- `Tours.json` **byte-stable under a Python re-dump before editing** (the build refuses otherwise).
- Diff **132 insertions / 0 deletions**, asserted **purely additive**: `tours` and `places`
  byte-identical, existing makers and pins unchanged **as a prefix**.
- The uuid5 scheme **reproduced the existing `@shivanidukhandee` maker id exactly**, so it merged
  rather than duplicating (the validator errors on `duplicate maker id`).
- Validator mirror — vocabulary parsed from **both** `Models/Tag.swift` **and** the Swift validator,
  refusing to run on disagreement or an empty parse (**385 tags**), plus the enum domains —
  **self-tested against 14 injected fault classes, 14/14 caught, control clean**, then **0 errors,
  0 warnings** across 1,552 tours + 789 pins + 100 places.
- **0** duplicate tour/stop/maker ids · **0** already-pinned `sourceURL`s · **0** byte-duplicate
  heroes · closest perceptual pair **39.6** (identical pictures score under 1).
- **0** filename collisions against **6,603** existing gh-pages `images/` paths, with the listing
  **asserted to hold >1,000 first**. ⚠️ **The bare-slug check came back clean** — the handle suffix
  was not load-bearing here, it simply was not called on.
- `seed_from_toursjson.py` clean at **263 / 2,341 / 2,713 / 100**; **0 `images//`** in the catalogue
  *or* the generated SQL.
- gh-pages `992a7ca4`: remote head **re-read in the same command as the push**, tree diff **exactly
  3 additions, 0 deletions, nothing outside `images/`**.
- ⚠️ **Nothing compiled locally** — CI is the authoritative validator.

## Where the batch stands

- **49 shipped · 67 deferred · 2 blocked · 1 dropped** (`DViylUIjGUg`, a reposted Disneyland clip —
  one line to restore).
- `drafts/hk-shivanidukhandee/ADDRESSES-NEEDED.txt` is down to **62 named venues**. ⚠️ **Its stated
  total was off by one from the start** — it said "63 venues" while listing 64 — so it is now
  **counted from the file rather than asserted**.
- The **2 blocked posts** (`contextJSON: null`) are worth a two-minute recheck in about a month; a
  restriction is often temporary. 🔴 **The test is `contextJSON` non-null on the embed, with a live
  control in the same pass — NOT "the link opens on my phone".**

## Still open

- The **67 deferred venues** need addresses from outside this container. Headless web search is
  blocked here and only three captions carried one, so this genuinely needs a person or a
  per-venue lookup.
- **6 place candidates** flagged in the previous handoff, none created.
