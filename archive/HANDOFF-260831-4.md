# HANDOFF — 2026-08-31 (session 127b, web, content)

> ⚠️ **Renumbered `-3` → `-4` at merge time.** A parallel session (128) had already claimed
> `HANDOFF-260831-3.md` for its own 105-pin batch — the documented add/add collision. Check
> `archive/` for the next free suffix before writing, and expect to renumber if a merge collides.

**Branch `claude/tour-links-upload-t5jk3n`, commit `6df08b8c`, pushed. NO PR OPENED.**
Restarted clean off `origin/main` at `308ad235` — the previous PR on this branch name
([#674](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/674), the 95-pin batch) is merged, so
this is a fresh change and not stacked on merged history.

**Places 56 → 66. Tours (1,552), link pins (483 after merging #675) and makers (208) all unchanged, byte-for-byte.**
Content only — no Swift, no SQL, no gh-pages push, no build.

---

## What the owner asked for

Verbatim, after being shown the ten Tier 1 candidates the 95-pin batch had produced:

> do tier 1. for tower of london, leave beauchamp tower seaprate. for westminster abbey, only put
> in the 2 tours that are general 'westminster abbey', leave out the others. for houses of
> parliament, only group 'houses of parliament and big ben' and 'house of commons'

**All three scoping calls are implemented and recorded in the commit message.** They are the
interesting part of this session — see § "The three exclusions" below.

## The ten places

Every one is an Atlas tour plus one or two `@history_alice` link pins of the same subject, except
CaixaForum and Ewha, which pair an Atlas tour with a `@domus` pin.

| Place | City | Members | Hero |
|---|---|---|---|
| Westminster Abbey | London | 2 | `westminster-abbey_6.webp` |
| The National Gallery | London | 3 | `national-gallery_2.webp` |
| The Tower of London | London | 2 | `tower-of-london_2.webp` |
| Natural History Museum | London | 2 | `natural-history-museum_5.webp` |
| Churchill War Rooms | London | 2 | `churchill-war-rooms_2.webp` |
| The Charles Dickens Museum | London | 2 | **`null`** |
| Leighton House | London | 3 | `leighton-house_3.webp` |
| CaixaForum Madrid | Madrid | 2 | `caixaforum-madrid_2.webp` |
| Ewha Campus Complex | Seoul | 2 | `ewha-campus-center_3.webp` |
| Houses of Parliament | London | 2 | `houses-of-parliament_2.webp` |

Ids are `uuid5(NAMESPACE_URL, "atlas-place:<city-slug>:<name-slug>")`, the scheme reverse-verified
against the existing places before minting. All names are pure ASCII, so the documented
accent-folding hazard (`Hackesche Höfe` → `hackesche-h-fe`) does not apply.

## The three exclusions — the owner's judgement, recorded so nobody "completes" them

- **🔴 The Beauchamp Tower stays its own pin, NOT inside The Tower of London.** Only `The White
  Tower` pin joined the place. The Beauchamp Tower is a specific building within the fortress with
  its own story (the prisoners' carved graffiti), and the owner's call is that it reads as its own
  subject rather than a member of the site. **Do not add it.**
- **🔴 Only the two general Westminster Abbey entries are members.** The batch put **six**
  `@history_alice` posts at the abbey — nave, sanctuary, the Confessor's shrine, the Lady Chapel,
  the Weston Tower and the abbey node — each deliberately placed on a distinct, reverse-geocoded
  interior point so the stack cap could never hide one. The place takes the Atlas tour plus the one
  general abbey pin; **the four interior-specific pins stay where they are.** That is what those
  distinct coordinates were for.
- **🔴 Houses of Parliament groups the Atlas tour with the House of Commons pin only.** The Big Ben
  pin is excluded — the tour is titled *Houses of Parliament and Big Ben*, so the tower is already
  covered by a member, and the owner wanted the place kept to the building rather than the
  landmark cluster.

⚠️ **All three are the same shape: a famous site has more posts than it has subjects, and the owner
decides which of them are the same place.** A future audit will see these as unresolved EXACT/NEAR
pairs. They are settled — honour the decision (the Ministry of Enterprise / Casa Lleó Morera
precedent).

## The pin moved, and the tour did not — with exactly one exception

Verified by diffing the parsed arrays rather than asserted: **0 makers changed**, and **exactly one
tour** changed a coordinate.

- **9 pins moved, 6.1 m – 34.7 m** onto their tour's coordinate (Ewha's pair was already exactly
  coincident, so its move is 0.0 m). Every one is `manual`, so no geofence is disturbed.
- **🔴 THE ONE TOUR THAT MOVED IS LEIGHTON HOUSE, 179 m, AND IT IS A PRE-EXISTING ERROR, NOT A
  CONCESSION TO THE PIN.** The tour sat at `51.4995, -0.201`, which reverse-geocodes to **Park
  Close** — a residential street 179 m from the museum — while the tour's own script opens *"From
  the street, Leighton House looks like a fairly stern red-brick Victorian villa"*. The pin sat on
  the museum. **The tour is `manual`, so nothing was ever firing there and nothing changed by
  moving it** — but at a geofence it would have been the IAC / Chelsea Hotel failure again. Its
  `triggerMode` and `triggerRadiusMeters` are byte-identical before and after; only the stop
  coordinate and the mirrored centroid moved.

## Heroes — nine third photographs, one deliberate null

**Every hero is a third photograph promoted from a member tour's own gallery** — already uploaded,
already verified, **nothing sourced** — and the build asserts it: `hero_url not in member_heroes`
is a hard check, so a place cannot print one picture three times (the fault found across 13 of the
first 24 places). All nine live URLs return **200**.

- **⚠️ The Charles Dickens Museum ships `heroImageURL: null`, and that is correct rather than
  lazy.** `Place.heroImageURL` is optional by design — *"when absent the place page falls back to
  the hero of its top-ranked tour"* — and the tour's only spare gallery image is a **19th-century
  engraving**, not a photograph of the house. A borrowed member hero would have been the
  Waterlooplein case; a fallback to the same image with no claim attached is honest. One sourced
  photograph fixes it whenever one exists.
- **⚠️ Natural History Museum took `_5` rather than the obvious `_2`, and the reason is a
  pre-existing bug — see below.**

## 🔴 FOUND, FLAGGED, NOT FIXED: the two Natural History Museums share a gallery

The London **Natural History Museum** tour and the Los Angeles **Natural History Museum of LA
County** tour reference the **identical seven image URLs**. It is a bare-slug collision from before
the handle-suffix convention: `natural-history-museum_2.webp` is **the LA County museum** and
`natural-history-museum_4.webp` is **Los Angeles Union Station**. So both tours' galleries mix
London and LA photographs today, and have for some time.

- **This is why the place hero is `_5`** — it was the highest-numbered image verified to be the
  London building.
- **The fix is not a byte swap.** Per the standing rule (#567), correcting an image means a **new
  filename**, never overwriting bytes at a live URL — a phone that has downloaded either tour reads
  its photographs off its own disk and would keep the wrong ones forever. So this needs new files
  under handle- or city-suffixed names plus a `Tours.json` repoint on both tours.
- **Pre-existing and out of scope for a place-card batch.** Flagged to the owner, not touched.

## Verification

- **Validator mirror** — vocabulary parsed from **both** `Models/Tag.swift` **and** the Swift
  validator, refusing to run if they disagree or either parse is empty (they agree at **385 tags**)
  — **self-tested against 20 injected fault classes, 20/20 caught**, including all seven place-layer
  checks; then **0 errors, 6 warnings across 1,552 tours + 483 pins + 66 places**, **all six
  pre-existing** (the same mirror against `origin/main` reports the identical set).
- `Tours.json` **byte-stable under a Python re-dump before editing**; diff **192 insertions / 40
  deletions** — ten places plus the coordinate moves and nothing else.
- **`check-place-candidates.py` 11 EXACT → 8, 41 NEAR → 35.** the three groups it resolved are **CaixaForum Madrid**, **The National Gallery** (two coincident pins) and **Leighton House + The Arab Hall**. ⚠️ **The eight that REMAIN are not the owner's exclusions** — those sit at deliberately distinct coordinates and never appear in EXACT at all. The eight are all pin-only pairs, seven from the 95-pin batch (Harvington Hall, Hatfield House + the Elizabeth Oak, Syon Park, York Minster + Roman York, Windsor Castle ×2 groups, Hever Castle) plus **Gracie Mansion** from #675 — every one still needs an owner decision, and a place for any of them would have to borrow a member's hero.
- `seed_from_toursjson.py` regenerates cleanly at **208 makers / 2,035 tours / 2,407 stops / 66
  places**, exercising its own `validate_places`.
- All 9 place hero URLs live **200**.
- **⚠️ Nothing compiled and CI has not run: no PR is open.**

## Owed / next

1. **Open a PR** — this session's harness forbids opening one unasked, and the owner has not asked
   for this batch (their earlier *"Open PR then merge"* was for the 95-pin batch, which is merged).
2. **The Natural History Museum shared-gallery bug** above — needs new filenames and a repoint on
   two tours.
3. **The Charles Dickens Museum place hero** — one photograph of the house closes it.

---

## Merge note (added at merge time)

`main` moved twice while this branch sat unpushed — **[#675](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/675)
added 105 more link pins from `@hereinnyc`** (pins 378 → 483) plus a STATUS re-derive. The PR
therefore opened `dirty`, **and a conflicted PR never fires CI** — which is why no `ci.yml` run
appeared for half an hour, not a webhook problem.

- **`Tours.json` auto-merged and was NOT trusted.** Taken from `main` and the **idempotent
  assembler re-run on that base**, per the documented rule. The result is identical: the same ten
  places, the same ten coordinate moves at the same distances, the same member counts, the same
  **192 insertions / 40 deletions**. None of the 105 new pins collided with a member title.
- **⚠️ `archive/HANDOFF-260831-3.md` was an add/add conflict** — session 128 had claimed it for its
  own batch. This file was renumbered **`-3` → `-4`**; theirs keeps `-3`. Every reference in
  `CLAUDE.md`, `ROADMAP.md` and `archive/README.md` was retargeted.
- **All the doc conflicts were both-sides-real inserts** (a Current State entry, a ROADMAP status, a
  STATUS board entry) and were resolved **keep-both**; the two `Last verified` lines collapsed into
  one.
- **Every check was re-run on the merged base, not carried over:** mirror selftest **20/20**, then
  **0 errors, 6 warnings** across 1,552 tours + 483 pins + 66 places — and the six were confirmed
  identical against `main`'s own catalogue rather than assumed. `check-place-candidates.py` **11
  EXACT → 8, 41 NEAR → 35** (the absolute base rose with the 105 new pins; **the delta is the same
  −3 / −6** measured before the merge). `seed_from_toursjson.py` clean at 208 / 2,035 / 2,407 / 66.
