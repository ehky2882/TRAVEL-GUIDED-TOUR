# Atlas — Handoff Notes (2026-05-29 evening, session 12)

Cut TestFlight build **1.0 (17)**. Build-bump-and-archive only — no
Swift / Tours.json / asset changes. Companion to `HANDOFF-260529.md`
(PR #95 bug fix, same day, earlier) — that session deliberately did
not bump; this one did.

---

## What shipped this session

### Build 1.0 (17)

- `CURRENT_PROJECT_VERSION` 16 → 17 in `project.pbxproj` (commit `f359f55`, direct to `main` per automation rule #7). App-target lines only; test-target stays at 1.
- `xcodebuild archive` clean. Archive at `/tmp/Atlas-20260529-1626.xcarchive`.
- Xcode Organizer opened automatically; owner did Distribute App → App Store Connect → Upload. Owner confirmed upload complete at end of session.

### What build 17 carries (release notes)

Five PRs landed on `main` between build 16 and build 17 (sessions 11 + the morning sessions that wrote `HANDOFF-260529.md`):

- **#91** — bottom-module chrome-shade seam fix (already in build 16, carried).
- **#92** — content: Casa das Histórias Paula Rego (Eduardo Souto de Moura, 2009) in Cascais. First Cascais tour; 2nd under Atlas Studio Lisbon. Single stop, 148s, geofenced 60m at 38.6947, -9.4236. Audio + 5 webp gallery on `gh-pages`.
- **#93** — feat(detail): masthead + toolbar + overflow menu (part 1 of 2). Toolbar X (left) · Save (right) · ellipsis (right), no title text. Overflow: Download · Save · Share · *Follow creator (disabled)* · Go to creator · *Report a concern (destructive)*. Masthead = square-cornered hero · title · maker row · subtitle line (`3 min · 1 stop · 455 ft away`; multi-stop swaps in `… · 1.2 mi walk`) · inline button row above description · description peek with soft fade-mask. Inline button row also at bottom of scroll body. Carousel gets `N photos` overlay pill when >5 images. Stops header unified to `Stops`. **Part 2** reshapes stops into numbered timeline + thumbnails + animated `waveform` indicator and rewires Start Tour to non-modal playback start.
- **#94** — content: 5 Porto-area architecture tours (Edifício Burgo, House at Rua do Crasto 213, Leixões Cruise Terminal, Majestic Café, Piscina das Marés). All single-stop, manual trigger + 30m radius. Audio + 20 webp images on `gh-pages`. **New city: Leixões.** Catalog 54 → 59 tours.
- **#95** — fix(bottom-module): track system appearance correctly in both modes. Inverted-appearance bug fixed (`PassThroughWindow` was missing `overrideUserInterfaceStyle`; SwiftUI's `.preferredColorScheme` doesn't reach manually-created `UIWindow`s). See `HANDOFF-260529.md` for full diagnosis.

Catalog now: **59 tours, 3 makers** (43 NYC + 14 Porto-area + 2 Lisbon-makers including 1 Cascais).

---

## Verification

- `xcodebuild archive` → `** ARCHIVE SUCCEEDED **`.
- Code signing: Apple Development cert `A84C…0E54`, profile auto-provisioned via `-allowProvisioningUpdates`.
- No tests run this session (build-bump only — last full run was 84/84 in `HANDOFF-260529.md`).

---

## Files touched this session

- **Modified:** `TRAVEL GUIDED TOUR.xcodeproj/project.pbxproj` — `CURRENT_PROJECT_VERSION` 16 → 17 (both app-target lines; test-target lines untouched).
- **Modified at end-of-session:** `CLAUDE.md`, `ROADMAP.md`, `archive/README.md`; **new:** this file.

---

## Parked / known follow-ups (carried forward)

- **PR #93 part 2** — track-row timeline + thumbnails + animated `waveform` now-playing indicator + non-modal playback start. Detail-rework is intentionally split into two PRs.
- Mini-player title type style (both lines are `caption` today — no hierarchy), align skip-forward (20) + play/pause (18) glyph sizes, bump avatar 32 → 36pt to match play-ring diameter (parked since session 11).
- Detail-body scroll-content technically pokes 8pt into the bar's safe-area strip before being clipped. Bars now occlude it visually; underlying reservation in `TourDetailView` is short. Fold into a future detail-sheet pass.

---

## How to resume

1. Run the session-start ritual (`git fetch`, `git status`, `gh pr list`, read this handoff). Tree should be clean on `main` after the docs commit at end-of-session.
2. Likely next work: PR #93 part 2 (track-row timeline) — owner-driven retool. Or whatever owner brings up.
3. TestFlight 1.0 (17) processing — owner can check email or App Store Connect → TestFlight tab.
