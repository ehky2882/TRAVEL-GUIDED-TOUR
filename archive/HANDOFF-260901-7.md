# HANDOFF 2026-09-01 (session 135) — Sixteen Hong Kong link pins from one creator

**Owner: *"new tour links"*, then sixteen Instagram reels — five carrying Hong Kong Plus Codes.**

Branch `claude/tour-links-9ynig6`, cut clean off `main` at `eaa7cea` (the Riverside Church place).
**linkPins 565 → 581 · makers 226 → 227 · tours unchanged at 1,552 · places unchanged at 87.**
Content only — no Swift, no SQL, no build.

---

## 1. One creator, sixteen posts, all Hong Kong

Every link resolves to **Instagram `@love.in.hongkong`** — a new maker row
(`7515572C-C41F-58C4-8A13-C2DB0EC9D962` = uuid5 `atlas-maker:instagram:@love.in.hongkong`), the
227th. **No maker row for that handle existed before**, checked rather than assumed.

**All 16 alive and pinnable** — 16 distinct canonical URLs, **0 already pinned** against the 565
existing pins, **0 in-batch duplicates**. No dead posts, no `/photo/` carousels.

⚠️ **`avatarURL: null`, by design** — the Instagram embed exposes no creator avatar, so **16 files
cover 16 pins** and there is no avatar to upload. This is the documented Instagram shape, not a
failed fetch.

⚠️ **Hong Kong is NOT a new country** — the catalogue already holds 73 entries at
`city: "Hong Kong", country: "Hong Kong"`, and all sixteen match that convention exactly (checked
against those 73, not guessed).

The subjects: Red Incense Burner Summit · the Tung O Ancient Trail · Green Hub at the old Tai Po
Police Station · the Hong Kong Railway Museum · the Hong Kong Design Institute · PMQ · the
Graveyard of Buddhas at Waterfall Bay · Waterfall Bay itself · Pok Fu Lam Village · The Pokfulam
Farm · Bethanie · Jamia Mosque · the Hong Kong Cemetery in Happy Valley · the Sai Wan Swimming
Shed · villain hitting under the Canal Road Flyover · the model boat pool in Victoria Park.

---

## 2. 🔴 The Plus Code decoder was written by hand again, and its ONE failure was my own arithmetic

`pip install openlocationcode` still cannot build a wheel here (fourth session running). Written
from the specification's own pair table — 20° / 1° / 0.05° / 0.0025° / 0.000125° — with exact
`Fraction` arithmetic, and **`encode` FLOORS** (rounding tips a point on a cell boundary into the
next cell; session 133 measured that failing 1,483 of 2,000 round-trips).

**Passing suite: 2,031 checks, 0 failures** —

| what | result |
|---|---|
| hand-derived pair table, 10 digit positions + all three cell sizes | ✓ |
| Eiffel `8FW4V75V+8Q` → **48.8583125, 2.2944375** | ✓ |
| `7FG49Q00+` → centre **20.375, 2.775**, area 20.35–20.4 / 2.75–2.8 | ✓ |
| the documented float bug — `encode(20.375, 2.775, 10)` must be `7FG49QGG+22` | ✓ |
| 2,000 random round-trips, both directions | 0 bad |
| `recover_nearest` across four hemispheres × two drop lengths | ✓ |
| grid digits (>10) **refused, not guessed** | ✓ |

⚠️ **THE ONE TEST FAILURE WAS THE TEST, NOT THE DECODER — exactly the documented pattern.** I
asserted `"22222222+32"` should put `0.000125` in *both* lat and lon; `+32` puts the `3` in the
**latitude** slot and `2` (index 0) in longitude. The decoder was right. Both orders — and the
both-digits case — are now pinned so the mis-split cannot recur.

⚠️ **My hand-estimate of the Jamia Mosque code was ~490 m out** because I used a rough reference
point for `recover_nearest`. **Run the code; do not hand-derive a recovery.**

### The five codes, and how each was settled

| # | short | full | decoded | verdict |
|---|---|---|---|---|
| 7 | `64XP+HJ` | `7PJP64XP+HJ` | 22.2489375, 114.1365625 | reverses to **神像山 "A Sky Full of Gods and Buddhas"**, Waterfall Bay Road — OSM names the statue site itself |
| 12 | `75J2+3P` | `7PJP75J2+3P` | 22.2801875, 114.1518125 | reverses to **回教清真禮拜總堂 Jamia Mosque**, Mosque Street |
| 13 | `75CH+8V` | `7PJP75CH+8V` | 22.2708125, 114.1796875 | **see below** |
| 15 | `75HJ+JF` | `7PJP75HJ+JF` | 22.2790625, 114.1811875 | reverses to **鵝頸橋打小人 "Bowrington Bridge villain hitting"** — OSM names the ritual spot |
| 16 | `75JP+VR` | `7PJP75JP+VR` | 22.2821875, 114.1870625 | reverses to **噴水池廣場 Water Fountain Plaza**, inside Victoria Park |

---

## 3. 🔴 #13 reverse-geocodes to a MOTORWAY TUNNEL and the coordinate is correct

`75CH+8V` reverses at zoom 18 to **香港仔隧道 Aberdeen Tunnel** — which reads like a bad point.
It is not. The **Hong Kong Cemetery climbs a terraced hillside** with no addressable feature of
its own, so the reverse-geocoder picked the nearest thing that has an address: the tunnel running
beneath.

Settled the documented way — **fetch the polygon, don't argue with the reverse** (the Inter&Co
Stadium / Swan House / Arthur Ashe rule). Point-in-polygon against **OSM way 26103953
`香港墳場 Hong Kong Cemetery`** (43-point ring): **INSIDE**, 48 m from the area-weighted centroid.

**And the hero settles it independently**: a colonial obelisk inscribed `DIED AD 1845` over a list
of names, rows of white crosses up a terrace, a headstone reading `KUHLAN 1855`, title card
"CEMETERY HIKE". Coordinate, polygon and pixels all agree.

⚠️ **Sensitivity:** this is a burial ground. The copy stays factual; **no mortality figure appears
in any title, caption or description** (the Eastland / Harbour Bridge convention), even though the
obelisk in frame carries one.

---

## 4. ⚠️ `countrycodes=hk` returns NOTHING — OSM files Hong Kong under `cn`

The first geocoding pass returned **zero results for all four district references** and looked like
Nominatim was down. It was the filter: Hong Kong is `cn` in OSM. Dropping it returned every
district immediately.

**Re-query before concluding a place is unmapped** — sixth batch running where re-querying was the
whole fix. The same pass also had to fall back from `Aberdeen, Hong Kong` to
`Aberdeen, Hong Kong Island, Hong Kong`.

### The eleven un-coded subjects — ten named exactly by OSM

`紅香爐峰 Hung Heung Lo Fung` (natural/peak) · `東澳古道 Tung-O Ancient Trail` (footway) ·
`綠匯學苑 Green Hub` · `香港鐵路博物館` · `香港知專設計學院` · `瀑布灣 Waterfall Bay` (natural/bay) ·
`薄扶林村` (place/village) · `薄鳧林牧場 The Pokfulam Farm` · `伯大尼修院 Béthanie` ·
`西環鐘聲泳棚 Sai Wan Swimming Shed`.

- **The Pokfulam Farm resolves to `141A Pok Fu Lam Road` — the exact address the caption gives.**
- **⚠️ Red Incense Burner Summit reverses to "Monkey Buttress"**, a climbing crag on the same
  hillside, while the *forward* search names the peak exactly at that coordinate. The
  forward-names-it / reverse-picks-a-neighbour case; the peak node is kept.
- **⚠️ PMQ is the one Nominatim cannot find by name** — both `元創方 PMQ` queries return nothing.
  The pin sits on `Glimpse PMQ`, a venue inside PMQ on **Aberdeen Street**, PMQ's own address. The
  inner-POI case (the Oslo opera gift shop, 150 N Riverside).
- **⚠️ The Tung O Ancient Trail is LINEAR** — a centroid is meaningless (the towpath case). It is
  pinned at the **Tung Chung Valley end**, which reverse-verifies to the trail by name and is the
  start the caption sends you to (*"Start: Tung Chung Station"*). Pinning the Tai O end would also
  have clustered it with the existing `Tai O` tour and `Tai O Promenade` pin.

---

## 5. ✅ All 16 heroes opened and read against their captions — zero wrong subjects

Ten name themselves in frame: *TUNG O ANCIENT TRAIL* · *Hong Kong's secret GRAVEYARD of BUDDHAS* ·
*WHY this waterfall name HONG KONG* · *THE LAST VILLAGE / Hong Kong Island* · *BETHANIE | HONG
KONG* · *THAT MINTY GREEN MOSQUE* · *CEMETERY HIKE* · *SAI WAN SWIMMING SHED* · *打小人婆婆 /
Villain Hitting Grandma* · *VICTORIA PARK / MODEL BOAT POOL*.

Two are confirmed by a detail rather than a title card:
- **#4** is the Railway Museum's preserved **locomotive No. 60**, the number legible on its nose.
- **#12**'s mint-green onion dome and horseshoe arches are unmistakably Jamia Mosque, which
  independently confirms the Plus Code decode.

### ⚠️ One hand re-crop — the vertical `--focus` gap, THIRTEENTH batch running

Every source is 9:16 (#16 is 640×1136), so the square crop is **width-limited and `--focus` does
nothing at all**. #16's centred square sliced *MODEL BOAT POOL* through the middle of its letters.
Re-rendered through a mirror of the tool's own pipeline — importing its `trim_bars`, same blur,
same pad, same quality, **same filename**, so `Tours.json` is untouched — at **vertical focus
0.10**, recovering the full card *"VICTORIA PARK / MODEL BOAT POOL / HONG KONG"*.

That is **recovering what the crop destroyed, not removing what the creator put there** — and it
also confirms the coordinate, since the card names the park. **#8 and #13 clip the top of a hook
line and were deliberately left alone**: what they lose is a strapline, not the subject's name
(the California Academy rule).

### ⚠️ Four weak heroes, flagged not resolved

A link pin re-hosts only the thumbnail, so **no other frame exists** for any of these.

- **#10 The Pokfulam Farm is a close-up of a blue COW SCULPTURE.** Nothing in frame identifies the
  farm, or Hong Kong. The weakest in the batch and the likeliest pull.
- **#6 PMQ is a basement** — pipework over the excavated 19th-century Central School foundations,
  titled *SECRET RUINS*. Correct subject (it is what the post is about) and unrecognisable as PMQ.
- **#3 Green Hub is an interior jail cell** with a person behind the bars.
- **#5 HKDI is a corridor detail** — the interlocking V-columns the post is about, not the famous
  floating campus.

⚠️ **#15 and #16 carry identifiable people.** In #15 the practitioner *is* the subject (the ritual
is performed by a person); #16 has three spectators in the foreground. Both are the creator's own
frames.

---

## 6. Three place candidates — flagged, none created, and **nothing was nudged**

Three subjects already exist as Atlas tours. Each new pin was sited on **its own independent
evidence** and deliberately **not** moved onto the tour's coordinate, which would manufacture an
EXACT group with no place behind it.

| pin | Atlas tour | gap | why they differ |
|---|---|---|---|
| Jamia Mosque | `Jamia Mosque \| 些利街清真寺` | **28 m** | pin on OSM's own mosque node, tour at 4-dp precision — the CalAcademy rounding artifact |
| Sai Wan Swimming Shed | `Sai Wan Swimming Shed \| 西環鐘聲泳棚` | **30 m** | pin on OSM's named viewpoint node |
| PMQ | `PMQ \| 元創方` | **45 m** | pin on Aberdeen Street, tour elsewhere on the block |

**All three could take a real third photograph** from the Atlas tour's own gallery if the owner
ever wants place pages — so the borrowed-hero problem does not arise here.

⚠️ **#7 and #8 are 460 m apart and are two different subjects** — the deity statues on the
shoreline, and the waterfall that named Hong Kong. Both are the owner's own links. **Do not tidy
them together.**

**`check-place-candidates.py`: 0 EXACT / 25 NEAR against a 0 / 21 baseline — EXACT unchanged at
zero and the script still exits 0**, so the clean-exit state is preserved and no coincident group
was manufactured. The +4 NEAR are exactly the three pairs above plus #7↔#8.
⚠️ The exit code was read **directly, not through a pipe** (the `PIPESTATUS` trap).

---

## 7. ⚠️ Three pins will not play inline — the licensed-music rights gate

`plays_inline` is **False** on #10 (Pokfulam Farm), #12 (Jamia Mosque) and #14 (Sai Wan Swimming
Shed): Instagram withholds `video_url` for reels using its licensed music library, so those three
open Instagram on tap instead of playing in the app. **The poster + `OPEN IN INSTAGRAM` fallback
is the correct outcome, not a defect** — asked and answered 2026-08-30.

⚠️ **The tool's REASON STRING is wrong while its verdict is right.** It reports
`licensed music (8K followers)` — the track-name regex has captured the follower count, the
documented session-125 cosmetic bug. **The verdict comes from the absent `video_url`, not from the
track name.** One line in `instagram_playability`.

---

## 8. ⚠️ Coldefy & Associés is named in a caption and is NOT in the vocabulary

#5's caption ends `Architect: Coldefy & Associés`. The name is absent from all 335 architect tags,
so HKDI ships the generic **`Designed by a Master`** — the caption-driven rule working. Adding the
name is a `Models/Tag.swift` **code** change and was kept out of a content batch.

**No other caption in the batch names an architect**, so no other pin carries a master tag — the
Jules Dalou rule. Bethanie carries `Architecture` and `Gothic` but no architect, correctly.

⚠️ **Where a subject exists as both a tour and a pin, the pin MATCHES the tour's tags** so the pair
shares shelves: PMQ takes `District, Architecture, Art, History, Power` and Jamia Mosque
`Religious Building, Faith, History, Immigration, Maritime`, both lifted from the Atlas tour.

---

## 9. Verification

- **Validator mirror** — vocabulary parsed from **both** `Models/Tag.swift` **and**
  `scripts/validate-tours.swift`, refusing to run if they disagree or either parse is empty (they
  agree at **385 tags across 5 facets**). **Self-tested against 40 injected fault classes — 40/40
  caught, control clean**, and ⚠️ **26 of the 40 were injected against the NEW pins specifically**,
  because a suite that predates the rows you just added is not evidence about them.
  Then **0 errors, 2 warnings** across 1,552 tours + 581 pins + 87 places — **both warnings
  byte-identical on `origin/main`**, so this batch contributes zero of each.
- `make-link-pin.py --selftest` **71/71** — ⚠️ **with Pillow**; a fresh container has none and
  reports 62/62, so install it before reading that as a pass.
- **0** duplicate tour / stop / maker ids · **0** already-pinned sourceURLs · **0** byte-duplicate
  heroes · **0** perceptual pairs even *nominated* at Hamming ≤ 45.
- **🔴 The first filename-collision check fetched nothing and would have reported a pass** — the
  GitHub trees API returned no `tree` key. **An assertion that the tree must hold >1,000 images
  caught it**; redone against a blobless clone: **0 collisions against 6,512 `images/` paths**.
  ⚠️ **The bare-slug check came back clean** — 0 of 16 would have overwritten a live hero, so the
  handle suffix was not load-bearing here; it simply was not called on.
- **`Tours.json` byte-stable** under a Python re-dump (`indent=2, ensure_ascii=False` + trailing
  newline) before editing. Diff **748 insertions / 0 deletions**, and asserted purely additive:
  `tours` and `places` byte-identical to `origin/main`, existing `makers` and `linkPins` untouched
  as a prefix, top-level keys unchanged.
- **0 `images//`** anywhere — in the catalogue *or* in the generated seed SQL, which is the only
  place the session-132 double-slash bug was ever visible.
- `seed_from_toursjson.py` clean at **227 makers / 2,133 tours / 2,505 stops / 87 places**.
- **gh-pages `47a686d`** — remote head **re-read in the same command as the push**, tree diff
  **exactly 16 additions, 0 deletions, nothing outside `images/`**, push status read through
  `PIPESTATUS`; then **all 16 live URLs hash-verified against the uploaded bytes**.
- ⚠️ **Nothing compiled — CI is the authoritative validator.**

---

## 10. What is left

- The **four weak heroes** in §5 are the owner's call — #10 (the cow) most of all.
- **Three place candidates** in §6, each needing copy, an address, a photograph and approval.
- `Coldefy & Associés` for the next architect vocabulary PR (§8).
- The `instagram_playability` reason-string regex (§7) — cosmetic, one line.
