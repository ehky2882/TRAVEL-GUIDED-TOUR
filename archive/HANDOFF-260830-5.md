# HANDOFF — 2026-08-30 (session 124)

⚠️ **This file was written as `-4` and renumbered on merge.** A parallel session claimed
`HANDOFF-260830-4.md` for its own work (which had itself been renumbered from `-3`) — an add/add
conflict, both real, neither wrong. Theirs keeps `-4` because it merged first. **Check `archive/`
for the next free suffix before writing, and expect to renumber anyway** — this is now the third
recorded collision on the same day's suffixes.

**An Instagram reel stops being cropped, and its fullscreen scrubber starts moving.**
[PR #662](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/662), branch
`claude/instagram-player-fit`, cut off `origin/main` at `2a222899`. **Swift only** — three files,
+112/−13. No SQL, no catalogue change, no build cut. `test_sim` **570/570**. **Open, not merged**:
app code, so it waits for owner OK and a device look. **TestFlight 1.1.1 (137)** carries it.

**🔴 THE BUILD FORCED A MARKETING-VERSION BUMP, and this is the durable part of the session.**
1.1 is `READY_FOR_SALE`, and **Apple refuses any further build on a released version string —
TestFlight included**. Build **136 compiled and signed and was then rejected at upload** with
altool **90062** (*"must contain a higher version than that of the previously approved version
[1.1]"*). **Build 135, 2026-08-26, was the last that could ever ship under 1.1.**
`MARKETING_VERSION` is now **1.1.1** on both app-target configurations; the test targets stay at
1.0. **1.1.1 (137) is VALID at Apple, verified by querying App Store Connect** rather than reading
the workflow's green tick. ⚠️ **The failure does not look like a version problem** — archive and
signing succeed and the run reddens at the final step, exactly like the certificate cap and the
non-ASCII build notes. Read the altool error first. Recorded in `docs/testflight-ci.md`.

---

## What was asked

Owner, asked which part of the Instagram pages they wanted to work on:

> *"2 main things: 1. on the tour details page the crop of the thumnail/preview/play window
> 2. in full screen mode maybe the cropping of the video to avoid the instagram banners is ok, but
> the scrubber doesnt work"*

Read as: **the inline crop is wrong; fullscreen's fitting is fine and its scrubber is broken.**
Fullscreen's picture handling was therefore not touched.

## Scale, so the size of this is on the record

**73 of the 242 live link pins are Instagram** (150 TikTok, 19 YouTube) — about a third of the
pinned catalogue, and the only platform of the three whose post is played from a resolved media
file rather than the platform's own embedded player.

---

## 1 — the crop

**🔴 THE CAUSE: A REEL WAS BEING SIZED BY THE PHOTO CAROUSEL'S SQUARE.**
`TourDetailView.instagramPlayer` passed `GalleryVideoView` a `height` of nil. That means "take
`AtlasSpacing.heroAspectRatio`" — and **that constant is `1.0`**. `VideoSurface` then fills its box
with `.resizeAspectFill`, so a **720×1280** reel was cropped to a square: the **middle 56% of every
frame**, with the creator's own titles cut off top and bottom. On the test pin both lines of
*"NYC's Forgotten Names & Places Volume 35"* / *"Division Street: Delancey vs Rutgers"* were sliced.

**⚠️ The `.aspectRatio(9/16, .fit)` the caller already wrapped the box in did NOT save it.** The
child imposes its own square, and the square wins. Reading the call site alone suggests the shape
was already handled; it was not.

**⚠️ THE FILL IS A DELIBERATE OWNER DECISION AND IT STAYS — FOR THE CAROUSEL.** `VideoSurface`
carries a 🔴 comment explaining it: there a clip is one page among photographs that are fill-cropped
into the same square, and the expand button is what shows the whole frame. **That reasoning is
about a clip sitting BESIDE PHOTOGRAPHS, and it does not survive the trip to a link pin, where the
post is the entire page and there are no photographs beside it.** So the fix is an **opt-in**, not a
change of default:

- `GalleryVideoView.fittedAspectRatio: CGFloat?` — nil keeps the hero square and the fill.
- Non-nil sizes the box to that ratio and switches the surface to `.resizeAspect`.
- `TourMediaCarousel` does not pass it, so the ordinary gallery is byte-for-byte unchanged —
  **re-checked on Shinsegae Media Facade after the change: still square, still filled.**

**⚠️ `VideoBoxSizing` is a `ViewModifier`, not an `if` around the content, and that is deliberate.**
A branch would give the two shapes different view identities and SwiftUI would tear down and rebuild
the `AVPlayerViewController` — and with it the player — whenever it changed.

**⚠️ The ratio is passed in rather than measured.** `LinkSource.embedAspectRatio(for:)` knows the
shape from the URL before a byte is fetched, so the box is right on the first frame; adopting the
asset's measured ratio later would resize the page under the reader's thumb. It is also **the same
expression the embed branch uses**, so the page cannot change size depending on whether a resolve
succeeded — and it is the shape a TikTok pin already gets, which is the whole point of the resolver.

**✅ The 9:16 box adds no letterboxing of its own — measured, not assumed.** Six live pins
(Division Street, Wave Hill, Park Side Donuts, IAC Building, Blenheim Palace, Museum of Ethnography)
read with AVFoundation are **all exactly 720×1280 → 0.562**. Black bands some reels show are baked
into the source by the creator; no crop removes them without taking picture with them.

## 2 — the scrubber

**🔴 THE CAUSE: THERE WAS NO CLOCK.** `scrubPosition` read `player?.currentTime().seconds` — a plain
`AVPlayer` call with nothing observable about it — so **SwiftUI had no reason to re-render**. On a
1m 49s reel the elapsed label sat at `0s` and the brass fill stayed empty for the whole clip, which
is exactly what "the scrubber doesn't work" looks like.

**⚠️ ONLY GALLERY CLIPS EVER SHOWED IT, and that is why it survived this long.** A `.narration` clip
is slaved to `AudioPlayerService`, which **is** `@Observable`, and the view already re-renders on
`.onChange(of: audioPlayer?.currentTime)` every tick. **Every link pin is a gallery clip.**

The fix is a periodic time observer at **0.25 s** writing into `@State playbackTime`, delivered on
the main queue (the same contract `collapse()`'s `asyncAfter` already relies on).

- **⚠️ Installed BEFORE `player` is published**, so a sample cannot land on a view that has not
  adopted the player yet.
- **⚠️ Removed in `onDisappear`** — an observer left on a player that outlives the view retains the
  closure and keeps firing.
- **⚠️ `playbackTime` is seeded from `request.startSeconds`**, so expanding mid-clip starts the bar
  where the picture already is rather than at zero.
- **⚠️ `seek(to:)` writes the position through immediately** on the gallery branch. The observer
  fires up to a quarter-second later, and until it did the bar would spring back to where the finger
  started.

---

## Verification

Everything below was watched or measured, not reasoned.

| check | result |
|---|---|
| inline crop, before | square box; both of the creator's title lines cut off |
| inline crop, after | full-width 9:16; whole frame, nothing cut |
| reel shapes | 6/6 live pins exactly 720×1280 → the box is the clip's own shape |
| fullscreen bar, before | `0s` after two minutes of playback, fill empty |
| fullscreen bar, after | **18s → 25s across seven seconds**, brass fill tracking |
| seek | probe: `seek(60)` → player **61.9s**, bar **61.75s** |
| CDN | `accept-ranges: bytes` — a drag has always been able to move the video |
| carousel regression | Shinsegae Media Facade unchanged (square, filled) |
| suite | `test_sim` **570/570** |

**⚠️ The drag itself was not performed by a finger.** No coordinate-drag tool is available in this
environment (`mcp__Claude_Code_iOS_Simulator__control` refuses — `xcode-select` on this Mac is not
pointed at Xcode, the documented condition), and the scrubber is not an accessibility target, so
`drag` had nothing to grab. The gesture code is unchanged from before this branch; what was proved
is that **the production `seek(to:)` moves this asset and the bar follows it**. A device check with
a real finger is the remaining confirmation, and it is the thing worth doing first.

**The probe technique, since it is the useful part:** a temporary `onAppear` block calling
`seek(to: 60)` five seconds in and `NSLog`-ing `scrubPosition` before and after. Removed before
committing; `grep` for `SCRUBPROBE`/`TEMP-SLOWMO` is clean.

---

## Things found on the way that are not fixed here

- **⚠️ 20 of the 73 Instagram pins have no playable file at all.** Probed live across all 73 on
  2026-08-30: **53 PLAY, 20 WITHHELD**, every withheld one carrying licensed music (`butterfly
  waltz`, `Sunlight.`, `Golden Embers`, `Pizzicato`, …) where the playable ones read
  `Original audio`. Those pins fall back to the poster and `OPEN IN INSTAGRAM`, exactly as designed
  — `make-link-pin.py` already reports it at authoring time. **Nothing in the app can reach it**;
  the only levers are editorial (drop them) or asking the creator.
- **⚠️ A SIMULATOR TRAP THAT IS NOT A PRODUCT BUG, and it cost twenty minutes.** On one launch the
  bottom-module window missed its install (the documented race). The fullscreen cover is hosted
  **inside that window** (`BottomModuleRoot`'s `.background`), so the expand button rendered, sat in
  the accessibility tree, and **did nothing at all**. Relaunching fixed it. **A dead expand button
  on a link pin is worth checking against the tab bar's presence before it is treated as a video
  bug** — and it is the session-107 lesson again: presence in the accessibility tree says nothing
  about whether the action can run.
- **⚠️ Map-pin taps are unreliable through `snapshot_ui`** — the placecard often did not change.
  Search is deterministic and much faster for reaching a named pin.

## Process notes

- **The branch this session started on was already merged.** `claude/instagram-best-effort` carried
  the #606 work whose content is on `main`; only its `scripts/make-link-pin.py` differed, and
  **main's copy is 295 lines newer**. Work was cut fresh off `origin/main`. **A squash-merged
  branch's commit is not in `main`'s history, so `git branch -r --contains` says nothing** — diff
  the files instead.
- `main` moved twice during the session (#658, #660); the branch was cut after the first and
  re-fetched before pushing.
- **`gh pr checks` showed only Vercel for several minutes while `ci.yml` was in fact running** —
  read `gh run list --workflow=ci.yml --branch <branch>` rather than concluding CI never fired.

## What is worth doing next, if the owner wants more of this

1. **The device check** — an Instagram pin's page (whole frame), a real finger on the fullscreen
   scrubber, and one ordinary tour with a gallery video to confirm the carousel is untouched.
2. **The 20 withheld pins** are the only remaining Instagram-specific gap, and it is editorial
   rather than technical.
3. **The scrubber has no accessibility representation at all** — VoiceOver cannot scrub any video in
   the app. Out of scope here, real, and small.
