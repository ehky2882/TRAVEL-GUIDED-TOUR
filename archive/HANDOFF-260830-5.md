# HANDOFF — 2026-08-30 (session 124): share links previewed a green sphere

**Owner:** *"when i share i link right now the preview image that people receive is
still of my very old green icon. additionally, i would really like for the preview
image be of the thumbnail of the tour."*

Shipped as [PR #661](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/661)
(squash `ff8dd283`), **merged and verified live on `dozent.world`**. Website only —
no Swift, no `Tours.json`, no SQL, no build. Catalogue untouched.

---

## The cause, which is two faults stacked

1. **`og:image` was hardcoded** in `site/t/index.html` (and `/m/`, `/p/`, `/l/`) to
   `ehky2882.github.io/…/images/atlas-icon.png` — **the pre-#353 green-sphere
   placeholder**. The brass icon shipped in the app and *this copy was never
   updated*, so a placeholder outlived the thing it stood in for by four months.
2. **🔴 THE ONE THAT MATTERS: the per-tour title and description were patched from
   JavaScript, in `render()`, after the catalog fetch resolved. LINK-PREVIEW
   CRAWLERS DO NOT RUN JAVASCRIPT.** They read the bytes the server sends and
   stop. So every shared link previewed identically whatever it pointed at — and
   **nothing in that script touched `og:image` at all**, so a tour hero could never
   have appeared however the JS ran.

Fixing (1) alone would have shipped a correct-looking icon and still no tour photo.

## The fix

`site/api/share.js` — one Vercel Node function serving `/t/`, `/m/`, `/p/`, `/l/`.
Reads one row from Supabase with the **publishable key** (client-safe, RLS-gated,
already ships in the app) and bakes real tags into the response.

- **⚠️ THE STATIC PAGES ARE DELETED, AND THAT IS LOAD-BEARING: VERCEL CHECKS THE
  FILESYSTEM BEFORE `rewrites`.** Leaving `site/t/index.html` in place means it
  silently wins and the function is never reached. Do not restore them.
- `/g/` stays static — a join code has no subject to look up — and only lost the
  green icon.
- The card is server-rendered too. **The old page downloaded the whole ~7 MB
  `Tours.json` in the browser to display one tour.**
- Query shapes, all anon-readable and verified live:
  `tours?id=eq.<uuid>&select=…,makers(display_name)` (one call, embed works) ·
  `places?id=eq.<uuid>` · `makers?id=eq.<uuid>` · `rpc/get_journey` with
  **`{"p_journey": "<uuid>"}"`** (note the param name — `p_journey_id` 404s).
- ⚠️ **`tours` has `total_duration_seconds`, not `duration_seconds`.**
- Link pins are ordinary `tours` rows with `kind='link'`, so they share through the
  same path; the eyebrow reads **"Pinned post"**, because calling somebody else's
  post an "Audio tour" misdescribes it and Dozent hosts none of that media.

## Measured platform gaps — recorded in the function header, not papered over

Heroes are linked at their real URL, so two quirks follow. **Both were measured per
user-agent, not assumed:**

| | |
|---|---|
| **Wikimedia 403s `facebookexternalhit`** | *"Unauthorized request"*, while serving a browser 200. **48 of 1,794 heroes** (older NYC tours) therefore preview pictureless **on Facebook/WhatsApp only**. iMessage, Slack, Twitter, Discord: 200. |
| **1,746 of 1,794 heroes are WebP** | Fine on iMessage, Slack, Facebook, Twitter, Discord; historically unreliable on WhatsApp. |

**🔴 THE FIX FOR BOTH IS ONE IMAGE OPTIMIZER EMITTING JPEG, AND IT WAS DELIBERATELY
NOT BUILT.** A misconfigured or quota-limited optimizer breaks **every** preview,
which is far worse than a gap on 2.7% of tours on two platforms. If a future session
adds one, verify it on a preview deployment first and keep the direct-URL path as a
fallback.

⚠️ **A correction made to the owner mid-session:** I first said those 48 tours "get no
image tag" on Facebook. **The tag is always emitted** — what fails is Facebook's own
fetch of the picture. Different failure, same visible outcome, but do not repeat the
wrong description.

## Also shipped

- **"Coming soon to the App Store" was on all five pages** — and was the fallback
  `og:description`, so the stale claim was travelling in previews. Dozent 1.1 has
  been live since 2026-08-28, **confirmed against Apple's lookup API rather than this
  repo's notes** (§ READ FIRST). Now a real App Store button; **owner chose this**
  over keeping the Notify-me mailto.
- **`site/og-default.png`** (1200×630) is the new fallback everywhere, including the
  site root, which carried **no `og:image` at all**. Regenerated by
  **`scripts/render-og-default.py`** from the app's own splash geometry — verified to
  reproduce the committed file byte for byte, following the
  `render-launch-mark.swift` precedent.
- ⚠️ **AASA untouched, Universal Links unaffected** — iOS matches the association
  file against the URL **path**, and a rewrite is server-side and invisible to it.

## Deliberately not done

- **The gh-pages legacy landing pages.** Share links pointed there only between #297
  and #542 — **two days, TestFlight-only, before the App Store release** — so the
  wild population is negligible, and a gh-pages push buys a Pages-deploy race for
  nothing.

## Process notes worth keeping

- **🔴 VERCEL PREVIEW DEPLOYMENTS ARE SSO-PROTECTED BY DEFAULT (302 → `vercel.com/sso-api`), SO A PREVIEW CANNOT BE VERIFIED ANONYMOUSLY.** The owner
  disabled *Settings → Deployment Protection → Vercel Authentication* so the check
  could run, then re-enabled it. **Budget for this whenever a `site/` change needs
  proving before merge** — and it did need proving here, because the static
  fallbacks were gone.
- ⚠️ **Chrome was not logged into Vercel**, so driving the owner's real browser did
  not work either. Credentials were not entered.
- **⚠️ A PR body containing backticks inside `$(cat <<'EOF' …)` GETS SHELL-EXPANDED**
  even with the quoted heredoc delimiter — the first `gh pr create` died on
  *"site/t/index.html: No such file or directory"*. **Write the body to a file and
  use `--body-file`.**
- **⚠️ `gh pr view --json statusCheckRollup` LAGS.** It showed "Run unit tests" empty
  long after the run finished. **Read the Actions run and its jobs directly**
  (`actions/runs?head_sha=…` then `/jobs`) — all three jobs were
  `completed/success` while the rollup still said pending. Same lesson session 74
  recorded.
- `main` moved twice mid-session (#659, #660); merged in before merging out.

## Verification

- Handler exercised **against live Supabase**: 10 shape cases (single, walk,
  bilingual title, place, maker, link pin, unknown list, unknown id, missing id,
  injection-shaped id, bad kind) — exactly one `og:image` each, always https, no
  leaked script tags, correct `twitter:card`, sane cache headers.
- **40-entry random sweep of the real catalogue: 40 real heroes, 0 fallbacks, 0
  broken.**
- **On the preview deployment and then again on production `dozent.world`:** tour,
  walk, bilingual, place, creator, link pin, path form and unknown-id all correct;
  **Facebook, iMessage, Slack, Twitter, Discord and WhatsApp each receive the page
  and the tag**; `/`, `/about/`, `/privacy/`, `/terms/`, `/acceptable-use/`, `/g/`,
  `/og-default.png` and the AASA file all still 200.
- Bilingual titles (`Bank of China Tower | 中銀大廈`) correctly use the primary half.
- CI green (validator, iOS Simulator build, unit tests) — no Swift was touched.

## Owed

- **Nothing blocking.** The owner was asked to restore *Vercel Authentication →
  Standard Protection* after verification.
