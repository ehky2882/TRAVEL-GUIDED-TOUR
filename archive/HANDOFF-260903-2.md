# HANDOFF — 2026-09-03 (session 141) — sixty-one @archimarathon architecture pins

**Branch:** `claude/new-tour-links-cytwc6`, cut clean off `origin/main` at `20c494b`.
**Scope:** content only — no Swift, no SQL, no build, no place created.

## What landed

**61 TikTok link pins from one creator, `TikTok @archimarathon`.** `linkPins` **986 → 1047**;
`tours` (1,552), `makers` (305) and `places` (112) all **byte-identical**. Diff **2,849
insertions / 0 deletions**, asserted purely additive.

The owner's heading read "Architmarathon" — a typo. **Read the payload, not the heading**
(session-135 rule): all 61 posts resolve to `@archimarathon`, and the uuid5 scheme reproduced
that maker row's live id exactly (`04A9A86D-…`), so **0 new maker rows**.

## The batch's character

**The best-grounded batch to date.** Every one of the 61 captions names the building, its
architect and its year outright — which is why 44 of 61 coordinates were named exactly by OSM
and why the hero audit found zero wrong subjects.

Geography: Germany 14 · Canada 13 · Netherlands 9 · Japan 7 · Taiwan 5 · Finland 3 ·
plus Estonia, China, Denmark, South Korea, India, Macau, Australia, Hong Kong.
**Estonia and China are the catalogue's 43rd and 44th countries.**

## What went wrong and how it was caught

- 🔴 **Nominatim 429'd the first geocoding pass and my script wrote the failures as
  "NO-MATCH".** That is exactly the session-137 failure — an errored request producing a
  verdict. Rewritten so a request that does not return HTTP 200 **writes no row at all**, then
  re-run. Every coordinate in this batch comes from a completed request.
- 🔴 **`The Hive` resolved to a BOULDERING GYM 1.2 km from the building.** The forward geocode
  returned `leisure/sports_centre` — Vancouver has a climbing-gym chain of that name. The
  subject is the 10-storey mass-timber office at **2150 Keith Drive**, found by address. The
  Komeda wrong-branch trap; a distance check alone would never have shown it.
- 🔴 **`Sulwhasoo` resolved to the Myeongdong branch**, 2 km from the Neri&Hu flagship at
  18 Bukchon-ro. OSM maps no node for the store, so Bukchon-ro was **walked by
  reverse-geocode** (the Crosby Street method): no. 15 is the Constitutional Court, so no. 18
  sits opposite. Pin is on the road beside it.
- ⚠️ **`Rozet` returned a footway** and **`Expo Green Roof` a bus stop**; both re-queried to the
  real building. Expo Green Roof's is the best result in the batch — OSM names it
  **`花博爭豔館（原中山足球場）`, literally "formerly Zhongshan Football Stadium"**, which is
  precisely what the caption describes.
- ⚠️ **`阿那亚` unbounded matched the Taipei Representative Office in the NETHERLANDS.** The
  documented unbounded-search trap. UCCA Dune was then found by its own Chinese name.
- ⚠️ **Aalto built TWO churches in Wolfsburg.** The caption's 1962 one is Heilig-Geist on
  Klieversberg, not the Detmerode church; resolved to the exact building at Röntgenstraße 81.

## Coordinates

All 61 reverse-verified at zoom 18. **44 named exactly by OSM**, several confirming the caption
independently: **`Reichstagskuppel`** for the Reichstag Dome, **`Tornado`** for the FENIX stair
the caption is entirely about, **`Dudokpark`** at Hilversum Town Hall, `Maison Jeanneret Museum`
in the right sector, `UCCA沙丘美术馆`, `鄭家大屋 Casa do Mandarim`.

**Documented shapes, all correct:** Säynätsalo reverses onto a hair salon literally named
"Aalto" (nearest addressed POI; the forward names the town hall); Richmond Olympic Oval onto a
capoeira school at its own address 6111 River Road; Penticton onto The Barking Parrot, the bar
inside it; Alliance Française onto a café at its own 6161 Cambie Street; the Sayama cemetery
onto the neighbouring Belluna Dome car park; KF Centre onto Kelowna International Airport,
which is where it is.

**⚠️ Four weak coordinates, stated not hidden:**
1. **Taipei Roofs** — no published address anywhere (ArchDaily gives only "Xinyi District");
   pinned in Xinyi. **The batch's weakest.**
2. **Tsuruoka House** — a private house; the caption names no location and no address is
   published. Pinned on a road in Nerima, the ward the record gives.
3. **The Temple of Light** — Yasodhara Ashram is unmapped in OSM; pinned on Walkers Landing
   Road, its published street. ⚠️ **Its own hero corroborates this** — the lotus temple above
   Kootenay Lake, which is where that road runs.
4. **Sulwhasoo** — street-level, beside no. 15, as above.

**The one supplied coordinate checks out.** `G952+C8 Berlin` decodes to `52.5085625,
13.3508125`, which reverse-geocodes to **1 Rauchstraße, Botschaftsviertel** and sits **7 m** from
OSM's own `Felleshus` — the Nordic Embassies' shared house. Code and map agree independently.
`decode-plus-code.py --selftest` clean first.

## Heroes

**All 61 opened and read against their captions — ZERO wrong subjects.** Many confirm the
caption exactly: Utzon's cloud ceiling at Bagsværd, Frei Otto's tensile net, the UCCA Dune
opening onto the Bohai beach, MOA's totem poles under Erickson's concrete beams, Hotel Siro's
stacked steel flights, the Städel's grid of circular skylights in the lawn, Böhm's concrete
rock at Neviges, Rietveld's red-blue-yellow planes.

**✅ No hand re-crop was needed anywhere — the second clean run in sixteen batches**, because
this creator burns in **no title cards at all**, so the vertical `--focus` gap never bit.

**⚠️ Four weak heroes, flagged not fixed** (a link pin re-hosts only the thumbnail, so no other
frame exists — the choice is keep or pull):
- **Rozet** — the interior stair, while the caption is entirely about the **ornamented concrete
  facade**. The sharpest mismatch of emphasis in the batch.
- **Villa Mairea** — two presenters fill the frame; the kidney pool the post is about is behind
  them.
- **Penticton Lakeside Resort** — a man pointing at a CLT ceiling.
- **Expo Green Roof** — creator-forward, though the planted stadium terraces read clearly.

## Place candidates — 2 EXACT groups, flagged NOT created

`check-place-candidates.py` goes **0 EXACT → 2** and therefore **exits 1**, ending the
clean-exit state. ⚠️ **Both are honest; neither coordinate was nudged:**
- **Depot Boijmans Van Beuningen ×2** — the existing pin and this batch's, both derived
  independently from OSM's own museum node. Convergence, not manufacturing (the Tribune Tower
  case). This batch's ships as **`The Mirrored Facade of Depot Boijmans`** so two identically
  titled pins do not sit on one point (the Railway Museum precedent).
- **Sayama Forest Chapel + Sayama Lakeside Cemetery Community Hall** — two different Hiroshi
  Nakamura buildings of 2013 at one cemetery. **OSM maps only the cemetery polygon**, so both
  take its centroid. Inventing two points inside it is the manufacturing session 132 rejected
  for Arthur Ashe.

Both groups have **two** members, inside `TourSetMap.maxStacked = 3`, so nothing is unreachable.

**NEAR 38 → 39**, the single addition being the Atlas **Reichstag** tour ↔ this batch's
**Reichstag Dome** at **9 m** — a genuinely distinct subject (the caption is entirely about
Foster's ramps and the mirrored cone), so it ships under its own title.

## Architect tags — the largest vocabulary gap yet

Only **6** architects in this batch are in the controlled vocabulary and are used by name:
`I. M. Pei` (Luce Memorial Chapel), `Sou Fujimoto` (Shiroiya), `Jørn Utzon` (Bagsværd),
`Norman Foster` (Reichstag Dome), `Peter Zumthor` (Kolumba), `SANAA` (Umekita) — each
**alongside** `Designed by a Master`, never replacing it.

🔴 **`Alvar Aalto` is NOT in the vocabulary and this batch carries three of his buildings**
(Säynätsalo, Villa Mairea, Wolfsburg). So are **Gottfried Böhm ×3** and **Arthur Erickson ×3**.
Also absent: MVRDV, Mecanoo, Neutelings Riedijk ×2, Patkau ×2, Piet Blom, Aldo van Eyck,
Gerrit Rietveld, James Stirling, UNStudio, Willem Dudok, MAD Architects, Frei Otto, Günther
Behnisch, Neri&Hu, Akihisa Hirata, Hiroshi Nakamura ×2, Kiyoaki Takeda, Mount Fuji Architects
Studio, OPEN Architecture, Berger + Parkkinen, Hariri Pontarini, Schneider + Schumacher, Fritz
Schaller, Cornelia Oberlander ×2, Peter Böhm, Shozo Uchii, Head Arhitektid, MX_SI, Pan Tianyi,
Wang Da Hong, Lahznimmo, CannonDesign, DIALOG, HDR, RLA, Public Architecture, McFarland
Marceau, Meiklejohn. **Adding them is a `Models/Tag.swift` code change and was deliberately kept
out of a content PR.**

⚠️ **`Le Corbusier` is deliberately NOT tagged on Maison Pierre Jeanneret.** The house is
Jeanneret's; Corbusier is named only as the cousin he worked for. The Sullivan rule.
⚠️ **Three pins carry no master tag at all** because their captions name no architect — Expo
Green Roof, The Hive, Mandarin's House (the Jules Dalou rule). **Do not "finish the job."**

## Titles that had to be disambiguated

- **`The Mirrored Facade of Depot Boijmans`** — an existing pin already holds `Depot Boijmans
  Van Beuningen`.
- **`The Cube Houses of Rotterdam`** — Toronto's Cube House is already in the catalogue and is
  a different building (its own caption says it takes the form from Blom's).
- **`The Reichstag Dome`** — the Atlas `The Reichstag` tour is 9 m away.

## Verification

- Mirror **self-tested 22/22 with a clean control**, then **0 errors, 0 warnings** across
  **1,552 tours + 1,047 pins + 112 places**. ⚠️ Exit code read **directly, not through a pipe**.
- **21 faults injected against THIS batch's own 61 pins — 17 caught, control clean before and
  after.** ⚠️ Two of the five apparent misses were the HARNESS's fault (it counted only errors;
  "no Place type" and "no Theme" are *warnings*) — the session-90 lesson. **The remaining three
  are the documented mirror blind spots** (centroid drift, negative duration, empty title), the
  same three session 139 recorded, and **all three were asserted directly on the 61 instead**.
- `make-link-pin.py --selftest` **71/71** ⚠️ **with Pillow installed first** — a bare container
  reports 62/62, which reads as a pass and is not one.
- **0** duplicate tour/stop/maker ids, **0** collisions with live ids, **0** already-pinned
  sourceURLs, **0** in-batch duplicate URLs, **0** byte-duplicate heroes; closest perceptual
  pair **27.51** (identical pictures score under 1).
- **61 heroes for 61 pins** — the check that catches two pins slugging identically.
- **0 filename collisions** against **6,805** gh-pages `images/` paths, the listing **asserted
  to hold >1,000 first**. ⚠️ The bare-slug check was **clean too**, so the handle suffix was not
  load-bearing here.
- ⚠️ **One file already existed and was EXCLUDED rather than overwritten** —
  `avatar-tiktok-archimarathon.webp` regenerated **byte-identically** to the live file
  (sha256 compared against the served bytes). **62 generated, 61 uploaded.** The
  `@urbanistariel` case, eleventh time.
- `Tours.json` **byte-stable under a Python re-dump before editing**; `tours`, `makers` and
  `places` asserted byte-identical and the existing 986 pins asserted unchanged as a prefix.
- `seed_from_toursjson.py` clean at **305 / 2,599 / 2,971 / 112**; **0 `images//`** in the
  catalogue *or* the generated SQL.
- gh-pages `317046a`: remote head **re-read in the same command as the push**, push status read
  through **`PIPESTATUS`**, tree diff **exactly 61 additions, 0 deletions, nothing outside
  `images/`**. ⚠️ **The Pages deploy took ~12 minutes and served 404 throughout** — checked
  against the Actions API and found **`in_progress`, never `cancelled`** — after which **all 61
  live URLs were hash-verified against the uploaded bytes: 61 ok, 0 mismatch, 0 non-200**.
- 🔴 **`check-image-duplicates.py --pins` was run AFTER the deploy** (the session-135 false-pass
  lesson — running it before the deploy returns an "OK" that has not seen the new files):
  **`OK — no suspicious duplicates`** over **1,042 images** (1,042 for 1,047 pins is the documented
  `@malata.antwerp` five-pins-one-URL case), shared-URL half **0 errors / 208 documented reuses** —
  identical to the recorded baseline, so this batch adds no shared URL.
- ⚠️ **Nothing compiled — no PR is open, so CI has not run.**

## Follow-up 2, same session — 48 architects, vocabulary 335 → 383

**Owner: *"Definitely add those architects"*.** ⚠️ **A CODE change** (`Models/Tag.swift` +
`scripts/validate-tours.swift`) — wants a simulator look; CI is the compile check.

- **Both vocabularies edited and asserted identical at 383 names.** Editing one alone is an error
  per tagged tour (session 104).
- Near-duplicates checked on **normalised token sets** with practice suffixes stripped — 0 collisions.
- **52 entries gained 62 tags**; `Designed by a Master` **kept on every one, never replaced**.
  Catalogue-wide after: **593 named-architect entries, 0 missing the shelf tag, 0 of 383 names
  unused.** The build refuses to run if a new name ends up unused.
- **🔴 The catalogue-wide sweep is what earns its keep.** 12 names appear outside the batch and
  almost all are false positives: `van Eyck` matched **Jan van Eyck the painter**, `Massey` a **chef**,
  `Böhm` the researcher **Böhme**, `Hirata` **restaurateurs**, `Nakamura` a **tea house**, and
  `Public Architecture` **eight uses of the ordinary phrase**. Two were real, both `MVRDV`.
- Mentions left untagged (Sullivan rule): **Aalto** at Triennale di Milano (a gold medal) and
  Stockholm Public Library (influence); **Stirling** at Palazzo Citterio (plans never built).
- Deliberate omissions inside the batch: Maison Pierre Jeanneret and Wang Da Hong House (caption
  attributes nothing), **Frank Lloyd Wright on Setagaya** (named as an influence — the most tempting
  one), **Böhm on Kolumba** (an enclosed earlier chapel, not the museum's design), **Erickson on the
  Gibson** (built "in keeping with" his vision by other hands), and `Spearhead` (a fabricator).

## Follow-up, same session — the two EXACT groups became places

**Owner: *"Make the places. Open PR"*.** Opened as [#729](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/729). Places **112 → 114**; `tours`, `makers` and `linkPins`
byte-identical, the 112 existing places unchanged as a prefix, **nothing moved** (both pairs were
already coincident). Diff **30 insertions / 0 deletions**.

- **Depot Boijmans Van Beuningen** (Rotterdam) — two creators on one building, the Cube House shape.
  Anchor reverse-geocodes by name to `Depot Boijmans Van Beuningen, 24, Museumpark`.
- **Sayama Lakeside Cemetery** (Tokorozawa) — one creator, two different Hiroshi Nakamura & NAP
  buildings of 2013 at one site. ⚠️ The anchor **reverse-geocodes to a bench in the Belluna Dome car
  park** (nearest-addressed-POI) while the **forward** search on the bare name returns OSM way
  `1501167136`, `landuse=cemetery`, `狭山湖畔霊園`, at exactly that point. **Appending the district
  to the query returns zero hits** — the bare name is the query that works.
- Both heroes borrowed from a member (structural: every member is a link pin with an empty gallery
  and there is no Atlas tour at either site). ⚠️ Sayama takes the **chapel exterior** over the
  community hall's finer interior, on the establishing-shot criterion — one line swaps it.
- 26 place-layer faults injected against these two places: **26/26 caught**, control clean both
  sides. `check-place-candidates.py` **0 EXACT, exits 0, NEAR unchanged at 39**. Seed clean at
  305 / 2,599 / 2,971 / 114. Both place heroes live 200.

## Process notes worth keeping

- **`sleep` is blocked in the foreground here.** Use a background task or an `until` loop.
- **The shell's cwd resets between tool calls** — a `nohup` redirect needs an absolute path, and
  `Resources/Tours.json` is at `TRAVEL GUIDED TOUR/Resources/Tours.json`, not the repo root.
- **`git fetch` timed out repeatedly at ~2 min.** Branch freshness came from the already-fetched
  `origin/main` ref plus a targeted `git fetch origin main`.
