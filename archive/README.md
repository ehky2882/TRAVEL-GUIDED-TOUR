# archive/

Snapshots of working docs from earlier in the project. Naming
convention: `<original-name>-YYMMDD.md`, where the date is the day
the snapshot was archived (or the day it represents, for handoffs).

**Reading rule.** Most files here are historical — not read at
session start; anything still relevant to current work belongs in
`CLAUDE.md` or `ROADMAP.md`. **Exception: the most recent
`HANDOFF-YYMMDD.md` is part of the session-start ritual** (see
`CLAUDE.md` § "Session-start ritual"). It bridges the gap between
durable project rules and the specific mid-flight state from the
prior session — queued user feedback, what was about to be tackled,
tribal knowledge not yet promoted.

| File | Purpose | Date |
|---|---|---|
| `ACCOUNT-TRANSFER-260520.md` | **One-time orientation doc for the successor Claude on a different account.** Covers project overview, recommended reading order, state at transfer (code / content / TestFlight / Apple Developer), what's queued, working-style notes about the owner, and tribal knowledge worth carrying forward. Read once at the first session under the new account. | 2026-05-20 |
| `HANDOFF-260901-3.md` | Session 133, 2026-09-01 (web, content): **eleven link pins from Joshua Charow** — 12 links, 10 distinct posts, one creator across TikTok (9) and Instagram (1), so **two maker rows**. **linkPins 554 → 565, makers 224 → 226**; tours and places unchanged. 🔴 **The two duplicate links were not the same kind of duplicate.** One was free — `87G8P378+H3` and `P378+H3` are the full and short spellings of one code and decode identically. The other was **the same Instagram reel with two different codes, and both are right**: one lands 14 m from the **Bedi Makky Art Foundry**'s own published address, the other **1 m** from OSM's `Charging Bull` node — the sculpture that foundry cast. It ships as **two pins** on the `@malata.antwerp` precedent, sharing one hero file. ⚠️ **The malata id scheme had to be reverse-engineered** (`atlas-tour:link:<url>#<fragment>`, city fragment where that distinguishes, subject fragment where it does not) — without it both pins collide on the same tour AND stop id, which is what the first build produced. 🔴 **The hand-written Plus Code decoder failed three of its own checks**: two were my own mis-derivations (I mis-split `87G8M2J6+HJ` into pairs) and **one was real — `encode` must FLOOR, not round**, which cost 1,483 of 2,000 round-trips and turned the Eiffel anchor into `+8R`. It **refuses grid digits rather than guessing** the row direction. ⚠️ **Two subjects are in no OSM record** and were closed against their published addresses (M&S Schmalberg 15 m, the foundry 14 m), both reverse-geocoding onto a neighbour — the documented nearest-node case. ✅ **All ten heroes opened, zero wrong subjects, no hand re-crop needed.** ⚠️ **Two place questions flagged, neither created**: the Charging Bull pin is **3 m** from the Atlas tour of the same name (deliberately not nudged onto it), and the Textile Conservation Lab is **37 m** from the St John the Divine place — which `check-place-candidates.py` **cannot see**, its title-containment blind spot. 🔴 **The first collision check fetched nothing and would have reported a pass**; an assertion caught it. Validator mirror self-tested **40/40**, then 0 errors / 2 pre-existing warnings; 11 live URLs hash-verified; EXACT unchanged so no coincident group manufactured. **No PR opened.** | 2026-09-01 |
| `HANDOFF-260830-3.md` | Session 123, 2026-08-30 (web, content + backend): **two duplicate LA link pins pulled on owner instruction** — Hotel Casa del Mar (the same script reposted months apart) and the YouTube Castle Green. **linkPins 244 → 242, makers 189 → 188, places 41 → 40.** 🔴 **The removal is a two-part change and the SQL is OWED** — all four rows were verified present in the live RPC first, and `seed_from_toursjson.py` is upsert-only, so `backend/pull_la_duplicates_260830.sql` is the only thing that takes them off phones. ⚠️ **"Doesn't matter which one" turned out to matter** — one Casa del Mar thumbnail is the hotel's lobby staircase, the other a patio seen from above with a palm trunk through it; a link pin re-hosts only its thumbnail, so the survivor's frame is the only one that subject will ever have. ⚠️ **The place's hero was the pin being deleted**, and a creator row went with the Castle Green pull (`YouTube @Thedesigndetourist` had exactly one entry) — a DIFFERENT row from `TikTok @thedesigndetourist`, which keeps 19. **Four LA place candidates flagged, not created:** Bradbury 9 m, Griffith 22 m, Petersen 50 m, Union Station 56 m — every one an Atlas geofenced tour beside a manual pin, and every one able to take a third photograph free from the tour's own gallery; **Petersen is the one judgement**, because the pin has the better coordinate (6060 Wilshire) and the tour has the geofence. The Westin Bonaventure was checked and deliberately left — its two pins are genuinely different posts. **FOLLOW-UP, same session: all four were then built on owner instruction — places 40 → 44.** The pin moved and the tour did not, verified by diff (0 Atlas tours changed a coordinate, trigger mode or radius); every place hero is a **third photograph promoted from the member tour's own gallery**, nothing sourced. 🔴 **Petersen's coordinate was decided by the script**, which opens *"You should be at Wilshire and Fairfax, on the corner opposite the Academy Museum's gold cylinder"* — the tour's South Fairfax point is a deliberate vantage, not a road-centroid error, so the place anchors there while its **address still names the building**. ⚠️ The place is **`Los Angeles Union Station`, not `Union Station`** — Toronto has one too. NEAR fell 15 → 11, EXACT unchanged at 3. **No owner SQL for the places** (the seed carries them); the removal SQL is still owed. | 2026-08-30 |
| `HANDOFF-260829.md` | Session 121, 2026-08-29 (local Mac, content): **nineteen link pins — 12 TikTok + 7 Instagram, all from ONE creator, `@about_buildings`.** All nineteen alive and pinnable; **linkPins 150 → 169, makers 153 → 154, Austria the 33rd country.** **🔴 FOUND WHILE VERIFYING AND NOT PART OF THE BATCH: the four pins the owner pulled on 2026-08-28 are STILL BEING SERVED** — `backend/pull_nycunfilteredstories.sql` (#635) has never been pasted into the SQL Editor, and `seed_from_toursjson.py` is upsert-only, so nothing else can remove them; the app reads Supabase first, so they are on every phone. **⚠️ The diff that exposes it is the session-99 trap** — `Tours.json` stores some UUIDs uppercase and Postgres returns them lowercase, so a naive comparison claims 173 pins and 133 makers are missing. **🔴 A creator on two platforms is two maker rows and the TikTok one already existed** — uuid5 reproduced it exactly, which is also what stopped its live avatar being overwritten (20 generated, 19 uploaded). **🔴 The same clip cross-posted is two pins on one coordinate**: the two Zacherlhaus pins, caught independently by the perceptual check (pixel-diff 2.1) and by `check-place-candidates.py`'s only new EXACT group — both shipped, flagged to the owner. **⚠️ One post had NO caption at all and the hero identified it** (Tribune Tower, from the *Chicago Tribune* lettering in frame). **⚠️ All six supplied coordinates checked out — the first batch where none moved**; two of the thirteen bare ones failed their first query and both failures were the query, not the place (`Otto-Wagner-Kirche`, not "Kirche am Steinhof"; **Brooke's** Court, not Brooke Street). **⚠️ The vertical `--focus` gap, seventh batch running** — two hand re-crops through a mirror of the tool's pipeline. **⚠️ Two weak heroes flagged, one badly: the Royal Hospital Chelsea thumbnail is a podcast talking head** with no view of the building. **Scarpa (4 pins) and Plečnik (3) are now the catalogue's most conspicuous architect absences**; `Herbert Baker` is in the vocabulary and was deliberately NOT tagged on the Bank of England, because the rule is what the source says, not what the pixels show. Validator (the Swift one) 0 errors, 2 warnings, both pre-existing; 19 uploads hash-verified live; live RPC and gh-pages mirror both confirmed after merge. | 2026-08-29 |
| `HANDOFF-260829-2.md` | Session 121b, 2026-08-29 (web, content): **thirty-two link pins from thirty-five links; 2 dead, 1 parked.** ⚠️ **A dead Instagram reel is identified by SIZE, not by a string** — the two dead ones return a ~215 KB embed shell with no owner blob on six spaced fetches, against ~257–262 KB with one for live posts in the same run, and neither carries a *"Video currently unavailable"* marker. **🔴 Yankee Stadium became a PLACE because five coincident pins plus the existing Atlas tour is six markers against `TourSetMap.maxStacked = 3`** — two would have been silently unreachable from the map; the pins moved onto the tour's coordinate, the tour did not move, and the place hero is a third photograph from the tour's own gallery. Two other new EXACT pairs (Chichén Itzá, Rosewood Mayakoba) were **left as pairs**, being inside the cap. **🔴 My own 420-character caption truncation hid the answer to a subject I then searched for half an hour** — pin #8's caption ends `📍 james rose center, ridgewood, new jersey`; **print full captions**. ⚠️ **The creator says Wakefield, the building is in Eastchester** — followed the pixels to Public School 15, the 1877 Little Red Schoolhouse. **One link parked for want of an identifiable subject** (a Greenwich Village garden that fits Jefferson Market and St Luke's equally). Three heroes re-cropped vertically; **the YouTube pin's hero is an unusable dark smear with no better thumbnail in existence** — flagged as the likely removal. Taiwan, Finland and Hungary are the 33rd–35th countries. 40/40 injected validator faults caught; 0 errors, 2 warnings, both pre-existing on `main`. ⚠️ **A bug in my own contact sheet matched heroes by slug prefix and nearly reported a duplicate that did not exist.** | 2026-08-29 |
| `HANDOFF-260828.md` | Session 120, 2026-08-28 (web, content): **thirty link pins; Luxembourg and Norway become the catalogue's 31st and 32nd countries.** All thirty links alive and pinnable. **🔴 Two coordinates arrived as PLUS CODES** — a format the catalogue had not met; `openlocationcode` will not build a wheel here, so OLC was implemented by hand and **checked against the official test vectors, not a remembered code** (my recollection was what failed the first sanity check). **🔴 The YouTube link names no place and its watch page is `/sorry/`-blocked from this IP — the thumbnail identified it** as Habitat 67. **🔴 Overpass is unreachable from this container on all three mirrors** (the proxy reports no relay failures, so it is them); Nominatim is fine. **🔴 The handle suffix prevented two live Atlas hero overwrites** (`habitat-67_hero`, `little-island_hero`), and one already-live avatar was **excluded rather than overwritten**. **🔴 A missing trailing newline silently dropped a file from the upload tree** — 44 staged instead of 45, caught by comparing the count against the list length. Three coordinates moved (Handel Hendrix House 24 m off on the opposite pavement; Amagansett 26 m; the Macy's holdout is the **Million Dollar Corner, 1313 Broadway**, and my first guess for it was 262 m wrong). **⚠️ Three heroes are synthetic and one shows a real building as a ruin** — flagged, not resolved. Five architects verified and absent, **Safdie and Roebling the most conspicuous**. **🔴 The Atlas `IAC Building` tour is 268 m off, on West 15th Street, and it is geofenced** — flagged, not fixed. 44/44 injected validator faults caught; 0 errors, 2 warnings, both pre-existing on `main`; 45 uploads hash-verified live. **⚠️ Amended after merge: the owner pulled four pins and two creators** (`@nycunfilteredstories` entirely — which took a fourth pin, the Verrazzano, that session 119 had cleared as genuine — plus `@theironwil` with their sole pin), leaving **150 pins / 153 makers**. **"Shipped and flagged" is not a resolution: this is the second flagged AI hero pulled in three sessions, so Mercedes-Benz Stadium, not DIFC Gate, is now the representative precedent.** | 2026-08-28 |
| `HANDOFF-260827-2.md` | Session 117, 2026-08-27 (web, content): **ten Atlanta TikToks; nine shipped, the tenth pulled by the owner** — the first link-pin batch where nothing was parked. **🔴 The handle suffix on a hero slug stopped a live overwrite:** gh-pages already carries `mercedes-benz-stadium_hero.webp` and `oakland-cemetery_hero.webp` from the Atlanta tour batch another session is staging, and two of these ten are those same subjects — a bare slug would have written over an Atlas tour's hero, which a downloaded tour would never see corrected (#567). **🔴 The Garden Room's caption names a venue and nothing else, OSM has no node for it, and the node literally tagged "88" on its published street reverse-geocodes to a bank at number 79 across the road** — the Warehaus trap; closed independently by the hero, which has the restaurant's name burned into the frame. ⚠️ **Swan House and Fernbank Forest both reverse-geocode to something else (a car park, a house) and both coordinates are right** — proved by fetching each OSM polygon and testing containment. ⚠️ Cosm Atlanta is not in OSM at all (pinned on The Interlock). ⚠️ **One hero flagged, not refused: the Mercedes-Benz Stadium frame may be AI-generated** — right subject, but a link pin re-hosts only the thumbnail so no other frame exists (DIFC Gate precedent). Three architects verified, none in the vocabulary (Shutze, Portman, Gund). 40/40 injected validator faults caught; 0 errors, 2 warnings, both pre-existing on `main`. | 2026-08-27 |
| `HANDOFF-260826.md` | Session 113, 2026-08-26 (web, code): **a link pin's fullscreen was not fullscreen** — branch `claude/link-fullscreen-module`, **not merged and no PR open.** 🔴 The platform's own fullscreen control puts the player into **WKWebView element fullscreen in the MAIN window**, while the mini-player and tab bar live in a separate `PassThroughWindow` at `.normal + 1` that paints over it; nothing in `LinkEmbedView` had ever observed `fullscreenState`. **TikTok and YouTube reproduce it, Instagram structurally cannot** (it resolves to a file and takes `GalleryVideoView`, presented from inside the top window — which is why the gallery path was never affected). **Giving the embed our own fullscreen was rejected on a hard constraint: the video is in a cross-origin iframe we may not script**, the same wall that stopped the Instagram player work. 🔴 **The fix is built around the RESTORE, not the hide** — whoever hides the module owns unhiding it, and losing the bars for a session is far worse than the bug; three restores (`onDisappear`, `dismantleUIView` for the tab-tap path, `Coordinator.deinit` as the only ARC guarantee). ⚠️ **The environment injection was missing from all four UIKit layers**, which would have made the whole fix a silent nil no-op — the batch-D Follow-button class again. ⚠️ Delivery is `DispatchQueue.main.async` and never `Task { @MainActor in }`, because a restore overtaking its own hide is the permanent failure. **🔴 NOTHING HAS COMPILED: `api.github.com` is egress-blocked here (403 at the proxy), no `gh`, no GitHub MCP — and `ci.yml` does not run on a feature-branch push.** Wants a device check; #603 and #604 both behaved differently on a phone than in the simulator. | 2026-08-26 |
| `HANDOFF-260826-2.md` | Session 113, 2026-08-26 (web, content): **Copenhagen launched as the 34th Atlas maker** — 40 tours under Atlas Studio CPH, Denmark being the catalog's 22nd country. 🔴 **Seven supplied coordinates wrong and every one displaced due NORTH**, the largest **2,137 m** (the M/S Maritime Museum, which its own hero disproves — Kronborg is in the frame). **The calibrated bias line is clean (p = 1) and that is not the same as fixed**, the same half-fixed split Stockholm found. ⚠️ **Two errors sat under the tool's GROSS threshold, which is well above the 30 m geofence**, and ⚠️ **the largest error of all was filed "UNVERIFIABLE", which is not a pass.** Four flags were false alarms — `Det Kongelige Teater` names three separate buildings. 40/40 heroes clean. ⚠️ Notes an **Atlanta batch staged on gh-pages with no tracker row** — the Dubai failure repeating. | 2026-08-26 |
| `HANDOFF-260826-3.md` | Session 114, 2026-08-26 (web, content + tooling): **nine Brick Award link pins, and the letterbox bars every YouTube hero had been carrying** — nine wienerberger films, each supplied with a name AND a coordinate. 🔴 YouTube's oEmbed always returns `hqdefault.jpg`, the 16:9 frame letterboxed onto a 4:3 canvas, so the square crop kept the bars and every earlier YouTube pin has black bands baked across it; fixed by preferring `maxresdefault` plus a deliberately conservative `trim_bars`. **NOT backfilled** — a correction means a NEW filename (#567). Also folds in the handle-suffixed hero slug #607 applied by hand, and adds `--title`/`--focus`. ⚠️ All nine are ONE company on TWO channels, so the catalogue carries two creators; four heroes show the architect being interviewed rather than the building, which the tool cannot fix. | 2026-08-26 |
| `HANDOFF-260824-2.md` | Session 108, 2026-08-24 (local Mac, content): **Stockholm launched as the 33rd city/maker** — 45 tours under Atlas Studio STO ([PR #575](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/575)). **Three coordinates wrong, every one due north**; **the systematic upstream bias is gone but the gross errors are not — half-fixed.** **Swedish capitalises only the first word of a proper-noun phrase.** Two heroes ship wrong-or-doubtful by owner decision. | 2026-08-24 |
| `HANDOFF-260824.md` | Session 107, 2026-08-24 (web, code): **the chrome row was a slightly different colour than its page, and the owner spotted the band.** [PR #573](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/573), **TestFlight 1.1 (112), device-verified.** 🔴 The cause is worth knowing generally: the row drew `secondaryBackground.opacity(0.8)` over `.regularMaterial` on a page that is a plain `secondaryBackground` — **and that token is a hardcoded RGB pair precisely so every surface resolves identically regardless of window or elevation.** Putting it at 80% over a material threw the guarantee away. ⚠️ **The better half: a material samples what is behind it, so the row's shade DRIFTED as a hero photo scrolled under it** — which is why this read as "slightly off" for months instead of being filed. Fixed on all three canonical chrome rows, verified still byte-identical after. **Do not reintroduce the material: one token paints both surfaces deliberately, so translucency here and an invisible boundary are mutually exclusive.** | 2026-08-24 |
| `HANDOFF-260823-5.md` | Session 106, 2026-08-23 (web, content + code): **planning the creator-video work, and the catalogue's first vertical video.** [PR #569](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/569) adds **VIA 57 West** (BIG) — a building with no tour and nothing within 900 m — carrying a generated 1080×1920 stand-in clip, plus **Bjarke Ingels** to the tag vocabulary. **The fullscreen video viewer is specced in full and deliberately NOT built** — owner moved it to a local session for simulator iteration, citing the eleven builds sessions 102–103 burned on the launch animation. 🔴 **Two traps documented for whoever builds it:** present from `BottomModuleRoot` or the mini-player window paints over it (session 24 hit this with `PlayerView`), and suppress the inline view's `resumeNarrationIfNeeded()` or narration plays behind the video. **Rotation measured and the obvious approach rejected:** the app is portrait-locked and `Info.plist` is a ceiling, so real rotation would open it app-wide and rotate the map and drawer behind the video — chosen instead to rotate the *video content* inside a portrait screen, which nothing else can notice. ⚠️ **Detect clip shape from the DISPLAY size, not `naturalSize`** — phone video is commonly 1920×1080 with a 90° transform. **`backend/add_video_urls.sql` has been applied** (verified live) and video was already shipping on one tour while Key facts claimed zero. **Link pins and creator import are planned and NOT blocked by the viewer** — an earlier draft wrongly said they were. ⚠️ Instagram oEmbed's token requirement is the one open question; TikTok and YouTube both confirmed keyless. | 2026-08-23 |
| `HANDOFF-260819.md` |  Session 94, 2026-08-19 (web, code): **keeping and sending a list** ([PR #517](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/517), **TestFlight 1.1 (74)**). Two owner asks in one PR — *"for a public profile. i want to be able to save a playlist"*, then on the mockup *"think there should be a '...' menu. a playlist should be able to be shared also."* **No SQL: `saved_journeys` and `get_journey` had existed, unused and granted to `anon`, since session 59** — both verified live before any Swift was written. *Read `backend/journeys.sql` before building list plumbing.* **A saved list is a REFERENCE, and the hidden case is the subtle one:** if an owner flips it to Only me, RLS returns a null embed and it drops out of the saver's Library **but the save row is kept** (re-sharing brings it back; a real delete cascades) — hence `savedListIds` is tracked separately from `savedLists`, because a list we can't render is still saved and un-saving must still work. **Saved lists get their own section** in both Library and the profile LISTS tab: one flat column would put *delete this list* and *remove my save* a gesture apart looking identical. Owner's name resolved through `Maker.userId` against the in-memory catalog — no extra query. **⚠️ Saving can't work signed out** (`saved_journeys` is account-keyed, unlike `LibraryStore`), so Save is absent rather than present-and-failing; Share stays. **One `…` menu, Share first on both** — yours gets Edit/Delete, theirs gets Save/Go to creator, neither offers an item that would fail. **🔴 A list marked Only me has nothing to share**, so Share becomes a prompt offering to make it visible first. Receiving works end to end: `DeepLink.list` on `/l/` (⚠️ single-letter markers matched as **whole path components** — a test pins it, since a substring match would route every link containing an "l"), `list(byId:)` calling `get_journey` (SECURITY INVOKER — **gone, hidden and never-existed are deliberately indistinguishable**), and an ordinary **sheet with its own nav stack** rather than the UIKit slide-up layer, because a shared list arrives with no screen behind it. **`/l/index.html` is live on gh-pages** (`9f9b854`). **🔴 Found here but NOT caused here: no `apple-app-site-association` is served from gh-pages at all** (root and `.well-known/` both 404), so **Universal Links don't open the app today for tours or creators either** — every share link is affected, fix before launch; `/g/` has no landing page either. **Process:** the owner reported CI red — it was `claude/place-cluster-counts` from a **parallel session**, green on its next run; #517's own checks were all green. *The repo-wide Actions tab mixes every session's branches; read the PR's checks page.* The mockup loop settled the design in one round again (the first pass shipped a bookmark-only button; the owner looked and asked for a menu). | 2026-08-19 |
| `HANDOFF-260817.md` | Session 93, 2026-08-17 (web, code): **24 map pins could never be opened — on the home map AND every affected creator page.** Two different tours sit at exactly the same coordinate (a walk's intro wired at the landmark that also has its own single-stop tour), and grid clustering puts identical points in one cell at **every** zoom, so the tap was an infinite no-op. 🔴 **Any map surface that clusters inherits this and needs a non-zoom escape hatch.** Affected: AMS 5 · YYZ 4 · ROM 4 · BER 4 · YUL 3 · DXB 3 · LAX 1 — **the data is correct, don't nudge a walk intro off its landmark.** Two more defects underneath: a cluster tap could **zoom OUT** (the framing helper's 1.1 km span floor exceeded the span clusters actually form at — inverting the gesture), and the drawer header counted *any-stop-in-view* while the In-view rail filtered on the **centroid**, so a walk 197 m from its own centroid was counted and not listed. Both maps now stack one placecard per tour; the maker map needed a new `region(anchoring:at:span:)` to sit the pin 72% down its 320pt frame so the cards fit. **Owner chose that treatment from an interactive mockup** — *"can you show me first before i decide?"* — over the recommended action sheet. ⚠️ **No check we run can see this bug class**: the validator passes, CI compiles, every URL resolves; only tapping the pin reveals it. Also: 🔴 a **"Set up job"** CI failure means the job died before checkout so **re-running is the diagnosis** (build 64 hit HTTP 429 on `ruby/setup-ruby`; the re-run kept the number and notes), and **CLAUDE.md's advice to trust the `Done` step for build notes was stale and is corrected in place** — that step is an unconditional `echo`; trust the fastlane step. [PR #512](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/512) → `6824b26`; **TestFlight 1.1 (64), owner device-verified.** Catalog untouched at 1350 / 30 / 1696. | 2026-08-17 |
| `HANDOFF-260818-4.md` | Session 98, 2026-08-18 (web, code only): **the place layer's device bugs, builds 69 → 73.** No content, no SQL, catalog untouched at 1418/31. Four bugs reported one per build; the fourth took three attempts and the two failures are the lesson. 🔴 **A presenter must tear down its own layer.** `dismiss()` only nilled the presented value and relied on an `.onChange` in `ContentView` — the **main window, which a slide-up layer covers completely** — to collapse the UIKit layer; SwiftUI can stop delivering updates to a covered hierarchy, so on *tour → maker → close maker → tap a tab* the state cleared while the layer stayed up and the tab bar read as dead. Fixed in **[#535](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/535)** with a `performDismiss` closure on every presenter. **Third instance of this class** after the session-93 tab bar and the batch-D Follow button — put the side effect in the window nothing can cover. 🔴 **[#528](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/528) was a wrong theory** ("a clustered place pin is unreachable") — a place sits at a coordinate distinct from its neighbours, so zoom separates it normally; worse, the exemption made the map **lie**, floating a lone `2` capsule beside a `100` cluster at world zoom. Reverted by **[#536](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/536)**, which also makes a cluster badge count **tours** not markers (a 2-tour place + two singles now reads 4). **Both wrong theories were reasoned from code; each owner screenshot settled in one image what a whole build of guessing had not — ask for the screenshot first.** ⚠️ #536's first CI run failed on its own test: buckets key off an **absolute (lat 0, lon 0) grid**, so at a 0.1° span a row boundary falls on **lat 45.5 exactly** and a marker at 45.5001 bucketed a row up — compute `floor(lat / cellSpan)` rather than eyeballing decimals. **Still open:** Square Saint-Louis is the one place repeating an image (needs one sourced photo, so needs the image-pipeline keys); two device-only checks owed (share a place link to a second phone; save a place and see it on another device); owner reports no "build ready to test" email since 70. | 2026-08-18 |
| `HANDOFF-260818-2.md` | Session 96, 2026-08-18 (web, content): **Barcelona launched — 68 tours (66 single-stop + 2 walks) under Atlas Studio BCN, the 31st maker.** Catalog **1350 → 1418 tours / 30 → 31 makers / 1696 → 1774 stops.** 78 MP3s, **10,517 s ≈ 2h55m — the largest narration drop to date**; 230 images, all owner-supplied and already 1200×900; the tenth consecutive complete drop (Dropbox `/scl/fo/`, 201 MB, first try), zero audio and zero image work. **🔴 The headline: TEN supplied coordinates were wrong and every one was displaced due north** — Nau Gaudí 3.2 km, Hotel Porta Fira 1.26 km, Tibidabo 1.14 km (over the ridge into Sant Cugat), Mercantic 964 m, Walden 7 604 m, Torre Bellesguard 488 m, Teresianes 339 m, Portal Miralles 309 m, Xavier Corberó 248 m, Casa Costa 203 m. At the 30 m geofence none would ever have fired — no error, no dead link, just silence in front of the building. All corrected against OSM and reverse-verified onto the named venue; **the uniform northward sign makes it a systematic upstream generation fault, not noise — fix it at source before the next city.** ⚠️ The 31–74 m offsets on Passeig de Gràcia (Batlló, Amatller, Lleó Morera, Museu Tàpies) and at Sant Pau are **deliberate vantage coordinates** and must not be 'corrected'. Two Nominatim traps recurred: a road hit with **no house number is a street centroid** whose distance is meaningless (it falsely flagged Dry Martini at 503 m; Bar Colòmbia was really **5 m**), and Casa Calvet only 'failed' on query formatting, resolving at **19 m** when re-asked. **✅ All 66 heroes opened — zero wrong-building errors**, with the real traps checked deliberately (Amatller's stepped gable vs Batlló's trencadís; the **six-armed Plaça Reial lamppost vs the three-armed Pla de Palau pair** from the same 1878 commission). ✅ Casa Lleó Morera keeps its interior hero — offered as a one-line swap (its script is about the facade, gallery image 3) and **the owner chose to leave it as delivered**; closed. **✅ Antoni Gaudí was missing from the tag vocabulary and was added the same day, at 18 tours — now the most-represented architect in the catalog, past Álvaro Siza's 15** (not Niemeyer's 11, a stale note this corrected); Domènech i Montaner, Puig i Cadafalch and Bofill likewise absent, all shipping `Designed by a Master`, while Toyo Ito, Jean Nouvel, Calatrava and Mies are present and used by name. Neither walk shipped an intro track → stop 01 became the manual stop 0 (Melbourne precedent). The Dreta walk's 7 stop images are byte-identical to the singles' heroes and were **not uploaded twice**; **230 uploaded = 230 referenced, 0 orphaned**. Validation **0 errors / 0 warnings across all 1418 tours** (Python mirror, vocab cross-checked between `Tag.swift` and the Swift validator, **41/41 injected faults caught** — after one miss that was the *test's* fault, since a 0.0 centroid is a warning not an error). gh-pages via pure plumbing: exactly **308 additions, 0 deletions**, 0 of 308 paths pre-existing (`5603b69`); Tours.json byte-stable at `indent=2`, diff **3,225 insertions / 0 deletions**. | 2026-08-18 |
| `HANDOFF-260818-3.md` | Session 97, 2026-08-18 (local, infra + docs + 1 code PR): **Stripe flagged the account as a restricted business, and answering it exposed a privacy policy that had been false for months.** No content change by this session (Barcelona, session 96, ran in parallel); 1.1 still `WAITING_FOR_REVIEW`. Response **submitted** — it covers **both** categories Dozent could be read into (*Content creation platforms* **and** *Travel reservation services*, since the App Store category is literally Travel) and answers money transmission unasked; the decisive fact for every reading is that **Apple is merchant of record for 100% of consumer transactions**. 🔴 **The live privacy policy claimed "There are no accounts" and no microphone or camera access** while `Info.plist` carries both usage strings and the App Privacy questionnaire correctly declares 9 data types — **the questionnaire and its own linked policy contradicted each other in front of a reviewer**; same trap session 84b fixed in the store *copy* and nobody checked the policy. 🔴 **An empty `$COMMIT` in a push refspec DELETED the gh-pages branch**, taking every audio file and image down for 13 minutes; recovery is `gh api -X POST .../git/refs` (a `git push` from a blobless clone times out — it lacks the 4 GB of blobs), then a full Pages rebuild. **Guard every refspec; prefer the Contents API for single-file edits.** ✅ Side finding: the **4.15 GB site rebuilt from scratch**, so GitHub is not enforcing the 1 GB Pages limit here — R2 stays optional. 🔴 **A React-controlled form held different text than the box showed**, and three `el.value` checks passed while the form still carried the old version; **the owner caught it, not the checks** — write through React's value setter and re-verify after a re-render. ⚠️ **"Our account is in test mode" was false** (live account, address on file) and nearly went to a financial institution — check the live system, not the project note. **`dozent.world` is live** (4 pages from `site/`, Vercel `dozent-world`, apex direct) — 🔴 **never attach it to GitHub Pages: 7,713 catalog URLs point at `ehky2882.github.io`**. Email works via ImprovMX (receive-only on free). **Owner-owed: accept the updated PLA** (Account Holder only, blocks updates) and **decide EU trader status** — currently **non-trader while selling paid IAPs**, and declaring trader publishes an address. **[PR #530](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/530) open, needs simulator review** — Legal links in Settings (there were none) + the About row **"All data stored on device"**, the same falsehood inside the binary. | 2026-08-18 |
| `HANDOFF-260818.md` | Session 94, 2026-08-17/18 (web, code): **the tour upload flow got its missing half.** Owner: *"polish my tour upload feature to the point where it's comparable to what instagram has."* Shaped by an HTML mockup reviewed on device, built in three increments, shipped in **TestFlight 1.1 (62)**, merged as [PR #515](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/515) — 9 files, all Swift, authoring only. **🔴 The headline gap: a tour could not be edited after creation** — title, descriptions, tags, pin and radius were frozen, so fixing a typo meant **deleting the tour and losing its audio and photos**. **🐛 And a silent bug found while building it: `MakerTour` carries NO stops** (`TourRow.asMakerTour` builds its `Tour` with `stops: []` because the feed only needs title/status/images), so an editor reading `tour.stops.first` falls back to a 30 m default — **saving that would have quietly reset the geofence radius of every tour anyone renamed**, with no error and no warning, the same silent class as the Cape Town coordinate 423 m off. Fixed via `stopLocation(tourId:)`; the same gap is why playback was impossible (`stopAudioURL(tourId:)`). **Anything reading `tour.stops` off a `MakerTour` is reading an empty array.** Photos are no longer append-only — drag-to-reorder, remove, **position one IS the cover**, cap 8; `setPhotos` replaces an ordered list and **deletes dropped files from Storage**, with `storagePath` returning nil rather than guessing (a plausible wrong path could delete the wrong object). Every photo now gets a 4:3 framing step with a **Skip** — the failure the image pipeline works around by hand, where a tower loses its top. **⚠️ Audio uploads deliberately bypass the Supabase SDK**: 2.48's `storage.upload` exposes **no progress callback**, so `StorageUploader` hits the same REST endpoint over a plain `URLSession` for real byte counts; photos stay on the SDK. **It is NOT a background session** — uploads survive moving around the app, not the app being killed. `AuthoringAudioPreview` is deliberately **not** `AudioPlayerService` (which owns the mini-player/lock screen) and **not** `AVAudioPlayer` (can't stream a remote URL). `AuthoringErrorText` replaces 6 raw `localizedDescription` sites and **deliberately does not guess** at an unrecognised cause. Owner decisions: published edits **re-enter review**, cap **8**, crop **always shown with a Skip**. ⚠️ Owed: device-verify the radius fix and the drag feel; **true background upload** and **create-form draft autosave** scoped out, not forgotten. Process: **`get_status` is useless here** (returns `pending`/`total_count: 0` forever — use `get_check_runs`, but 0 runs can also mean *conflicted*, so check `mergeable_state`: `dirty` = conflict, `unstable` = checks in flight); a CI job hung >30 min and **cancel-and-rerun** cleared it, exactly as documented. | 2026-08-18 |
| `HANDOFF-260818.md` | Session 95, 2026-08-18 (web, code + content + SQL): **the place layer — a site described by several tours gets its own pin, card and page, and that page can now be saved and shared.** The durable answer to session 93's coincident-pin bug. **Identity is exact coordinate equality** (owner decision) — a 40 m proximity rule was *measured against the live catalog first and rejected*: 43 places of which 19 were wrong, merging LACMA with the Academy Museum and chaining three La Boca venues transitively. **Ranking is newest-first, explicitly provisional** — the design brief assumed listen counts, which **do not exist anywhere in the codebase**, so the page states the order rather than offering a sort it cannot honour; both tiebreaks in `Place.ranked` are load-bearing because 22 of 24 places have tours from one batch with identical `createdAt`. 🔴 **The costliest mistake: "nothing for you to run yet" was wrong.** Build 68 shipped the code and showed the old behaviour, because **the app reads Supabase first** and `places.sql` had never been applied — the RPC served no `places` key. **Adding a key to `Tours.json` does not put it in front of users.** The page was then **rebuilt on `TourDetailView`'s structure** at the owner's direction, settled from an HTML mockup of three phones side by side at true 390 pt — nine divergences, three decisions, all taken by the owner (Gallery/Map strip yes · brass `N TOURS AVAILABLE` header **kept**, the one deliberate divergence · place carousel yes). Save is a separate `SavedPlacesStore` because `LibraryStore`'s `onChange` means *push the tour library to Supabase*; a place bookmark is a plain **toggle** since a place has no lists a second tap could destroy. ⚠️ **Saved places do not sync across devices yet.** Share adds `DeepLink.place`, marker `p`, and a `p/index.html` landing page on gh-pages (`ee5c1fb`) so a shared link degrades to a real page. 🔴 **A Python mirror of a Swift validator proves the LOGIC, never the TYPES** (`[String]` vs `UUID` passed the mirror and failed Swift). 🔴 **`key_path` must precede `revoke-dev-certs.py` in the fastlane lanes** — it is lazy, was reached three steps late since the July rewrite, and build 67 died at the certificate cap with the revoke step logging a warning nobody read. | 2026-08-18 |
| `HANDOFF-260817.md` |  Session 93, 2026-08-17 (web, code): **24 map pins could never be opened — on the home map AND every affected creator page.** Two different tours sit at exactly the same coordinate (a walk's intro wired at the landmark that also has its own single-stop tour), and grid clustering puts identical points in one cell at **every** zoom, so the tap was an infinite no-op. 🔴 **Any map surface that clusters inherits this and needs a non-zoom escape hatch.** Affected: AMS 5 · YYZ 4 · ROM 4 · BER 4 · YUL 3 · DXB 3 · LAX 1 — **the data is correct, don't nudge a walk intro off its landmark.** Two more defects underneath: a cluster tap could **zoom OUT** (the framing helper's 1.1 km span floor exceeded the span clusters actually form at — inverting the gesture), and the drawer header counted *any-stop-in-view* while the In-view rail filtered on the **centroid**, so a walk 197 m from its own centroid was counted and not listed. Both maps now stack one placecard per tour; the maker map needed a new `region(anchoring:at:span:)` to sit the pin 72% down its 320pt frame so the cards fit. **Owner chose that treatment from an interactive mockup** — *"can you show me first before i decide?"* — over the recommended action sheet. ⚠️ **No check we run can see this bug class**: the validator passes, CI compiles, every URL resolves; only tapping the pin reveals it. Also: 🔴 a **"Set up job"** CI failure means the job died before checkout so **re-running is the diagnosis** (build 64 hit HTTP 429 on `ruby/setup-ruby`; the re-run kept the number and notes), and **CLAUDE.md's advice to trust the `Done` step for build notes was stale and is corrected in place** — that step is an unconditional `echo`; trust the fastlane step. [PR #512](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/512) → `6824b26`; **TestFlight 1.1 (64), owner device-verified.** Catalog untouched at 1350 / 30 / 1696. | 2026-08-17 |
| `HANDOFF-260816.md` | Session 92, 2026-08-16 (local, repo hygiene): **No content, no app features — a full cleanup of the repository and working copy, plus PR triage.** **41 branches deleted** (32 remote + 9 local-only), every one verified `MERGED` *and* its squash commit present in `origin/main` before deletion. **`delete_branch_on_merge` is now ON** — merged branches self-delete; expect your branch to vanish after merge, and note it does **not** fire for a reused branch (three survived because two PRs had been opened from them). 🐛 **The primary checkout held 20,640 untracked files — `web/node_modules` (453 MB) + `web/.next` (94 MB) — that `.gitignore` did not cover**, one stray `git add -A` from entering history permanently; confirmed it never did (0 matching objects across all refs), fixed at both layers ([#506](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/506) ignores them; disk went 547 MB → 4 KB, `git add -A` now stages 1 file). Safe because nothing was running, the files were untouched since 2026-07-24, and the full landing-site source incl. `package.json` lives on `claude/web-landing-site-preserve` — **which holds a complete, unmerged Next.js marketing site** worth knowing about pre-launch. **A hidden stash from 2026-05-29 was cleared**: `git stash show --stat` reported it empty because **it hides the untracked component** — the real contents were in `stash@{0}^3` (a 670-line `TourDetailView.swift`, since superseded the same day by `cba0755b` and now 1,646 lines on main). Backed up to the Desktop, then dropped. **Merged #493** (39 architects, 95 tours; Niemeyer had 11 tours and no tag; **Sullivan rejected entirely** and **Eiffel tagged on 2 of 5** — a mention is not authorship), **#502** (pricing correction), **#503** (TestFlight double signing flags), **#498** (price badges — reviewed and clean: the maker grid gates the WALK pill and price badge independently, so a paid single-stop renders correctly). **Closed #501** (superseded by #493 three minutes after it opened; verified 0 architect-tagged tours missing the label) and **#504** (this session's own duplicate of #502 — check for an existing PR before writing one). **Pricing is per-maker, not a platform rule** — #500's "the rule is exactly one sentence" framing corrected by #502. Three-way catalog sync verified: main / gh-pages / Supabase all **1350**. ⚠️ Deliberately not done: `git gc` on a 4.17 GB / 34-pack repo (safe but locks the checkout; four sessions were live). | 2026-08-16 |
| `HANDOFF-260812.md` | Session 90, 2026-08-12 (web, content): **Cape Town launched as the 30th city/maker — the catalog's first South African city and Africa's second bureau.** New maker **Atlas Studio CPT** 🇿🇦 (`38456828-e395-5fd7-9525-a10329fabb15` = uuid5 `atlas-maker:cpt`), **30 single-stop tours, 30 MP3s**, 4,367 s (~1h13m). Catalog **1320 → 1350 tours / 29 → 30 makers / 1666 → 1696 stops**. **Ninth consecutive complete drop** (84 MB `/scl/fo/`, first try; MP3s already 44.1/128, 98 images already 1200×900, scripts 1–30 with no gaps, zero processing); rebased mid-session onto #491/#492/#494. **🐛 Chandler House's coordinate was 423 m off** — on Morris Street in the Bo-Kaap while the venue is the OSM POI at 53 Church Street; **at a 30 m geofence the tour would silently never have fired**, and nothing else catches that class. Re-geocoded, corroborated by the hero's "53"/"CHANDLER HOUSE" signage. All 29 others verified — **four reverse-geocode to a cross-street and are right anyway** (Fyn 10 m from 37 Parliament, Gin Bar 23 m from 64A Wale, Gigi on St George's Mall, Club Kloof 70 m from its own sibling restaurant), so the check must end in a distance, not a road name; ⚠️ Nominatim house numbers on Kloof Street are a street-centroid fallback (84 and 158 return the same point) and zoom 16 misreads Between Us — use zoom 18. **✅ Second consecutive clean open-every-image audit** (30/30 heroes + all 98 images; the two penguin tours, three mountains and two gardens checked as look-alike traps). **Heatherwick present + used by name** on Zeitz MOCAA; **Herbert Baker still absent after #492/#494's 21 additions** → `Designed by a Master`, and the name for the next architect PR. Dylan Lewis ships `city: "Stellenbosch"`; Cape Point/Boulders/Muizenberg are genuinely Cape Town. Sensitivity carried in six places; no mortality figures; no CREDITS rows. Validation **0/0 across 1350 tours** (mirror self-tested 43/43, vocab cross-check confirmed Tag.swift ↔ validator in sync at 59 architects); uuid5 reverse-verified 10 makers + 29/29 SYD pairs; all 128 asset URLs hash-verified against blobs; gh-pages pure plumbing (128 additions exactly, commit `5a4528f`, ⚠️ a publish-catalog run landed on top — verify ancestry); Tours.json diff **1,341 insertions / 0 deletions**. 1 tour hero-only (Maiden's Cove); Gigi's hero is the indoor room not the pool (gallery 2 — one-line swap). | 2026-08-12 |
| `HANDOFF-260811-2.md` | Session 89, 2026-08-11 (web, content): **Sydney launched as the 29th city/maker — Australia's second bureau, wired the same evening Melbourne merged.** New maker **Atlas Studio SYD** 🇦🇺 (`e676aade-c4c6-53dd-9141-48c46d3fe743` = uuid5 `atlas-maker:syd`), **29 single-stop tours, 29 MP3s**, 3,958 s (~1h06m). Catalog **1291 → 1320 tours / 28 → 29 makers / 1637 → 1666 stops**. **Eighth consecutive complete drop** (74 MB `/scl/fo/`, first try; MP3s already 44.1/128, 106 images already 1200×900, zero processing); rebased onto post-Melbourne `main` after #489 merged mid-session. **🐛 Camp Cove Beach's folder coordinate was ~30 km wrong (Broken Bay)** — re-geocoded to Watsons Bay via Nominatim (Tokyo precedent), double-confirmed against both supplied photos. Chinatown Country Club deliberately NOT in Chinatown (222 Clarence St — Nominatim + the hero's street sign both confirm). **✅ Open-every-image audit clean — a first** (29/29 heroes + 12 gallery spot-checks; the five look-alike ocean pools all verified distinct). Sensitivity: Harbour Bridge caption drawn from paragraph 2 so the opener's death toll stays script-only; Camp Cove's 1942 loss likewise. **⚠️ Utzon missing from the tag vocabulary** (Opera House ships `Designed by a Master`); SANAA present + used by name on AGNSW. Validation **0/0 across 1320 tours** (mirror self-tested 44/44, re-run post-rebase); uuid5 reverse-verified 8/8 incl. MEL; all 135 asset URLs hash-verified against blobs; gh-pages pure plumbing (135 additions exactly, commit `b40cd8a`); Tours.json diff **1,310 insertions / 0 deletions**; dup check clean, 106 = 106. 3 tours hero-only (Barangaroo, Fairy Bower, Freshwater). | 2026-08-11 |
| `HANDOFF-260811.md` | Session 88, 2026-08-11 (web, content): **Melbourne launched as the 28th city/maker — the catalog's first Australian city.** New maker **Atlas Studio MEL** 🇦🇺 (`117eebb3-5e5d-51e9-83cf-8ae0f3747bf0` = uuid5 `atlas-maker:mel`), **34 single-stop tours + the Federation Square walk (intro+6, 550 m), 41 MP3s**, 4,987 s (~1h23m). Catalog **1256 → 1291 tours / 27 → 28 makers / 1596 → 1637 stops**. **Seventh consecutive complete drop, the cleanest yet** — MP3s already 44.1/128, images already 1200×900, zero processing. The walk arrived with 7 content stops and no intro; Flinders Station wired as manual stop 0 (Chicago Riverwalk precedent). **🐛 The open-every-image audit caught a Royal Arcade clock (Gog & Magog) filed under Flinders Street Station — dropped pre-upload**, 145/146 images shipped, 0 orphans. Ando (MPavilion) + Kurokawa (Shot Tower cone) used by name; Grounds/Reed/Pitt/Peebles/Askew/McGlashan/Powell/Thompson/Hudson & Wardrop absent → `Designed by a Master`. Four Pillars + TarraWarra ship `city: "Healesville"`. Validation **0/0 across 1291 tours** (mirror self-tested 45/45); uuid5 reverse-verified 8/8; all 186 asset URLs hash-verified against blobs; gh-pages pure plumbing (186 additions exactly, commit `a6cc859`); Tours.json diff **1,689 insertions / 0 deletions**. | 2026-08-11 |
| `HANDOFF-260809.md` | Session 87, 2026-08-09 (web, content): **Chicago launched as the 27th city/maker — and the audio-pending queue is EMPTY for the first time ever.** New maker **Atlas Studio ORD** 🇺🇸 (`f34cd76e-1e41-5c38-865d-d8eccd775cd3` = uuid5 `atlas-maker:ord`), **25 single-stop tours + 5 walks, 53 tracks**, 6,238 s (~1h44m). Catalog **1226 → 1256 tours / 26 → 27 makers / 1543 → 1596 stops**. The last queue drain: staged 2026-07-29/30, narration arrived and wired same day. **First non-MP3 delivery — 53 WAVs (48 kHz mono, 600 MB), transcoded to 44.1 kHz/128 kbps MP3**; matched staging 1:1 incl. the five documented numbering gaps (the master list says 18/19/22/26/27 WERE drafted — a future second batch). **🐛 The open-every-image audit caught two wrong images PR #475's reuse-only audit couldn't:** tour 02's hero showed the WRONG BRIDGE (LaSalle-area bascule, not the DuSable; gallery `_2` was the real one → promoted, both walk reuses corrected) and riverwalk stop 1 faced east at the St. Regis instead of northwest at Wolf Point (→ `merchandise-mart_hero`, shot from the stop's exact position). Two byte-identical cross-tour pairs deduped; **93 referenced = 97 uploaded − 4 documented orphans**. Willis hero cleared the crop43 check. Eastland sensitivity honored end-to-end; Federal Plaza Calder-free; 🔴 Pilsen stops 1+3 ship with the owner-directed OPEN mural rights. Validation **0/0 across 1256 tours** (mirror self-tested 39/39 fault classes); uuid5 reverse-verified (staging READMEs' id forms were wrong — live convention used); all 53 audio URLs hash-verified against blobs; 93 images 200. gh-pages pure plumbing (53 additions exactly); Tours.json diff **1,675 insertions / 0 deletions**. | 2026-08-09 |
| `HANDOFF-260808.md` | Session 86, 2026-08-08 (web, content): **Buenos Aires launched as the 26th city/maker — the catalog's first Argentine city.** New maker **Atlas Studio BUE** 🇦🇷 (`64f37bdd-7cb4-5727-b525-6a801162ff9a` = uuid5 `atlas-maker:bue`), **34 single-stop tours + 2 walks, 46 MP3s**, 6,156 s (~1h43m). Catalog **1190 → 1226 tours / 25 → 26 makers / 1497 → 1543 stops**. **Not from the audio-pending queue** — arrived complete (audio + scripts + images in one Dropbox `/scl/fo/` drop, 111 MB, first try) and wired the same day; **queue unchanged: Chicago alone, 30 tours / 53 MP3s**. **Fourth zero-image-work delivery** (all 120 images already 1200×900, byte-distinct) and **the first drop with pre-structured walks** — `Multi Stop <name>` folders with per-stop coordinate subfolders; intro folders carry no coordinate, wired at the first stop's. The walks: `bue-uba-walk` "Universidad de Buenos Aires" (intro+3, 1.0 km, architecture) · `bue-tresdefebrero-walk` "Parque Tres de Febrero" (intro+7, 4.8 km, natureAndParks). **Script headers are TWO lines** (title + location), both proven un-narrated against the `_tts-safe` twin and stripped; strip on the `— Atlas Audio Tour` suffix, not the first em-dash. ⚠️ **First-ever cross-city slug collision:** Rio owns `catedral-metropolitana`, so BUE ships `catedral-metropolitana-bue`. ⚠️ **Three tours outside the capital** ship with their own `city` (Estación de La Plata / Museo de Arte de Tigre / Alo's Bistro → San Isidro), per the Campinas/Niterói convention. ⚠️ **The Argentine canon (Testa, Palanti, Meano, Tamburini, Catalano) is absent from the tag vocabulary** — `Designed by a Master` fallback; Calatrava's Puente de la Mujer uses the named tag. Validation **0 errors / 0 warnings across all 1226 tours** (Python mirror, vocab cross-checked between `Tag.swift` and the Swift validator, **self-tested 36/36 fault classes**); uuid5 reverse-verified against 4 live makers + the São Paulo walk-stop pattern; `check-image-duplicates.py --maker BUE` clean, **120 uploaded = 120 referenced, 0 orphaned**; **all 166 asset URLs hash-verified against uploaded blobs**. gh-pages via pure plumbing (166 additions exactly, 0 deletions, 0 pre-existing paths of 5,885 checked); Tours.json diff **1,766 insertions / 0 deletions**. ✅ No tour ships hero-only. | 2026-08-08 |
| `HANDOFF-260807.md` | Session 85, 2026-08-07 (web, content): **Marrakech launched as the 25th city/maker — the catalog's first African city.** New maker **Atlas Studio RAK** 🇲🇦 (`c4e51efc-846e-5e78-b699-67e7f9d203e8` = uuid5 `atlas-maker:rak`), **26 single-stop tours, 26 MP3s**, 3,093 s (~51.5m). Catalog **1164 → 1190 tours / 24 → 25 makers / 1471 → 1497 stops**. **Not from the audio-pending queue** — arrived complete (audio + scripts + images in one Dropbox `/scl/fo/` drop, 66 MB, first try) and wired the same day, like Rio/São Paulo; **queue unchanged: Chicago alone, 30 tours / 53 MP3s**. **Third zero-image-work delivery** (all 109 images already 1200×900, byte-distinct, `01` = hero) and **the cleanest scripts of any city yet — 0 `[beat]` markers across all 26**. **First Arabic-script city:** bilingual `English \| العربية` titles on 18 of 26 (proper-noun venues single-name per SGN convention); two supplied Arabic names cleaned (Nomad spacing, YSL folder-truncated final letter). ⚠️ **Aït Benhaddou is ~180 km southeast** (near Ouarzazate), ships with `city: "Aït Benhaddou"` per the Campinas/Niterói convention. ⚠️ **Studio KO not in the tag vocabulary** — and Marrakech is *their* city (YSL Museum + Sahbi Sahbi both ship `Designed by a Master`), sharpening the Schinkel/Niemeyer/Bo Bardi case. Validation **0 errors / 0 warnings across all 1190 tours** (Python mirror, vocab cross-checked between `Tag.swift` and the Swift validator, **self-tested 34/34 fault classes**); uuid5 reverse-verified against 7 live makers + tour/stop pairs; `check-image-duplicates.py --maker RAK` clean, **109 uploaded = 109 referenced, 0 orphaned**; **all 135 asset URLs hash-verified against uploaded blobs**. gh-pages via pure plumbing (135 additions exactly, 0 deletions, no pre-existing paths); Tours.json diff **1,191 insertions / 0 deletions**. ✅ No tour ships hero-only. | 2026-08-07 |
| `HANDOFF-260806.md` | Session 83, 2026-08-06 (web, content): **Berlin launched as the 24th city/maker** — **Atlas Studio BER** 🇩🇪 (`a0717b10-a295-5ab5-a875-d5a9587d0274` = uuid5 `atlas-maker:ber`), **31 single-stop tours + 5 walks, 57 MP3s**, 7,489 s (~2h05m) — **the largest single narration drop to date**. Catalog **1128 → 1164 tours / 23 → 24 makers / 1414 → 1471 stops**. **The first drain from the audio-pending queue since Montreal** (Rio, São Paulo and Dubai all arrived complete and jumped it); **queue is now Chicago alone, 30 tours / 53 MP3s**. Scripts + images had been staged since 2026-07-21, so zero image work. Delivered via a Dropbox `/scl/fo/` shared-folder link, 116 MB, first try — the shape that keeps working, unlike `/t/` Transfer links. **Three README deviations applied**: singles set `stop0.imageURL` to the hero (README says null); walk galleries drop whichever stop image is also the walk hero (else the validator hard-errors) — **both are the exact Dubai-era errors the tracker predicted would recur, and they did, and they are still unfixed for Chicago**; and 🐛 **`ghostline_hero.webp` is BYTE-IDENTICAL to `ghostline_stop4.webp`** (sha256-verified on the live URLs), which would have shown the same photo as the Ghost Line's cover and again as gallery slide 4 — **a class `validate-tours.swift` structurally cannot catch, since the URLs differ and both 200**. 🐛 **Imperial Spine stop 4 (Lustgarten) was staged with the wrong photograph** — `museum-island_hero` is the **Bode Museum from the water**, 600 m north, while the script names the Altes Museum colonnade, the Dom and the palace facade; now `museum-island_3`. Same finding as the then-unmerged [PR #475](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/475) 94-image reuse audit, **verified independently by opening both files**, after which **all 21 Berlin walk-stop images were opened and checked against their scripts** (other 20 correct, including the sensitivity-critical Große Hamburger and Topography images). ⚠️ The single-stop `museum-island` hero is still that Bode shot — defensible as a cover, one-line swap if the owner disagrees. Validation **0 errors / 0 warnings across all 1164 tours** via the Python mirror of `validate-tours.swift`, which **cross-checks its parsed vocabulary against `Models/Tag.swift` and raises on disagreement or an empty parse**, **self-tested against 30 injected fault classes — 30/30 caught**. uuid5 reverse-verified against 16 live makers plus a São Paulo single and all 7 stops of its walk; 0 duplicate ids across 1164/1471/24. **127 images uploaded = 127 referenced, 0 orphaned**; all **184** asset URLs live-checked 200. gh-pages via pure plumbing with both guards — none of the 57 paths pre-existed, tree diff proven exactly 57 additions / 0 deletions / nothing outside `audio/`; Pages lagged 404 but the Actions API showed **`in_progress`, not `cancelled`**, and assets were confirmed by hashing downloaded bytes against uploaded blob SHAs. `transcriptText` = each script minus its 42 `[beat]` markers — **no headers to strip at all**, the cleanest script set any city has delivered; shortest caption **63**. ⚠️ **Karl Friedrich Schinkel is not in the tag vocabulary** despite the Neue Wache, Altes Museum and Konzerthaus (nor Scharoun, Endell, Henselmann), while Foster/Piano/Mies/Calatrava are and are used — with São Paulo's gap, one combined `Models/Tag.swift` PR is now clearly worth it. ⚠️ `east-side-gallery_hero` is Vrubel's in-copyright mural on **German freedom of panorama** (§59 UrhG) — not the same footing as the Chicago Pilsen murals. ⚠️ 2 tours hero-only (Mauerpark, Nollendorfplatz). ✅ The `crop43` portrait-decapitation scare was checked and cleared — the Wasserturm is intact. | 2026-08-06 |
| `HANDOFF-260804.md` | Session 82, 2026-08-04 (web, content): **São Paulo launched as the 23rd city/maker** — **Atlas Studio SAO** 🇧🇷 (`b366d042-881b-5226-aaa8-1dce36c7a2cb` = uuid5 `atlas-maker:sao`), **41 single-stop tours + 1 walk, 48 MP3s**, 5,279 s (~1h28m). Catalog **1086 → 1128 tours / 22 → 23 makers / 1366 → 1414 stops**; Brazil now has two bureaus. **Not from the audio-pending queue** — like Rio it arrived complete in one Dropbox `/scl/fo/` drop (106 MB, first try) and was wired the same day; **Berlin and Chicago untouched**. The walk is `sao-ibirapuera-walk` "Ibirapuera Park" (manual intro + 6 geofenced stops at 40 m, 3.0 km) — Auditório → Oca → Museu Afro Brasil → Planetário → Pavilhão Japonês → Mirante MAC USP; unlike Dubai/Montreal it reuses **no** single-stop hero, so the hero-in-gallery trap applied only within its own image set. **✅ Second delivery running that needed zero image work** — all 173 images arrived already 1200×900, byte-distinct, `01` = hero per folder; 48 MP3s + 48 clean/TTS pairs, 1:1, nothing spare or missing; coordinates from the `output <Name> <lat>, <long>` folder names, no geocoding. **Two cities in a row like this is a pattern, not luck — a drop shaped this way turns a multi-session city launch into one session.** Coverage splits between the modernist canon (MASP, Copan, SESC Pompéia, Casa de Vidro, Pinacoteca, Memorial da América Latina, the Ibirapuera ensemble) and the restaurant/bar/design scene (A Casa do Porco, Nelita, Jacó, KOTORI, Hirá, Varal, Dōmo, Misci, Monica Pondé, VERNIZ). **⚠️ The missing-architect-tag problem is far sharper than Rio's**: Lina Bo Bardi ×4, Paulo Mendes da Rocha ×3, Niemeyer ×3 plus the whole walk, Vilanova Artigas ×2, Ramos de Azevedo ×3, Rino Levi ×2, Burle Marx — all absent from the vocabulary and shipping as `Designed by a Master`, while `Kengo Kuma` (Japan House) and `Jean Nouvel` (Cidade Matarazzo) **are** in it and used by name. Adding the rest is a `Models/Tag.swift` **code** change needing owner OK + sim review, so again kept out of a content PR. **⚠️ Mercado Municipal de Campinas is in Campinas** (~90 km away), shipped with `city: "Campinas"` per the Niterói/Củ Chi convention. **⚠️ MAC USP appears twice deliberately** — single-stop tour about the museum, walk stop 6 about its public rooftop; different scripts, ~30 m apart, not a duplicate. **⚠️ 4 tours ship hero-only** (Estádio do Morumbi, Feira Benedito Calixto, Nubank Parque, Residências Vilanova Artigas). Validation **0 errors / 0 warnings across all 1128 tours** via the Python mirror of `validate-tours.swift`, which now **cross-checks its parsed vocabulary against `Models/Tag.swift` and raises if they disagree or either parse is empty** (closing the Dubai failure mode) and was **self-tested against 26 injected fault classes — 26/26 caught**. uuid5 reverse-verified against live RIO/DXB/YUL/ROM/MAD, a live tour/stop pair and a live walk's stops; 0 duplicate ids. gh-pages via **pure plumbing** (commit `2984f48`) with both guards repeated — **none of the 221 target paths pre-existed** and the **tree diff was proven exactly 221 additions, 0 deletions, nothing outside `audio/`+`images/`**. Pages lagged, serving 404; the Actions API showed **`in_progress`, not `cancelled`**, and assets were confirmed by hashing downloaded bytes against the uploaded blob SHAs. **Reusable finding: don't copy the first tour's key order** — the first single in `Tours.json` is unrepresentative; matching the dominant convention (explicit nulls + full-precision centroid) made the diff **1,989 insertions / 0 deletions**. `transcriptText` = each script minus its `— Atlas Audio Tour` header and every `[beat]`; **two header shapes** (singles `# Name — …`, walk stops bare `Name — …`). Captions span paragraphs; shortest shipped exactly **60**. **Queue unchanged: Berlin (36 / 57) + Chicago (30 / 53) = 66 tours / 110 MP3s** — the two Dubai-era staging-README errors remain unfixed and will recur on both. | 2026-08-04 |
| `HANDOFF-260801.md` | Session 81, 2026-08-01 (web, content): **Rio de Janeiro launched as the 22nd city/maker** — **Atlas Studio RIO** 🇧🇷 (`016bddbe-c759-56fd-8558-869df74b179b` = uuid5 `atlas-maker:rio`), **46 single-stop tours, 46 MP3s**, 5,102 s (~1h25m). Catalog **1040 → 1086 tours / 21 → 22 makers / 1320 → 1366 stops**. **Not from the audio-pending queue** — Rio arrived complete (audio + scripts + images in one Dropbox `/scl/fo/` drop, 106 MB, first try) and was wired the same day; Berlin and Chicago untouched. **✅ The cleanest delivery yet, and the reason is nameable: all 149 images arrived already 1200×900**, byte-distinct, numbered `01..NN` with `01` = hero — so the image pipeline, cropping, sourcing, owner picks and the Gemini gate were **all zero work**, the step that normally dominates a city launch. 46 MP3s + 46 clean/TTS script pairs, 1:1, nothing spare or missing; coordinates came out of the `output <Name> <lat>, <long>` folder names (the HCMC convention) so no geocoding. Coverage splits between landmarks (Christ the Redeemer, Sugarloaf, Maracanã, Theatro Municipal, Museu do Amanhã, Escadaria Selarón) and the boteco/restaurant scene (Jobi, Bip Bip, Guimas, Oseille, Balcão 201). **⚠️ Oscar Niemeyer is NOT in the controlled tag vocabulary** despite five Rio tours being his — Reidy, Portzamparc, Costa and Burle Marx likewise absent; those carry **`Designed by a Master`** as the honest fallback, while `Le Corbusier` and `Santiago Calatrava` **are** in the vocabulary and are used. Adding him means editing `Models/Tag.swift` — a **code** change needing owner OK + sim review — so it was deliberately kept out of a content PR. **⚠️ Two tours are in Niterói** (MAC Niterói, Teatro Popular Oscar Niemeyer — both Caminho Niemeyer), shipped under the Rio maker with `city: "Niterói"` per the Kyoto/HCMC convention. **⚠️ 4 tours ship hero-only** (Álef Antiguidades, Bip Bip, Hotel Nacional, Santa Teresa Tram); 7 more have hero + 1. Validation **0 errors / 0 warnings across all 1086 tours** via a Python mirror of `validate-tours.swift` that **parses the vocabulary out of the Swift source and raises on an empty parse** (the Dubai near-miss) and was **self-tested against 23 injected fault classes first — 23/23 caught**, with the baseline asserted clean so no fault could hide behind a pre-existing error. Its one real finding was **fixed, not suppressed**: Prainha had no Theme tag → `Maritime`. uuid5 reverse-verified against live DXB/YUL/ROM/MAD **and** a DXB tour/stop pair; 0 duplicate ids; `check-image-duplicates.py --maker RIO` clean over 149 images; all 46 MP3s byte-distinct. gh-pages via **pure plumbing** (commit `76d86e1`) with two guards worth repeating — **none of the 195 target paths pre-existed** (nothing could be silently overwritten) and the **tree diff was proven to be exactly 195 additions, 0 deletions, nothing outside `audio/`+`images/`**. Pages lagged ~5 min serving 404; checked the Actions API and found the run **`in_progress`, not `cancelled`**, then confirmed **all 195 assets by hashing the downloaded bytes against the uploaded blob SHAs** — not by a 200. `transcriptText` = each script minus its single `[beat]` (46 of them); captions span paragraphs, shortest shipped **73** chars. **Queue unchanged: Berlin (36 / 57) + Chicago (30 / 53) = 66 tours / 110 MP3s** — and the two Dubai-era staging-README errors are **still unfixed** and will recur on both. | 2026-08-01 |
| `HANDOFF-260731.md` | Historical (superseded by `-260801`). Session 80, 2026-07-31 (web, content): **Dubai launched as the 21st city/maker** — **Atlas Studio DXB** 🇦🇪 (`e94b8814-2c31-5113-963c-1743e6c86b4b` = uuid5 `atlas-maker:dxb`), **26 tours = 22 single-stop + 4 walks** (Creek Crossing, Old Quarter, Downtown Loop, Marina & JBR), **40 MP3s**, 5,012 s (~1h24m). Catalog **1014 → 1040 tours / 20 → 21 makers / 1280 → 1320 stops**. Fourth city wired straight from the audio-pending queue; the delivery matched the staging **1:1** — nothing spare, nothing missing. **✅ Transport settled: a Dropbox SHARED-FOLDER link (`/scl/fo/…`) downloads headlessly with `dl=1`** — 82 MB zip, first try, exactly as Ho Chi Minh City did. **Do not confuse it with a Dropbox *Transfer* link (`/t/…`), which cannot** — that was Montreal's four-attempt fight, along with SharePoint work-tenant links and Chromium being unable to reach the network at all. **Check the URL shape before declaring a link undownloadable.** **⚠️ Two staging-README errors found and corrected, both of which will recur on Berlin and Chicago:** (1) the batch README says singles take `stop0.imageURL: null`, but Montreal/Rome/Madrid set it on **100%** of their singles — Dubai now sets it to the tour hero; (2) each walk README lists `additionalImageURLs` as *every* stop image, but each walk's hero is picked from among those same images, so that spec **hard-errors** the validator's `heroImageURL also appears in additionalImageURLs` check and would show the same photo twice — Montreal already drops it (6 stops → 4 entries), Dubai now does too. **Fix those READMEs before the next city wires in.** **⚠️ The Python mirror validator nearly lied:** its first revision parsed **zero** tags because `load_vocab` matched `(facet:…, tags:…)` while `Tag.swift` writes `(.placeType, [ … ])` — it emitted 4,687 bogus errors, which is the only reason it got noticed; a subtler parse failure would have passed everything. **Assert a non-empty parse, don't just run it.** Final **0 errors / 0 warnings across 1040 tours**, self-tested against **18 injected fault classes (18/18 caught)**; 0 duplicate tour/stop/maker ids; uuid5 reverse-verified against live YUL/ROM/MAD/AMS/SGN; `check-image-duplicates.py --maker DXB` clean over 72 images. **⚠️ gh-pages needs PURE PLUMBING, not a worktree:** a full fetch times out, and after a blobless fetch every index operation (`checkout`, `add`, `diff --cached`, plain `write-tree`) **hangs** fetching missing blobs on demand. Use `read-tree` into a temp `GIT_INDEX_FILE` → `hash-object -w` → `update-index --cacheinfo` → **`write-tree --missing-ok`** → `commit-tree` → push the sha; then prove the tree diff is exactly the intended additions and **0 deletions**. Pages lagged ~9 min serving 404 — checked the Actions API and found the run **`in_progress`, not `cancelled`**, then confirmed all 40 MP3s by **hashing the downloaded bytes against the uploaded blob SHAs**. All **112** Dubai asset URLs 200. **✅ Caption rule now spans paragraphs** — several scripts open with a one-sentence bare instruction that the old first-paragraph-only splitter could not extend past (the Rome "Start with the holes." failure in a new guise); shortest shipped is exactly 60 chars. **Open for the owner:** two heroes ship flagged at their prior explicit direction — `al-shindagha_hero` (licence unverifiable, upscaled ~1.6× so visibly soft) and `difc-gate_hero` (garbled signage, likely AI-generated; **`difc-gate_2` is a verified photo and a one-line swap**). 11 credit-required images in `drafts/CREDITS.md`; ⚠️ `al-shindagha_2` is **FAL**, copyleft like BY-SA. **Queue after Dubai: Berlin (36 / 57) + Chicago (30 / 53) = 66 tours / 110 MP3s.** | 2026-07-31 |
| `HANDOFF-260728-3.md` | Historical (superseded by `-260731`). Session 79, 2026-07-28 (infra, no code): **paid tours Phase 2 went LIVE** — the merged backend is now running. Owner applied `backend/paid_tours.sql` ("Success. No rows returned."); verified live: `get_catalog` 200 with **26 makers / 1014 tours**, all carrying `priceTier`, **0 non-null** (catalog still entirely free), the five new objects readable-but-empty to anon and every anon **write** 401, `maker_net_cents(299)` → **203** ($2.99→$2.03 per the design doc). Created the App Store **In-App Purchase** key — ⚠️ a *different key type* from the TestFlight signing key, on its own ASC tab, with **no role selector**, so the Admin-role gotcha does NOT apply — **Key ID `95442B44CC`**, **Issuer `f34324bd-aa34-4de0-8acb-2537b0e9325e`**, `.p8` downloaded once and stored by the owner (ASC now marks it downloaded; lost = revoke + regenerate). Four secrets set (owner pasted `APPSTORE_IAP_KEY`; Claude handles no key material) and both Edge Functions deployed via the dashboard editor with the **owner pasting the code** — Monaco auto-inserts closing brackets, so typing 300+ lines of TS through automation corrupts it, unacceptable for payment code. JWT gates **proven by request, not by UI**: `record-purchase` ON → unauth POST `401 UNAUTHORIZED_NO_AUTH_HEADER`; `appstore-notifications` OFF → unauth POST `200 ok`. **Reusable proof:** POSTing the publishable key to `record-purchase` returns `"sign in required"` rather than `"App Store API secrets not configured"` — the secrets are checked *before* auth, so that response proves all three (incl. the pasted `.p8`) are present and non-empty. Apple's **Production + Sandbox** notification URLs both set to the `appstore-notifications` endpoint (ASC's dialog offers **no version selector** — confirm V2 at the Phase 6 rehearsal). **⚠️ Phase 3 test item:** the toggle says *"Verify JWT with **legacy secret**"* but this project signs users with **ES256** (JWKS returns an EC key; `SUPABASE_ANON_KEY` DEPRECATED) — untestable until a logged-in user exists. If purchases 401, verify the JWT against the JWKS **inside** `record-purchase` **then** turn the toggle off; **never off first**, since the function decodes `sub` unverified and leans on the gateway. Functionality risk, not a live hole. **NEXT: Phase 3 buyer UI.** | 2026-07-28 |
| `HANDOFF-260728-2.md` | Historical (superseded by `-260728-3`). Session 78, 2026-07-28 (web, content): **Montreal launched as the 20th city/maker** — **Atlas Studio YUL** 🇨🇦 (`4f7241f0-…`), **29 tours = 25 single-stop + 4 walks**, 46 MP3s, **90m13** of narration. Catalog **985 → 1014 tours / 19 → 20 makers / 1234 → 1280 stops**. Third city wired straight from the audio-pending queue; the drop matched the staged drafts **1:1**, and its numbering gaps (13, 18–20, 28, 29) are exactly the scripts never written — no surprise extras, unlike Rome. **⚠️ The session's real lesson is file transport, not content.** A **Dropbox Transfer link (`/t/`) cannot be downloaded headlessly** (JS-gated list; the download sits behind an undocumented internal API — every guessed endpoint 404s). **Chromium cannot reach the network through the agent proxy at all** — even `example.com` returns `ERR_CONNECTION_RESET` with **no** proxy-side failure recorded — so "drive it in a real browser" is not a fallback in this environment. And a **corporate SharePoint/OneDrive link is an identity boundary, not a link-format problem**: `tishman-my.sharepoint.com` issued **zero** cookies on redemption, Graph returned **401 "Access token is empty"**, `api.onedrive.com` returned **"User migrated"** — it works for the owner only because their browser is already signed in to the work tenant, and the `?e=` token is a tracking param, not a key. **Do not try to route around a work-tenant link; ask for a personal account or a chat attachment.** **What worked: attaching the MP3s in chat** → `/root/.claude/uploads/<session>/` with a hex prefix per file. **For any 20–60 file batch, lead with that** — it needs no link, no sharing settings, and cannot be blocked by the owner's IT. Verification: **0 errors / 0 warnings** across 1014 tours via a Python mirror of `validate-tours.swift` that was **self-tested against 8 injected fault classes first** (a validator that passes everything is worse than none); uuid5 reverse-verified against the live **MAD + ROM** maker ids; all 46 MP3s **byte-distinct** (the Thyssen bug applied to audio); `check-image-duplicates.py --maker YUL` clean over 74 images. **✅ The `St.` caption trap CLAUDE.md predicted for Montreal was real and handled** — the abbreviation-aware splitter kept "the St. Lawrence" whole instead of producing a 40-char fragment; **Berlin has the same hazard.** **⚠️ Both gh-pages pushes served 404 for ~10 minutes** while the Pages deploy ran `in_progress` — confirmed only by **hashing the live URL against the uploaded bytes**, never by the push succeeding. **Open for the owner:** the **Mount Royal walk hero** (Kondiaronk Belvedere vs the Cross — the staged README marked it owner-to-confirm; wired with the belvedere, its own stated default, one-line swap); **5 singles ship hero-only** (Mary Queen, Place Ville Marie, Square Saint-Louis, The Main, Plateau staircases); and **no PR is open** — merging publishes over the air to real users, so that is the owner's call. **Audio-pending queue after Montreal: Berlin (36 / 57) + Dubai (26 / 40) = 62 tours / 97 MP3s.** ⚠️ **I first told the owner "Berlin only" and was wrong** — Dubai was script- and image-staged on 2026-07-27 by a parallel session that never updated `drafts/AUDIO-PENDING-SURVEY.md`, so the tracker's own "last verified" stamp was misleading and I repeated it into four documents before the owner corrected me. **Re-derive the queue from `git ls-tree … drafts/` when a parallel content session may have run.** ⚠️ **Dubai has no master pick-map README** — its slug↔coord↔category↔image assignments are written down nowhere, so a wire-in session must reconstruct them or staging must write it first. | 2026-07-28 |
| `HANDOFF-260728.md` | Session 77c, 2026-07-28 (web, code + SQL): **lists on other people's maker pages, and LIKED on everyone's** ([PR #462](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/462)). Pass 2 of the maker-page work. **Owner has one thing to run: `backend/public_liked.sql`** — until then every profile's LIKED shows empty. (`backend/public_lists.sql` already run + verified live.) **The blocker was one missing field:** lists are filed under an **auth account id** and the creator profile the app holds carried none — so `get_catalog()` now emits `makers.user_id` → `Maker.userId: UUID?` → `TourListService.publicLists(ofUser:)`. **⚠️ That optional is load-bearing** — the gh-pages mirror and the bundled offline seed come from `Tours.json` and will never carry it. **Verified before shipping that `seed_from_toursjson.py` does not name `user_id`**; if it ever does, the next content merge nulls every user's maker link and silently breaks lists, follows and the profile. Owner decisions applied: **new lists visible by default** and **every existing list flipped visible** (one-way, explicitly requested — disclosed that it touched **two accounts, not one**), and the editor toggle now reads **"Only me"**. **LIKED now appears on every profile, Atlas studios included** — it is the one list everybody has, and a page without it read as broken rather than empty (that was the **"black square"** in the 1.1 (54) review). **⚠️ Reading someone else's Liked needed a server path, and its shape matters:** Liked is backed by `LibraryStore` so saving works signed out, and that store only holds *yours*; `liked_tour_ids(p_user)` is `SECURITY DEFINER`, so its body is the security boundary and is deliberately tiny — **downloads, playback progress and completion sit in the same `user_library` row and are not returned.** Also: a **key badge** on the cover of a list only its owner can see; the **MAP tab draws the world centred on the Atlantic when a maker has no tours** (`initialRegion(for:)` is non-optional now); and **LIKED's permanence is written into `LikedListView`** — it is not a `journeys` row with its controls hidden, and the moment it becomes one, an un-save stops being the only way to remove a save. **⚠️ Two corrections worth carrying:** I told the owner this needed no backend work and was **wrong** (I'd taken `userId` from a report describing `MakerRow`, the private Supabase DTO, not the rendered `Maker`); and the owner couldn't follow my explanation of it — *"i'm really not understanding what you're saying"* — because I led with "this needs a backend change" instead of the plain fact, **"the app doesn't hold their account ID."** **Still deferred:** saved tours on the map (treatment approved, colour + timing open) · Shared/Only-me **follower** visibility · **private-hides-everything** (its own project) · the rest of the owner's *"lots to do"* list, still undescribed. | 2026-07-28 |
| `HANDOFF-260727-4.md` | Session 77b, 2026-07-27 (web, code): **maker page gains a TOURS / LISTS / MAP tab strip, and the app's two segmented controls convert to the same strip** ([PR #461](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/461)). Code only — no `Tours.json`, no SQL, nothing for the owner to run. **Net −249 lines** (+293/−542). Driven by **four rounds of HTML mockup reviewed on device**; every decision was made by the owner looking at a thing, not reading an argument. **⚠️ The owner corrected me on the central design question and was right:** I argued a pannable map can't live in a `ScrollView`; they pointed out **tour detail has done exactly that for months** — 320pt, inset 24pt, square corners, and **the 24pt gutters either side are the trick** (that's where a drag scrolls the page instead of panning the map). *Check whether the codebase already solved it before reasoning about whether it can be.* Five new shared files, each replacing something duplicated or unreachable: `AtlasTabStrip`, `MapPins`, `MapClustering`, `TourListRows`, `MakerMapSection`. **The pin extraction fixed a live drift** — `TourDetailView` had hand-reimplemented `StopPin` + `UserLocationDot` with a comment saying it had to (they were `private` to `HomeMapSection`), and the copies had diverged to a **14pt dot against Home's 16pt**; the maker map would have been the third copy. **Deleting the segmented controls also deleted two global `UISegmentedControl.appearance()` mutations** that were restyling every segmented control the app could ever show, including in system sheets — plus **`TourDetailView.init(tour:)` entirely**, which existed only to run one and was duplicated from `LibraryView.init` to dodge a cold-launch ordering bug that no longer exists. `cellsAcross` is now a parameter (counts cells across the **region**, not the screen — a 320pt map needs ~12, not 20). **🐛 A bug CI could not catch, found by re-reading after the build went green:** the camera and the clustering region were seeded in **two different `.task` blocks**, so whichever ran first read `.automatic` (region `nil`) and `currentRegion` stayed nil — **a maker with 40 tours in one city would have opened to a pile of unclustered dots and stayed that way** until the first pan. Fixed in `6f31c7a`; *two tasks that must agree are a race*. **⚠️ Scope not delivered as asked:** the owner wanted all three tabs on **every** maker page, but **LISTS is own-profile only** — reading another creator's public lists needs their `auth.users` id and **`get_catalog()` never emits `makers.user_id`**, so the client `Maker` has no `userId`. I had earlier told the owner this needed no backend work; **that was wrong** (I'd taken `userId` from a report describing `MakerRow`, the private Supabase DTO, not the rendered `Maker`). **Owner's list-visibility model is decided and designed, not built:** a list is **Shared or Only me**, and the *account* setting decides who "Shared" reaches (public → anyone; private → accepted followers); "Only me" hides from followers too. **Two schema traps recorded:** `follows.followee_id` is a **MAKER** id while `journeys.owner_user_id` is an **AUTH USER** id — the bridge `makers.user_id` is unique only via a **partial** index and the 19 Atlas studios are all `NULL`, so a careless join **matches every seed studio at once**; and `makers.user_id` is `on delete set null` while `journeys.owner_user_id` is `on delete cascade`. **Device review, riskiest first: Library and tour detail** — shipped screens that were not broken. **Open:** saved-tours-on-the-map (treatment approved — solid dot vs hollow ring so it survives greyscale — only timing deferred); whether Library's Lists tab shrinks; the strip's full-bleed rule vs inset; and the rest of the owner's *"lots to do"* maker-page list, not yet described. | 2026-07-27 |
| `HANDOFF-260727-3.md` | Session 77, 2026-07-27 (web, code + docs): **closing out the saving consolidation — TestFlight 1.1 (51) verified, and `Journey` renamed to `List` throughout the app.** Two PRs: [#457](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/457) (`8f29626`, docs) and [#458](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/458) (`370b9f0`, the rename). **⚠️ The most transferable lesson has nothing to do with the rename: build 50 carried the saving branch's bottom-module fix, but the merge took `main`'s better one from [#443](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/443) — so the combination that shipped was never the combination that was tested.** Both branches were verified; their merge wasn't. A build from `main` (`dfc3a5d`) closed it and the owner verified the real thing: *"SHINSEGAE VIDEO WORKS, IS FINE. TAB BAR IS PRESENT FROM FIRST FRAME. DEAD TAB SEEMS FINE."* **I also burned two wrong theories on the tab bar** (orphaned window, then zero-size window) before the owner's clarification — *"only not there momentarily after launch… missing long enough to look like a mistake"* — identified it as a **late** install, not a failed one, which #443 had already solved by rendering the bars **inline in `ContentView`** when the secondary window is absent. All my bottom-module work was discarded; **check whether `main` already fixed it first**, and note I re-added `w.frame = scene.coordinateSpace.bounds` even though CLAUDE.md flags it as a known dead end. **The rename:** `Journey`→`TourList`, `JourneyItem`→`TourListItem`, `JourneyService`→`TourListService`, `Features/Journeys/`→`Features/Lists/`; identifiers + comments only, no behaviour change. **Not plain `List`** — SwiftUI's `List` is used in three views, so a same-named type makes every `List { }` ambiguous. **⚠️ The Supabase tables deliberately STAY `journeys` / `journey_items` / `journey_id`:** renaming needs a hand-run migration and **every build already on a tester's phone queries the old names** — they'd break the moment it ran, for a name no user sees; literal counts verified identical before/after. **⚠️ Rename trap worth carrying:** a word-boundary search **cannot see `_journeyService`**, the property-wrapper storage for an `@State` — underscore is a word character, so `\bjourneyService\b` never matches, and it fails to compile. Also landed **`docs/tag-taxonomy.md`** (unmerged since June) reframed as **shipped, not proposed** — it wasn't stale, but it read as a proposal for a system live since session 57; kept because it's the only written record that *"Designed by a Master" is implied by any Architect tag*, which no code enforces. **No build owed; 1.1 (51) is current.** **Owed:** delete `claude/journey-bookmarks-default-folder-c8ur85`, `claude/handoff-260726`, `claude/dreamy-wozniak-tags-260612`, `claude/rename-journey-to-list` in the GitHub UI. | 2026-07-27 |
| `HANDOFF-260727-2.md` | Historical (superseded by `-260727-3`). Session 76, 2026-07-27 (web, content + tooling): **a wrong hero shipped for a month — root-caused, fixed, and the whole catalog swept clean of duplicate images** ([PR #453](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/453) `7b5762c`, [PR #455](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/455) `7c1d48b`, + 3 gh-pages asset commits). Owner spotted Museo Thyssen-Bornemisza showing the Reina Sofía building. Cause, provable from git: gh-pages commits `41a03d5` and `103c90c` are **40 seconds apart and their hero blobs share the identical hash** — the Thyssen commit re-used the image decoded 40s earlier, so the owner's paste was never written. **⚠️ It was unrecoverable:** inline-pasted images live as base64 in the session transcript, but **a web session's container is fresh-cloned, so prior transcripts are gone**. Owner re-pasted; that paste *was* recovered from this session's `.jsonl`. **No `Tours.json` change was needed** — the URL was always right, only the bytes were wrong — and replacing the one file also fixed the Paseo del Arte walk, which was showing Reina Sofía twice. New **`scripts/check-image-duplicates.py`** hashes every referenced image and groups by SHA-256; this class is **invisible to `validate-tours.swift`** (distinct URLs, all 200, only bytes match). **Not in `ci.yml`** — network-bound; run `--maker <CODE>` when staging a city. **⚠️ Read a full run before trusting your rules:** v1 flagged 18 groups, **4 legitimate**, and Ponte Sant'Angelo exposed a real gap (a walk's **stop** image doesn't resolve to the parent walk's slug) — fixed in the tool, kept narrow so a walk's *gallery* matching a single-stop hero still errors. **A URL-level check in the Swift validator was planned and dropped — it would have caught neither Madrid bug.** All **14** other groups resolved, **no hero changed**; two were genuine mis-stages (a Mission mural in **Haight-Ashbury**, a Munttoren sunset in **De Wallen**), two dead swipes in Fifth Avenue Walk, the rest adjacent-landmark shares. **Also found: 2 dead 404 URLs** (Madison Square Garden, Riverside Church stop images) + a double-URL-encoded Ford Foundation reference. Madrid de los Austrias got its **own** hero (owner-picked CC0) instead of reusing Círculo de Bellas Artes. **Catalog now 0 errors across 3,615 images.** **⚠️ Gotchas:** `git push … | tail` inside an `if` reads **tail's** exit code — a rejected push reported "PUSH OK"; a gh-pages Pages deploy came back **`cancelled`** and served stale ~10 min, so **verify by hashing the live URL, not by the push succeeding**; `Tours.json` round-trips byte-identically as `json.dumps(indent=2, ensure_ascii=False)` with **no trailing newline**; `pull_request_read get_status` reads `total_count: 0` even when CI is green (those are check *runs*). **Open:** King of Kowloon dedupe is the most debatable (both tours are about the same artist) — one-line revert; Champs-Élysées hero is an aerial of the **Arc de Triomphe**, arguably the wrong subject. | 2026-07-27 |
| `HANDOFF-260727.md` | Historical (superseded by `-260727-3`). Session 75, 2026-07-27 (web/PM, content): **Rome launched — 30 tours + 19th maker Atlas Studio ROM** ([PR #450](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/450), squash `4598ed6`, auto-merged on green as content class). 25 single-stop (geofenced 30 m) + **5 walks** (Ancient Rome 1.5 km, The Baroque Heart 2.0 km, The Ghetto and Trastevere 2.5 km, The Vatican and the Borgo 1.25 km, The Aventine and Testaccio 2.5 km), 53 MP3s / 6,866 s. **Catalog 948 → 978 tours / 18 → 19 makers / 1174 → 1227 stops.** Second consecutive city wired straight from the audio-pending queue — all 116 images were already staged by slug, so narration was the only missing ingredient and the image work was zero. **⚠️ The delivery was 60 MP3s, not the 53 staged: 7 extra singles (master-list 25–31 — Quirinale, Monti, Santa Maria Maggiore, San Giovanni in Laterano, Trajan's Column, Porta San Sebastiano, Testaccio) arrived with NO scripts and no images for five of them** (the Dropbox drop carried only MP3s + handoff `.md` files, zero `.txt`). **Not wired** — a tour with no hero can't ship and one with no script would carry `transcriptText: null` + an invented caption — but **their audio is banked on gh-pages under its eventual slug**, so the follow-up needs scripts + owner image picks, not a re-upload. Judgement call open: Trajan's Column + Testaccio *do* have a hero and could ship as thin singles if the owner prefers. **⚠️ Two staging-README errors, corrected at wire-in and expected again in Montreal + Berlin (same author):** the READMEs say `kind: "singleStop"` but the catalog's real value is **`"single"`**, and their suggested tags (`ancient-site`, `square`, `fountain`, `castle`…) are **not in the controlled vocabulary** — **`Models/Tag.swift` is the vocabulary, not a batch README**. Walk intro audio is `{walkslug}_stop0.mp3`, not `_intro.mp3`. **Verification:** CI Swift validator 0/0 (locally a **Python mirror** stood in for the absent `swift`, and was **self-tested against 5 injected faults** so its pass meant something); **all 169 asset URLs 200**; uuid5 ids reverse-verified against live Madrid; diff additions-only. **Gotchas:** `while read` silently skips a final line with no trailing newline (first sweep checked 168 of 169 — assert the count, not just failures); in a `--no-checkout` gh-pages worktree **every file reads as an unstaged deletion**, so stage by explicit path and **never `git add -A`**; CI job-state API lags (one poll showed zero jobs in flight mid-build). Confirmed live on Supabase at 978 tours / Rome 30. **Owed:** delete merged branch `claude/rome-audio-stage-tours-m05vud` in the GitHub UI (proxy blocks it). **Queue after Rome: 65 tours / 103 MP3s** — Montreal 29, Berlin 36. | 2026-07-27 |
| `HANDOFF-260726.md` | Historical (superseded by `-260727`). Session 74 (1 of 2 — parallel session). Session 74, 2026-07-26 (code/web): Group Listen polish driven turn-by-turn off owner screenshots across TestFlight **1.1 (42)→(46)**, plus two real bugs it surfaced. [#441](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/441) `57095f0` **deleted the bottom session banner** — the tour page's group icon turns **green** while a session runs and opens the sheet (which carries Leave); glyph 17→16pt. [#443](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/443) `bf9f98e` carries the rest. **🐛 The "dead" tab bar was NOT group-specific:** taps registered (icon lit) but the tour-detail layer was never dismissed, so the new tab loaded *behind* it — `ContentView`'s `.onChange(of: selectedTab)` lives in the main window, which the modal covers, and **SwiftUI can stop updating a hierarchy hidden behind a modal**. The tab-bar binding now dismisses as part of the tap, from the secondary window. **🐛 Missing mini-player + tab bar, third attempt:** the bars existed ONLY in the secondary window, so a failed install left no bars and no fallback; `ContentView` now renders them **inline** whenever that window is absent (controller is `@Observable`). Also **reverted a self-inflicted `w.frame = scene.coordinateSpace.bounds`** that pins a geometry snapshot and, on an unconfigured scene, pins *zero* — the invisible window it was meant to prevent; readiness moved into `foregroundActiveScene()`, and `setInteractiveBottomInset` now clamps to ≥ `AtlasBottomModule.height()`. **Sheet:** caption type throughout, `.medium` detent, **overlap of the nav title fixed by a ScrollView** (a fixed frame let overflow render *upwards* through the nav bar), bottom inset so nothing hides behind the mini-player, and all three screens as two aligned columns (`LEAD A TOUR`/`JOIN A TOUR`; `SCAN TO JOIN` beside `OR ENTER CODE`). **⚠️ QR floor is 110pt** — owner-verified as still scannable; if that screen needs room, open a taller detent, don't shrink the code. **Owed: two-phone sync still never run end-to-end.** | 2026-07-26 |
| `HANDOFF-260726-2.md` | Historical (superseded by `-260727`). Session 74 (2 of 2 — parallel session). Session 74, 2026-07-26 (code/web): **saving consolidated — one save action, "Liked" is the default list** ([PR #447](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/447), open). There were **two** ways to keep a tour with **two stores and no knowledge of each other** — the bookmark (`LibraryStore.savedAt`) and "Add to a Journey" (`journey_items`); a tour in a list wasn't bookmarked, a bookmarked tour was in no list. Same redundancy killed for makers in #398. **The brief sharpened mid-session and that mattered:** the first plan only merged the *views* (rename the tab, pin a Liked row) while leaving two write paths — it would have shipped the exact redundancy the owner was removing. **"Add a default folder" and "consolidate saving" are different briefs.** Now: **saved = in at least one list**; **Liked is the default** (owner: *"if the user doesn't specify, there should always be a default 'liked' folder"*) — filing into a named list puts it *there*, not also in Liked, and nothing moves implicitly. **Bookmark tap:** nothing → add to Liked · exactly one list → remove it (second tap always undoes the first) · several → open the membership sheet, everything ticked, user unticks what they want out (the owner's own UX call, better than the blunt "remove from everything?" confirm first proposed). **⚠️ The constraint that shaped it all: bookmarking works SIGNED OUT, lists are cloud-only** — so **Liked stays on `LibraryStore`** and named lists stay in Supabase; one concept, two backends, no visible seam. **Making Liked a real server row would gate bookmarking behind an account — don't.** `LibraryStore`/`SyncService` untouched; **no backend change, no migration.** New `SaveState` (pure, unit-tested rules) + `TourSaveActions` (binds them to the stores so cards / detail / player can't drift); `AddToJourneySheet` → **`TourListMembershipSheet`** (removes as well as adds, leads with Liked). **Library is the single home** for kept things; the profile's Journeys row **removed**, `JourneysListView` deleted. **Perf:** `isSaved` is read by every card in every rail, so per-tour queries were never viable — `loadMyJourneys()`'s embed already returns every listed tour id, making `allListedTourIds` free. **Fixed in passing:** `JourneyService.clear()` existed but was **never called**, so a stale list survived account switches. **⚠️ Naming still open** — copy says "list", types/tables still `Journey`; "Journey" breaks on the default bucket ("Liked" isn't a journey) and collides with **Walk**. **⚠️ Device check owed:** the signed-out bookmark path is the whole design constraint and the sim has no session. | 2026-07-26 |
| `HANDOFF-260725-4.md` | Historical (superseded by `-260726`). Session 73, 2026-07-25 (content/PM, local Mac): **Madrid launched — 34 tours + 18th maker Atlas Studio MAD** ([PR #435](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/435)). The owner asked whether they could hand over audio for tours already **staged**; they could, and did (`~/Downloads/260630_MADRID ALL`). **First city wired in end-to-end from the audio-pending queue** — scripts + all 118 images had been staged since 2026-06-24/29 on `claude/dreamy-wozniak-nM6a4`, so the job was: match audio → read durations → upload → assemble. 30 single-stop + **4 walks** (Madrid de los Austrias 1.1 km, Paseo del Arte 1.3 km, El Retiro 1.8 km, Royal Madrid 1.4 km), 55 MP3s, 6,459 s. **Catalog 914 → 948 tours / 17 → 18 makers / 1119 → 1174 stops.** The drop matched the staged drafts **1:1**. **Two decisions:** singles ship geofenced at **30 m** (the catalog-wide default) not the "~40 m" the READMEs suggested, walk stops 40 m + `manual` intro; and the tracker's Madrid count was **one over** (31/56 → actual 30/55), now corrected. **Gotchas:** two files share the `madrid_04_` prefix (match full stems, never the NN index); `[beat]` markers must be stripped (validator hard-errors on bracketed stage directions); captions needed a two-sentence fallback because many scripts open with a bare instruction; on this Mac Python `urllib` fails SSL verification (use `curl`) and **`xargs -P` silently exited 0 after "command line cannot be assembled, too long"** — use `while read`. Validator 0/0; **all 173 asset URLs 200**; 0 duplicate ids. **Key-facts drift caught again** (read 871/16/1076, `main` was 914/17/1119 — Chiang Mai never folded in) — third resync in a row. **Queue after Madrid: 95 tours / 156 MP3s** — Montreal 29, Rome 30, Berlin 36. | 2026-07-25 |
| `HANDOFF-260725-3.md` | Historical (superseded by `-260725-4`). Session 72, 2026-07-25 (code/web): **Group Listen ("SharePlay") went from "does nothing" to device-verified working**, then tightened. [PR #423](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/423) (`3e9a6d9`) fixed it; [PR #428](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/428) (`18ba375`) added tighter sync, QR joining, a tappable banner + polish. It had shipped in 1.1 (8) **never device-verified** — the owner was its first tester. Root causes were all discovery/connection: **no `didNotStart…` delegates and no connection state**, so a denied **Local Network** permission was a silent dead-end (the headline); false "Leader left" during the handshake; **epoch poisoning** (a former leader joining as follower ignored every broadcast); intro-audio desync; a `peerParticipants` data race. **Sync:** the 1.25 s drift dead zone became two-tier — ignore <0.15 s, **trim speed ≤3%** to glide back 0.15–2 s, precise seek beyond; new `AudioPlayerService.syncTrim` kept separate from the user-facing `rate` (solo playback unchanged) + `seek(to:precise:)`; heartbeat 1.0→0.5 s. Owner: **"good enough to ship"** — don't tune further; genuinely tighter needs shared-clock architecture, not tuning. **QR join:** Core Image generates, AVFoundation scans, payload is a Universal Link (`…/g/?code=`) so the system Camera app works; `DeepLink.group(code)`; adds `NSCameraUsageDescription`. **Banner Leave was untappable** — the bottom-module window claimed a fixed 126 pt touch strip while the banner sits above it; it now reports its measured height. **Apple cert-cap permanently auto-fixed** in `testflight.yml` (no more manual revoking). **Device-verified:** QR join ✅, sync ✅, Leave ✅; **solo-playback regression check still owed**. Deferred: banner/content overlap (needs a bottom-inset sweep), anonymous followers, leader handoff. **`shareplay` SF Symbol is a SINGLE figure — no symbol matches Spotify's Jam icon; reverted to `person.2.wave.2.fill`.** **Addendum (session 73, [PR #441](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/441), squash `57095f0`, owner-verified on 1.1 (42)): the bottom banner is GONE** — `GroupBanner.swift` deleted; the tour page's group icon turns **green** while a session runs and opens the sheet (which carries Leave); glyph 17→16 pt. Retires the banner/content-overlap item. `BottomModuleRoot`'s measured-height fix stays — it's the general fix for anything above the mini-player. Trade-off: no app-wide active-session indicator any more. | 2026-07-25 |
| `HANDOFF-260725-2.md` | Historical (superseded by `-260725-3`). Session 71, 2026-07-25 (backend, no app code): **paid tours Phase 2 — the money backend is written, awaiting 3 owner dashboard steps.** New `backend/paid_tours.sql` (idempotent, one paste): `tours.price_tier` (NULL = free, CHECK-constrained to the 10 Phase-1 ASC tiers), **`purchases`** (source of truth for entitlements AND earnings; `apple_transaction_id` UNIQUE → replay-safe), `payouts`, `maker_payout_accounts`, `maker_net_cents()` (68% of sticker), `maker_sales` + `maker_earnings` views, and `get_catalog()` rebuilt to emit `priceTier`. New Edge Functions **`record-purchase`** (Verify JWT **ON**) and **`appstore-notifications`** (Verify JWT **OFF**). **Three deliberate deviations from the design doc:** the Stripe id lives in `maker_payout_accounts`, not `makers.stripe_account_id` (makers is public-read → a column there would leak it to every client), and that table is read-only to clients; neither function trusts its caller — each takes only a transaction id from the JWS then re-fetches the authoritative record from **Apple's App Store Server API** under our signed ES256 key, so forged POSTs can't mint entitlements or fake refunds (needs an ASC In-App Purchase `.p8` key); and makers never see buyer identities. **RLS: no client writes to `purchases`, and only the buyer (+admin) can read it.** **⚠️ Includes a hardening pass ([PR #433](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/433)) after an adversarial review caught a critical hole in #431 — `record-purchase` never compared the tier bought against the tour's price, so a genuine $0.99 receipt re-sent with any `tourId` would unlock a $19.99 tour and credit that maker 99¢. Apple can't catch that (tier products are reusable); the tier↔tour check is load-bearing — don't remove it.** Same pass: verify the buyer via GoTrue instead of decoding the JWT, 503 (not "not found") on Apple outages, `paid_out_cents` from `payouts.amount_cents`, UUID-validated `tourId`, 409 on a transaction redeemed by someone else. `price_tier` is deliberately absent from `seed_from_toursjson.py` (price is DB-side, not in Tours.json) — don't add it. SQL not executed (no Postgres in-session) — the owner's step 1 is its first run. **Owner steps + runbook: `backend/README.md` § "Paid tours".** NEXT: Phase 3 buyer UI (StoreKit 2). | 2026-07-25 |
| `HANDOFF-260725.md` | Historical (superseded by `-260726`). Session 70, 2026-07-25 (CI/docs): **build notes now land in TestFlight's "What to Test" field — owner-confirmed working.** Took **three PRs and three blank builds**: [#425](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/425) added the mechanism via fastlane `set_changelog`, which **cannot target a TestFlight build** (sets the App Store version's notes, rejects `build_number`) → build 36 blank; [#430](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/430) switched to **`upload_to_testflight` + `distribute_only: true`**, which then *prompted* for the platform and crashed in CI → builds 39/40 blank; [#437](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/437) added **`app_platform: "ios"`** (mandatory in CI though the docs call it optional) and it worked. **Durable lesson is the retry classifier, not fastlane** — both failures hid for 20–25 min because every error was treated as "Apple still processing" (second time via a bare-word `processing` grep matching fastlane's own option list); it now defaults to **permanent**, since a wrong permanent call costs nothing and a wrong retryable call hides the error. Also codified: **`workflow_dispatch` runs the workflow file from the branch you build FROM**, so build from `main` or merge `main` in first. A green run does **not** imply notes attached — read the `Done` line. Branch `claude/build-release-notes-f1samw` merged, needs UI deletion. | 2026-07-25 |
| `HANDOFF-260724.md` | Session 69, 2026-07-24 (infra, no code): **paid tours Phase 1 done — all 10 IAP tier products created in App Store Connect** (app 6771030927 → In-App Purchases, "Drafts (10)"). The V2 Step 6 design doc `docs/paid-tours-design.md` merged via [PR #422](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/422) (`c154807`). **Owner widened pricing from 3 to 10 tiers** — $0.99–$19.99 low-end-dense; product IDs `tour.tier.<price×100>` (`tour.tier.099`…`tour.tier.1999`, per-product Apple IDs in the handoff). Each: Non-Consumable, US base price + Apple auto-pricing 175 regions, en-US localization "Premium Audio Tour" / "Unlocks this audio tour". **Deliberately left in "Prepare for Submission"** (sandbox works; review screenshot + "Add for Review" pair with an app version at go-live). Claude drove the owner's Chrome through ASC (first time; `form_input` fails on ASC textboxes — click+type). **NEXT: Phase 2 backend** (purchases table/RLS, price_tier, Apple-JWS Edge Function, ASSN refunds endpoint, get_catalog tier). | 2026-07-24 |
| `HANDOFF-260722.md` | Historical (superseded by `-260724`). Session 68, 2026-07-22 (web/PM content): **Ho Chi Minh City launched — 43 tours + 16th maker Atlas Studio SGN** ([PR #419](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/419), `1ad9e38`, squash, owner-authorized merge on green CI). Vietnam's first city; new maker Atlas Studio SGN (`40a6f268-…`, 🇻🇳); 43 single-stop geofenced (30 m) tours; bilingual `English | Tiếng Việt` where a Vietnamese name exists. **Catalog 828 → 871 tours / 15 → 16 makers / 1033 → 1076 stops.** Delivered as a ~123 MB Dropbox folder (`dl=1`), 43 `output <Name> <lat>,<long>` folders with coords in the names (`#UXXXX`-escaped); parsed + durations via `mutagen`. Assets-first to gh-pages via a **blobless `--no-checkout` worktree** (178 new blobs only), all URLs 200. Deterministic uuid5 ids (`atlas-{maker,tour,stop}:sgn`). **`[beat]` stripped from every transcript** (validator hard-errors on bracketed stage directions); a **Python port of `validate-tours.swift`** stood in for the missing `swift` (0 errors). CI green (validator + sim build + unit tests); confirmed live on Supabase `get_catalog` + gh-pages mirror (assert on tour count — RPC over-reports makers via upsert). Outliers kept as supplied: Củ Chi Tunnels (~75 km NW) + Bửu Long Pagoda (far east). **Owed:** delete merged branch `claude/tours-upload-audio-photos-3pcty4` in GitHub UI (proxy blocks it). | 2026-07-22 |
| `HANDOFF-260721.md` | Historical (superseded by `-260722`). Session 66, 2026-07-21 (code, web): **maker tour list + grid now distinguish multi-stop walks from single stops** — [PR #413](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/413) (`ac6e421`, TestFlight 1.1 (29)) + [PR #414](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/414) (`ed4daa9`, TestFlight 1.1 (30)), both owner-authorized merges on green CI. Owner: the maker page didn't tell walks apart from single stops and any fitting icon was illegible (two same-named "The Jordaan" tours — a 2m single stop, a 12m multi-stop walk — looked identical). Chose words over glyphs via an HTML mock. **List:** `MakerView.tourRow` gains a brass `WALK` pill (new `walkPill`, reuses the `statusBadge` shape in `AtlasColors.accent` `#8B7535`) + a stop count leading the subtitle (`6 stops · 12m 39s · 1.2 mi away`); fixed-size pill so distance truncates first; single stops unchanged. **Grid:** the same pill as a top-leading tile badge (clear of the bottom-leading Draft/In-review badge, soft shadow over photos). One file (`Features/Maker/MakerView.swift`), no data-model/backend change (reads `tour.kind`/`stops.count`). `testflight.yml` built 1.1 (29)/(30) clean (no cert snag). **Owed:** device-eyeball grid-pill legibility over real photos (light + dark); delete merged branch `claude/tour-list-stop-distinction-kyk2xr` in GitHub UI (proxy blocks it) — branch was restarted off `main` after #413 for the #414 follow-up per the merged-branch rule. | 2026-07-21 |
| `HANDOFF-260720-3.md` | Historical (superseded by `-260721`). Session 64, 2026-07-20 (code, web): **Follow-request UX polish batch → TestFlight 1.1 (25)** ([PR #409](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/409) → `main`, squash `f0e77d1`; CI green, owner device-reviewed, authorized merge). Five owner-driven tweaks in the batch-D social layer, no backend change: (1) Follow Requests title → caption font (principal-toolbar `Text`); (2) Approve/Decline → green ✓ / red ✗ circular icon buttons; (3) **gold notification dot on the ME tab** when the user has pending follow requests — `FollowService.ownPendingRequests` (observed, cache-seeded) drives `AtlasTabBar.badgedTabs` via `BottomModuleRoot` (needed injecting `FollowService` + `MakerProfileService` into the bottom-module window); (4) **gold heart badge** over the own-profile followers count (replaces the "N follow requests" line); (5) **requests page merged into the Followers list** — a "PENDING REQUESTS" section pinned atop `FollowListView` (`showsPendingRequests`), approving moves the person into followers in place + refreshes header counts + Me-tab dot; **deleted `FollowRequestsView.swift`** (fs-synchronized target → no pbxproj edit). Login-gated → owner device-reviewed on 1.1 (25) with a private test account. **Owed:** delete merged branch `claude/caption-font-checkmark-styling-5x5w6g` in GitHub UI (proxy blocks it). | 2026-07-20 |
| `HANDOFF-260720-2.md` | Historical (superseded by `-260720-3`). Session 63, 2026-07-20 (code, web): **Library-tab jitter fixed → TestFlight 1.1 (21)** ([PR #404](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/404) → `main`, squash `98f2993`; +307/−32; owner device-confirmed fixed, authorized merge). Owner: "clicking home→library has a slight jitter when it loads or re-formats." Cause: the Library tab is switch-swapped (rebuilt each entry), so `followingMakers` `@State` reset to `[]` and the **Saved** tab drew tours-only, then re-formatted a `list_following` round-trip later (`TOURS` header popped in at top + `FOLLOWING` section below). Fix = the app's stale-while-revalidate pattern applied to the follow **list**: `FollowService` caches `following(of:)` in an observed, per-viewer map backed by a new **`Data/FollowingListStore.swift`** (per-account `UserDefaults` blob of `[Maker]`), hydrated **synchronously at init**; `LibraryView` reads `followingMakers` from the cache **synchronously in `body`** (+ `.task` refresh). Every Library open — warm re-entry or cold relaunch (after the first seed) — now renders the final layout on the first frame. Adds `FollowingListStoreTests`; behavior-preserving. CI green (sim build + unit tests + validator). **Build-cut gotcha codified:** two `testflight.yml` runs failed fast (~50s) at Archive with *"account has reached the maximum number of certificates"* — **Apple Development** cert cap (not code; CI was green); owner revoked the Apple **Development**-type certs (Distribution didn't help; ~5-min propagation) → rebuild signed clean. Recurs per cloud build (each mints a dev cert); durable fix = distribution-only archive signing (offered, deferred). **Owed:** delete merged branch `claude/library-tab-jitter-wha8o3` in GitHub UI (proxy blocks it, 403). | 2026-07-20 |
| `HANDOFF-260720.md` | Historical (superseded by `-260720-2`). Session 62, 2026-07-20 (code, web): **drawer rails re-anchored to the user's location → TestFlight 1.1 (16)** ([PR #405](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/405) → `main`, squash `18b96ea`, owner-authorized merge on green CI). Owner: "the primary sort for the rails in the drawer should still be based on distance to the user's location, especially if the user's location is within view." Only `HomeRailsViewModel.viewerLocation` changed — the anchor feeding `sortedByDistance` (curated shelves) + `filteredResults`. Now returns the **user's own location** whenever known and (no settled region yet **or** region contains the user, via `MKCoordinateRegion.contains`); falls back to the map viewport center only when panned away to a region that no longer shows the user (§1.5). The `Near you`↔`In view` rail *swap* (500m `isPannedFar`) is untouched — ordering *within* shelves only. +2 `HomeRailsViewModelTests` (user-in-view → user anchor; user-off-screen → viewport-center); CI #961 green. Build notes attached to 1.1 (16). **Owed:** device check (drawer over your location → nearest-to-you tours lead each shelf); delete merged branch `claude/rail-drawer-distance-sort-rx65l1` in GitHub UI (proxy blocks it, 403). | 2026-07-20 |
| `HANDOFF-260719-3.md` | Historical (superseded by `-260720`). Session 61, 2026-07-19 (code + docs, web): **maker bookmark removed — Follow is the single "keep a creator" concept** ([PR #398](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/398) → `main`, squash `8890cc7`; +57/−236). Owner: "just have one, follow not bookmark." `MakerView` `…` menu is now **Share · Follow · Report** (bookmark button + "Save" item gone); Library → Saved's maker list is now a live **"Following"** list via `FollowService.following(of: myMaker.id)` (owner chose this over tours-only); deleted `SavedMakersStore`/`SavedMakerEntry` + all `user_saved_makers` sync in `SyncService` + its unit test; App/ContentView no longer inject the store. **Tour bookmarking (`LibraryStore`) unchanged.** Built + owner-reviewed as **TestFlight 1.1 (9)** via `testflight.yml` `workflow_dispatch` on the branch (first web-session build cut purely for review); PR CI green incl. unit tests. **Owed:** delete merged branch in GitHub UI (proxy blocks it); `user_saved_makers` table unused-but-not-dropped. | 2026-07-19 |
| `HANDOFF-260719-2.md` | Historical (superseded by `-260719-3`). Session 60, 2026-07-19 (code + docs, web): **(1) home map/animation performance fix → TestFlight 1.1 (10)** ([PR #397](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/397) → `main`, squash `51c628a`, owner device-verified smoother). The home map + drawer did catalog-scaled work every pan/settle; fixed behavior-preservingly (pin shadow kept): `HomeMapSection.cluster` viewport-culls markers to the visible region + 1 viewport of margin before bucketing (was diffing an annotation for all ~969 stops); `HomeRailsViewModel` memoizes distances (decorate-sort, ~100k `CLLocation` allocs/settle → n); `HomeDrawerContent` computes the in-view count once. Scales with what's on screen now, not the catalog. **(2) catalog docs resynced** 509→**790** tours / 9→**14** makers / 561→**969** stops (undocumented bureaus: Bangkok, Paris, Seoul, LA, Naoshima; 760 single + 30 multi-stop; bilingual now `日本語`/`中文`/`한국어`/`ไทย`). Branch-delete owed via GitHub UI (proxy blocks it). | 2026-07-19 |
| `HANDOFF-260719.md` | Historical (superseded by `-260719-2`). Session 59, 2026-07-19 (code/infra, web session): **(1) on-demand signed-TestFlight CI** (`.github/workflows/testflight.yml`, PR #393) — a web/Linux session can now build device-testable TestFlight builds with no Mac (PR label `build` or Actions → Run workflow; runbook `docs/testflight-ci.md`). Owner set 3 Actions Secrets + a `build` label; cloud signing needs the API key at **Admin** role; build numbers use `github.run_number`, `MARKETING_VERSION` bumped to **1.1** (PR #394). **(2) Journeys — Phase 1 shipped** ([PR #395](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/395) → `main`, squash `9fe6149`; TestFlight **1.1 (7)**, owner device-verified): user-curated ordered collections of whole tours (never split). New `Models/Journey.swift`, `Data/JourneyService.swift` (Supabase CRUD), `Features/Journeys/` (list/detail/add-to-journey); "Journeys" row on own-profile + "Add to a Journey" in tour menu; `JourneyService` injected app-wide + into both UIKit slide-up layers. `backend/journeys.sql` applied to Supabase by owner (app build is independent of it). First feature built + shipped entirely from a web session via the new CI. **(3) Group Listen — Phase 1 shipped** ([PR #396](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/396) → `main`, squash `2ba2f58`; TestFlight **1.1 (8)**): synced group listening, leader-driven, free "Listen Together" over **MultipeerConnectivity** (offline). `Features/GroupListen/` (state + transport seam + `MultipeerTransport` + `GroupListenCoordinator` + sheet + banner); "Listen together" in the tour ⋯ menu; `Info.plist` local-network + Bonjour keys. Additive; **device-only sync (2+ phones) owner-owned, not yet verified**. Second feature shipped from a web session today. **⚡ Capability:** the "features need a local Mac session" rule is retired — web sessions build + ship via `testflight.yml` (proven twice today). **Backlogs:** Journeys polish (`docs/journeys-design.md` §14); Group Listen remaining = Hosted mode + Pro Guide + leader-handoff/QR (`docs/group-listen-design.md`). | 2026-07-19 |
| `HANDOFF-260705-5.md` | Historical (superseded by `-260719`). Session 58, 2026-07-05 (code): **Me-tab first-open lag fully killed → TestFlight 1.0 (74).** Build 73 cached only the follow counts, so the Me tab still flashed a placeholder profile + empty tour feed on the first tap after a cold launch (owner: "i dont EVER want to see the lag"). [PR #365](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/365) extends stale-while-revalidate to the **whole profile**: new `Data/ProfileSnapshotStore.swift` (generic per-signed-in-user `Codable` snapshot in `UserDefaults`); `MakerProfileService.myMaker` (name/bio/avatar/links) + `MakerTourService.myTours` **hydrate from it synchronously at init** (uid available because `AuthService` restores the session synchronously) and write through on load/save; `MakerTour` made `Codable`; the App pre-warms both at launch so the refresh is in flight before Me opens; `hydrateIfUserChanged()` swaps accounts without clobbering fresher data. First Me paint now shows the real profile + tours instantly (avatar frame-zero too, since `myMaker`/`avatarURL` is known). `test_sim` **193/193** (+8 `ProfileSnapshotStoreTests`). Bump **73 → 74** ([PR #367](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/367)); archived clean off `origin/main`, binary-verified (`1.0 (74)` / `Dozent` / no `TEMP_LOCAL_DEMO`); owner uploading via Organizer. **Device-verify owed:** build 74 (cold-launch → tap Me → instant, no flash) + build 73's #357 launch race / #358 counts+avatar. | 2026-07-05 |
| `HANDOFF-260705-4.md` | Historical (superseded by `-5`). Session 58, 2026-07-05 (code): **Build 73 cut — profile perf + create-tour tag picker + bottom-module fix.** Built the Me-tab perf/polish fix ([PR #358](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/358)) — stale-while-revalidate: `Data/FollowStateStore.swift` caches follow counts (in-memory + a bounded, **per-signed-in-user** `UserDefaults` blob; returns last-known on a failed refresh, never clobbers to 0) so the Me tab shows counts instantly; `ImageCache` gained an **avatar disk layer** (SHA-256 files in `Caches/AvatarCache`, memory-first so #326 is preserved) so the profile photo renders frame-zero across relaunches (no monogram flash). Then, on owner direction, **merged #358 + [#359](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/359) (create-tour tag picker) + [#357](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/357) (bottom-module fix)** and cut **TestFlight 1.0 (73)** (bump #361, normal squash on green CI; archived clean off `origin/main`, binary-verified `1.0 (73)` / `Dozent` / no `TEMP_LOCAL_DEMO`; owner uploading via Organizer). **Device-verify owed on 73:** #358 counts+avatar (login-gated) and #357 launch race (not sim-reproducible). Kyoto batch 2 (#360, +41) rode along as content — catalog **9 makers / 550 tours / 602 stops**. | 2026-07-05 |
| `HANDOFF-260705-3.md` | Historical (superseded by `-4`; **#357 merged into build 73**). Session 58, 2026-07-05 (code): **Bottom-module install race fixed — mini-player/tab bar could be missing on launch** ([PR #357](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/357), CI + `test_sim` green, **NOT merged — awaiting owner device verification**). Root cause: the mini-player + tab bar live in a **secondary `UIWindow`** (`BottomModuleWindowController.install()`) that required a `.foregroundActive` scene, returned silently if none, and was called **only once** from `.onAppear` — so on cold launches where `.onAppear` beat the scene to active, install bailed with no retry and the module was absent all session. Fix: also call install on **`scenePhase == .active`** (factored into `installBottomModule()` so both sites inject identical environments) + harden `install()` with a **one-shot `UIScene.didActivateNotification` retry** (iPad-multi-scene-correct); same `window == nil` guard → created exactly once; deferred install re-applies the cached color scheme. New `installOutcome(...)` pure decision + `BottomModuleWindowTests`. **`test_sim` 162/162** (159+3); normal launch still renders the module (sim-verified). NEXT: **owner launches the next build repeatedly (cold+warm) to confirm, then merge #357** — no build cut yet. | 2026-07-05 |
| `HANDOFF-260705-2.md` | Session 57, 2026-07-05 (code): **Tag Phase 2 (browsable tag UI) + Dozent identity → TestFlight 1.0 (72).** Phase 2 ([PR #352](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/352)) turns the Phase-1 tag vocabulary into browsable Home UI: `Models/Tag.swift` (curated shelves + filter chips + D6 matcher + D5 primary — the single editorial edit point); curated shelves replace category shelves; `TagFilterChipRow` multi-select filter chips + a "Walks" format chip; filtered results = a flat feed of **full-width 4:3 cards** (195pt = rail height, owner-reviewed), Continue-listening filter-scoped; location rails anchor to map-view center with a near/far swap (§1.5); `validate-tours.swift` enforces the vocab (15 backfill warnings). `test_sim` 159/159. Shipped alongside **#353** (brass app icon + rename to **Dozent**). Build 72 (bump #354) archived + binary-verified (`1.0 (72)`, `CFBundleDisplayName=Dozent`); owner uploading. NEXT: 15 tag-backfill tours (over-the-air); Tag Phase 3 (drop `primaryCategory`) is later. | 2026-07-05 |
| `HANDOFF-260705.md` | Session 56, 2026-07-05 (code): **Directed polish pass → TestFlight 1.0 (71).** Owner pivoted from features to polish after an honest app assessment. **Brand identity decided: dark gold (brass) `#8B7535`** — the color the app already wore, confirmed as THE accent, same value light+dark (no dark variant); terracotta `#B85042` fully removed; `AtlasColors.mapPin` now = `accent` ([PR #344](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/344)). Same PR: **haptics** (`AtlasHaptics` — save/follow/download + the geofence "you've arrived" bump; device-only) + **error toasts** (`AtlasToast` — a `ToastCenter`/`ToastHost` in the bottom-module window, above every modal; wired to follow + approve/decline failures). Also merged the owner's **home-lag fix** ([PR #347](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/347)): `HomeView` stays mounted across tab switches so the `MKMapView` isn't rebuilt on return. Dead `TourListCard.swift` deleted. `test_sim` 140/140; build 70→71 (#351) archived + binary-verified — **live on TestFlight 2026-07-05 (owner-confirmed).** NEXT: upload-progress, empty-states sweep, gold-led app icon (still placeholder). Memory: `project-brand-color-brass-gold`. | 2026-07-05 |
| `HANDOFF-260704-4.md` | Historical (restored 2026-07-26 — written in a parallel session and never merged; the CLAUDE.md/ROADMAP half of that sync landed, the handoff itself did not). 2026-07-04 (code): **perf — Home + its map kept alive across tab switches, fixing the return-to-Home lag the owner reported on device** ([PR #347](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/347), squash `b3441c6`, owner-reviewed on device → merged). `ContentView.tabContent` was a `switch` that unmounted `HomeView` on tab-away → returning rebuilt the SwiftUI `Map` (reload tiles + re-cluster ~509 annotations) and reset the `@State` camera to the NYC default. Profiling (temp timing, reverted) proved the Swift recompute negligible (`filteredTours` ≈0.02 ms, cluster ≈1–3 ms) — the cost is the map's UIKit reconstruction. Fix: `tabContent` → a `ZStack` keeping `HomeView` permanently mounted (visibility toggled via `.opacity`/`.allowsHitTesting`/`.accessibilityHidden`), Library/Me overlaid opaque; off-screen Home made inert via `allowsHitTesting(false)` + a new `isActive` guard on the camera callbacks. The map is SwiftUI's **native `Map`, not a `UIViewRepresentable MKMapView`** (correction to the task's premise). `test_sim` **140/140**; CI green; overlay composition sim-verified, tab-switch timing owner-device-verified (the modal-window tab bar isn't automatable). Tradeoffs: one persistent map (small memory cost); an open Search now persists across a tab-away. | 2026-07-04 |
| `HANDOFF-260704-3.md` | Session 55, 2026-07-04 (code): **Batch D COMPLETE — D2 follow-lists + D3 requests shipped in TestFlight 1.0 (70)** ([PR #334](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/334), one owner-authorized merge; no new backend). **D2:** follower/following counts on any maker page tap through to `FollowListView` (shared `MakerAvatarView` rows) via `list_followers`/`list_following`. **D3:** a private account's `pending` follows surface as a "N follow requests" link → `FollowRequestsView` with Approve (`status→accepted`) / Decline (delete), gated by the existing RLS. `MakerRow.userId` made optional (seed studios have null `user_id`). `test_sim` 140/140; count-tap nav code-verified, populated lists + approve/decline owner-device-verified. Build 70 (bump #338) archived + binary-verified; owner uploading. **Batch D done** (D1→D2→D3). NEXT: a "creators you follow" home feed (later add-on, no schema change) or Step 6 payments. | 2026-07-04 |
| `HANDOFF-260704-2.md` | Session 54, 2026-07-04 (code): **Batch D (social layer) begun — designed + D1 shipped in TestFlight 1.0 (68).** Owner chose the follow model (asymmetric, Instagram-style), new accounts default public, v1 = counts + lists + approve/decline (feed later). Backend (owner-applied, verified live): `makers.is_private` + a `follows` table + trigger + RLS + SECURITY-DEFINER RPCs (`follow_state`/`list_followers`/`list_following`/`list_follow_requests`); `get_catalog` returns `isPrivate`. **D1 ([PR #329](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/329)):** `Maker.isPrivate`; `Data/FollowService` (follow_state RPC + follow/unfollow); MakerView header follower/following counts + Follow/Following/Requested button; a Private-account toggle in the editor. `test_sim` 140/140; counts + toggle sim-verified, Follow button + writes device-verified. Build 68 (bump #330) archived + verified; owner uploading. Design in `docs/social-design.md` + `backend/social.sql`. NEXT: D2 (Followers/Following lists), D3 (private requests). | 2026-07-04 |
| `HANDOFF-260704.md` | Session 53, 2026-07-04 (code): **Device-testing polish batch (8 fixes) → TestFlight 1.0 (67) + a live moderation-email upgrade** ([PR #326](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/326)). From the owner running the authoring loop on build 66: (1) public maker page reflects a profile edit immediately; (2) **Play recording** to review a take; (3) no avatar flash in Library (shared `ImageCache`); (4) tighter profile editor + countdowns; (5) tighter New Tour form + countdowns; (6) Settings theme **D→L→D** fix; (7) signed-out redesign (icon 72→20pt, JOIN DOZENT mono, 44pt Sign-in — the app's universal control diameter); (8) shared self-hiding `JoinDozentPrompt` below all 3 Library empty states. **Server-side:** `notify-moderation` rewritten to a rich review email (title/maker name/description/transcript/▶ Listen/photos) with **one-click Approve & publish / Take down** — owner-deployed (Verify JWT off + `MODERATION_TOKEN`). `test_sim` 140/140. Build 67 (bump #327) archived + verified; owner uploading. NEXT: batch D — social layer (design-first). | 2026-07-04 |
| `HANDOFF-260703-4.md` | Session 53, 2026-07-03 (code): **Profile/maker polish + audio-write bug fix → TestFlight 1.0 (66)** ([PR #324](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/324)). Owner device-testing found five items: **(1)** audio/transcript writes failing with `"failed to parse order (eq.0)"` — the `stops.order` column collides with PostgREST's reserved sort param (`.eq("order",0)`→`?order=eq.0`→400); fixed by filtering single-stop drafts by `tour_id` alone (memory `reference-postgrest-order-column-collision`); **had shipped broken in 62/63** (login-only path). **(2)** editor Save ran under the bottom module → added the missing `AtlasBottomModule.height()` inset (also New Tour). **(3)** avatar **pinch-zoom+drag crop** (`AvatarCropSheet`/`ImageRenderer`). **(4)** discard-changes prompt (+`interactiveDismissDisabled`). **(5)** New Tour "Save draft & continue" + "Step 1 of 2" caption, and save now pushes the editor (step 2). `test_sim` 140/140; writes device-verified. Build 66 (bump #325, admin-merged) archived + verified; owner uploading. NEXT: batch D — social layer (design-first). | 2026-07-03 |
| `HANDOFF-260703-3.md` | Session 53, 2026-07-03 (code): **Profile/maker polish batch C → TestFlight 1.0 (65)** ([PR #322](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/322)). Two features on one owner-run Supabase migration (`link_2_url`/`link_3_url`/`avatar_initials`/`avatar_color` + `get_catalog` rebuild; RPC verified). **C1 — up to 3 links:** `Maker.link2URL`/`link3URL` + computed `links`; editor "LINKS (up to 3)"; maker page renders up to 3 inline blue links; `MakerRow` custom encoder writes cleared links as explicit null. **C2 — custom profile icon:** new shared `Components/MakerAvatarView` (photo → emoji → custom initials+colour → display-name monogram) reused across header/mini-player/Library/Search/lock-screen — closes the "no saved-list icon" report; editor avatar section (live preview + PhotosPicker square-crop upload to `tour-images/{maker_id}/avatar-*.jpg`, or initials + 10-colour grid). Seed studios have emoji → pages unchanged. `test_sim` 140/140; monogram + editor sim-verified. Build 65 (bump #323, admin-merged) archived + binary-verified; owner uploading. NEXT: batch D — social layer (follow/followers, public/private, friend requests; design-first, new `follows` table). | 2026-07-03 |
| `HANDOFF-260703-2.md` | Session 53, 2026-07-03 (code): **Profile/maker polish batches A + B → TestFlight 1.0 (64).** Owner pivoted from pricing to polishing the profile/maker experience; 11 notes grouped into 4 batches (A visual · B delete/recover/avatar · C links+custom-icon · D social). **Batch A ([PR #319](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/319)):** inline blue website `Link` under bio, display name keeps user casing + 40-char limit, public empty state = single grey box (own keeps "+"), Library Saved = tours-first. **Batch B ([PR #320](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/320)):** delete a tour (`MakerTourService.deleteTour` + destructive button/confirm in `TourAuthoringView`), Forgot-password (`AuthService.resetPassword` + sign-in link), saved-list avatar honors `avatarURL`. `test_sim` **140/140**. Build 64 (bump #321) archived clean + binary-verified; owner uploading. NEXT: batch C (3 links + custom profile icon — small `makers` schema add) · batch D (social layer — `follows` table, design-first). | 2026-07-03 |
| `HANDOFF-260703.md` | Session 51 cont'd, 2026-07-03 (code): **V2 Step 4 maker-authoring Phase-1 loop COMPLETE — LIVE in TestFlight 1.0 (63).** A signed-in user can now do the whole thing, all to Supabase: profile → create draft (metadata + map pin/radius) → editor → **record** (`AVAudioRecorder`, `NSMicrophoneUsageDescription`) **or import** audio → `tour-audio` → **photos** (PhotosPicker → crop 1200×900 → `tour-images`) → **transcript** → **Submit for review** (status→`in_review`). Increments: **#307** (create-a-tour draft form + My-Tours feed w/ status badges; `MakerTourService`, `TourStatus`, `ensureMaker`) → **#310** (full `TourAuthoringView` editor: `attachAudio`/`attachPhotos`/transcript/submit; `AudioRecordSheet`). Builds **62** then **63**, both owner-confirmed live; archived clean w/ `-allowProvisioningUpdates` (mic key present, no poison). `test_sim` **140/140**. **Every write is owner-device-verified** (sim holds no session). **Remaining (both small):** submit-EMAIL owner webhook (`public.tours` UPDATE → `notify-moderation`, reports-twin) + admin **Publish** (`publish_tour`). Phase 2 multi-stop needs no backend change. Gotchas: direct table selects snake_case (CodingKeys) vs camelCase `get_catalog`; recording works in-sim via host mic; once committed to local `main` by mistake → recovered via branch+reset+rebase. Catalog grew parallel: Toronto #306 + Kyoto #309 → ~509/9. | 2026-07-03 |
| `HANDOFF-260702-2.md` | Session 52, 2026-07-02 (web/PM — content): **Kyoto launched — 30 tours + 9th maker Atlas Studio KYO ([PR #309](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/309), `af02819`, auto-merged).** New maker Atlas Studio KYO (`50b53af5-…`, 🇯🇵) + 30 single-stop geofenced tours (30 m), bilingual `English \| 日本語`, owner-supplied audio + images (no pipeline), deterministic uuid5 (`atlas-*:kyo`, same scheme as Tokyo). **Catalog 479 → 509 tours / 8 → 9 makers / 561 stops; Kyoto = 30.** Validator PASS. Assets-first to gh-pages (30 mp3 + 111 webp, lowercase-hyphen slugs). **Confirmed live** on Supabase RPC (~1 min, 509/Kyoto 30) + gh-pages mirror (~7 min CDN lag, 9/509/30). **🚩 Flag:** La Collina Ōmi-Hachiman (`35.16193, 136.08880`) is in **Ōmi-Hachiman, Shiga Prefecture** (~40 km, different prefecture) — `city`=Ōmi-Hachiman, owner to confirm; other out-of-city tours set to locality (Byōdo-in→Uji, d-matcha→Wazuka, Asahi villa→Ōyamazaki, Kibune→Kibune). Supplied JP for 7 folders (brief said 6 — HARIO CAFE also lacked it). **Kyoto is the 9th city, not the 8th** — Toronto (#306, YYZ 🇨🇦, +10) landed as the 8th first; Kyoto rebased additively onto the moved main. New memory: `reference-live-poll-maker-count` (Supabase RPC reports more makers than the file — assert on tour counts). Also backfilled the Toronto content block + drifted CLAUDE.md counts (were 469/7). | 2026-07-02 |
| `HANDOFF-260702.md` | Session 51, 2026-07-02 (code): **V2 Step 4 (maker authoring) BEGUN — the profile-as-maker-page architecture + first Supabase content writes.** Owner's key idea: the **Me tab is a Profile = a maker page** (one `MakerView`, modes; "each maker is a user too"). Three increments merged/near: **#300** Me tab → `ProfileView` (`MakerViewMode` `.publicMaker`/`.ownProfile`; Settings behind a gear; "+" add affordance); **#302** public creator page is now **its own standalone screen** via `MakerPresenter` (bottom-layer slide-up + X, mode `.publicStandalone`; deep-link/Search/Library route through it; **replaced #297's `.sheet` placeholder**; "Go to creator" stays an in-stack push); **#304 (OPEN, owner review)** create/edit creator profile → real `makers` row (**first client→Supabase content write**; `MakerProfileService` + `ProfileEditorView` + "Edit Profile" pill). Owner decision: **one login = one profile**, starts empty; seed studios stay separate. `test_sim` **140/140**. Backend needed no setup (RLS/grants already applied). Also merged parallel: **#297** (Universal Link deep-linking, share) + **#301** (report emails include tour title). **TestFlight = 1.0 (61)** (`main` build 61, rode in with #297); Step-4 work may not be in the live 61 — **cut a fresh build after #304** to get maker authoring on device. **Next: increment 2b** — create-a-tour form (metadata + map pin/radius → `draft`), then audio/photos/transcript/submit. New memory: `reference-two-repo-clones-build-target`. | 2026-07-02 |
| `HANDOFF-260701-2.md` | Session 50, 2026-07-01 (infra, no code): **report-a-concern EMAIL notifications now LIVE — owner-confirmed end-to-end.** Closed the last open checkbox on the shipped "Report a concern" feature (the in-app insert already worked; only the server email was unset). Pure Supabase-dashboard + Resend hand-holding — the `notify-moderation` Edge Function code already existed in the repo. Set up on project "Dozent" (`apkcihljybvuyuzpbnqd`): Resend free account (`edward.yung@gmail.com`) → API key; Edge Function `notify-moderation` deployed (Verify-JWT ON); three secrets (`RESEND_API_KEY`, `MODERATION_EMAIL=edward.yung@gmail.com`, `FROM_EMAIL=Atlas <onboarding@resend.dev>`); Database Webhook `report-notify` on `public.reports` INSERT → the function (Webhooks moved out of the Database sub-menu → use `…/database/webhooks` URL). **Verified:** curl insert (anon key, `Prefer: return=minimal`) → HTTP 201 → owner confirmed the "Atlas: tour reported — …" email arrived. Caveats: Resend free tier + `onboarding@resend.dev` only emails the Resend account owner's own address (fine for the recipient; custom FROM domain = later upgrade); `MODERATION_EMAIL` is a temp recipient (swap the secret to change); recipient + key are server-side only. **Deferred:** optional `tours` UPDATE→in_review webhook (maker moderation) until Step 4. **Owner cleanup owed (SQL Editor):** delete the test report row + old throwaway `auth.users` test users. | 2026-07-01 |
| `HANDOFF-260701.md` | Session 49, 2026-07-01 (code): **fixed the owner's real-device sync bug — un-saving a tour then signing out/in no longer resurrects it in Saved — and archived TestFlight 1.0 (58).** Two PRs: **#291** (pre-sign-out flush; shipped in 57, *necessary but not sufficient*) added `SyncService.flushPendingWrites()` awaited via a new `AuthService.preSignOut` hook *before* `client.auth.signOut()` (uid still valid; `[weak self]` avoids a retain cycle). Owner retested 57 → still resurrected, because that flush's push was itself a no-op. **#294** (the real fix, build 58): `LibraryStore.toggleSaved` un-saves by nil-ing `savedAt` but **keeps** the entry, so the row is *upserted, not deleted* — and Swift's **synthesized `Encodable` omits nil optionals**, so `saved_at` was absent from the upsert body and PostgREST's `ON CONFLICT DO UPDATE` left the old (saved) value in place. Fix: custom `UserLibraryRow.encode(to:)` emits nullable columns as **explicit JSON `null`** so the upsert clears them (only `UserLibraryRow` affected). New test `test_libraryRow_encodesNilOptionalsAsExplicitNull`; `test_sim` **121/121**. Build 58 (bump #295) archived clean (`/tmp/Atlas-20260701-0023-b58.xcarchive`, `1.0 (58)`, binary greps clean). **Pending owner Organizer upload + device retest.** Gotcha logged: memory `reference-supabase-upsert-null-omission` (upserts that must NULL a column need explicit-null encoding). Not fixed (flagged): cross-device *offline* un-saves still won't propagate (additive-union merge → needs tombstones). Did all work in a `git worktree` off `origin/main` — shared checkout was mid-flight on `claude/share-universal-links`. | 2026-07-01 |
| `HANDOFF-260630-2.md` | Session 47, 2026-06-30 (code): **V2 Step 3 (accounts & auth) COMPLETE — shipped TestFlight 1.0 (56).** Took the auth foundation through the full sign-in trio + cross-device sync. Builds: 52 **Apple** (#274, `signInWithIdToken(.apple)` + nonce + `applesignin` entitlement; device-confirmed) · 53 **Google** (#277, supabase-swift OAuth web flow via `ASWebAuthenticationSession`, no SDK; `dozent://` scheme; owner set up a GCP **Web** OAuth client + Supabase Google provider) · 54 **library + saved-makers sync** (#279, new `Data/SyncService.swift`: sign-in pull→merge(union)→push + debounced write-through as upsert/delete-not-in via PostgREST; stores gained `onChange`+`applyMerged`) · 55 **logout-clear** (#283, sign-out wipes local synced data, no remote delete; anonymous unaffected) · 56 **Recents-recording fix** (#285 — progress recording moved from the full-screen-only `PlayerView` down to `AudioPlayerService.onProgressCheckpoint` so it fires from the mini-player too; bonus: `.ended` marks completed) + **recently-viewed sync** (#287, `RecentlyViewedStore` reshaped to `{tourId, viewedAt}` with same-key legacy migration; `user_recently_viewed`). `test_sim` **120/120**; merge logic unit-tested. **Every archive needs `-allowProvisioningUpdates`** (since the Apple capability). Owner device-confirmed sign in/out + wipe/restore; **build-56 Recents fix + recently-viewed still pending owner device retest.** Gotcha logged: a stacked-PR `--delete-branch` auto-closed the child PR — recovered via `rebase --onto main`. **Addendum (same session):** shipped **"Report a concern"** (#290) + **build 1.0 (57)** (bump #292; also carries sync-flush fix #291) — ••• menu → `ReportSheet` inserts into `reports` (`returning: .minimal`; email removed from client). Two bugs fixed live: RLS-blocked insert (read-back tripped admin-only SELECT → `.minimal`; owner re-applied the missing `reports` insert grant/policy) + a crash (`AuthService` env absent in the UIKit-hosted layers → made it optional). **Next:** report-email notifications (owner Resend + `notify-moderation` deploy + webhook — owner doing Resend next); optional "Sign in to sync" nudge; then **Step 4 maker authoring UI**. Cleanup: throwaway `auth.users` test rows. | 2026-06-30 |
| `HANDOFF-260630.md` | Session 48, 2026-06-30 (web/PM — content): **Tokyo launched — 63 tours + 7th maker Atlas Studio TYO ([PR #280](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/280), `4ab886a`, auto-merged).** New maker Atlas Studio TYO (`be5797bb-…`, 🇯🇵) + 63 single-stop geofenced tours (30 m), bilingual `English \| 日本語`, owner-supplied audio + images (no pipeline). **Catalog 406 → 469 tours / 6 → 7 makers / 521 stops; Tokyo = 63.** Validator PASS. Assets-first to gh-pages (63 mp3 + 215 webp, lowercase-hyphen slugs; Sumida Hokusai `.jpg.webp` cleaned); idempotent assembler off `origin/main` (uuid5 ids). **Confirmed live** on both Supabase RPC (~1 min) + gh-pages mirror (~6 min CDN lag): 7/469/63, assets 200. **3 geocoded data fixes flagged:** Hōrin-ji folder coord was in Kyoto → Waseda (`35.70729, 139.71889`); Edo-Tokyo Open Air Museum (no coord) → Koganei (`35.71637, 139.51274`); Nanago-Dori toilets (no coord) → Hatagaya (`35.67902, 139.67477`). Supplied/cleaned Japanese for 9 folders + garbled Shibuya Crossing → 渋谷スクランブル交差点. *6th city was SF (SFO, 35 tours), already on main — brief's 371/5 baseline was stale.* | 2026-06-30 |
| `HANDOFF-260627.md` | Session 46, 2026-06-27 (code): **V2 Step 2 app cutover — the app now reads its catalog from the live Supabase backend ([PR #255](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/255), `fa8ec2a`).** `RemoteCatalogLoader` now fetches the `get_catalog` RPC on project "Dozent" (`apkcihljybvuyuzpbnqd.supabase.co`) **first**, with gh-pages `Tours.json` as an automatic **fallback mirror** (then on-disk cache, then bundled seed). **No SDK** (owner decision) — a plain `URLSession` `SupabaseCatalogFetcher` (apikey/Bearer headers) covers the RPC POST; `supabase-swift` deferred to Step 3 (auth). New `Data/SupabaseConfig.swift` holds the project URL + **client-safe anon/publishable key** (RLS-gated, ships in binary; service_role NOT in repo) + `isConfigured` guard. New ordered `CatalogSource`s; `refresh()` tries Supabase→gh-pages; existing callers route through a backward-compatible convenience init. +3 fallback tests; **`test_sim` 113/113**, CI green. **Live sim-verified:** debug log (removed before commit) showed `CATALOG REFRESH OK from apkcihljybvuyuzpbnqd.supabase.co — 370 tours, 5 makers`; Home rendered the full catalog. RPC curl-checked: 200, 5/370/396. **⚠️ New rule:** Supabase is now primary, so content changes must reach the DB (`seed_from_toursjson.py`), not just gh-pages. **Shipped in TestFlight 1.0 (50)** (bump 49→50 via PR #257), live 2026-06-27 — also carries the geofence fix (#251). | 2026-06-27 |
| `HANDOFF-260626-2.md` | Session 45, 2026-06-26 (code): **geofence "already-inside" fix — AMNH stop 2 now triggers at tour start ([PR #251](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/251), `2eb89c0`).** `CLLocationManager` only fires `didEnterRegion` on a boundary **crossing**, never for a region the user is already inside at monitoring start — AMNH's manual intro and geofenced stop 2 share identical coords, so stop 2 never fired. `ProximityMonitor` now `requestState(for:)` + `didDetermineState`: on `.inside`, a pure unit-tested `decideInsideStopAction` plays now if the player's idle else **holds the stop until the intro ends** (`withObservationTracking`); de-dupe via `playedStopIds`; overlapping → first stop in tour order. `PlayerView` passes `startedStopId` + cancels the pending hold on the intro→stop-0 auto-advance so the two paths never double-play. **General fix** (4/5 multi-stop tours lead with a geofenced stop-0); **tour data unchanged.** +7 tests, `test_sim` **110/110**, CI green. Sim-verified (stop 2 fires untouched; stop 3 still crosses normally). **Owner authorized the merge but hasn't done the in-sim review yet; not in a TestFlight build — needs a 49→50 bump when wanted.** UX choice (revisitable): inside stop plays *after* the intro, not mid-intro. | 2026-06-26 |
| `HANDOFF-260626.md` | Session 44, 2026-06-26 (code): **catalog refresh hardened (PR #245) + shipped in TestFlight 1.0 (49).** Resolves the build-46 stuck-tester bug — the remote `refresh()` gave up after one 15s timeout, silently, only at cold launch. Four fixes in `RemoteCatalogLoader`/`DataService`/the App entry: **retry + ~1s/2s backoff** (3 attempts; transient-only — network/timeout/5xx/408/429, not a clean 4xx; local copy never clobbered), **30s/60s timeouts** (was 15s), **refresh-on-foreground** (`scenePhase → .active`, 60s debounce + in-flight guard → reopen picks up new content, **no force-quit**), and a **`CFBundleVersion`-stamped cache** discarded across app-version changes (47→48 case). +8 tests (**103/103** via `test_sim`). **Proven live in the sim:** ★ on one gh-pages title → background+reopen (same pid) → ★ appeared ~1s → reverted **byte-exact** (sha256, 0 ★). `CatalogFetching` seam preserved. Build bumped 48 → 49 via PR #249; build 49 also carries #244 (detail Nearby Tours), #246 (maker sort/view persist), #247 (home polish), #248 (search polish). Content unchanged (~370 tours / 5 makers, ships live via the remote catalog). | 2026-06-26 |
| `HANDOFF-260623.md` | Session 42, 2026-06-23 (web/PM — docs sync): refreshed `CLAUDE.md` + `ROADMAP.md` Current State to reality after the docs drifted to ~307/4 while the catalog grew. **Verified live: 362 tours / 5 makers / 381 stops / 4 multi-stop** — NYC 100 · London 97 · Lisbon 66 · Porto 54 · **Hong Kong 45** (5th maker, newest city, built 0→45 over a few days, **all 45 bilingual `English \| 中文`**). 4 multi-stop walks: AMNH Four Facades + Fifth Avenue Walk (NYC), After the Fire: Wren's City + Albertopolis (London); 3 more London walks drafted on `claude/london-batch3-scripts-260616`. **Remote-catalog era re-stated:** since build 46 content ships with no app build (PR #209 fetch + PR #212 auto-publish → live ~5 min after merge + relaunch); **TestFlight 1.0 (46) current, last content-driven build.** In flight: Paris (6th city, `claude/paris-scripts-260622`) + V2 creator-platform design branches. Docs only — no app/content/build change. Carried TODO: 2 dead gallery images (The Oculus, The Charging Bull). | 2026-06-23 |
| `HANDOFF-260621.md` | Session 41, 2026-06-21 (web/PM — V2 backend design): designed + merged the near-term V2 maker-platform backbone as docs + non-shipping SQL / one Edge Function (**nothing ships in the app yet**). **#218** catalog foundation (Supabase schema + `get_catalog()` RPC + seed, 5/307/316 parity), **#220** accounts/auth (self-serve makers + per-tour moderation, Apple+email+Google, consumer accounts now; profiles/RLS/reports/sync tables), **#222** maker dashboard P1 + media storage (public buckets, `{maker_id}/{tour_id}` path RLS), **#223** Phase 2 multi-stop (no backend change needed), **#224** minimal moderation ("email me" Edge Function + `publish_tour`/`takedown_tour` helpers), plus **#219** doc-sync (307/5/316) and **#221** tracked V2 roadmap. Owner decisions recorded. **TODO:** 2 dead gallery images (The Oculus, The Charging Bull — Wikimedia 404) deferred by owner. **Next (owner/Mac):** stand up Supabase + seed; supabase-swift + RPC swap + sign-in/sync + maker UI; Step 6 payments needs pricing/Stripe calls. #223/#224 green-pending at session end (repo auto-merge off). | 2026-06-21 |
| `HANDOFF-260618.md` | Fortieth session 2026-06-18 (verify/build): **PR #209 (catalog detach) verified + merged (`de8ff6a`) and TestFlight 1.0 (46) cut and shipped.** The app now fetches `Tours.json` from gh-pages at launch — local copy first (last-good network cache → bundled seed) for an instant, offline-capable first frame, then a background refresh republishes the `@Observable` `tours`/`makers` live; any network/decode failure keeps the local copy. New `Data/RemoteCatalogLoader.swift` (fetch behind a `CatalogFetching` protocol) + injectable loader/`autoRefresh` on `DataService`; bundled `Tours.json` kept as the offline seed; 95/95 tests (7 new `RemoteCatalogLoaderTests`). **Content can now ship by pushing one gh-pages file — no rebuild/App Store review.** **Caught a drift bug:** the published gh-pages file was stale at 272 while `main` was 300 (sessions 39's #205/#206 never re-published) — a fresh launch would have regressed 300 → 272; **republished gh-pages to 300** (verified live), proved the live update end-to-end (added ★ to one title on gh-pages → relaunch showed it with no rebuild → reverted clean), and caught the PR branch up to 300 before merging. Bump 45 → 46 via short-lived **PR #210** (`6418fba`, admin-merged); archive `/tmp/Atlas-20260618-2257-b46.xcarchive`; embedded `1.0 (46)`; no validation 90474. **Upload gotcha:** Organizer's *PLA Update available* + *No iOS Distribution certificate* = unaccepted Program License Agreement (cert error is downstream) — owner accepted at developer.apple.com/account, retried, **1.0 (46) live.** **Follow-up flagged: auto-publish `Tours.json` → gh-pages on merge-to-main** so the remote can't drift from the bundle again. | 2026-06-18 |
| `HANDOFF-260616.md` | Thirty-ninth session 2026-06-16 (local build cut): **TestFlight 1.0 (45) cut and shipped — catalog crosses 300 tours.** Build cut to ship everything on `main` since build 44 (`7f675a3`): **PR #205** (6 Lisbon tours — Conserveira, Dolls Hospital, Oceanário, Palace Fronteira, Ponte 25 de Abril, Vasco da Gama Bridge; LIS 60 → 66) + **PR #206** (22 London tours into Tours.json; LDN 58 → 80). Content + images only, no app-code change. **Catalog 272 → 300 tours / 4 makers** (100 NYC + 80 LDN + 66 LIS + 54 OPO). Bump 44 → 45 via short-lived **PR #207** (`acc05b4`); archive clean at `/tmp/Atlas-20260616-2045-b45.xcarchive` (~23s warm); embedded `1.0 (45)` verified; no validation 90474; owner uploaded via Organizer. Worktree pattern: `git worktree add -b claude/build-45-bump /tmp/build45 main` (plain `worktree add /tmp/build45 main` fails — `main` already checked out in the primary). **Recurring snag (3rd time): `gh pr merge --delete-branch` throws `'main' is already used by worktree` after the squash already landed server-side — recover by deleting the remote branch + ff-pull in the primary checkout, then archive from there.** | 2026-06-16 |
| `HANDOFF-260615.md` | Thirty-eighth session 2026-06-15 (web/PM): **Lisbon batch 3 — 14 tours (PR #200) + TestFlight 1.0 (44) cut and shipped.** LIS 46 → 60 (Belém — Monument to the Discoveries + National Coach Museum; the four Alfama/Bairro Alto miradouros; the Estrela basilica + garden; Casa dos Bicos; Avenida da Liberdade; Jazigo dos Duques de Palmela; Ribeira das Naus; Village Underground Lisboa); **catalog 258 → 272 tours / 4 makers / 281 stops** (100 NYC + 58 LDN + 54 OPO + 60 LIS). Audio `d4bd503` + images `b56899a` (38 webp) on gh-pages, all URLs 200; owner-supplied assets, no sourcing pipeline; append-only assembler (clean 640-insertion diff); squash-merged `cccbd9b`. Build also carries **PR #201** (`0ea562b`, parallel session) — home placecard polish (ALL-CAPS title, 2/3-screen width, secondary distance). Bump 43 → 44 via short-lived PR #202 (`7f675a3`); archive clean at `/tmp/Atlas-20260615-2246-b44.xcarchive`; embedded `1.0 (44)` verified; owner uploaded via Organizer. Category note: Ribeira das Naus = `culturalHeritage` (shipyard heritage, not green space). **Parallel-session win: all mutating work ran in isolated worktrees — no branch-flip this session.** Quirks: accented drop-folder name (`Graça`) broke shell globs (macOS NFC/NFD); `gh pr merge --delete-branch` threw a local `'main' is already used by worktree` error after the merge already landed server-side. | 2026-06-15 |
| `HANDOFF-260612-4.md` | Thirty-seventh session 2026-06-12 (web/PM): **Lisbon batch 2 — 15 tours (PR #198) + TestFlight 1.0 (43) cut and shipped.** LIS 31 → 46 (funiculars, the big museums, Pink Street); **catalog 243 → 258 tours / 4 makers / 267 stops** (100 NYC + 58 LDN + 54 OPO + 46 LIS). Audio `a7d532c` + images `4116b8e` (48 webp) on gh-pages, all 63 URLs 200; owner-supplied assets, no sourcing pipeline. Bump 42 → 43 via short-lived PR #199 (`30db8fa`); archive clean at `/tmp/Atlas-20260612-1923-b43.xcarchive`; embedded `1.0 (43)` verified; owner uploaded via Organizer. Quirks: São Roque drop folder named by coords only (identified by inner filenames); Pink Street ships hero-only; two transcripts needed CSV quote-artifact cleanup. **Parallel-session lesson: another session flipped this checkout back to `main` mid-task — re-verify the current branch immediately before committing.** | 2026-06-12 |
| `HANDOFF-260612-3.md` | Thirty-sixth session 2026-06-12 (implementation): **home drawer pivoted to category rails** (PR #194, owner-driven at the sim) + **TestFlight 1.0 (42) cut and shipped** (bump PR #196). Drawer renders `HomeRailsViewModel.rails()` — Continue-listening row (player's loaded tour first, last-listened fallback) → NEAR YOU → IN VIEW → whole-catalog category shelves, viewer-distance-sorted; chips jump-scroll. Rail cards 260pt / 4:3 uncropped heroes, one-line all-caps titles. Detent-persistence fix (drawer stays mounted under the tour-detail layer — `tourLayerCoversDrawer` + `BottomLayerController.dismiss` completion); compass relocated beside the recenter button (needs ⌥-drag hand check); 44pt pin hit areas. `TourListCard.swift` now unused. 88/88 tests. Classifier note: direct-to-main bump push was blocked — use the short-lived-PR pattern. | 2026-06-12 |
| `HANDOFF-260612-2.md` | Thirty-fifth session 2026-06-12 (local build cut): **TestFlight 1.0 (41) cut and shipped.** Build bumped **40 → 41 direct-to-main** (`7cf3590`, app-target `CURRENT_PROJECT_VERSION` lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). `xcodebuild archive` clean at `/tmp/Atlas-20260612-0832.xcarchive` (~1–2 min, warm DerivedData); embedded version verified `1.0 (41)`; **no validation 90474**. Owner uploaded via Organizer. **TestFlight 1.0 (41) is live.** Carries **PR #193** (London batch 2 — 18 Bloomsbury/South Bank tours) + #195 docs. **London 40 → 58 — now the #2 city ahead of Porto; catalog 225 → 243 tours / 4 makers.** Bump + archive ran in a `/tmp/build41` worktree — primary checkout was mid-flight on `claude/home-drawer-rails` (**PR #194 intentionally NOT included**; owner reviewing separately). | 2026-06-12 |
| `HANDOFF-260612.md` | Thirty-fourth session 2026-06-12 (web/PM, multi-day): **33 London tours shipped via the audio-pending staging workflow** — its first at-scale run. Tours text-drafted + image-staged on per-batch branches before audio existed (`drafts/pending-tours.json` + gh-pages images); MP3s uploaded 5-at-a-time when ready (durations via `mutagen`); per-tour captions + longDescriptions authored at assembly. **PR #192 (batch 1: 15 West End/Soho)** + **PR #193 (batch 2: 18 Bloomsbury/South Bank)** squash-merged, CI green. **Catalog 4 makers / 243 tours / 252 stops; London 25 → 58.** Batch 1 in TestFlight 1.0 (40); batch 2 awaits the next cut. Lessons: conflicted PRs never trigger CI; take main's Tours.json + re-run the idempotent assembler on conflict; remote-env git proxy blocks branch deletion (delete `claude/dreamy-wozniak-nM6a4` + `-batch2` via UI); racing gh-pages deploys 401 harmlessly. | 2026-06-12 |
| `HANDOFF-260611.md` | Thirty-third session 2026-06-11 (web/PM): **TestFlight 1.0 (40) cut and shipped.** Build bumped **39 → 40 direct-to-main** (`0cf79b3`, app-target `CURRENT_PROJECT_VERSION` lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). `xcodebuild archive` clean at `/tmp/Atlas-20260611-1900.xcarchive` (~3 min); embedded version verified `1.0 (40)`; **no validation 90474** — `UIRequiresFullScreen=YES` from build 34 held. Owner uploaded via Organizer. **TestFlight 1.0 (40) is live.** Carries **PR #192** (London batch 1 — 15 West End / Soho tours: theatre district, shopping, Soho landmarks). Content + images only, no app-code change. **London 25 → 40** (West End / Soho, on top of the City + Westminster/Whitehall shipped in build 39 `ea197f0`); **catalog 210 → 225 tours / 4 makers** (100 NYC + 54 OPO + 40 LDN + 31 LIS). | 2026-06-11 |
| `HANDOFF-260610.md` | Thirty-second session 2026-06-10 (web/PM): **TestFlight 1.0 (39) cut and shipped** (backfilled 2026-06-11 from git `ea197f0`). Build bumped **38 → 39 direct-to-main** (app-target lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). **TestFlight 1.0 (39) live.** Carries **PR #191** (`56f7a30`) — 26 Lisbon tours (Belém, Jerónimos, Castelo de São Jorge, Tram 28…), **Lisbon 5 → 31**, **catalog 184 → 210 tours / 4 makers** (100 NYC + 54 OPO + 25 LDN + 31 LIS). Content + images only. Caveat: the bump message said "26 Lisbon + 19 London", but the 19 London already shipped in build 38 — only the 26 Lisbon were new. Superseded by 1.0 (40). | 2026-06-10 |
| `HANDOFF-260609-2.md` | Thirtieth session 2026-06-09 (web/PM): **London expansion II** — 10 Westminster/Whitehall tours under Atlas Studio LDN (Westminster Abbey, Houses of Parliament & Big Ben, Westminster Hall, Trafalgar Square, The National Gallery, St Martin-in-the-Fields, Banqueting House, The Cenotaph, Churchill War Rooms, Parliament Square), London 15 → 25, catalog → 4 makers / 174 tours / 183 stops. Merged via PR #188 (one consolidated PR). Unsplash deep for famous landmarks; restricted-interior subjects (Westminster Hall roof, Banqueting House Rubens ceiling, Churchill War Rooms Map Room) used owner-pasted images (pulled from the session-transcript `.jsonl`). Verify-gate caught many look-alikes (V&A vs National Gallery, Budapest parliament vs Parliament Square, Women-of-WWII/Battle-of-Britain vs the Cenotaph). No build bump (TestFlight 1.0 (37); 19 newer London tours ship in the next cut, 37 → 38). Westminster Abbey hero swap to the west front in progress. | 2026-06-09 |
| `HANDOFF-260609.md` | Twenty-ninth session 2026-06-09 (web/PM): **London expansion** — 9 more London tours under Atlas Studio LDN (Lloyd's of London, Bank Junction, St Stephen Walbrook, St Bartholomew the Great, Smithfield Market, Postman's Park, The Barbican, Guildhall, Temple Church), London 6 → 15, catalog → 4 makers / 164 tours / 173 stops. Merged via PR #185 (Lloyd's) + PR #186 (consolidated 8). Lessons: interior-famous City churches have ~no usable CC0 modern photo (Unsplash = other churches, Wikimedia = CC BY-SA), so owner supplied images by pasting them in chat — **owner-pasted images are pulled from the session-transcript `.jsonl` (base64 image blocks)**, not the uploads dir; apply `exif_transpose` with no manual rotate; batch = one consolidated PR. No Swift/asset/project changes; no build bump (TestFlight stays 1.0 (37); these 9 ship in the next cut). | 2026-06-09 |
| `HANDOFF-260608.md` | Twenty-eighth session 2026-06-08 (web/PM): **London launch.** Added **Openverse** as a CC0-only image source to the pipeline (owner policy: public-domain `cc0,pdm` only — no attribution UI; verify gate mandatory since Openverse titles are unreliable; Wikimedia 429-throttles bursts). Formalized the "upload tours without images" protocol with **inline full-size** candidate presentation (grid too small). Created **fourth maker Atlas Studio LDN** (🇬🇧) and added the **first 6 London tours** (St Paul's Cathedral, The Monument, The Tower of London, Tower Bridge, Leadenhall Market, The Gherkin) — all single-stop, geofenced, Unsplash imagery (owner picked modern photos over CC0 historical art). **Tall-subject crop rule:** columns/spires need a top-biased crop (The Monument's urn) since the 1200×900 hero is landscape. Catalog 149 → **155 tours / 3 → 4 makers / 164 stops**. No Swift/asset/project changes; build bumped 36 → 37 (`49a81ac`), **TestFlight 1.0 (37) live** with the 6 tours. All merged to `main` (PR #181 + #182). | 2026-06-08 |
| `HANDOFF-260607-2.md` | Twenty-seventh session 2026-06-07 (web/PM): TestFlight 1.0 (36) cut and shipped. Build bumped 35 → 36 direct-to-main (`ca671c8`, app-target `CURRENT_PROJECT_VERSION` lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). `xcodebuild archive` clean at `/tmp/Atlas-20260607-2112.xcarchive` (fast — DerivedData warm from build 35; embedded version verified 1.0 (36)); **no validation 90474** — the `UIRequiresFullScreen=YES` iPad-orientation fix from build 34 (`62d00df`) held. Owner uploaded via Organizer. **TestFlight 1.0 (36) is live.** Carries PR #163 (full-edge module + no drawer leak on pushed details), #164 (place results limited to cities/landmarks, businesses dropped), #166 (Maker + Search background pinned to module shade), #167 (maker editorial typography + square thumbnails), #168 (new galleries: The Shed, Citi Field, LOVE Sculpture), #170 (save-maker bookmark + nav chrome + Library section). No Swift / asset / project changes beyond the pbxproj bump. Catalog unchanged at 149 tours / 3 makers. | 2026-06-07 |
| `HANDOFF-260607.md` | Twenty-sixth session 2026-06-07 (web/PM): TestFlight 1.0 (35) cut and shipped. Build bumped 34 → 35 direct-to-main (`ce32d88`, app-target `CURRENT_PROJECT_VERSION` lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). `xcodebuild archive` clean at `/tmp/Atlas-20260607-0649.xcarchive` (~4 min, embedded version verified 1.0 (35)); **no validation 90474** — the `UIRequiresFullScreen=YES` iPad-orientation fix from build 34 (`62d00df`) held. Owner uploaded via Organizer. **TestFlight 1.0 (35) is live.** Carries PR #159 (Player presented from top window — gapless transition + floating island on retract), PR #160 (place-search perf via `MKLocalSearchCompleter` + tap spinner), and 7 tours' new photo galleries (Intrepid, Little Island, Manhattan Bridge, Chelsea Hotel, Four Freedoms Park, Unisphere, Cooper Union). No Swift / asset / project changes beyond the pbxproj bump. Catalog unchanged at 149 tours / 3 makers. | 2026-06-07 |
| `HANDOFF-260606-3.md` | Twenty-fifth session 2026-06-06 (implementation): Search polish + place-search performance, owner-directed at the simulator. [PR #154](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/154) — Search typography flattened to the `caption` token (result/maker titles stay `body` ALL CAPS); result rows single-line all-caps with maker-only subtitle + square thumbnails (removes the two-line category•maker wrap); new **Makers** section deep-links to `MakerView`. `SearchView.swift` only; shared `SearchBar` (Home) untouched. [PR #160](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/160) — place-search typing lag fixed by replacing per-keystroke `MKLocalSearch` with `MKLocalSearchCompleter` (instant type-ahead; heavy geocode deferred to a single on-tap resolve, spinner on the resolving row); supersedes session 23's 300ms-debounced `PlaceSearchService` internals (Search→map channel + `HomeView.flyTo` + no-tours overlay untouched). No build bump (34); 88/88 tests pass. | 2026-06-06 |
| `HANDOFF-260606-2.md` | Twenty-fourth session 2026-06-06 (implementation): full-screen Player polish, round 2 (continues session 22). Structural change — `PlayerView` is now presented as a `.fullScreenCover` from `BottomModuleRoot` (the top window) instead of `ContentView`, so the cover slides over the mini-player + tab bar in the same window; removed the hide/show-the-module-window dance (`BottomModuleWindowController.setHidden` deleted) that left a transition gap, and `PassThroughWindow.hitTest` now claims all touches while presenting a modal. Opening the player dismisses the underlying detail sheet so retracting returns to the tab root (Home floating island, not edge-to-edge). Also: single-line/marquee stop title, caption that always reserves 3 lines, system `MPVolumeView` bracketed by speaker icons with a 12pt thumb. No build bump (33); no `AudioPlayerService` API change; 88/88 tests pass. Drag-to-dismiss + volume/AirPlay device-only (sim HID can't drag). | 2026-06-06 |
| `HANDOFF-260606.md` | Twenty-third session 2026-06-06 (implementation): place/location search added to Search. A **Places** section (Apple `MKLocalSearch` — no new deps) sits above the Makers/Tours catalog results; tapping a place dismisses Search and glides the Home map camera to that region via a one-shot `HomeSharedState.pendingMapMove`. New `PlaceSearchService` (debounced 300ms, per-feature zoom from placemark radius) + `MapRegionGeometry.anyStop` (reuses the antimeridian-aware `MKCoordinateRegion.contains`). When the destination has no tours, a transient "No Atlas tours here yet" hint shows (attached via `.overlay` — a ZStack sibling of the `Map` didn't composite). Owner approved lifting the "Home map camera is settled" note for this additive change; recenter / pin-tap / startup paths untouched. Folds in the same-day Search-polish commit (caption typography, single-line result rows, maker results). No build bump (stays 33); 88/88 tests pass (4 new). Sim-only: the no-tours hint can paint a few seconds late under MKMapView tile streaming — verify prompt on device. One cosmetic `placemark` iOS-26 deprecation warning left as a follow-up. | 2026-06-06 |
| `HANDOFF-260605.md` | Twenty-second session 2026-06-05 (implementation): full-screen Player polish, owner-driven at the simulator. Two `PlayerView`-focused PRs — [#148](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/148): full-screen cover (bottom-module window hidden while the player is up via new `BottomModuleWindowController.setHidden`), caption typography, carousel parity with the detail sheet; [#150](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/150): drag-to-dismiss grab-handle sheet, ••• overflow menu mirroring the detail sheet (player wrapped in its own `NavigationStack` so "Go to creator" pushes `MakerView`), five-equal-column transport with play screen-centered (`speed · −10 · play · +10 · next-track`; skip ±10s always live, next-track disabled on single-stop), thin gold knob-less scrubber, 0.5×/0.75× speeds, and a system `MPVolumeView` slider (device-only — blank in the sim by design). No build bump (stays 32); no `AudioPlayerService` API change; 84/84 tests pass each PR. Mini-player design untouched; native iOS menus kept (UIMenu font isn't customizable). Drag-to-dismiss feel + volume + AirPlay need a real-device check (sim can't drag; MPVolumeView is device-only). | 2026-06-05 |
| `HANDOFF-260604.md` | Twentieth session 2026-06-04 (web/PM): pure image-pipeline pass on existing NYC tours. No new tours, no Swift changes, no build bump. Catalog stays at 138 tours / 147 stops / 3 makers. Image pipeline codified as CLAUDE.md Rule #8 (+ full § Image Pipeline procedure). Fourteen NYC tours received new hero/gallery images via Unsplash + Gemini verify + owner pick → 1200×900 WebP q82 gh-pages: Empire State Building, Chrysler Building, Brooklyn Bridge, Met Museum, Bethesda Terrace, Grand Central (gallery only), High Line (gallery only), Rockefeller Center, One WTC, Guggenheim, Statue of Liberty, Washington Square Park (gallery only), Flatiron Building, Lincoln Center. Times Square skipped at owner request. ~25 NYC tours still need gallery images; 9/11 Memorial queued next (background fetch script running at session end). Branch `claude/session-012bd7xvvgfz8cpkucw3bqy8-0MeY7` open, not yet merged to main. TestFlight still at 1.0 (28). | 2026-06-04 |
| `HANDOFF-260603-2.md` | Nineteenth session 2026-06-03 (implementation, same-day continuation of session 18 `260603`): six small home-screen polish PRs driven turn-by-turn at the simulator, plus the build bumps that cut 27 (left unshipped by session 18) then 28. PR #128 muted standard map style (`emphasis: .muted`). PR #129 `StopPin` 14/18 → 16/20pt; cluster count text to 12pt SF Mono regular. PR #130 `HomeMapSection.tourPOI` `.including(...)` allowlist — cultural / civic / nature / transit kept; ATMs / gas stations / retail / nightlife / activity venues hidden. PR #131 drawer's `displayedTours` now filters to `visibleRegion` + sorts by map-center distance; header count collapses to `displayedTours.count`; strip-clipping helpers and unused `UIKit` import dropped; doc comment flags the future rails direction. PR #132 `BottomModuleRoot` + `BottomSheet` flipped from `.ignoresSafeArea(.container, edges: .bottom)` to `.ignoresSafeArea(.all, edges: .bottom)` — focusing a TextField (SearchView) now lets the keyboard overlay the bottom module + drawer instead of pushing them up. PR #133 `onPinTapped` animates `cameraPosition` to centre the tapped pin at the current visible span. PR #134 bumped 27 → 28 defensively (session 18's `89dd5df` direct-to-main bump to 27 left dangling). `xcodebuild archive` clean at `/tmp/Atlas-20260603-2123-b28.xcarchive`; owner uploaded via Organizer. **TestFlight 1.0 (28) is live.** Catalog grew to **138 tours / 147 stops / 3 makers** with session 18's PR #127 (7 central Porto classics — Cathedral, São Bento, Clérigos, Ribeira, São Francisco, Bolsa, Dom Luís I) absorbed into the same build. | 2026-06-03 |
| `HANDOFF-260603.md` | Eighteenth session 2026-06-03 (web/PM): 18 new NYC tours that had landed direct-to-main between sessions 17 and 18 (catalog **113 → 131 tours**, NYC-area **73 → 91**) bundled into a TestFlight cut. Notable adds: MoMA, Bryant Park, Federal Hall, Coney Island, Schomburg Center, Eldridge Street Synagogue, Grand Concourse, Strivers' Row, IAC Building (Gehry, 2007), The Strand Bookstore, Four Freedoms Park, Green-Wood Cemetery, African Burial Ground, Cooper Union, Tompkins Square Park, Columbus Park (Chinatown), Grand Army Plaza (Brooklyn), plus **Fifth Avenue Walk — second multi-stop tour ever** in the catalog (joining AMNH Four Facades from 2026-05-26). Two validator-caught typo fixes preceded the build: `triggerMode geofence → geofenced` across tours 114–131 (`3235d33`) and `TourKind multi → multiStop` on Fifth Avenue Walk (`7c11003`). Build bumped 25 → 26 in `17dba88` (direct-to-main per established pattern — single-line pbxproj edit, test-target lines stay at 1). `xcodebuild archive` clean at `/tmp/Atlas-20260603-1840.xcarchive` (~3 min); owner uploaded via Organizer. **TestFlight 1.0 (26) is live.** No Swift / asset / project structure changes this session beyond the pbxproj bump. | 2026-06-03 |
| `HANDOFF-260602-2.md` | Seventeenth session 2026-06-02 (implementation, same-day continuation of session 16 `260602`): owner sat at the sim and asked for one or two changes at a time across a long iterative polish pass. PR #113 bundles ~30 1-2-line tweaks plus the cluster smoothness fix that closes item #6 from the original 11-item home brief — typography overhaul (`AtlasTypography.caption` is now 13pt SF Mono regular; `body` is now 15pt SF Pro regular, fixed-size so Dynamic Type stops scaling that token — flagged); home-chrome dimensions tightened (search/chips 24→16pt horizontal inset, 46→44pt height; map control glyphs 16→20pt; tab bar icons 22→20pt; drawer top-corner 30→28pt); mini-player paddings completely retuned (avatar-left-to-bar-left and ring-outer-to-bar-right both 16pt; avatar→text 16pt; skip-glyph→ring outer 16pt via outer HStack spacing 0); ALL CAPS pass across home header, mini-player title line, tab bar labels (display site only — VoiceOver-safe); new `Maker.avatarEmoji` field with Atlas Studio NYC set to 🍎 (resolution order emoji → URL → bundled `AtlasStudioAvatar`); cluster smoothness via new `HomeMapSection.snappedSpan(_:)` static that rounds the visible region's span to two significant figures before deriving cluster cell pitch, killing MapKit's sub-percent settle drift on pure pans. PR #114 bumped 24 → 25 (admin-merged). `xcodebuild archive` clean at `/tmp/Atlas-20260602-2310.xcarchive`; owner uploaded via Organizer. **TestFlight 1.0 (25) is live.** Catalog unchanged at 113 tours / 3 makers. `xcodebuild test` succeeds throughout. | 2026-06-02 |
| `HANDOFF-260602.md` | Sixteenth session 2026-06-02 (web/PM): 11-tour Portugal batch via PR #110 — 8 Atlas Studio Porto (Batalha Centro de Cinema, Building in Senhora da Luz, Mosteiro Santo Agostinho da Serra do Pilar, Teatro Rivoli, Trindade Metro Station, Vodafone Headquarters, Municipal Library of Viana do Castelo, **Biblioteca Pública e Arquivo Regional Luís da Silva Ribeiro — first Azores tour**) + 3 Atlas Studio Lisbon (Adega Mayor / Campo Maior — first Alentejo tour, Óbidos, Capela do Monte / Lagos — first Algarve tour). Audio + images uploaded to `gh-pages` across four chunked commits (`f4e849d`, `259309d`, `7a67dc9`, `24c6e36`) after the combined push repeatedly hit HTTP 408 (no SSH key on machine, fell back to HTTPS chunks). Catalog 102 → 113 tours / 105 → 117 stops. Atlas Studio Porto 22 → 30; Atlas Studio Lisbon 2 → 5. New cities: Viana do Castelo, Angra do Heroísmo, Campo Maior, Óbidos, Lagos. PR #111 bumped build 23 → 24 (admin-merged). `xcodebuild archive` clean at `/tmp/Atlas-20260602-2146.xcarchive`; owner uploaded via Organizer. **TestFlight 1.0 (24) is live.** All 22 post-merge spot-checks (11 audio + 11 heroes) returned 200. No Swift / asset changes. | 2026-06-02 |
| `HANDOFF-260601-3.md` | Fifteenth session 2026-06-01 (implementation, same-day continuation of `260601-2`): eleven-item home-screen polish pass. PR #103 (default location at launch geolocates user with NYC fallback + Look Around button + `LookAroundView.swift` removed) and PR #104 (drawer `.large` detent capped below search/chips via new `BottomSheet.topReservedHeight` + `AtlasSpacing.searchAndChipsBlockHeight` token; placecard fill swapped from `.regularMaterial` to `AtlasColors.secondaryBackground`; bookmark Button replaces hero-corner category chip with `LibraryStore.toggleSaved` wiring; animated `. / .. / ...` ellipsis in tours-in-view header while panning via `TimelineView` + `HomeSharedState.isMapMoving`). Drawer-gap geometry bug caught mid-review via magenta diagnostic — `BottomSheet.heightForDetent(.large)` was double-counting top safe-area inset (`geo.safeAreaInsets.top` reports the device value even when the reader's bounds already start below it); fix removed `topInset` from the `.large` formula and both `drawerVisibleHeight` mirrors. Initial-launch zoom span widened to `0.1°` (~Manhattan length) after owner review. Items #2, #3, #8 reported as informational only — no code change. Item #6 (clustering smoothness) deferred pending visual verify. PR #105 bumped `CURRENT_PROJECT_VERSION` 21 → 22 (merged with `--admin`). `xcodebuild archive` clean at `/tmp/Atlas-20260601-2233.xcarchive`; owner uploaded via Organizer → TestFlight 1.0 (22). Owner then caught a real-device bug — drawer could be dragged past the `.large` cap during the gesture before snapping back — fixed in PR #107 (clamp drag-time ceiling to `heightForDetent(.large, ...)` instead of `geo.size.height - horizontalInset`). PR #108 bumped 22 → 23 (admin-merged); `xcodebuild archive` clean at `/tmp/Atlas-20260601-2302.xcarchive`. **TestFlight 1.0 (23) is live.** Catalog unchanged at 97 tours / 3 makers. 84/84 tests pass throughout. | 2026-06-01 |
| `HANDOFF-260601-2.md` | Fourteenth session 2026-06-01 (web/PM, same-day continuation of `260601`): 6 new Porto-area tours under Atlas Studio Porto via PR #100 — Porto Tram Museum, Igreja de Santa Maria de Marco de Canaveses (Siza, 1996), Fundação Livraria Lello (foundation HQ at the 14th-c Mosteiro de Leça do Bailio), Livraria Lello (Art Nouveau bookstore, 1906), Parque de São Roque (former Calém Port-wine Quinta da Lameira), and Pavilhão Multiusos de Gondomar (Siza, 2007). Both Lello entries shipped as distinct tours (owner-confirmed); briefing-tagged "Souto de Moura" for the Gondomar pavilion was a transcript-confirmed Siza correction. Audio + 28 webps to gh-pages (`332367f` + `2f3dc04`). New cities in catalog: Marco de Canaveses, Matosinhos (Leça do Bailio), Gondomar. Catalog 91 → 97 tours. Then PR #101 bumped build 20 → 21 (auto-mode classifier blocked direct-to-main bump per historical pattern; PR route used instead — see § 5 for follow-up). `xcodebuild archive` clean at `/tmp/Atlas-20260601-2057.xcarchive`; owner uploaded via Organizer. **TestFlight 1.0 (21) is live.** No Swift changes. | 2026-06-01 |
| `HANDOFF-260601.md` | Thirteenth session 2026-06-01 (web/PM): 10 new NYC tours added (Domino Park, Wave Hill, Queens Museum, Museum of the Moving Image, Snug Harbor, Yankee Stadium, Citi Field, Madison Square Garden, Riverside Church, One World Trade Center). Hero image audit — all 10 had broken Wikimedia URLs, all fixed. Coordinate corrections for The Cloisters + Beacon Theatre. Flatiron Building hero replaced with square-friendly portrait (3024×3903). No Swift changes. Catalog 81 → 91 tours; 73 NYC-area total. Build still 1.0 (19). Superseded by `260601-2`. | 2026-06-01 |
| `HANDOFF-260529-2.md` | Twelfth session 2026-05-29 (evening): build-bump-and-archive only — cut TestFlight build 1.0 (17). `CURRENT_PROJECT_VERSION` 16 → 17 in `f359f55` direct to `main`; `xcodebuild archive` clean at `/tmp/Atlas-20260529-1626.xcarchive`; owner uploaded via Organizer. Build 17 carries PR #91 (chrome seam, already in 16), PR #92 (Casa das Histórias Paula Rego — first Cascais tour), PR #93 (detail masthead + toolbar + overflow menu, part 1 of 2), PR #94 (5 Porto-area architecture tours — Burgo, Crasto 213, Leixões Cruise Terminal, Majestic, Marés), PR #95 (light-mode bars appearance fix). Catalog 53 → 59 tours, 3 makers unchanged. No Swift / Tours.json / asset changes this session. | 2026-05-29 |
| `HANDOFF-260529.md` | Same-day earlier session 2026-05-29: shipped PR #95 — bottom-module appearance tracks Settings picker correctly in both directions. The mini-player + tab bar live in a manually-created secondary `UIWindow`; SwiftUI's `.preferredColorScheme(...)` doesn't propagate into that window, so its `userInterfaceStyle` always tracked SYSTEM and the dynamic-provider colors from PR #91 resolved inverted whenever the picker disagreed with the system. Fix: new `BottomModuleWindowController.apply(preference:)` writes `overrideUserInterfaceStyle` directly; driven from App body via `.onAppear` + `.onChange(of: colorSchemePreference)`. Removed the install-closure `.preferredColorScheme` that was freezing the host VC's override at install time. Preserves PR #91's chrome seam and the single-form rule. 84/84 tests pass. PR #93 (detail masthead) also landed earlier the same date from a different session. TestFlight still at 1.0 (16). | 2026-05-29 |
| `HANDOFF-260528.md` | Eleventh session 2026-05-28: shipped PR #91 — bottom-module chrome-shade seam fix. Two coordinated changes: (1) `AtlasColors.secondaryBackground` swapped from `Color(uiColor: .secondarySystemBackground)` to a hardcoded `UIColor(dynamicProvider:)` block that keys only on `userInterfaceStyle` (no elevation variance) — light `#F2F2F7` / dark `#1C1C1E`. New `secondaryBackgroundUIColor` companion for UIKit consumers (`BottomLayerPresentation`'s detail hosting view). (2) `MiniPlayerBar` + `AtlasTabBar` gained `extendsToScreenEdges` parameter; bars grow edge-to-edge on Library / Me / detail-up, stay floating island on Home-no-detail. Button x positions identical across both forms via inner padding (design rule holds). Separate window-2 Rectangle removed. Diagnosed with bright contrasting test colors — workflow saved as `feedback-visual-debugging.md`. Build bumped 15 → 16; archive built; owner uploaded to TestFlight. Three polish suggestions parked: mini-player title type style, control glyph size alignment, avatar/play-ring balance. 84/84 tests pass. | 2026-05-28 |
| `HANDOFF-260527-3.md` | Tenth session 2026-05-27 (night): discovery-only kickoff of the tour-detail-sheet retool pass. Surveyed `TourDetailView.swift` post-PR #79 (X close + hero/carousel + title + chip row + maker row + meta row + description + stops + inline Start/bookmark/download row + bottom inset). Built + launched on iPhone 17 Pro sim (dark, NYC) and captured Braga gallery sheet at `/tmp/atlas-detail-DARK-gallery-*.jpg`. Asked Group A (share-button placement + shape); owner dismissed and called night. **No code or content changed.** `main` unchanged from session 9. To resume: re-pose Group A as a single question, then walk B→G per the brief. | 2026-05-27 |
| `HANDOFF-260527-2.md` | Ninth session 2026-05-27 (late): content-only PR train. Backfilled `additionalImageURLs` gallery for six existing Porto/Braga tours whose hero links were broken in PR #81 (Bouça, Chapel of Souls, Capela do Senhor da Pedra, Cantareira/Rua do Passeio Alegre 212, Casa de Chá da Boa Nova, Braga Municipal Stadium). Added 3 new single-stop Portugal tours: Expo'98 Portuguese National Pavilion (Lisbon, Siza), Piscina da Quinta da Conceição (Matosinhos, Siza), Porto School of Architecture / FAUP (Porto, Siza). New maker "Atlas Studio Lisbon" added (`B1A9EAF0-…`) for the Expo'98 tour. Catalog now: 53 tours, 3 makers, 57 stops. PRs #84–#90 all auto-merged. No `*.swift` changes; build stays at 13. | 2026-05-27 |
| `HANDOFF-260527.md` | Earlier handoff (session 8 — UIKit slide-up + chrome unification). Superseded by `260527-2`. Eighth session 2026-05-27: shipped UIKit-backed slide-up presentation for tour detail + chrome unification + action bar removal + Tours.json hero URLs to 1280px + TestFlight 1.0 (12). Replaces the SwiftUI `.offset` slide layer with a `UIPresentationController`-driven modal — mini-player + tab bar hoisted into a secondary higher-level `UIWindow` (Apple Music pattern). Fixes three regressions surfaced during the session: Library/Me tabs not switching (PassThroughWindow.hitTest was rejecting legitimate Button taps), tap-tab while detail open leaves user appearing stuck (added auto-dismiss on tab change), and the "bump" hairline at the bar's top edge (MiniPlayerBar.topGap → 0). **Known issue: subtle chrome shade mismatch in dark mode** — `.elevated` trait override on the detail's hosting controller narrows the gap but doesn't close it. See handoff for three follow-up leads. 84/84 tests pass. | 2026-05-27 |
| `HANDOFF-260526.md` | 2026-05-26 remote session: shipped PR #78 (merged — enter-slide mirror fix) + AMNH Four Facades multi-stop tour (39 tours, unblocks M-qa items 6+7). Superseded by `260527`. | 2026-05-26 |
| `HANDOFF-260525-7.md` | Seventh session 2026-05-25: shipped PR #78 — enter-slide mirror fix. Owner confirmed PR #77's exit-slide is "working perfectly" and asked for enter to be the "exact opposite." Remaining asymmetry was `AsyncImage`'s 250ms phase crossfade firing during the slide-up but not the slide-down (hero is pre-loaded by exit time). New `disableLoadAnimation: Bool` flag on `HeroImageView` lets `TourDetailView` opt out of the crossfade; other surfaces keep the gentle polish. 84/84 tests pass. Superseded by `260527`. | 2026-05-25 |
| `HANDOFF-260525-6.md` | Sixth session 2026-05-25: shipped PR #77 — tour-detail slide animation fix, closes the open issue from session 5. Two changes: (a) `displayedTour` moves to `TourPresenter`, updated synchronously inside `present(_:)` so the inner content lands in the view tree on the same SwiftUI tick the layer's offset starts animating — kills the one-tick gap SwiftUI was filling with a default opacity-fade transition; (b) drawer no longer animates opacity, instead swaps `.zIndex` based on `displayedTour` (z-4 above mini-player when no detail, z-1 below detail layer when a detail is up). Mini-player + tab bar stay at z-3 so their buttons remain tappable through the detail session. 84/84 tests pass. | 2026-05-25 |
| `HANDOFF-260525-5.md` | Fifth session 2026-05-25: shipped PR #76 — tour detail as a slide-up layer (NOT a sheet), drawer hoisted from `HomeView` into `ContentView` so it z-stacks above mini-player + tab bar, X close replaces back chevron, SearchBar + chips chrome unified to `secondaryBackground`. Three new files (`HomeSharedState.swift`, `HomeDrawerContent.swift`, `TourPresenter.swift`) + rewrites of `HomeView`, `ContentView`, `TourDetailView`, `TourListCard`, `RailCarousel`, `LibraryView`, `MakerView`, `SearchView`, `SearchBar`, `CategoryChipRow`, `BottomSheet`. Bumped to TestFlight 1.0 (10). Left one **open issue (slide animation reads as fade)** that PR #77 above closes. Superseded by `-6`. | 2026-05-25 |
| `HANDOFF-260525-4.md` | Fourth session 2026-05-25: shipped PR #70 (`643cbd7`) — buttons identical across every surface; only the edge-to-edge background fill behind the inset bar differs between Home (no fill, map shows through gaps) and the rest (filled, gaps blend into continuous strip). `AtlasTabBar` + `MiniPlayerBar` dropped their `extendsToScreenEdges` flags — single form everywhere. Bumped build to 1.0 (9); `xcodebuild archive` ran cleanly at `/tmp/Atlas-20260525-1746.xcarchive`; owner running Organizer upload. Verified via `snapshot_ui`: tab buttons at x=8/136.67/265.33 w=128.67 on every surface, matching OLD Home exactly. Owner approved this iteration. New feedback memory `feedback-atlas-module-design.md` captures the single-form rule. Superseded by `-5`. | 2026-05-25 |
| `HANDOFF-260525-3.md` | Third session 2026-05-25: shipped PR #69 (`8d928b3`), fixing two regressions PR #68 introduced on Home. (1) `AtlasTabBar` added the home-indicator safe-area inset to the view's *height* in full-edge mode, which physically pushed buttons + mini-player up by ~34pt. Fixed by making the view a constant 64pt in both modes; only what's painted in the 8pt outer strip changes (transparent on Home, opaque elsewhere). The safe-area zone is already covered by the painted button row because of `.ignoresSafeArea(.bottom)` on the parent ZStack. (2) PreferenceKey-driven `moduleGeometry` got stuck at `.fullEdge` after popping back from a detail. Replaced with `@Observable AtlasNavigationState` tracking `pushedDepth` via push/pop from each pushed view's onAppear/onDisappear — deterministic. `AtlasBottomModule.height` is a constant 126pt across modes. Verified via `snapshot_ui`: NEW Home buttons at y=807.33 (matches OLD Home exactly); NEW Library / Me also at y=807. 84/84 tests pass. Auto-merged on green. | 2026-05-25 |
| `HANDOFF-260525-2.md` | Second session 2026-05-25: shipped PR #68 (`fe11d99`), three connected follow-ups to PR #66 surfaced by the visual review. (1) `SearchBar` switched from `.sheet` to `NavigationLink` so the mini-player + tab bar stay visible while searching. (2) `AtlasTabBar` button row anchored at the same screen-y in both geometries — safe-area inset moved OUT of the painted button area into a separate background rectangle below. (3) Detail screens always render with the full-edge module now via a typed `AtlasModuleGeometry` preference (replaces the deleted `\.atlasIsHomeTab` env value). 84/84 tests pass. Auto-merged on green. **Introduced two regressions on Home that PR #69 (handoff `-3`) fixes**, then PR #70 (handoff `-4`) finished the design refinement. Superseded by `-4`. | 2026-05-25 |
| `HANDOFF-260525.md` | First session 2026-05-25: shipped PR #66 (`2452f52`), extending PR #60's bottom-module geometry to the rest of the app. New `AtlasBottomModule.height(extendsToScreenEdges:)` helper + `\.atlasIsHomeTab` env value + `extendsToScreenEdges` flag on `MiniPlayerBar` / `AtlasTabBar`. Non-Home tabs (Library / Settings / Manage downloads / Tour Detail / Maker) now show the module flush to the screen edges; their scrollable content reserves bottom space via `.safeAreaInset(.bottom)`. TourDetailView's `actionBarHeight` + trailing spacer rebuilt around the helper — also fixes a pre-existing too-small 72pt spacer. Auto-merge policy changed this session (commit `b06284b`): all PRs auto-merge on CI green now, code included. 84/84 tests pass. Superseded by the `-2` handoff above (which replaced the env value with a typed preference and added the cross-tab consistency fixes the visual review caught). | 2026-05-25 |
| `HANDOFF-260524-3.md` | Late-evening session 2026-05-24: shipped PR #60 (home polish bundle + player-state hardening, `e5b31da`). Visual-review pass against the running build surfaced three audio-state bugs the original polish scope didn't cover — added to the same PR: Open-player no longer disabled mid-load, `AudioPlayerService.seek(to:)` synthesizes `.ended` on scrub-to-end (AVPlayer doesn't fire `didPlayToEndTime` on manual seek), full-player `togglePlayPause` now replays on `.ended` via new `replayCurrent()`. 84/84 tests pass. TestFlight 1.0 (7) still queued for owner Archive/Upload. Superseded by the 260525 handoff above. | 2026-05-24 |
| `HANDOFF-260524-2.md` | Afternoon/evening Mac session 2026-05-24: shipped PR #61 (mini-player end-of-tour state fix — kills the post-tour "Loading…" flicker, adds in-place replay via new `AudioPlayerService.replayLast()`). Sibling session pushed `5db7aaa` to `claude/home-visual-polish-bundle` (drawer-stacking + sizing + tours-in-view iteration) — branch is on origin but not PR'd yet at session end. Superseded by the `-3` handoff above. | 2026-05-24 |
| `HANDOFF-260524.md` | Mac session 2026-05-23/24 morning: populated `heroImageURL` for all 38 tours via CC-licensed Wikimedia Commons photos (landscape-optimised batch), bumped build number to 7, opened Xcode for TestFlight upload. Superseded by the `-2` / `-3` handoffs above. | 2026-05-24 |
| `HANDOFF-260522.md` | Mac session 2026-05-21 evening → 2026-05-22: picked up remote PR #54, fixed CI, simulator UX-polish batch, 9 new tours (catalog 20), merged PR #54, uploaded **TestFlight build 1.0 (5)**. Historical. | 2026-05-22 |
| `HANDOFF-260521.md` | Short handoff from a parallel 2026-05-21 remote session, written just after PR #54 merged (build 5 bumped but not yet uploaded). Superseded by the 260522 handoff, which covers the same ground through the build 5 upload. Historical. | 2026-05-21 |
| `HANDOFF-260520.md` | Consolidated end-of-day snapshot for 2026-05-20, covering both that day's sessions: (1) morning remote session — Times Square images, P1/P2/P3 audit cleanup, download retry, Option A data layer (landed on `main` via PR #51); (2) evening Mac session — home-screen UX batch, location-button rework, PlayerView carousel, MakerView fixes, ESB GPS fix, and the **TestFlight build 1.0 (4) upload**. Historical — superseded by the 5/22 handoff. | 2026-05-20 |
| `HANDOFF-260519.md` | Consolidated end-of-day snapshot from 2026-05-19. Covers both that day's sessions: (1) morning remote session — TestFlight prep code work + 8 new real tours + ROADMAP expansions; (2) evening local Mac session — Xcode signing setup + first Archive + Upload to App Store Connect (build 1.0/1 uploaded at 9:38 PM, awaiting Apple processing). Its "Tomorrow's queue" — complete TestFlight setup → install on iPhone → run M-qa checklist — is still the live TestFlight plan; read alongside the 5/20 handoff. The morning-only version of this file is preserved in `git log -- archive/HANDOFF-260519.md`. | 2026-05-19 |
| `HANDOFF-260518.md` | End-of-day snapshot from the 2026-05-18 session. Historical — superseded by the 5/19 handoff. Kept for context on what shipped that day (AllTrails-style home, unit test target wiring, session-start ritual, P0 audit closure). | 2026-05-18 |
| `pre-qa-audit-260518.md` | Pre-QA code self-audit (22 findings, P0–P3). Produced under PR #21; most P0 + 2 P1 findings closed by PRs #22 / #23 / #24. Remaining findings tracked as live checklist in `ROADMAP.md` § M-qa. | 2026-05-18 |

- [HANDOFF-260819-2.md](HANDOFF-260819-2.md) — the website restyled to the app's tokens; splash home page, /about/, pinned chrome.
- [HANDOFF-260819-3.md](HANDOFF-260819-3.md) — AMNH becomes a place (its tour's pin was 107 m off its own script), and the bottom bars stop painting as a floating island under a slide-up layer. TestFlight 1.1 (78), owner-verified.
- [HANDOFF-260819-4.md](HANDOFF-260819-4.md) — Universal Links finally work (gh-pages could never have served the association file — a project path has no root), every hero becomes a **ratio** rather than a fixed height (the old 320pt cropped 8% on one phone and 23% on another), and the list page is rebuilt on the place page with both maps collapsed into one shared `TourSetMap`. TestFlight 1.1 (77)→(81).
- [HANDOFF-260819-5.md](HANDOFF-260819-5.md) — the tour upload becomes a five-step wizard and three screens are deleted; plus the saved-tour watchdog hang: seven builds, six falsified diagnoses, finally caused by `Map(position:)` sitting on `.automatic` with no content to frame on the edit path only. **A crash log is one sample of a spin — diff the working path against the broken one.** TestFlight 1.1 (90), **owner device-verified — hang closed.**
- [HANDOFF-260819-6.md](HANDOFF-260819-6.md) — the Settings screen stops lying about itself: the wordmark matches the launch screen and is **actually** brass (a screen-wide `.tint` had been repainting it white for months while its call site said `mapPin`), the version reads the bundle instead of a hardcoded `1.0`, Makers becomes Dozents, and Cities/Countries are added — `country` going into the catalog data, not a Swift lookup, so it updates over the air. TestFlight 1.1 (85)→(86), owner device-verified, merged.
- [HANDOFF-260820.md](HANDOFF-260820.md) — the Library launch jitter (lists had no disk cache, so three stacked round-trips re-laid-out the tab on every launch) and the 1,418-tour linear scan behind every `by id` lookup. 
- [HANDOFF-260820-2.md](HANDOFF-260820-2.md) — the list page stops being the odd one out: its `…` was drawing its own ring and taking the environment accent (the session-99 wordmark bug in a second place), and the page itself becomes a slide-up layer with an X, from every entry point including a shared link. TestFlight 1.1 (94), owner device-verified, merged.
- [HANDOFF-260820-3.md](HANDOFF-260820-3.md) — Stripe's second request answered: a website URL and an optional free-text box. **The apex is a COMING SOON splash, so `/about/` is the URL that shows a product** — any future "send us your website" takes it. The box restates the four facts that decide every reading of the flag, Apple-as-merchant-of-record first. Owner submitted; Stripe's standing still cannot be checked from a session.
- [HANDOFF-260820-4.md](HANDOFF-260820-4.md) — Liked stops being a screen of its own: `LikedListView` is deleted and Liked renders through `TourListDetailView` with a `.liked` target, because two screens that must look alike will drift. Includes the owner decision that someone else's Liked cannot be bookmarked (Follow already is that), and a `dismiss()` leftover from #553 that left the layer up after deleting a list. TestFlight 1.1 (95), merged.
- [HANDOFF-260821.md](HANDOFF-260821.md) — the upload wizard's second review round: eleven owner notes across six steps. **`maxHeight: .infinity` inside the wizard's step area has never done anything** — a flexible child gets no room from a ScrollView sized by its own content — which caused three separate complaints; and the symptom only shows where the child has no size of its own, which is why step 3 was fine and reporting it as broken was an overstatement. Also: a TestFlight build that **succeeded and reported failure** because Apple rejected a `✕` in the notes *after* the upload had already processed. TestFlight 1.1 (97)→(98), owner device-verified. 

- [HANDOFF-260822.md](HANDOFF-260822.md) — the launch: a readiness-gated splash (build 99, owner-confirmed) and then eleven builds of hand-off animation, ending owner-approved at **1.1 (109)**. 🔴 Read it for the trap that generated most of those rounds: **`withAnimation` on a plain `Double` does not step through the intermediate values** — SwiftUI jumps every view to the end state and animates each rendered property across the whole duration, so every delay and window in the choreography is evaluated at 1 and discarded. Also: whichever body reads a 60Hz progress value redraws 60×/s (that was `ContentView` and `HomeView`), `.offset` does not move a `.background`, and two ramps meant to coincide will not under load. Carries the owner decisions and the rejected approaches.

- [HANDOFF-260822-2.md](HANDOFF-260822-2.md) — **Milan launches**: 48 tours and a 32nd maker, Atlas Studio MIL, live on both sources. **Two supplied coordinates were wrong and both were displaced due north — the Barcelona signature, recurring exactly as that handoff predicted** (one sat on a motorway; at a 30 m geofence neither tour would ever have fired). Four *other* coordinates looked wrong and were fine — a distance alone proves nothing, and reverse-geocoding the supplied point is what settles it. Also: a wrong hero fixed from the tour's own gallery, and `check-image-duplicates.py` printing OK after every fetch failed on SSL.

- [HANDOFF-260822-3.md](HANDOFF-260822-3.md) — light mode: the splash's ground was a hardcoded `.black` while `UILaunchScreen` is an empty dict, so **every light-mode launch flashed white → black → white**; and the two light surface values were **backwards** — the raised surface was the darker one, which is why all the chrome sat in a haze. Chrome is `#FFFFFF` over a `#F2F2F7` ground now, dark untouched. Carries why no fill value can fix the Home/map boundary, why the bars and the page they sit on must stay one colour, and why warm paper lost.

- [HANDOFF-260823.md](HANDOFF-260823.md) — the blank screen before the splash was **iOS's own launch screen**, and it now carries the brass mark in both appearances. Read it for the trap: **the rendered launch screen is cached so hard it survived reinstall and a build-number bump**, serving a stale asset while the file on disk said otherwise — only `simctl erase` cleared it. Also: `UIImageName` renders centred at natural size (measured), the launch screen follows the SYSTEM appearance and not the in-app picker, and the owner's subway report — **a downloaded tour brings its audio but not its photos**, which is the next thing to fix.

- [HANDOFF-260823-2.md](HANDOFF-260823-2.md) — the subway report: photographs did not load on weak reception. **The cause is not the splash** — gh-pages serves photos with `Cache-Control: max-age=600`, so after ten minutes iOS revalidates before using bytes it already has, and with no server the photo is dropped. **Do not make the splash wait for "everything loaded"**; with no signal that never completes. Shipped: a downloaded tour now carries its hero and stop photographs, in the tour's own folder where Clear Cache cannot reach them. 

- [HANDOFF-260823-3.md](HANDOFF-260823-3.md) — three fixes that each exposed a check which was not checking. **The coordinate fault measured and proven upstream** (pipeline cities 41/50 north at p=5.6e-06; old-sourced cities dead centred — that control is what makes the result believable), with the mechanism traced to a ~20px map-click offset that scales with zoom. **A byte check is not a duplicate check** — a JPEG and a WebP of one photo share no bytes, which hid three doubled Milan carousels and **five live wrong buildings** nobody had ever caught. And **architects 94 → 279**, with 24 candidates rejected because a mention is not authorship.

- [HANDOFF-260824-3.md](HANDOFF-260824-3.md) — the fullscreen video viewer, and a backend hazard it uncovered. **Three hit-testing traps with one symptom:** a control that renders, sits in the accessibility tree, and does nothing (a paged `TabView`'s page control spans its whole dot strip; two `.fullScreenCover`s on one view drop the second; a 24pt inset lands inside the Dynamic Island — and `GeometryReader` reports safe insets of **zero** once `.ignoresSafeArea()` is applied, so the obvious fix changes nothing). **Presence in the accessibility tree says nothing about hit-testing — probe the action.** Then: `TourVideoRole` so the play bar and a clip that IS the tour move together. And the big one — **`create or replace get_catalog()` now DESTROYS the place layer**, because the live RPC is a composition; four files in `backend/` still do it while claiming to be safe to re-run. **Shipped as TestFlight 1.1 (114)**; a landscape test clip now exists so rotation has something to run against, but **rotation on a real phone and a locked paid tour with a narration clip are both still unproven**.

- [HANDOFF-260823-4.md](HANDOFF-260823-4.md) — the other half of the subway report: **a failed request does not mean we do not have the photograph.** gh-pages sends `max-age=600`, so ten minutes on iOS revalidates before using bytes already on the phone — and offline that means discarding them. The failure path now reads `URLCache` directly. **Must stay tied to an actual failure; `.returnCacheDataElseLoad` would freeze photographs permanently.** Verified by A/B with a forced-failure switch, including the control that reproduces the grey box.

- [HANDOFF-260824.md](HANDOFF-260824.md) — the chrome row and the page under it were not the same colour, and the owner spotted the band. **The row drew `secondaryBackground.opacity(0.8)` over `.regularMaterial` — defeating the one token that is a hardcoded RGB pair specifically so every painted surface resolves identically.** ⚠️ Read it for the half nobody had named: **a material samples what is behind it, so the row's shade drifted as a photograph scrolled underneath**, which is why this read as vaguely off rather than as a bug. Fixed on all three canonical chrome rows; **do not reintroduce the material** — one token paints both surfaces on purpose.

- [HANDOFF-260824-2.md](HANDOFF-260824-2.md) — **Stockholm launched as the 33rd city/maker** (45 tours, Atlas Studio STO). **Three coordinates wrong, every one displaced due north**, none of which would have fired at a 30 m geofence. **The systematic upstream bias is gone** (p = 0.71 against Barcelona/Milan's 5.6e-06) **but the gross errors are still northward — half-fixed, not fixed.** **Swedish capitalises only the first word of a proper-noun phrase**, so `Gamla stan` and `Tekniska högskolan` must never be title-cased — and the supplied script headers get this wrong while the folder names get it right. Two heroes ship wrong-or-doubtful **by owner decision**, flagged for the drop's author.

- [HANDOFF-260824-4.md](HANDOFF-260824-4.md) — **link pins: someone else's TikTok, Reel or Short as a map pin that PLAYS INSIDE THE APP.** All three platforms publish an embed needing **no key, no registration, no app review** — verified live (`tiktok.com/player/v1/{id}` returned HTTP 200 unauthenticated; Instagram's tokenless oEmbed works again since Meta reversed the token requirement in June). **An embed, never a copy** — TikTok's API exposes no video-file field at all. **🔴 The ordering hazard: `TourKind` is a closed enum that THROWS on an unknown value, so one `kind: "link"` in the live catalogue silently stops every older build receiving catalog updates** — merge and build before publishing a pin. Three things made it far smaller than planned (`Stop` untouched; the geofence safe by construction; the map pin free). **A look-alike-domain bug — `tiktok.evil.com` passed — was caught by the tool's own self-test, in both the Swift and the Python.** Also: two migrations would not compile (`RAISE` arity), fixed and now covered by **`backend/test-migrations.sh`**, which runs them against a real Postgres. **The embedded player has never been on a screen — §4 is a paste-ready simulator fixture.**

- [HANDOFF-260825.md](HANDOFF-260825.md) — **the ordering hazard the entry above predicted actually happened, and this is the fix that reaches builds already shipped.** Four `kind: "link"` pins went live on 2026-08-24 at 22:51 and **froze every build before 116, silently** — one unknown value fails the WHOLE catalogue decode, and `RemoteCatalogLoader`'s `try?` reads that as a failed fetch. **🔴 An unknown top-level KEY is free; an unknown VALUE in a known field is fatal**, so pins now travel under a sibling `linkPins` array the app merges back at decode. **🔴 Tolerance cannot fix this — it protects only builds shipped after it, and build 66 is at Apple in "Waiting for Review", strict forever** (verified from its own source at `2bcf0df2`; its bundled catalogue is 1,350 tours against 1,516 live, missing all of Barcelona, Milan and Stockholm). Proven against the **real shipped catalogue**: build 66's transcribed decoder goes from 0 tours to 1,512. The backend migration **patches the core and never touches `get_catalog()`** — it renames `get_catalog_core` aside to `get_catalog_core_base` and wraps it, so nothing is parsed and nothing can be dropped; **⚠️ the new layer is deliberately SECURITY INVOKER**. **✅ SHIPPED AND CLOSED — [PR #597](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/597) merged `433879ce`, and `backend/split_link_pins.sql` was applied by the owner on 2026-08-25 and verified against the live RPC. Nothing is owed.**

- [HANDOFF-260825-2.md](HANDOFF-260825-2.md) — **the seatbelt for next time: decode tolerance.** Four fields had the fatal shape (`kind`, `primaryCategory`, `videoRole`, `triggerMode`). **🔴 Optional does NOT protect** — `decodeIfPresent` returns nil for an absent key or an explicit null but **propagates the throw** when the key is present with an unfamiliar value, so `videoRole` was exactly as fragile as the other three; pinned with a strict control enum so the point survives future changes. **Layer 1** gives each field a default chosen for what it *cannot* do (`triggerMode` → `.manual`, which is load-bearing: only `.geofenced` stops get a region, so an unknown rule can never auto-fire). **🔴 `kind` deliberately does not fall back** — an unfamiliar pin shown as `.single` gets a play button with no audio, so it drops instead. **Layer 2, the real protection:** every array decodes element by element. ⚠️ **The obvious implementation spins forever** — `currentIndex` is not guaranteed to advance past an element that threw; wrap each element in a type that cannot throw. ⚠️ Drops are **counted**, and two tests that encoded the old contract were changed deliberately with comments saying why. **🔴 Tolerance protects only builds shipped AFTER it — that is why the `linkPins` split is part 1.** **✅ SHIPPED — [PR #598](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/598) merged `d80465b0`.**
- [HANDOFF-260825-3.md](HANDOFF-260825-3.md) — **both catalogue PRs merged, the migration applied and verified, and old builds confirmed reading the live payload again.** **🔴 Nothing is owed on this thread — do not tell the owner to run `split_link_pins.sql` again.** **🔴 Both branches were ORPHAN commits** (no parent at all), which GitHub refuses with *"no history in common with main"* — and **`git merge-base` piped through `xargs` hides this**, printing HEAD when it finds nothing, which produced a confident wrong "no conflicts" call. Rebuilt with `git commit-tree` onto real parents, trees byte-identical. The verification that counts: **build 66's own decoder, from its source at `2bcf0df2`, replayed against the LIVE payload — 1513 tours / 1885 stops / 45 makers, clean**, with `places` and `linkPins` skipped as unknown keys. ⚠️ A first pass at that check reported 9,540 failures and was **the checker's own bug** — it swept up *computed* properties as decoded fields. **🔴 A live ordering hazard:** `restore_catalog_keys.sql` on the unmerged tracking-dashboard branch replaces `get_catalog_core` inline and **would silently re-freeze every old build if run now**. ⚠️ `publish-catalog` went green while the CDN served the stale file for ~15 min — verify a mirror by hashing the live URL. **Superseded by the entry below.**

- [HANDOFF-260825-4.md](HANDOFF-260825-4.md) — **a place page can be read as a grid, and its tour header becomes the profile page's.** Owner asked for the grid, then for the sort that was missing, then for the wording and colour: *"it should just look exactly like the example from profile page"*. Both controls move into shared components (`AtlasLayoutToggle`, `AtlasTourSort`) and `MakerView` is rewired onto them — the third time this repo has paid for a private view a second screen needed (`StopPin`/`ClusterPin` drifted 14pt vs 16pt; `AtlasChromeButton` exists because three pages grew the same button). **🔴 The shared sort is STABLE and that is load-bearing** — `Array.sorted` is not, and a place's tours tie on `createdAt` exactly (22 of 24 places), so an unstable sort would have thrown away `Place.ranked`'s tiebreaks the moment the page used its own default. ⚠️ **The hero reads the ranked order, never the sorted one**, or sorting by name changes the photograph at the top of the page. ⚠️ **Two deliberate 2026-08-18 decisions are retired on purpose** — the brass "N TOURS AVAILABLE" count and the *stated* "NEWEST FIRST" order; the note saying so sits at the row so neither is restored from a stale comment. **TestFlight 1.1 (119) → (120), owner device-verified: "works".** **Superseded by the entry below.**

- [HANDOFF-260825-6.md](HANDOFF-260825-6.md) — **fourteen TikTok/YouTube link pins, and the Dozents count that stopped meaning what it says.** One pin was sited on the wrong subject because the nearest Atlas tour was the building, not the market held inside it. `createdAt` is served on zero tours, which silently disables four sort controls. Pillow and the `gh` CLI are both absent from a fresh web container.
- [HANDOFF-260825-5.md](HANDOFF-260825-5.md) — **the same two controls reach list pages, and a list needed a sort state the other pages do not.** A maker feed and a place have only rules to order by; a list has an **arrangement somebody made by hand**, and Liked has an order that cannot be rearranged at all — so the stored criterion is `AtlasTourSort?` and **nil means the collection's own order**, where the page opens and where the menu can always return ("As arranged" / "Recently saved"). A sort offering only the four rules would have been a quiet way to lose a curation. **🔴 Edit mode forces the arranged order AND the row layout** — you can only rearrange what you can see in the order you are rearranging, and a photo grid has nowhere to put reorder arrows; both are computed, so leaving edit mode restores the reader's choice. ⚠️ **A tile has nowhere to put the curator's note**, the one thing a list row carries that no other row in the app does — which is why rows stay the default. ⚠️ Noticed but not acted on: **`backend/restore_catalog_keys.sql` reached main in #596** and would re-freeze old builds if run on its own (it carries its own 🔴 warning). **[PR #601](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/601) merged `3f0de399`; TestFlight 1.1 (121), owner device-verified.**

- [HANDOFF-260826-3.md](HANDOFF-260826-3.md) — **nine Brick Award link pins, and the letterbox bars every YouTube hero had been carrying.** Nine wienerberger films, each supplied with a venue name AND a coordinate, so nothing was parked. **🔴 YouTube's oEmbed always returns `hqdefault.jpg` — 480×360, the 16:9 frame LETTERBOXED onto a 4:3 canvas — so the square crop kept the bars and every YouTube pin before this has black bands baked across it**; fixed by preferring `maxresdefault` plus a deliberately conservative `trim_bars` (dark AND near-uniform, ≤30% an edge, implausible trims returned whole, pinned by a dark-textured-photo test). **NOT backfilled — the 29 older pins keep theirs, and a correction means a NEW filename (#567).** Also folds in the handle-suffixed hero slug #607 applied by hand, and adds `--title`/`--focus` for the judgement the tool cannot infer — an awards channel's house-format titles and photo-beside-text-panel thumbnails. **⚠️ All nine are ONE company on TWO channels, so the catalogue carries it as two creators.** **⚠️ Four heroes show the architect being interviewed, not the building — the tool cannot fix that, a pin re-hosts only the thumbnail.** 16/16 injected validator faults caught; 0 errors, 0 warnings.

- [HANDOFF-260826-4.md](HANDOFF-260826-4.md) — **ten Orlando TikToks; nine shipped, one dead at the source.** The owner sent URLs and nothing else, so **not one coordinate came from them** — each location was read out of the post's own caption, forward-geocoded and reverse-verified at zoom 18. **🔴 The tenth link resolves to a real TikTok id whose page reads "Video currently unavailable" and whose oEmbed returns an empty author, no caption and no thumbnail — no subject, no hero, no creator name, so it is reported rather than guessed at.** ⚠️ Two coordinates looked wrong to the reverse-geocoder and were right: Inter&Co Stadium resolves to the *Lymmo bus route* passing over it while the forward search names its own `leisure=stadium` relation, and Old Town resolves to "Celebration" while OSM's own record gives house number 5770, Kissimmee, 34746 — matching the caption's address exactly. ⚠️ **Evermore Orlando Resort is not in OSM as a named POI at all** and a guess would have been ~4 km out; the pin sits on Evermore Bay, which a bounded search did find and which is what the video shows. All nine heroes opened and read against their captions, six confirmed by lettering burned into the frame. ⚠️ **One hero was re-cropped by hand** — `render_hero` centres the square vertically and `--focus` only moves it sideways, so "ORLANDO HAS" was sliced through its letters. **🔴 Pinned creators (36) now outnumber Atlas studios (34) in the Settings → About count.** 33/33 injected validator faults caught; 0 errors, 2 warnings, both pre-existing on `main`.

- [HANDOFF-260826-5.md](HANDOFF-260826-5.md) — **eleven Orlando architecture TikToks; ten shipped, one unpinnable twice over.** **🔴 The eleventh is dead (oEmbed HTTP 400, page reads "Video currently unavailable") AND is a `/photo/` URL — and a TikTok photo carousel can NEVER be a link pin:** `derivable_embed()` returns `None` for `/photo/` and a player URL for the same id under `/video/`, because the app builds its embed from `/video/{id}`. **🔴 Two OSM ways are both tagged "1529 Vassar Street" 170 m apart, both road segments with interpolated numbers, and the top-ranked one reverse-geocodes to an elementary school on a different street** — resolved by reversing each candidate and reading the road back, which is the general lesson. ⚠️ Susuru was nearly placed 15 km wrong from a hashtag-inferred neighbourhood. Three captions name no place: two identified from painted signage in the frame, one from the architecture and then verified as Adjaye's Winter Park Library — whose OSM look-alike is the OLD library it replaced. All ten heroes opened, zero wrong subjects. Four architects verified and all four absent from the vocabulary (Schweizer, Adjaye, Johansen, Gamble Rogers II). 33/33 injected validator faults caught; 0 errors, 2 warnings, both pre-existing.

- [HANDOFF-260826-6.md](HANDOFF-260826-6.md) — **four Orlando architects join the vocabulary, 323 → 327** (Nils M. Schweizer, Adjaye Associates, John M. Johansen, James Gamble Rogers II), all verified against published records this session rather than recalled, and the four link pins that shipped the generic fallback now carry the name. **🔴 Both vocabularies edited** — `Models/Tag.swift` and `scripts/validate-tours.swift` each keep their own copy and editing one alone errors per tagged tour; asserted identical at 327. **🔴 `Designed by a Master` kept, not replaced** — `Tag.matches` performs no implication and the home shelf keys on that literal string; verified 0 named-architect tours missing it and 0 unused names. ⚠️ `Richard Rogers` was already present and is a different person; checked on normalised token sets. ⚠️ **A code change — CI is the only compile check and a simulator look is owed.** 33/33 injected faults caught; 0 errors, 2 warnings, both pre-existing.

- [HANDOFF-260827.md](HANDOFF-260827.md) — **twenty San Francisco architecture TikToks; nineteen shipped, the twentieth is gone from TikTok's own servers.** **🔴 A hashtag is not a location:** one caption reads only *"designed by frank lloyd wright … #sanfrancisco"*, and SF has exactly one famous Wright building (the V.C. Morris Gift Shop, which OSM names precisely) — but the frame is the **Marin County Civic Center in San Rafael**, 30 km north. **🔴 "Piazza Angela" does not exist; it is Piazza Angelo** at Trinity Place, named for Angelo Sangiacomo — caught only because the misspelling returned ZERO geocode hits, and the caption is kept verbatim so the error stays the creator's. One caption (*"Tours at 1pm!!!"*) names no place at all: identified from the frame as the **Internet Archive**. **The hero-slug handle suffix prevented THREE collisions in one batch** — `grace-cathedral`, `california-academy-of-sciences` and `chinatown` are all live Atlas tour stems on gh-pages. Two pins share one coordinate (two creators, one Legion of Honor) — correct, and what the stacked placecards exist for. Three heroes hand re-cropped: **`--focus` still has no vertical lever**, third batch running. All nineteen heroes opened, zero wrong subjects. 43/43 injected validator faults caught; 0 errors, 2 warnings, both pre-existing. **Merged as [#668](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/668)** (`a997860a`), CI green on all three jobs, verified live on the Supabase RPC and the gh-pages mirror (which lagged ~8 min behind). **Follow-up in the same session on owner instruction: the Chelsea Hotel becomes a place, 48 → 49** — nothing moved to make it (the pair became coincident when the tour was corrected), the hero is a **third photograph** from the tour's own gallery so place + both members are three distinct pictures, and `check-place-candidates.py` goes **4 EXACT → 3**. ⚠️ **A defect of my own caught there: all seven pin heroes shipped `…/images//file.webp`** — harmless (both forms 200, bytes hash-verified) but inconsistent with the other 276 pins and a `URLCache` key split; **pass `--image-base` without a trailing slash**.

**Active handoff: `HANDOFF-260829.md`** (session 121 — nineteen link pins from one
creator, and the four pins the owner pulled found still live in Postgres).
Previously: `HANDOFF-260828.md` (session 120 — thirty link pins; Luxembourg and
Norway join the catalogue), then `HANDOFF-260827-4.md` (session 119 — twenty-three
link pins) and `HANDOFF-260827-3.md` (session 118 — sixteen mixed-city
TikTok/YouTube links, all sixteen shipped). ⚠️ **Two content sessions ran in parallel on 2026-08-27 and both
wrote a handoff for that date**, so read `HANDOFF-260827.md` (session 116 — twenty
San Francisco TikToks, nineteen shipped) alongside it; the `-2` suffix is the
renumbering that resolved the add/add conflict, not a second draft of one file.

When the next handoff is written, name it for the new date
(`HANDOFF-YYMMDD.md`) and update the entry above to point to the
latest one as the "" Older handoffs stay as
historical entries, but only the latest is part of the session-start
ritual.


- [HANDOFF-260827-2.md](HANDOFF-260827-2.md) — **ten Atlanta TikToks; nine shipped, and the owner pulled the tenth.** The owner sent ten share links under the heading *"Atlanta Architecture"* and nothing else — no coordinates, no captions — so every location was read out of the post's own caption or frame, forward-geocoded against a bounded Atlanta viewbox and reverse-verified at zoom 18. **🔴 THE HANDLE SUFFIX ON A HERO SLUG STOPPED A LIVE OVERWRITE, one batch after session 114's comment predicted it:** gh-pages already carries `images/mercedes-benz-stadium_hero.webp` and `images/oakland-cemetery_hero.webp` from the Atlanta tour batch another session is staging, and **two of these ten pins are those same places** — a bare subject slug would have written straight over an Atlas tour's hero, and since #567 a downloaded tour reads its photographs off its own disk and would never have seen the correction. **🔴 The Garden Room was the thin caption and its address is the Warehaus trap:** the caption names a venue and no location, OSM has no Garden Room node, and of the three OSM candidates at its published address **the one literally tagged "88" reverse-geocodes to First Citizens Bank at number 79, across the street** — resolved by reversing each candidate and reading the road back, then closed independently by the hero, which has *"Dinner vibe at The Garden Room"* burned into the frame. **⚠️ Swan House reverse-geocodes to a car park and Fernbank Forest to a stranger's house, and both coordinates are correct** — each was proved to lie inside its own named OSM polygon by fetching the geometry and testing containment, so **a reverse-geocode landing on a road, a car park or a neighbour's house is not evidence of a bad point.** ⚠️ Cosm Atlanta is not in OSM at all and is pinned on The Interlock, the building it is inside. ⚠️ Fernbank Museum and Fernbank Forest are two pins 626 m apart on one campus, deliberately. ⚠️ **One hero is flagged rather than refused — the Mercedes-Benz Stadium frame may be AI-generated**: the subject is unmistakable but the halo crests are blobs and the streetscape is smeared, and a link pin re-hosts only the thumbnail so no other frame exists (the DIFC Gate precedent). All ten heroes opened, zero wrong subjects, six naming themselves in the frame. Three more architects verified and none in the vocabulary — Shutze, Portman, Gund. 40/40 injected validator faults caught; 0 errors, 2 warnings, both pre-existing.


- [HANDOFF-260827-4.md](HANDOFF-260827-4.md) — **twenty-three TikTok/Instagram links; all twenty-three shipped.** Eight arrived with a subject and coordinate, fifteen bare; none was dead and none was a `/photo/` carousel. **⚠️ The first batch with Instagram at scale** — six reels across four creators, against exactly one Instagram pin in the catalogue before today; all six play inline, and **all four Instagram makers ship `avatarURL: null` by design**, because Instagram's embed exposes no creator avatar (the tool's own selftest pins this). **🔴 THE CORRECTION WORTH CARRYING: this batch adds NO new country, and I wrote the opposite into the first commit message.** 22 is what the `tours` array spans — it is **not** the catalogue's; link pins already carried India, Mexico, Colombia, Ethiopia, Greece, Poland, Switzerland and Vatican City beyond it. Derived against `origin/main`: **30 countries before, 30 after.** Two platform-split figures in the first `STATUS.md` edit were likewise guessed and wrong (`79/11/9` against a real **83 TikTok / 11 YouTube / 5 Instagram**). **Re-derive every count.** **🔴 165 Crosby Street is the Warehaus trap again:** the forward geocode returned **Crosby Street the ROAD** with an interpolated house number, and Overpass finds **no addressed building on Crosby at all** — resolved by walking the street with reverse-geocodes and reading the road *and* the number (158 → 166 → 170 northward), placing the pin between them, 28 m north of the interpolated point. **⚠️ Two venues are absent from OSM entirely** (Hotel Komugi Skytree, Park Side Donuts), so the venue's own published address is the authority, resolved through **Japan's GSI national address geocoder** — a method that **validated itself on the third Tokyo pin**, where Yonemoto Coffee *is* in OSM by name and GSI agrees to **13 m**. ⚠️ **Three reverse-geocodes land elsewhere and all three are right** (ICC → the Ritz-Carlton inside it; Michelin House → Claude Bosi at Bibendum inside it; Lantern House → the High Line it straddles). ⚠️ **Cuadra San Cristóbal ships `Atizapán de Zaragoza`** though both caption and burned-in overlay say Mexico City — a different state, the Noisy-le-Grand shape. **All 23 heroes opened, zero wrong subjects**, twelve naming themselves in the frame; **Mount Everest Deli closed on three independent checks**, the last being **"-09"** legible on its own awning. **Three hand re-crops** — the vertical `--focus` lever `render_hero` still lacks, **fifth batch running** — because the centred square sliced the subject's own name on the Verrazzano and Park Side Donuts, and kept a stale asking price on Castel Béranger. **🔴 Three heroes flagged for the owner, not resolved unilaterally: Hugo de Grootplein is a PURE TITLE CARD** with no photograph of the place at all, and Yonemoto Coffee and Hotel Komugi show a person and an interior — none wrong, all weak, and unfixable because a link pin re-hosts only the thumbnail. ⚠️ **The Verrazzano caption self-declares "images were recreated"** (the Mercedes-Benz Stadium signature) **and its thumbnail is genuine** — checked at pixel level: a real double-deck span, period cars, a real tugboat wake. **Place candidate flagged, not created:** the Barcelona Pavilion pin is 77 m from the Atlas tour of the same subject (NEAR, never auto-created). **Gropius and Barragán are the two most conspicuous architect absences in the catalogue.** 54/54 injected validator faults caught; 0 errors, 2 warnings, both pre-existing on `main`. **No PR opened.**

- [HANDOFF-260827-3.md](HANDOFF-260827-3.md) — **sixteen mixed-city TikTok/YouTube links; all sixteen shipped — the first fully intact batch since the link-pin work began.** The owner supplied a subject name AND a coordinate for every link (the Brick Award shape), which removed the geocoding work and replaced it with verification work. **🔴 A SUPPLIED COORDINATE IS NOT A VERIFIED COORDINATE — one was 7.6 km wrong:** the Depot Boijmans Van Beuningen point sat on a residential street in **Bergschenhoek, Lansingerland**, a different municipality, and was corrected to Museumpark 24, Rotterdam (`51.9139529, 4.4711320`), which reverse-verifies onto the museum by name; at a 30 m geofence a link pin fires nothing, so this would have produced no error at all, just Rotterdam's most photographed building in a suburban cul-de-sac. **🔴 BOTH YouTube links "failed" and both are alive** — each resolved to Google's `/sorry/` interstitial from this datacenter IP, so `derivable_embed` returned `None`; the bypass is deterministic rather than a guess, because `youtu.be/ID` canonicalises to `youtube.com/watch?v=ID` and nothing else, and that URL satisfies `is_already_canonical` so the redirect hop is skipped entirely. ⚠️ **Do NOT "fix" this by adding youtu.be to `CANONICAL_HOSTS`** — the tour id is uuid5 over `sourceURL`, so the same video shared both ways would hash to two pins. **⚠️ The creator's own on-screen text beat their hashtags on the city:** the Espaces d'Abraxas frame reads *NOISY-LE-GRAND* while the caption says `#paris`, so the pin ships Noisy-le-Grand. ⚠️ **Three coordinates looked wrong and were right** — Iscte reverse-geocodes to the *adjoining* Universidade de Lisboa campus (the forward geocode puts it 77 m from Iscte's own address), Leinster Gardens reverse-geocodes to a hotel at number 30 while the house-number node for **23** is 7 m away, and Marriage Skate Shop is not in OSM at all but sits 24 m from the mapped 1631/1633 W Sunset Blvd. All sixteen heroes opened, **zero wrong subjects**, eight naming themselves in the frame. **One hand re-crop** (Abraxas, whose centred square clipped the subject's own name — **the vertical `--focus` lever is still missing, fourth batch running**) and one deliberate non-fix (the Depot's `MVRDV` overlay spans 785 px of a 1280 px frame, so **no square crop can contain it**; the California Academy precedent). ⚠️ **The Banksy pin is flagged, not resolved:** its caption says Mayfair, its coordinate reverse-geocodes to Waterloo Place ~1 km away, and it ships on the owner's coordinate titled for the object rather than the district — and as `Banksy-Signed Sculpture`, never "by Banksy", because the caption itself says the work is unclaimed. Three architects verified and absent from the vocabulary: **MVRDV, Raúl Hestnes Ferreira, Giancarlo Mazzanti**; `Louis Kahn` correctly NOT tagged on Iscte (a mention is not authorship). **Greece is the catalogue's 23rd country.** 39/39 injected validator faults caught; 0 errors, 2 warnings, both pre-existing. **No PR opened.**

- [HANDOFF-260829-3.md](HANDOFF-260829-3.md) — **twenty-three TikTok/Instagram links; all twenty-three shipped, from three creators (18 TikToks from `@thedesigndetourist`, 4 Instagram reels from `@shaunbirley`, 1 from `@meliluu__`).** 23 pins and 3 maker rows added — linkPins 201 → 224, makers 184 → 187 after merging the two parallel batches that landed first; tours unchanged at 1,552; **places 39 → 41** after merging the parallel Duddell Street place on the owner's follow-up instruction to make both double-pinned subjects places, which resolves both EXACT groups. Eight cities enter the catalogue, no new countries. **🔴 TWO VENUES HAD MOVED AND IN BOTH CASES THE STALE ADDRESS GEOCODED BEAUTIFULLY.** Tung Po left the Java Road Municipal Services Building in November 2022 for 2/F Konnect, 303 Jaffe Road, Wan Chai — and the North Point coordinate reverse-geocoded onto *"渣華道街市熟食中心 Java Road Market Cooked Food Centre"*, an exact named confirmation **of the wrong building, ~3 km away**; OpenRice and Yelp still carry the old address, which is the trap. Papaya King's 179 E 86th flagship closed in 2022 and reopened at **206 E 86th**, where OSM has a node literally named `Papaya`. **The durable rule: a reverse-geocode confirms a coordinate sits on a building with that name — it cannot tell you the business left.** **⚠️ TWO SUBJECTS ARE PINNED TWICE, deliberately, because both links were sent** — the Westin Bonaventure and Hotel Casa del Mar each carry two posts on one coordinate; perceptual scores of **42.5** and **58.3** prove these are different footage, not cross-posts (unlike Zacherlhaus), and `check-place-candidates.py` therefore gains **2 EXACT groups** over the base (5 against main's 3 at merge time) — expected, not a defect; one line removes either. **🔴 The handle suffix prevented THREE live-hero overwrites** — `bradbury-building_hero`, `la-union-station_hero` and `griffith-observatory_hero` are all live Atlas LAX tour heroes — which also makes those three, plus Petersen at 50 m, **place candidates** (**+4 NEAR pairs**, 15 against main's 11 at merge time; none created). **🔴 A THIRD parallel session is pinning the same creator and both create the identical `TikTok @thedesigndetourist` maker row** (`67CA14A6-…`, uuid5) — **whichever merges second must drop the duplicate or the validator errors**; 0 sourceURL overlap, checked. ⚠️ Their avatar regenerated byte-identically, so **24 files staged produced 23 additions** (the `@urbanistariel` case, third time). **⚠️ Beverly Hills City Hall:** Nominatim's *centre* for the polygon lands in a car park; the **area-weighted centroid** reverse-verifies by name. **⚠️ The one supplied location was a Plus Code** (`75HR+HV Causeway Bay`), decoded by a hand-rolled Open Location Code implementation — **two of four remembered test vectors "failed" and both were the recollection, not the arithmetic**; the decisive check was a published code decoding to the Eiffel Tower, and the recovered point sits **16 m** from OSM's `Shun Hing` node. All 23 heroes opened, **zero wrong subjects**, twelve naming themselves in frame. **✅ Three weak heroes and the non-inline pin were raised and CLOSED by the owner** — Papaya King and Shun Hing are talking heads and The Royal Hawaiian shows **Diamond Head, with the hotel absent from the frame**; all ship as made. **Re-running the open-every-hero audit will flag them again; they are settled.** **One hand re-crop** (Papaya King at vertical focus 0.72, recovering the creator's own *"Best Hotdog in NYC?!"* — the vertical `--focus` gap, **eighth batch running**). **⚠️ One pin will not play inline** — Instagram withholds the media file for the Tung Lung Chau reel's licensed music. **Every architect named is absent from the vocabulary** — Portman (twice, now the most conspicuous absence), Warren and Wetmore, KPF, Gensler, Roehrig, Charles Moore, the Parkinsons; **`Frank Gehry` IS in the vocabulary and is deliberately NOT tagged** on Beverly Hills City Hall, because the caption names him as the man who **lost** (the Sullivan rule). 40/40 injected validator faults caught; 0 errors, 2 warnings, both pre-existing on `main`. **Opened and merged on owner instruction; they review the flagged pins on device.**

- [HANDOFF-260830-8.md](HANDOFF-260830-8.md) — **an expand button on every inline map: back to the Home map, framed on what you were reading.** Owner: *"in the 'map' view i want to be able to click on a button that 'expands' the map which effectively takes me back to the home map view at the location of that particular item … with the placecard showing, similar to if i were to get to a tour by searching."* Every inline map is a **preview** holding only that page's own pins; this is the door back to the one map. **The landing is the one Search already writes** (`PendingMapMove`, session 102), deliberately — the map should not look different depending on which door you came through. **Owner decisions taken before any code:** a creator or list page raises **no card** (no single subject — it frames the lot), a place raises **its own place card**, not a member tour's (a site with ≥ 2 tours draws as ONE capsule pin, so a member's card would hang over a pin that stands for the place). **🔴 Setting state is not enough, and `performExpand` exists for the reason `performDismiss` does** — every surface with the button sits inside a UIKit layer that covers the main window, and SwiftUI can stop delivering updates to a covered hierarchy; the closure tears all four layers down directly (each `dismiss()` idempotent), and **the move is delivered on the next main-queue turn** because a synchronous write lands while the window is still covered. `DispatchQueue.main.async`, not `Task { @MainActor in }` — FIFO, so it cannot overtake the dismissals. **`HomeView` consumes from three triggers**, and `isActive` becoming true is the reliable one when Home was not the showing tab. **⚠️ One component for all four surfaces** — three previous map idioms were copied per surface and drifted. **⚠️ The expand region is deliberately NOT the inline map's region**, and `regionFraming` takes the **pin** coordinate (stop 0 for a walk), never the centroid — the session-102 drawer bug. **⚠️ Hidden, never inert**, where there is nothing to expand to. `test_sim` **578/578** (+8); driven in the simulator on tour detail, a place and a creator page. **🔴 It shipped bottom-trailing and the owner caught it within minutes** (*"dozent page map expansion doesnt seem to work"*): these maps sit in a scrolling page while the mini-player and tab bar render in a window **above** it, and the creator page's header pushes the square map down far enough that **its bottom third is under the bar at the resting scroll position** — the control rendered, sat in the accessibility tree, and was hit-tested by the tab bar (the `UIPageControl` family of #571). Moved to **top-trailing**, which is visible the moment any of the map is; in `TourSetMap` it also hides while a placecard stack is up, since those stack upward into that corner. **⚠️ The verification failure is the more useful lesson: I hit this myself, scrolled the map into view, watched it work, and wrote it up as "a harness artifact, not a product bug." A tap that only works after you scroll the control somewhere convenient is not a passing test** — verify a map control at the page's *resting* scroll position, on the page with the tallest header. **Owner device-verified on TestFlight 1.1.1 (138) — *"LOOKS GOOD"* — and merged as `01c70f63`.**
- [HANDOFF-260830-7.md](HANDOFF-260830-7.md) — **seven Instagram reels from one creator (`@nikola.matus`), and an Atlas tour that can never fire.** Fallingwater, Castell Gala Dalí at Púbol, Villa E-1027, Twin Palms, Casa Milà, the Marilyn Monroe house and the Chelsea Hotel. **linkPins 276 → 283 · makers 205 → 206 · tours 1,552 · places 45; no new country.** **🔴 THE FINDING IS PRE-EXISTING AND WAS ONLY VISIBLE BECAUSE A PIN COLLIDED WITH IT: the Atlas `The Chelsea Hotel` tour sits 290 m from the Chelsea Hotel and is geofenced at 60 m, so it can never fire.** Its own transcript opens *"You're on West 23rd Street, between 7th and 8th Avenues, outside the Chelsea Hotel"* while its coordinate reverse-geocodes to **315 West 21st Street** — no error, no dead link, the tour simply does nothing while you stand at the building. **✅ FIXED the same session on owner instruction** — stop 0 and the centroid moved to `40.7443742, -73.9968175`, which reverse-geocodes to `Hotel Chelsea, 222, West 23rd Street`; **the radius was re-derived, not inherited, and stays 60 m**, with **0 other geofenced markers within 500 m**. ⚠️ `check-place-candidates.py` **3 EXACT → 4**: the tour and pin are now coincident, a genuine place candidate, **not created**. ⚠️ **Do not read the resulting 290 m NEAR pair as a place candidate — it is one wrong coordinate.** **⚠️ 7 of 7 will not play inline** — `video_url` absent from every embed, six naming the same licensed track; the poster + `OPEN IN INSTAGRAM` fallback is correct, but this is the first batch where a creator's *entire* output is withheld, which is a curation question for the owner rather than a technical one. ⚠️ On one of the seven the tool's track-name regex mis-captured *"28K followers"*, so **its reason string is wrong while its verdict is right**. **🔴 `Le Corbusier` is deliberately NOT tagged on Villa E-1027 — the sharpest form of the Sullivan rule yet:** he is in the vocabulary, and the post's whole thesis is that he did *not* design it and was miscredited for decades; **OSM's own label even reads `Villa E-1027, Le Corbusier`**, so the misattribution is in the map data too. **Eileen Gray and E. Stewart Williams are absent from the vocabulary**, Gray conspicuously so. **⚠️ Casa Milà returned ZERO results for its own famous address** (OSM files it under Carrer de Provença, not Passeig de Gràcia) — *re-query before concluding a place is unmapped*, fifth batch running; the other six were named exactly first time, including **`Cursum Perficio`**, OSM's name for the Marilyn house taken from the Latin porch tiles the caption is about. **⚠️ Two hand re-crops — the vertical `--focus` gap, TENTH batch running** (Twin Palms 0.66, Casa Milà 0.80), both recovering the creator's own burned-in subject name; four others deliberately left alone because they lose a strapline, not a name. **⚠️ Two heroes are portraits of a person rather than a place** (Marilyn, Edie Sedgwick) — flagged, shipped, no other frame exists. 40/40 injected validator faults caught; **0 errors, 2 warnings, both pre-existing on `main`**; `check-place-candidates.py` **+2 NEAR, +0 EXACT**; all 7 live URLs hash-verified against the uploaded bytes. **No PR opened.**

- [HANDOFF-260830-5.md](HANDOFF-260830-5.md) — **every shared link previewed as a green sphere, and the tour photo could never have appeared.** Owner: *"the preview image that people receive is still of my very old green icon… i would really like for the preview image be of the thumbnail of the tour."* Two faults stacked: `og:image` was **hardcoded to the pre-#353 green-sphere placeholder** on gh-pages, never updated when the brass icon shipped; and **🔴 the per-tour title and description were patched from JavaScript after load — LINK-PREVIEW CRAWLERS DO NOT RUN JAVASCRIPT**, so every link previewed identically, and **nothing in that script touched `og:image` at all**. Fixing the icon alone would have shipped a correct-looking placeholder and still no tour photo. `/t/`, `/m/`, `/p/`, `/l/` are now one Vercel function reading a single Supabase row with the publishable key. **⚠️ THE STATIC PAGES ARE DELETED AND THAT IS LOAD-BEARING: VERCEL CHECKS THE FILESYSTEM BEFORE `rewrites`**, so leaving them silently wins. The card is server-rendered too — **the old page downloaded the whole ~7 MB `Tours.json` in the browser to show one tour**. **Measured gaps recorded rather than papered over:** Wikimedia **403s `facebookexternalhit`** while serving browsers 200, so **48 of 1,794 heroes** preview pictureless on Facebook/WhatsApp only, and **1,746 heroes are WebP**; **🔴 the one optimizer that fixes both was deliberately NOT built, because a misconfigured or quota-limited one breaks EVERY preview.** ⚠️ **A correction made mid-session:** the tag is always emitted — it is Facebook's own *fetch* that Wikimedia refuses. Also: **"Coming soon to the App Store" was still on all five pages and was the fallback description**, so a stale claim was riding along in previews (Dozent 1.1 live since 2026-08-28, confirmed against Apple's API, not this repo's notes); a reproducible `og-default.png` replaces the green sphere everywhere, including the site root, which had **no `og:image` at all**. ⚠️ **AASA untouched — iOS matches the path, and a rewrite is invisible to it.** ⚠️ **Vercel preview deployments are SSO-protected by default, so a `site/` change cannot be verified anonymously** — the owner disabled protection for the check. ⚠️ **A PR body with backticks inside `$(cat <<'EOF' …)` gets shell-expanded — use `--body-file`**; and **`statusCheckRollup` lags, so read the Actions run's jobs directly**. Verified on the preview *and* on production: 10 shape cases, a **40-entry catalogue sweep (40 real heroes, 0 fallbacks, 0 broken)**, and all six crawlers receiving the tag.
- [HANDOFF-260830-4.md](HANDOFF-260830-4.md) — **fourteen link pins from three creators; all fourteen alive, nothing parked.** Seven Sydney/Brisbane TikToks from `@mckernarchitecture`, five land-art TikToks plus one Instagram reel from Avant Arte, one from `@thedesigndetourist`. **linkPins 244 → 258 · makers 189 → 192 · New Zealand the 37th country.** **🔴 The sculpture is not where its own thumbnail was shot** — `Diminish and Ascend` moved Bondi → Waiheke → **permanently to the Christchurch Botanic Gardens in 2016**, and the creator's frame is the coastal Waiheke siting; the pin goes to Kiosk Lake, where the work now stands, and the mismatch is flagged rather than hidden. ⚠️ **A bounded viewbox is what found that coordinate** — `Kiosk Lake` unbounded returns nothing. ⚠️ **My own Instagram dead-post diagnosis was wrong and the tool was right**: the embed page came back at ~620 KB with zero `owner` occurrences and no thumbnail, which by CLAUDE.md's documented size test reads as dead — **the reel is alive**, so run `make-link-pin.py` before concluding anything. ⚠️ **Two Glasshouse Theatre pins share one coordinate, deliberately** (both links sent; interior and facade, not a cross-post — they were not even nominated as a perceptual pair), taking `check-place-candidates.py` **3 EXACT → 4** with **NEAR unchanged at 15**. ⚠️ **The `@thedesigndetourist` maker row already existed and uuid5 reproduced its id exactly**, so the duplicate was dropped and its avatar — byte-identical to the live file — excluded: **17 generated, 16 uploaded**. **Avant Arte now holds two rows, one per platform.** ⚠️ **Three heroes hand re-cropped — the vertical `--focus` gap, ninth batch running**; Sun Tunnels at 0.74 is the Walt Disney World Swan case, the subject nearly cropped out of frame. ⚠️ **My first filename-collision check fetched nothing and reported a pass** (the trees API returned no `tree` key) — redone by HEAD-checking all 16 live URLs. **Only `Frank Gehry` of this batch's architects is in the vocabulary**; Kalach, Candalepas, Snøhetta, Blight Rayner, Robin Gibson and the artists Ross, Holt, Okamoto, Oka and McCracken are all absent. 41/41 injected validator faults caught; **0 errors, 2 warnings, both pre-existing on `main`**.
- [HANDOFF-260830-2.md](HANDOFF-260830-2.md) — **the duplicate-image checker had never seen a link pin.** Not "could not scope to a batch," which is what six sessions in a row wrote down — `scripts/check-image-duplicates.py` reads `catalog["tours"]` and nothing else, so **all 244 link-pin heroes were invisible to it, `--all` included**, from the day #597 split them into a sibling `linkPins` array. Measured before the fix: **`--all` saw 5,595 images and 0 of the 240 pin heroes**, reporting success over a set it was not fully checking. ⚠️ **My own first description of this to the owner was wrong** — I called it a convenience gap and said nothing in the catalogue was wrong because of it; the catalogue *is* clean, but that is now a measurement (**5,835 images, 0 errors, 27 INFO, 0 fetch failures**), not an assumption. Adds **`--pins`** and makes **`--all`** honest; **`--maker <CODE>` stays tours-only deliberately**, because pinned creators' handles collide with city codes as substrings (**`STO` matches `@urbanstoriesyt`; 31 collisions catalogue-wide**). ⚠️ **A link pin carries no audio and `tour_slug` derives from `audioURL`**, so all 244 collapsed onto one empty slug until the hero-filename fallback. Byte-identical pin heroes error (a cross-post is two downloads and never byte-identical — it surfaces perceptually); **places stay out of scope**, since a place hero may legitimately be a member's hero at the *same* URL. **Selftest 13/13, and 3/3 injected faults caught**; the perceptual pass confirmed to have teeth — 165 pairs nominated, all rejected, closest 23.0 against a threshold of 8.0.
- [HANDOFF-260830.md](HANDOFF-260830.md) — **five architects join the controlled vocabulary (329 → 334), and one of them is the Brooklyn Bridge's.** Owner: *"add 5 architects to vocab"* — `Moshe Safdie`, `John Augustus Roebling`, `William Henry Barlow`, `KieranTimberlake`, `José Ignacio Linazasoro`, the five verified while wiring the 2026-08-28 batch. **🔴 BOTH VOCABULARIES WERE EDITED** (`Models/Tag.swift` **and** `scripts/validate-tours.swift`, asserted identical at 334) — editing one alone produces an error per tagged tour. **🔴 THE BROOKLYN BRIDGE TOUR HAD NO ARCHITECT AND NO SHELF TAG AT ALL**, while its own narration says *"the bridge that killed its designer"* — the #493 mirror-image defect on one of the catalogue's most famous structures; it gains both the name and `Designed by a Master`. ⚠️ **The tag is `John Augustus Roebling` though the tour says "John Roebling"** — the vocabulary follows published full names; `Brooklyn Bridge Park` keeps `Michael Van Valkenburgh` and gains nothing, because he made the park. ⚠️ **Where a subject exists as both an Atlas tour and a link pin, both were tagged** so the pair shares shelves — St Pancras takes Barlow **alongside** the `George Gilbert Scott` both already carried, since Scott built the hotel and Barlow the shed. ⚠️ **Two are judgements and reversible:** neither the Escuelas Pías nor the U.S. Embassy caption names its architect in text, and **the Embassy pin carried no generic tag and now gains one**, on the reading that its post is entirely about the building's *designed* defences. ⚠️ **A correction to CLAUDE.md — the 2026-08-28 entry claimed all five "ship the generic tag"; the Embassy had none.** **0 near-duplicates on normalised token sets, 0 unused names, 0 named-architect entries missing the shelf tag.** Also records that **`backend/pull_nycunfilteredstories.sql` HAS been run** — all four pins and both creator rows verified gone from the live RPC, closing the two-part removal. 44/44 injected validator faults caught; 0 errors, 2 warnings, both pre-existing. **⚠️ Nothing compiled locally — this is a code change and CI is the only compile check.**
- [HANDOFF-260829-4.md](HANDOFF-260829-4.md) — **twenty links, all twenty shipped; fifteen from one Hong Kong creator.** 18 Instagram reels and 2 TikToks; four carried location information, sixteen were bare, none was dead and none was a `/photo/` carousel. **🔴 THREE LINK-PIN SESSIONS WERE IN FLIGHT AT ONCE ON NEAR-IDENTICAL BRANCH NAMES** (`…-tbcerj`, `…-wa3e0g` = [#640](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/640), plus a third pushing 23 heroes straight to gh-pages), so **deduping against `main` alone is no longer sufficient** — source URLs were checked against the open PR's branch too, and re-checked after it merged; **`main` moved four commits mid-session** and the catalogue edit was redone by resetting to the new `main` and re-running the idempotent assembler, never hand-resolving a 942-line JSON conflict. **🔴 #640's BRANCH DIFF WAS 33 PINS, NOT 32** — it carried the Instagram Zacherlhaus the owner had pulled hours earlier in #641; flagged before the merge and **checked again after, where the pull held**. The durable point: a long-lived branch cut before a deletion carries that content forward, and **the diff, not the PR title, is what says so.** **🔴 THE PLUS-CODE DECODER WAS WRITTEN BY HAND AGAIN AND ITS FIRST ENCODE WAS WRONG** — and CLAUDE.md's "check the official vectors" warning was earned a second time in a **new** way: three of the four vectors written into the first self-test were **misremembered while the code was right**. Every expectation is now derived from the specification's own pair arithmetic, which then exposed a real bug: the float encode computes `0.375 / 0.05` as `7.4999…` and emits the wrong symbol two pairs later, so **encode is integer-only**, self-tested with 2,000 random round-trips. Both supplied codes reverse-verified **by name** — `P2QG+2W` → **Waterside Plaza**, `9XGH+RW` → **嘉道理碼頭 Kadoorie Pier**. **🔴 A PLACE CANDIDATE IS FLAGGED AND NOT CREATED: an Atlas tour of the Duddell Street Steps already sits 9 m from the new pin**, same subject and same name — the catalogue's tightest NEAR pair; the pin was deliberately **left on OSM's own node rather than moved onto the tour's coordinate**, because moving it would manufacture an EXACT group with no place behind it. ⚠️ **Eight heroes hand re-cropped — the vertical `--focus` gap, eighth batch running**; every source was 9:16, so the square is width-limited and `--focus` does nothing, and the centred band sliced the creator's own burned-in title. **Pan Pacific Park and Murray House were deliberately left alone** — what they lose is the creator's hook, not the subject's name. ⚠️ **Four heroes are weak and one badly so: the Bird Bridge pin's thumbnail is a red X the creator drew over a photograph** to retract an earlier 300k-view reel, so it renders as a red X on the map (the Hugo de Grootplein shape, and worse). ⚠️ **A bounded viewbox is what finds a Chinese place name** — `虎山` unbounded matched a bicycle-rental stand in Taipei — and the Guild Chapel's first query failed **because of the query**: it is on Chapel Lane, not Church Street. ⚠️ **Overpass is still unreachable from this container, all three mirrors, third batch confirming it**, which cost exactly one coordinate: Chai Wan Breakwater is anchored on the Public Cargo Working Area because OSM maps no breakwater there. ⚠️ **Two reverse-geocodes land elsewhere and both are right** (Kowloon Park's columns on the Nathan Road frontage; Sha Kiu Tsuen on the village public toilet its own caption names). ⚠️ **One post names no place and the hero answered it** — a caption that is only a joke over a frame reading `猛鬼美利樓／赤柱`: Murray House, Stanley. ⚠️ **The gh-pages Pages deploy was cancelled by the third session's push** and proved harmless the documented way: the commit is still an ancestor of the head and all 21 paths are in the head tree. **Piero Portaluppi and S. Charles Lee verified and absent from the architect vocabulary.** 39/39 injected validator faults caught; 0 errors, 2 warnings, both pre-existing on `main`. **No PR opened.**
- [HANDOFF-260830-6.md](HANDOFF-260830-6.md) — **an Instagram reel stops being cropped, and its fullscreen scrubber starts moving.** Owner: *"1. on the tour details page the crop of the thumnail/preview/play window 2. in full screen mode maybe the cropping of the video to avoid the instagram banners is ok, but the scrubber doesnt work"*. Both reproduced in the simulator first, both re-checked there after. **🔴 THE CROP: THE REEL WAS BEING SIZED BY THE PHOTO CAROUSEL'S SQUARE** — `instagramPlayer` passed `height: nil`, which means `AtlasSpacing.heroAspectRatio`, and that constant is **1.0**, so a **720×1280** reel was fill-cropped to a square and showed **the middle 56% of every frame** with the creator's own titles gone. ⚠️ **The `.aspectRatio(9/16, .fit)` already wrapped around the box did not save it** — the child imposes its own square and the square wins, so reading the call site alone suggests the shape was handled when it was not. ⚠️ **The fill is a deliberate owner decision and it stays — for the carousel**, where a clip is one page among photographs cropped the same way; **that reasoning is about a clip sitting BESIDE PHOTOGRAPHS and does not survive the trip to a link pin**, so fitting is a new opt-in (`fittedAspectRatio`) and `TourMediaCarousel` is untouched, re-checked on Shinsegae. ✅ **The 9:16 box adds no letterboxing of its own — measured, not assumed:** six live pins read with AVFoundation are all exactly **720×1280**, and the black bands some reels show are baked into the source. **🔴 THE SCRUBBER: THERE WAS NO CLOCK** — `scrubPosition` read `AVPlayer.currentTime()`, which is not observable, so SwiftUI never re-rendered and the elapsed label sat at `0s` for a whole 1m 49s reel; a periodic observer now samples it 4×/second. ⚠️ **Only gallery clips ever showed it** — a narration clip re-renders off the `@Observable` audio player, and **every link pin is a gallery clip**. Verified: bar **18s → 25s across seven seconds**; seeking proved with a temporary probe calling the production `seek(to:)` (`seek(60)` → player **61.9s**, bar **61.75s**) and Instagram's CDN answering `accept-ranges: bytes`. ⚠️ **The drag was never performed by a finger** — no coordinate-drag tool exists here and the scrubber is not an accessibility target — so a device check is the remaining confirmation. ⚠️ **Measured and NOT fixed: 20 of the 73 live Instagram pins have no playable file** (licensed music) and still fall back to the poster; **53 play**. ⚠️ **A simulator trap that is not a product bug:** when the bottom-module window misses its install, the fullscreen cover it hosts cannot appear, so the expand button renders, sits in the accessibility tree and does nothing — check the tab bar's presence before treating that as a video bug. `test_sim` **570/570**; opened as [#662](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/662), not merged.
- [HANDOFF-260831.md](HANDOFF-260831.md) — **an audit of un-placed place candidates, then seven of them built: places 49 → 56, and EXACT reaches zero.** `check-place-candidates.py` reported **4 EXACT / 12 NEAR**; a wider hand sweep (**every marker pair within 40 m, plus every pair within 200 m sharing a distinctive word — 86 pairs**) was run alongside it, because **the checker's NEAR tier matches on title containment and therefore cannot see Grand Central** — `{south, facade, grand, central}` and `{grand, central, terminal}` are neither a subset of the other, the same blind spot CLAUDE.md already records for Washington Square Arch/Park. Built: **Casa Milà — La Pedrera** (11 m), **Operaparken** (28 m), **Wave Hill** (63 m), **Grand Central Terminal** (88 m), each a tour + a link pin of one subject. **The Chelsea Hotel was the fifth and was left to the session already handling it — it merged as #669 mid-session**, so this branch was rebuilt on top of it by taking `main`'s catalogue and re-running the idempotent assembler. **🔴 24 of the 48 existing places are a walk paired with a single tour, so that pattern is the dominant place shape, and 47 of 72 walks belong to no place** — but reading each walk's **stop 0 title** settles it: only **Dreta de l'Eixample** (stop 0 *is* Casa Lleó Morera) and **The Monumental Rosary of Montserrat** (stop 0 *is* the Monestir) are genuinely the same site; *The Loop* sits 24 m from the Rookery while its stop 0 is **LaSalle & Adams**, and the other twelve have generic `"— Introduction"` stop 0s naming no landmark. **The pin moved and the tour did not, all four times** — proved by diffing the parsed `tours` array against `origin/main`, which is byte-identical. ⚠️ **Grand Central is the one judgement and follows the Petersen precedent:** the pin sat on OSM's own terminal node with the better address, but the tour's 88 m offset is a **deliberate vantage** its own text names (*"standing at 42nd Street and Park Avenue, looking up at the south face"*), so the place anchors on the tour and the **address** names the building. **🔴 Every place hero is a third photograph, opened and looked at, promoted from a member tour's own gallery — nothing sourced**; ⚠️ Wave Hill's was the **only** option (its gallery holds one image) and Grand Central's carries a **Christmas wreath**, chosen anyway because an establishing shot beats the alternative close-up. ⚠️ **The place-id slug does NOT fold accents** (`Hackesche Höfe` → `hackesche-h-fe`) — that rule reproduces **46 of 48** existing ids against an accent-folding rule's 44, and would otherwise have minted the wrong id for Casa Milà. **Flagged, not built:** Chichén Itzá (3 pins, a borrowed hero, and the Great Ball Court 224 m off — also invisible to the checker), Rosewood Mayakoba, Tribune Tower and Petit Palais (both tours cover *two* subjects, so the name is a decision). **Validator mirror self-tested 14/14 against injected faults**, then **0 errors, 2 warnings**, both pre-existing on `main`; `check-place-candidates.py` **3 EXACT / 9 NEAR** against `origin/main`'s 3 / 12 — **NEAR fell by exactly the three pairs resolved and EXACT is unchanged, so no new coincident group was manufactured.** Diff **76 insertions / 16 deletions**. **No PR opened.** **A second round on owner instruction added Chichén Itzá and Rosewood Mayakoba** — both pairs already exactly coincident, so **nothing moved**; **The Great Ball Court is deliberately excluded** and stays its own pin 224 m away. 🔴 **Both those heroes are BORROWED from a member and that is unavoidable** — neither site has an Atlas tour, every member is a link pin with an empty gallery, so the catalogue holds no third photograph (the Waterlooplein case, already closed by the owner); ⚠️ Chichén Itzá had exactly one usable frame, because the other member's thumbnail is a reaction screen. 🔴 **A Grand Palais place cannot be built:** it has one entry in the catalogue, and the pin beside it is the **Petit Palais** — a separate building 155 m across the avenue. ⚠️ And, because the owner asked: **a place's two members need not both be tours** — 19 of the existing places are a link pin beside a single tour, which is why one Petit Palais tour still makes a candidate. Final: **0 errors, 2 warnings**, both pre-existing; **0 EXACT / 9 NEAR** against main's 3 / 12. 🔴 **The last round, Casa Lleó Morera, closes the catalogue's final EXACT group — the Barcelona deferral that had stood since session 116 — so `check-place-candidates.py` prints "EXACT — none" and exits 0 for the first time ever, and CLAUDE.md's standing note that a clean exit is NOT the expected state has been corrected in place.** Its members are the single tour and the *Dreta de l'Eixample* walk, whose **stop 0 IS Casa Lleó Morera**; they were already coincident, so nothing moved, and the hero is a genuine third photograph — the corner facade, which neither member carries (the tour's hero is the interior tribune the owner confirmed keeping, the walk's an aerial of the grid). ⚠️ **Its coordinate reverse-geocodes to a metro passageway and that is correct** — the documented Barcelona opposite-pavement vantage, 31 m from its own OSM node.
- [HANDOFF-260901-5.md](HANDOFF-260901-5.md) — **the last three place cards, and Windsor Castle finally gets a photograph of Windsor Castle.** Grove at Grand Bay (3 pins), Bellevue (William O. Lockridge) Library (2), Vancouver House (2). **Places 82 → 85, and `check-place-candidates.py` reaches ZERO exact groups and exits 0** — the place backlog is empty, and **a clean exit is the expected state again**. All three are pure additions with **nothing moved**. ✅ **Every coordinate reverse-geocodes to its subject BY NAME**, so no polygon test was needed; two match text the creators burned into their own frames. ⚠️ **The Ribbon is a member of Grove at Grand Bay, not a place of its own** — its caption places it *"within the site"* (the Arab Hall shape, not the Beauchamp Tower exclusion). ⚠️ **All seven heroes were rendered and looked at**; Vancouver takes the **people-free** frame over the one showing the full twist, **trade-off stated not hidden**. ⚠️ **No storey count is asserted for Grove** — sources disagree, so it is left out rather than picked. ✅ **Windsor's borrowed hero is replaced with a sourced CC0 Round Tower photograph**, CC0 confirmed twice and **natively 4:3** so nothing is cropped or upscaled; **no CREDITS row owed**. ⚠️ **It is a photograph, not a print** — the PD-only hazard on a famous old landmark; rejected Rijksmuseum plates, a 17th-century painting, Yale drawings, **two Windsor Castle pubs**, and St George's Chapel (parked cars and a person's head). 🔴 **Wikimedia rate-limited this IP globally**: an error page gets written to the file and looks like a download, hand-built thumbnail paths return 400 while the MediaWiki API returns a correct `thumburl`, and ~8 s pacing with backoff is what works. Mirror **self-tested 40/40** then **0 errors, 2 pre-existing warnings**, plus **11 place-layer faults injected against the NEW places, 11/11 caught**; seed clean at 226 / 2,117 / 2,489 / 85.
- [HANDOFF-260901-4.md](HANDOFF-260901-4.md) — **Arthur Ashe Stadium and Banyan Tree Mayakoba become places, and seven slashes finally go.** Owner-instructed. **Places 80 → 82**, both **pure additions**: `makers`, `tours` and `linkPins` byte-identical to `main`, the 80 existing places unchanged, and **nothing moved** — both member groups were already exactly coincident, so the identity rule held with no pin relocated to make it hold (**32 insertions / 0 deletions**). 🔴 **Arthur Ashe is the point of the exercise**: four coincident pins against `HomeView.maxStackedPlacecards = 4` with **no headroom**, and **a place collapses its members into one capsule pin, so the cap stops applying** — the Windsor Castle lesson at the site it was flagged for. ⚠️ **The park's other markers are correctly NOT members** (the Unisphere, Queens Museum and Citi Field are separate destinations inside Flushing Meadows–Corona Park — the Great Ball Court precedent). 🔴 **The Ashe coordinate was proved by polygon, not by reverse-geocode** — it reverses to "Fountain Plaza", type `pitch`, so the point was tested against **OSM way 284859611's real 13-point ring: inside**, and the forward geocode returns that same way with its centre at **exactly** the pins' coordinate, which is why they are coincident at all. **Banyan reverse-geocodes to the hotel by name** and is **deliberately not merged with the Rosewood Mayakoba place 1,208 m away** (the Fernbank shape). 🔴 **Both heroes are borrowed from a member and that is structural** — every member is a link pin with an empty gallery, and a 5 km sweep found **no third photograph of either site**; count **16 → 18**, and **do not go sourcing replacements**. ⚠️ **All six candidates were rendered and looked at**, and **the renovation pin is excluded as a hero because it is an architectural rendering, not a photograph** (the Portsmouth Square precedent) — it stays a member. ⚠️ **Ashe's facts were verified, not recalled**, and **he won the first US Open at Forest Hills, not here**, dying in 1993 four years before the arena opened — the copy says so because the opposite was the easy error; his cause of death is not named. ✅ **The seven `@nikola.matus` double slashes are fixed and the catalogue now holds zero `images//`** — both spellings returned 200 so nothing was broken, but the byte-comparing duplicate checker reported **seven phantom groups on every run**, each printing one filename twice against a single hash; **proven by measurement, `--pins` goes 557 images with 7 groups → 550 with none**, and each corrected string was asserted equal to that pin's own hero before it was written. Mirror **self-tested 40/40** then **0 errors, 2 pre-existing warnings**, plus **10 place-layer faults injected against the NEW places specifically, 10/10 caught with the control clean**; place ids reverse-verified against **78 of 80**; `check-place-candidates.py` **EXACT 5 → 3, NEAR unchanged at 23**; seed clean at 224 / 2,106 / 2,478 / 82.
- [HANDOFF-260901-2.md](HANDOFF-260901-2.md) — **twenty-one link pins from nineteen creators, and a caption that named no place at all.** 8 TikTok + 13 Instagram; all 21 alive and pinnable, 0 already pinned. **linkPins 533 → 554 · makers 208 → 224 · tours and places unchanged.** Miami, Vancouver and Cozumel are new cities; no new country. 🔴 **The hero audit closed the one post whose caption names nothing** — *"Btw the marble in this place was 👌🏾"* over a frame reading **OSLO'S WALKABLE ROOF**, i.e. Snøhetta's Opera House, where the marble *is* the point; a second was identified as **Edward Meshekoff's 1957 Mosaic Map** on the Times Square NYPD sub-station, which is none of the four Times Square markers already in the catalogue. 🔴 **Arthur Ashe Stadium takes FOUR coincident pins — exactly `HomeView.maxStackedPlacecards`** — so all four are reachable today with no headroom, and a fifth would silently put one out of reach; a genuine place candidate, not created. ⚠️ **A separated placement was considered and rejected**: OSM maps no US Open food village by name, and inventing a coordinate to relieve a cap is the manufacturing the rules forbid. `check-place-candidates.py` **0 EXACT → 5, 22 NEAR → 23**, ending #683's clean exit — all five groups are subjects the owner sent twice, none nudged either way. 🔴 **A defect of my own, caught before the commit by reading the seed SQL: every URL shipped `images//`**, because the `--image-base` already ended in a slash; both forms 200, but `URLCache` keys on the string. ⚠️ **And CLAUDE.md's claim that session 125 fixed this is wrong — all seven `@nikola.matus` double-slash URLs are still on `main`.** Coordinates: 9 of 13 named exactly; **Arthur Ashe reverses to "Fountain Plaza" and was confirmed by testing the point against OSM way 284859611**, its own stadium polygon. Three hand re-crops (vertical `--focus` gap, 12th batch) recover each subject's own name. ⚠️ **Five weak heroes flagged** — the Grand Central Stones hero is the subway platform you start from, not the monoliths. Mirror self-tested **40/40**, then **0 errors, 2 warnings**, both pre-existing; gh-pages tree diff exactly **29 additions, 0 deletions**.
- [HANDOFF-260901.md](HANDOFF-260901.md) — **four place cards, and the Empire State Building moved onto its own address.** Empire State Building, Cathedral of St. John the Divine, Rockefeller Center and The Morgan Library & Museum, built from the four EXACT groups #682 left behind. **Places 76 → 80; `makers` byte-identical, exactly one Atlas tour changed.** 🔴 **The checker's groups were the smaller half of each site** — each was a coincident pin pair, but all four subjects also have an Atlas tour with a gallery and three have further pins the checker never grouped, so **sweep the catalogue for the subject rather than building from the group alone**. ✅ Every hero is a **third photograph** from a member tour's own gallery, nothing sourced, all four live 200, each rendered and looked at; Rockefeller's is seasonal, the Grand Central trade-off resolved the same way. 🔴 **The Empire State anchor was put to the owner and they moved it onto the building — settled, do not revert.** The pin reverse-geocodes to `350 5th Avenue, 10118`, the building's own address and dedicated ZIP; the tour landed on a Chase branch across the street, which its *"From 34th and Fifth"* opening makes a deliberate vantage, so it was built on the Grand Central precedent and flagged. The owner chose to move the tour: **the place sits on the building and the Atlas tour moved 92 m onto it**, while the two pins were already there and are byte-identical to `main`. 🔴 **The radius was re-derived, not inherited** — 30 m would not have reached the 92 m vantage the script sends you to, so it is **120 m**, precedented, with 0 geofenced markers within 500 m. ✅ **The stop was then set to `geofenced` on owner instruction.** 🔴 **It still changes nothing, and the reason generalises to 1,146 tours: `triggerMode: geofenced` on a SINGLE-STOP tour is inert in the current app** — `startMonitoring` runs only from `PlayerView.onAppear` and covers that tour's stops alone (no catalogue-wide background monitoring), and the same `onAppear` plays stop 0 first and seeds its id into `playedStopIds`, which `handleEntry` guards against. The field does real work only on multi-stop walks; **walking up to a landmark and having its tour start by itself is a capability the app does not have.** 🔴 **Exactly one Atlas tour changed in the catalogue**; makers byte-identical, 9 pins in four coordinate fields each, every entry `manual` so no geofence changed. ⚠️ **Membership went past the coincident pairs twice** — the Penn Station lamppost joins the cathedral (its caption places it *inside*), and the Rink and Lost Theatre join Rockefeller Center under the Windsor precedent — both reversible in one line. ⚠️ **`Dante at the Morgan Library` carries `#morganpartner`**, flagged and shipped on the Coca-Cola precedent. Place ids reverse-verified against 74/76; mirror self-tested 40/40 then **0 errors, 2 pre-existing warnings**, plus **3 place-layer faults injected against the new places, 3/3 caught** — a suite that predates the layer you changed is not evidence about it; `check-place-candidates.py` **EXACT 4 → 0, exits 0**, NEAR 24 → 22; seed clean at 208 / 2,085 / 2,457 / 80.
- [HANDOFF-260831-8.md](HANDOFF-260831-8.md) — **fifty link pins from `@hereinnyc`, and a Plus Code 970 m from its own subject.** 51 short links resolve to **50 distinct posts** (one pasted twice), all TikToks by the creator behind the 105-pin batch in #675, with **zero overlap** against the 483 pins already in the catalogue. **linkPins 483 → 533**; tours, makers and places unchanged. The maker row already existed and uuid5 reproduced its id exactly, so the pins merged into it and **the regenerated avatar — byte-identical to the live file — was excluded rather than overwritten** (51 generated, 50 uploaded). **🔴 The hand-written Plus Code decoder FAILED its own self-test and the bug was real** — the pair section's first place value is **20⁴, not `PAIRPREC`**, which put every decode ~105 degrees out. CLAUDE.md warns twice that the *memory* of a test vector is usually at fault and the arithmetic usually right; **this time it was the arithmetic**, caught only because every expectation is derived from the specification's own pair table rather than recalled. **Keep deriving them.** The passing self-test covers 8 codes against hand-derived corners, 3 published anchors, the documented integer-encode bug (`7FG49QGG+22`), 2,000 round-trips both directions, and `recover_nearest` across four hemispheres — including the **−74° pair boundary NYC straddles**, where three of this batch's codes correctly recover to `87G7` rather than `87G8`; a decoder that got that wrong would put pins in the Hudson. **🔴 A supplied coordinate is not a verified coordinate:** all 18 were reverse-geocoded at zoom 18, sixteen land on their subject (several named exactly by OSM), and two did not — **Pomander Walk was corrected 970 m** onto OSM's own footway, which reverse-geocodes to "19 Pomander Walk", while **the Will & Grace location was kept and FLAGGED**, its code landing at 547 Riverside Drive in Morningside Heights while the caption hashtag *and* the creator's burned-in text both say Upper West Side and nothing settles which building it is (the session-128 #9 rule: flag, do not override). **All 50 heroes were opened and read against their captions — zero wrong subjects**; the burned-in title cards settled four subjects the captions alone would not have (the urn is at **Grace Church**, the skyscraper is the **Bayard-Condict**, the theatre is the **Longacre**, the faded ad is **Griffon Cutlery Works**) and **found one post that is not in New York at all** — the Palazzo Vecchio carving, shipping `city: "Florence", country: "Italy"`, which is this maker's established shape. ⚠️ **Four pins flagged, not resolved:** the Tiffany Building's hero is **Venice** (the palazzo that inspired it, not the New York building — the likeliest pull), German Doner Kebab is a **paid partnership**, WildPlay Jones Beach is a **paid AD for a venue that has paused operations for 2026**, and the Weather Star's square crop **clips the star itself** (star at 9% of frame height, title card at 71%, a square covers 56% — they cannot both fit, so the creator's card was kept). **No hand re-crop was needed anywhere else, the first clean run in many batches.** ⚠️ **`check-place-candidates.py` goes 0 EXACT → 4 and now exits 1**, ending the clean-exit state #679 established: four genuine same-building pairs (Empire State, St. John the Divine, Rockefeller Center, the Morgan Library), all two-member and inside the 3-card stack cap, **none manufactured** — the pins were deliberately not nudged apart. ⚠️ **`Louis Sullivan` is still absent from the vocabulary and the Bayard-Condict is his only New York building**, but its caption names no architect so it correctly carries neither his name nor the generic master tag. ⚠️ **Two process traps paid for again:** `echo $?` after a pipe reported a **false pass** on the place check (the documented `PIPESTATUS` trap), and a transient TikTok CDN SSL failure killed the first generation run outright because the driver had no retry — it now checkpoints after every pin. Validator mirror **self-tested 40/40**, then **0 errors, 2 warnings** across 1,552 tours + 533 pins + 76 places, both pre-existing; diff **2,291 insertions / 0 deletions**; gh-pages `3eac368` tree diff exactly 50 additions, 0 deletions, nothing outside `images/`, and all 50 live URLs hash-verified against the uploaded bytes after the deploy — 50 ok, 0 bad. **CI green on [#682](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/682)**, opened and merged on owner instruction.
- [HANDOFF-260831-7.md](HANDOFF-260831-7.md) — **the two checks that could have caught the Natural History Museum, added to `check-image-duplicates.py`.** Owner: *"add the two missing checks"*. **🔴 The byte check is blind to this by construction, not by oversight** — everything the script did before today is about two DIFFERENT urls holding the same bytes (the Thyssen bug), while the NHM case is two entries pointing at the SAME url, and **one file hashed once is one file, so it never forms a group at all**; nothing anywhere touched `audioURL`. New `asset_refs` / `classify_shared_url` / `report_shared_urls`: a shared **`audioURL` is an ERROR** (audio is never legitimately shared — at most one holder can be right), **link pins from ONE `sourceURL` are INFO** (the `@malata.antwerp` five-pins-one-video case), **holders spanning two cities are an ERROR** (the slug-collision signature), **any `multiStop` holder is INFO** (a walk reusing a landmark's imagery), otherwise ERROR. ⚠️ **The one-source-post rule must stay BEFORE the city rule** — those pins are legitimately in five cities, so reversing the order turns the documented case into five false errors. ⚠️ **An empty `audioURL` must never group**: all 483 link pins carry one, so without the filter every pin lands in a single bogus finding. **🔴 Deliberately NOT scoped by `--maker`/`--pins`** — the NHM collision spanned two cities and two makers, so any convenient scope would hide the exact bug it exists to find; it runs on every invocation, costs nothing (pure JSON, no network), and **places stay out** because a place hero is *allowed* to be a member's hero at the same url. **Verification: 9 injected fault classes, 9/9 caught** — and **🔴 the two wiring faults were MISSED first time, for a reason that generalises: `--selftest` calls `sys.exit(selftest())` before `main()`'s body ever runs**, so neutering the call site (or the exit path acting on its result) is invisible to every logic case. **A check nothing calls is not a check** — the selftest now reads `inspect.getsource(main)` and asserts the wiring. ⚠️ One "MISSED" was the *harness's* own bug: the anchor `if stop.get("imageURL"):` appears twice in the file, so `replace(..., 1)` patched the wrong function; the harness now asserts anchor uniqueness. **Regression proof:** run over `521bbb5b` (the catalogue immediately before #680) it reports **8 errors** — all seven shared images plus the shared `natural-history-museum.mp3`; against the current catalogue **0 errors, 207 documented reuses**. ⚠️ It cannot see a collision that has not reached `Tours.json` yet — a gh-pages upload about to overwrite a live file is still the staging-time filename check's job (#567). Tooling only: 255 insertions, 0 deletions.
- [HANDOFF-260831-9.md](HANDOFF-260831-9.md) — **Dozent 1.1.1 submitted: build 139, ten new App Store screenshots, and two metadata fields that would have shipped wrong.** One submission (`4130a508`), **12 items** — the version plus all 11 pending IAP tiers, `releaseType` MANUAL. **🔴 Adding a version for review LOCKS ITS SCREENSHOTS and nothing in the UI says so** — `409 STATE_ERROR, "Can't Delete Screenshot while Ready For Review"`, with no `disabled` attribute anywhere and only dimmed thumbnails as a hint (which an editable version shows too). Do screenshots BEFORE `Add for Review`; past that, remove the version from the submission → replace → re-add, which the submission survives. ⚠️ **Text is NOT locked** — `description` and `promotionalText` both PATCH 200 in that state. **🔴 `promotionalText` does NOT inherit between versions and fails silently** — 1.1.1 inherited the description and screenshots but came back `None`, so submitting as it stood would have **blanked the promotional line on the live store page**; it is also the one field editable on a live app with no submission, so it now carries the catalogue size. **🔴 The standing "nine IAP tiers are missing review screenshots" note is wrong on both count and cause** — `images = 0` on all 14 including the three APPROVED, and the single `MISSING_METADATA` tier lacked an **availability record**; ⚠️ creating that did NOT clear it, the owner's image did, so the real gate is still uncharacterised. Ladder now 3 approved / 11 in review. **The description was rewritten and the count re-derived from the LIVE RPC** after the first draft **undercounted by 250** — written against a checkout `session-start.sh` had flagged as 13 commits behind; live was **1,553 tours + 533 pins = 2,086 on the map**, and the copy now says "More than 2,000 stops", deliberately **not** calling pins "tours". **The API beats the UI for screenshots** (deletion is hover-reveal, upload is drag-drop, neither has a DOM handle): all 10 md5s matched the local files, no duplicates, and **1.1 (live) was never at risk** — separate asset set. ⚠️ **Raw coordinate clicks are unreliable** (screenshot frame is scaled against the real viewport — two landed nowhere, silently; use `ref`), a long `type` timed out mid-stream while the text *did* land (**verify by checksum, not the success line**), and **ASC relationships return links not values** — `primaryCategory` read as "NOT SET" until queried directly (it is TRAVEL). ⚠️ **ADDENDUM 2026-09-01 — APPLE REJECTED IT (Guideline 2.1(b)) and the cause was structural.** Eleven of the twelve items could not be located because **zero tours are priced at 2.99–19.99** — `priceTier 99` sits on 66 walks and NULL on 1,487 tours, so those tier products had no purchase path. **A tier is only real once content uses it: submit each AS IT COMES INTO USE, never bank the ladder**, or review fails by construction. ⚠️ **Deferring costs nothing** — once a version is live, tiers submit on their own, no build, ~a day. **⚠️ The "options for creators" argument is true in design and false in the build**: `MakerTourService` writes `priceTier: nil` and there is **no maker pricing UI**, so the chicken-and-egg is **tiers ↔ the pricing UI**, not tiers ↔ content; the owner's point that price should track **quality not length** is right and my length-based ladder was the wrong frame. **🔴 REPLY TO APPLE BEFORE CANCELLING A SUBMISSION** — items cannot be removed once reviewed (`409 "Item was already submitted"`), so the route is cancel → new submission, **but cancelling closes the thread's reply box**; the explanation went into App Review notes instead. **Resubmitted version-only as `3e422d7d`, ONE item, 2026-09-01 13:54 UTC**, build 139 and listing unchanged, 11 tiers parked at `READY_TO_SUBMIT`. ✅ **Paid Apps Agreement accepted (owner-reported)** — no API exists for agreement state.
- [HANDOFF-260831-6.md](HANDOFF-260831-6.md) — **the London Natural History Museum tour had been playing the Los Angeles narration, and the reported gallery bug was the smaller half.** Both museums used the bare slug `natural-history-museum`, so both tours pointed at the **identical `audioURL`** — one file, one path. **The live file measures 129.7 s** against LA's declared 130 and London's 160, so London had played LA's narration since the LA batch overwrote it on **2026-07-15**: six and a half weeks, **no error, no missing asset, nothing any check would flag**. ⚠️ **The durable rule: when two entries share an image URL, check whether they share an AUDIO URL too** — one bare slug produces both. ⚠️ **A third tour was affected and was not in the report** — the `Albertopolis` walk shows the LA *T. rex* at its NHM stop, found by sweeping every reference to the eight URLs rather than assuming only the two named tours were involved. **Everything was recoverable from gh-pages history**: the pre-overwrite blobs are still in the object store, so all eight London assets came back **byte-identically** and were republished under `natural-history-museum-london` — **a new address, never an overwrite** (#567: a downloaded tour reads its audio off its own disk and would never see an in-place fix). **The recovered audio measures 159.9 s against London's declared 160**, which is what proves the right file came back rather than a plausible one. Repointed the London tour (audio, hero, stop image, 6 gallery), Albertopolis and the place hero; the LA tour drops the three London images and ⚠️ **deliberately keeps the bare slug**, which now serves LA content. All 11 image versions were **opened and identified**, not trusted by filename. gh-pages tree diff exactly 8 additions / 0 deletions; validator 0 errors, 16 warnings unchanged; `makers` and `linkPins` byte-identical, 3 tours + 1 place changed, URL fields only. ✅ **SWEEP RUN AND THE CATALOGUE IS CLEAN (read-only, same session).** Across all 1,552 tours and 483 pins: **0 shared `audioURL`** — the Natural History Museum was the only instance, so no other tour is playing the wrong city's narration — and **exactly 1 cross-city shared image URL**, which is the **documented deliberate** `@malata.antwerp` case (five link pins from one *Top 5 Italian antique markets* video sharing one thumbnail by design). ⚠️ **Neither check exists as tooling** — `check-image-duplicates.py` compares image *bytes* and structurally cannot see two entries sharing ONE URL, and nothing checks `audioURL` at all. Both are ~20 lines and would be worth folding into that script.
- [HANDOFF-260831-5.md](HANDOFF-260831-5.md) — **nine Tier 2 place cards for the pin-only clusters — places 66 → 75.** Windsor Castle · Hampton Court Palace · Hever Castle · Harvington Hall · Syon Park · Hatfield House · York Minster · The Guild Chapel · Blenheim Palace. **The `tours` and `makers` arrays are BYTE-IDENTICAL** — no Atlas tour is a member of any of these, asserted in the build — so no geofence is disturbed; all 10 moved pins are `manual`. **🔴 Windsor is the point of the exercise:** #674 deliberately spread six Windsor pins across three coordinates to dodge `TourSetMap.maxStacked = 3`, which would otherwise have left three unreachable on the creator's own page. **A place collapses its members into ONE capsule pin, so the cap stops applying** and all six go back onto the castle. **The durable lesson: when pins are being spread to dodge the stack cap, a place is the real fix and the spread is the stopgap.** **🔴 The hero is borrowed from a member in all nine, structurally** — no Atlas tour at any site and every member is a pin with an empty gallery, so **no third photograph of these sites exists in the catalogue** (the Waterlooplein / Chichén Itzá case the owner has closed). Borrowed-hero warnings go **6 → 15**. ⚠️ **Every frame this creator shoots has her in it**, so `blenheim-palace-aboutbuildings_hero.webp` is the **only clean architectural shot among all 23 candidates**; ⚠️ **Windsor has no picture of the castle at all** — an armour, three portraits, a drawing room and a postbox — so its hero is the least-bad and one sourced photograph would fix it. **Three membership judgements, each one line to reverse:** the **Tudor Kitchens** joins Hampton Court (64 m) on the Arab Hall / Elizabeth Oak precedent rather than the Beauchamp Tower exclusion; **Roman York** joins York Minster (coincident, and the Minster stands on the Roman `principia`) though its caption is only hashtags; and **the Temple of Diana is EXCLUDED** from Blenheim at 262 m — the Great Ball Court precedent. ⚠️ **The captions are almost entirely hashtags**, so unlike tier 1 there is no script to ground the copy in — each description is written from established fact about the site, with Hatfield saying *"tradition places her under an oak"* rather than asserting it. The Guild Chapel ships **Chapel Lane**, not Church Street (the documented session-122 finding). **Verification:** mirror self-tested 20/20, then **0 errors, 15 warnings** (6 pre-existing); `tours`/`makers` byte-identical and exactly 10 pins changed in exactly four coordinate fields; **`check-place-candidates.py` 8 EXACT → 1, 35 NEAR → 21** — ⚠️ **the one left is Gracie Mansion from #675**, never part of tier 2; ✅ **Gracie Mansion was then built too, on owner instruction — places 75 → 76, and `check-place-candidates.py` reaches ZERO EXACT.** Not part of tier 2 (it arrived in #675 from `@hereinnyc`): a **pure addition** with `tours`, `makers` and `linkPins` all byte-identical, both members already coincident so nothing moved, and its hero borrowed on the same structural grounds — though ⚠️ **both its frames are interiors with no person in them**, which is better than most of tier 2 managed. Its coordinate reverse-geocodes onto `Gracie Mansion, 181 East End Avenue` by name, and ⚠️ **its description deliberately drops the member caption's tour day**, which would go stale (the Grace Cathedral precedent). seed clean at 208 / 2,035 / 2,407 / 75; all nine hero URLs live 200.
- [HANDOFF-260831-4.md](HANDOFF-260831-4.md) — **ten place cards for the tour-plus-pin sites the 95-pin batch created — places 56 → 66.** Owner: *"do tier 1. for tower of london, leave beauchamp tower seaprate. for westminster abbey, only put in the 2 tours that are general 'westminster abbey', leave out the others. for houses of parliament, only group 'houses of parliament and big ben' and 'house of commons'"* — and **the three exclusions are the substance of the session, not a footnote.** The Beauchamp Tower stays its own pin (a specific building with its own story, not a member of the fortress); **only the two general Westminster Abbey entries join**, because the batch had deliberately put its other four abbey posts on distinct verified interior points precisely so the 3-card stack cap could never hide one — that work is what those coordinates were for; and Houses of Parliament takes the House of Commons pin but **not** Big Ben, whose tower the tour's own title already covers. ⚠️ **All three are the same shape — a famous site has more posts than it has subjects, and the owner decides which are the same place.** A future audit will read them as unresolved EXACT groups; they are settled. **The pin moved and the tour did not, nine times out of ten** (6.1 – 34.7 m, every pin `manual`, 0 makers changed). **🔴 THE ONE TOUR THAT MOVED IS A PRE-EXISTING ERROR, NOT A CONCESSION TO THE PIN: Leighton House sat 179 m away on Park Close**, a residential street, while its own script opens *"From the street, Leighton House looks like a fairly stern red-brick Victorian villa"*. It is `manual`, so nothing was firing there — but at a geofence this is the IAC / Chelsea Hotel failure again. `triggerMode` and radius byte-identical; only the stop coordinate and mirrored centroid moved. **Nine heroes are third photographs promoted from a member tour's own gallery** — nothing sourced, and the build **asserts** `hero not in member_heroes`, so a place cannot print one picture three times. ⚠️ **The Charles Dickens Museum ships `heroImageURL: null` deliberately** — the field is optional by design and falls back to the top-ranked tour's hero, and that tour's only spare image is a **19th-century engraving**, not a photograph of the house; an honest fallback beats a borrowed member hero. **🔴 FOUND, FLAGGED, NOT FIXED: the London Natural History Museum tour and the LA County one reference the IDENTICAL seven image URLs** — a bare-slug collision predating the handle suffix, so `natural-history-museum_2.webp` is **the LA museum** and `_4.webp` is **LA Union Station**, and both galleries have been mixing cities. It is why this place's hero is `_5`. **The fix is not a byte swap** — per #567 a correction means a new filename, or a downloaded tour keeps the wrong photographs forever. **Verification:** validator mirror (vocabulary from both Swift files, **385 tags**, refuses to run on disagreement) **self-tested 20/20** including all seven place-layer checks, then **0 errors, 6 warnings** across 1,552 tours + 483 pins + 66 places, **all six pre-existing**. `Tours.json` byte-stable before editing; diff **192 insertions / 40 deletions**. `check-place-candidates.py` **11 EXACT → 8, 41 NEAR → 35** — it resolved CaixaForum Madrid, The National Gallery and Leighton House + the Arab Hall. ⚠️ **The eight that remain are NOT the owner's exclusions**, which sit at deliberately distinct coordinates and never appear in EXACT; they are seven pin-only pairs from the 95-pin batch plus Gracie Mansion from #675, each still needing an owner decision. `seed_from_toursjson.py` clean at 208 / 2,035 / 2,407 / 66. All 9 place heroes live 200. **⚠️ Nothing compiled and CI has not run: no PR is open.**
- [HANDOFF-260831-2.md](HANDOFF-260831-2.md) — **ninety-five link pins from Alice Loxton (`@history_alice`, 92), Domus (2) and Rome Art Stories (1) — the largest batch to date by a wide margin, against a previous record of 35.** 96 links sent, **95 distinct posts**: two short links resolve to the same Winchester Great Hall video, and **the one kept is the one carrying the `3M7J+33` Plus Code**, so the dedupe cost nothing. **linkPins 283 → 378 · makers 206 → 208 · tours unchanged at 1,552 · places unchanged at 56.** All 95 pinnable — no dead posts, no `/photo/` carousels. **🔴 THE INSTAGRAM REEL IS NOT DOMUS** — it sat directly above the "Domus" heading and is `@romeartstories`, on the Sistine Chapel's *exterior*; read the payload, not the heading. `@history_alice`'s maker row **already existed** and uuid5 reproduced its id exactly, so it merged. **🔴 THE STRUCTURAL PROBLEM AND THE FIX: six posts are about Windsor Castle and six about Westminster Abbey, while `TourSetMap.maxStacked` is 3** (`HomeView` is 4) — and because the batch is essentially one creator, **her own maker page is exactly where six coincident pins would put three out of reach.** Solved with **distinct verified coordinates inside each site**, not a place page: Westminster Abbey's six sit on six interior points **each reverse-geocoded to confirm it falls inside the building** (nave, sanctuary → *Sir Isaac Newton*, the Confessor's shrine, the Lady Chapel → *Weston Tower*, the abbey node, and the supplied Pyx Chamber code), ⚠️ **a candidate at the Great West Door reverse-geocoded to Broad Sanctuary — the road — and was rejected**, which is what makes the other five trustworthy; Windsor's six split across the State Apartments (3), the castle node (2) and the Lower Ward. **Max coincident is 3, so nothing is unreachable.** **🔴 A NEW FAILURE CLASS: 13 posts whose oEmbed thumbnail TikTok's CDN will not serve** — 52 bytes of `{"code":5009,"error":"fail to make process filters"}`. **Not a dead post** (oEmbed returns full metadata), **not transient** (5 spaced attempts, all 13 failed), and **not a host or transform pattern** — working and failing posts share both, so it is per object. `make-link-pin.py` behaved correctly and refused rather than shipping a broken hero. **The fix is the SAME post's `originCover`**, read off the post page, which uses `tplv-tiktokx-origin` instead of `tplv-tiktokx-dmt-logom` and serves fine — fed in by **monkeypatching the tool's own `best_thumbnail`**, so the crop, pad, filenames and ids are all its code. **13/13 recovered; if it recurs, fold the fallback into `best_thumbnail` itself.** **ALL 95 HEROES OPENED** (tiles mapped through `heroImageURL`, never by slug prefix), catching **four wrong subjects the captions alone would have shipped**: #21 is **Avebury MANOR**, not the stone circle; #28 is the **Beauchamp Tower**; #91 is a **Charles I portrait**, not the Round Tower (and the perceptual pass independently scored it against #62 at **11.5**, the batch's closest pair); and #4/#16 name no place at all in their captions — they are the **Royal Crescent** and **Christ Church Cathedral, Dublin**, both named in the creator's own on-screen text. ⚠️ **Avebury Manor is the second subject this batch a bounded search called "not mapped" and an unbounded one with the county found instantly.** ⚠️ **Weak hero flagged not fixed: #88 Windsor Castle is a postbox.** **18 Plus Codes recovered** with a hand-written decoder **self-tested against four official vectors in BOTH directions plus 2,000 round-trips**, every one reverse-verified onto a named subject — including **Hougoumont Farm AND the Lion's Mound** for the two Waterloo posts, 1.2 km apart and genuinely different places; ⚠️ **`Al Qarna, Egypt` does not geocode** and had to be re-queried as `Al Qurna, Luxor`. **🔴 THE HANDLE SUFFIX PREVENTED SEVEN LIVE-HERO OVERWRITES — the most ever, against a previous record of three**: `westminster-abbey`, `national-gallery`, `natural-history-museum`, `leighton-house`, `churchill-war-rooms`, `charles-dickens-museum` and `caixaforum-madrid` are all live Atlas tour heroes. `avatar-tiktok-history-alice.webp` regenerated **byte-identically** and was **excluded rather than overwritten** — 97 generated, 96 uploaded. **`check-place-candidates.py` goes 0 EXACT → 10 and 9 NEAR → 30**; ⚠️ **#673 had just driven EXACT to zero, and this is not a regression of that audit** — every one of the ten is a same-subject pair the owner sent twice (Harvington ×2, Hatfield + the Elizabeth Oak, Syon ×2, York Minster + Roman York, **Windsor ×3**, Hampton Court ×2, Hever ×2, the National Gallery ×2, Leighton + the Arab Hall) plus **CaixaForum Madrid**, where the new Domus pin lands on the existing Atlas tour. Flagged, not created. ⚠️ **Both Domus subjects already exist as Atlas tours** (Ewha, CaixaForum) so both pins **match the tour's tagging** and the pairs share shelves. **Verification:** validator mirror — vocabulary parsed from **both** Swift files and refusing to run if they disagree or either parse is empty (**385 tags**) — **self-tested 20/20 against injected faults**, then **0 errors, 6 warnings** across 1,552 tours + 378 pins + 56 places, **all six pre-existing** (the same mirror against `origin/main` reports the identical set). **0** duplicate ids, **0** already-pinned sourceURLs, **0** byte-duplicate heroes. `Tours.json` byte-stable under a re-dump before editing and re-checked after **rebasing onto #673**. gh-pages `12b2764e`: tree diff **exactly 96 additions, 0 deletions, nothing outside `images/`**, deploy read **`in_progress`, never `cancelled`**, and after it landed **all 96 live URLs hash-verified against the uploaded bytes — 96 ok, 0 bad**. `check-image-duplicates.py --pins` **OK** over 381 images. **⚠️ Nothing compiled and CI has not run: no PR is open.**
- [HANDOFF-260831-3.md](HANDOFF-260831-3.md) — **one hundred and five link pins from a single NYC history creator (`@hereinnyc`) — the largest batch to date, against a previous record of 95.** 107 links sent, **105 distinct posts**: one pasted twice with different `web_id` parameters (stripped by `normalize_url`, so both canonicalise identically) and one pasted twice with Venice coordinates 2 m apart. **linkPins 378 → 483; tours (1,552), makers (208) and places (56) unchanged.** All 105 alive and pinnable — **no dead posts, no `/photo/` carousels, and no `5009` CDN refusals**, the failure class the previous batch hit on 13 of 95 thumbnails. **🔴 The creator burns a title card into every frame, and that is what made 105 tractable** — all 105 heroes opened and read against their captions, **zero wrong subjects**, and the burned-in text settled **four subjects the captions alone would not have**: Big Apple Corner (a `W 54 St` sign in frame), the Pearline ghost sign (a `2 Avenue & East 72 Street` subway sign), Jericho Cider Mill (its own address printed on the bag) and the Schinasi Mansion (caption names no building; reverse-geocode gives 351 Riverside Drive and the frame shows the marble mansion that stands there). **🔴 Two posts are different Park Avenue subjects and one's supplied coordinate reverse-geocodes to the other's** — a tunnel and a planted mall; the owner's point was kept and the tunnel pin moved to its south portal, putting ~190 m between two pins that would otherwise have sat 23 m apart under near-identical names. ⚠️ **`1 Cherry Street` unbounded matched Johnson City, 250 km upstate**, and the 91st Street Garden returned nothing by name at all; ⚠️ **three supplied coordinates reverse-geocode onto a neighbour and are all right** (Breakfast at Tiffany's lands on 164 E 72nd while the actual brownstone at 169 E 71st is **26 m** away). **🔴 The stack cap was checked and is clear — deepest coincident group 2 against `maxStacked = 3`** — which matters because the batch is essentially one creator. **🔴 Two heroes are the same vintage subway carriage** (perceptual Hamming **6 of 256**, diff **12.2**, where every other pair sits at 32+/30+) because the creator reused the footage, so **the Yankee Stadium pin shows a Transit Museum carriage** and no other frame exists; shipped and flagged with **one paid Coca-Cola advertisement** (`AD` in its own caption) and **the one building the batch could not name**, which ships under the creator's own frame title. ⚠️ **Seven hand re-crops — the vertical `--focus` gap, eleventh batch running**, and **a programmatic clipping detector was written and discarded as untrustworthy** (it measures "a dark card touches the bottom edge", not "text is cut" — it missed one entirely and flagged ~22 whose text was wholly legible), so all 105 outputs were eyeballed. **🔴 The handle suffix prevented one live-hero overwrite** (`empire-state-building_hero.webp`); the maker row already existed, uuid5 reproduced its id exactly, and the regenerated avatar was byte-identical and **excluded rather than overwritten** — 106 generated, 105 uploaded. ⚠️ **Exactly one architect tag** (`Giles Gilbert Scott`, named outright in his post) while **Wren, McKim Mead & White and Calvert Vaux are in the vocabulary and deliberately untagged** because no post names them. `check-place-candidates.py` **10 EXACT / 30 NEAR → 11 / 41**, the one new EXACT group being this batch's own Gracie Mansion pair — honest rather than manufactured, and **not nudged apart to dodge the checker**. Validator mirror **self-tested 42/42** against injected faults, then **0 errors, 2 warnings, both pre-existing**; **all 105 live URLs hash-verified against the uploaded bytes — 105 ok, 0 bad**; `check-image-duplicates.py --pins` **OK**. **No PR opened; CI has not run.**
