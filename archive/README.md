# archive/

Snapshots of working docs from earlier in the project. Naming
convention: `<original-name>-YYMMDD.md`, where the date is the day
the snapshot was archived (or the day it represents, for handoffs).

**Reading rule.** Most files here are historical — not read at
session start; anything still relevant to current work belongs in
`CLAUDE.md` or `ROADMAP.md`. **Exception: the most recent
`HANDOFF-YYMMDD.md` is part of the session-start ritual** (see
`CLAUDE.md` § "Session-start ritual"). It bridges the gap between
durable project rules and the specific mid-flight state from the
prior session — queued user feedback, what was about to be tackled,
tribal knowledge not yet promoted.

| File | Purpose | Date |
|---|---|---|
| `ACCOUNT-TRANSFER-260520.md` | **One-time orientation doc for the successor Claude on a different account.** Covers project overview, recommended reading order, state at transfer (code / content / TestFlight / Apple Developer), what's queued, working-style notes about the owner, and tribal knowledge worth carrying forward. Read once at the first session under the new account. | 2026-05-20 |
| `HANDOFF-260812.md` | **Active handoff.** Session 90, 2026-08-12 (web, content): **Cape Town launched as the 30th city/maker — the catalog's first South African city and Africa's second bureau.** New maker **Atlas Studio CPT** 🇿🇦 (`38456828-e395-5fd7-9525-a10329fabb15` = uuid5 `atlas-maker:cpt`), **30 single-stop tours, 30 MP3s**, 4,367 s (~1h13m). Catalog **1320 → 1350 tours / 29 → 30 makers / 1666 → 1696 stops**. **Ninth consecutive complete drop** (84 MB `/scl/fo/`, first try; MP3s already 44.1/128, 98 images already 1200×900, scripts 1–30 with no gaps, zero processing); rebased mid-session onto #491/#492/#494. **🐛 Chandler House's coordinate was 423 m off** — on Morris Street in the Bo-Kaap while the venue is the OSM POI at 53 Church Street; **at a 30 m geofence the tour would silently never have fired**, and nothing else catches that class. Re-geocoded, corroborated by the hero's "53"/"CHANDLER HOUSE" signage. All 29 others verified — **four reverse-geocode to a cross-street and are right anyway** (Fyn 10 m from 37 Parliament, Gin Bar 23 m from 64A Wale, Gigi on St George's Mall, Club Kloof 70 m from its own sibling restaurant), so the check must end in a distance, not a road name; ⚠️ Nominatim house numbers on Kloof Street are a street-centroid fallback (84 and 158 return the same point) and zoom 16 misreads Between Us — use zoom 18. **✅ Second consecutive clean open-every-image audit** (30/30 heroes + all 98 images; the two penguin tours, three mountains and two gardens checked as look-alike traps). **Heatherwick present + used by name** on Zeitz MOCAA; **Herbert Baker still absent after #492/#494's 21 additions** → `Designed by a Master`, and the name for the next architect PR. Dylan Lewis ships `city: "Stellenbosch"`; Cape Point/Boulders/Muizenberg are genuinely Cape Town. Sensitivity carried in six places; no mortality figures; no CREDITS rows. Validation **0/0 across 1350 tours** (mirror self-tested 43/43, vocab cross-check confirmed Tag.swift ↔ validator in sync at 59 architects); uuid5 reverse-verified 10 makers + 29/29 SYD pairs; all 128 asset URLs hash-verified against blobs; gh-pages pure plumbing (128 additions exactly, commit `5a4528f`, ⚠️ a publish-catalog run landed on top — verify ancestry); Tours.json diff **1,341 insertions / 0 deletions**. 1 tour hero-only (Maiden's Cove); Gigi's hero is the indoor room not the pool (gallery 2 — one-line swap). | 2026-08-12 |
| `HANDOFF-260811-2.md` | Session 89, 2026-08-11 (web, content): **Sydney launched as the 29th city/maker — Australia's second bureau, wired the same evening Melbourne merged.** New maker **Atlas Studio SYD** 🇦🇺 (`e676aade-c4c6-53dd-9141-48c46d3fe743` = uuid5 `atlas-maker:syd`), **29 single-stop tours, 29 MP3s**, 3,958 s (~1h06m). Catalog **1291 → 1320 tours / 28 → 29 makers / 1637 → 1666 stops**. **Eighth consecutive complete drop** (74 MB `/scl/fo/`, first try; MP3s already 44.1/128, 106 images already 1200×900, zero processing); rebased onto post-Melbourne `main` after #489 merged mid-session. **🐛 Camp Cove Beach's folder coordinate was ~30 km wrong (Broken Bay)** — re-geocoded to Watsons Bay via Nominatim (Tokyo precedent), double-confirmed against both supplied photos. Chinatown Country Club deliberately NOT in Chinatown (222 Clarence St — Nominatim + the hero's street sign both confirm). **✅ Open-every-image audit clean — a first** (29/29 heroes + 12 gallery spot-checks; the five look-alike ocean pools all verified distinct). Sensitivity: Harbour Bridge caption drawn from paragraph 2 so the opener's death toll stays script-only; Camp Cove's 1942 loss likewise. **⚠️ Utzon missing from the tag vocabulary** (Opera House ships `Designed by a Master`); SANAA present + used by name on AGNSW. Validation **0/0 across 1320 tours** (mirror self-tested 44/44, re-run post-rebase); uuid5 reverse-verified 8/8 incl. MEL; all 135 asset URLs hash-verified against blobs; gh-pages pure plumbing (135 additions exactly, commit `b40cd8a`); Tours.json diff **1,310 insertions / 0 deletions**; dup check clean, 106 = 106. 3 tours hero-only (Barangaroo, Fairy Bower, Freshwater). | 2026-08-11 |
| `HANDOFF-260811.md` | Session 88, 2026-08-11 (web, content): **Melbourne launched as the 28th city/maker — the catalog's first Australian city.** New maker **Atlas Studio MEL** 🇦🇺 (`117eebb3-5e5d-51e9-83cf-8ae0f3747bf0` = uuid5 `atlas-maker:mel`), **34 single-stop tours + the Federation Square walk (intro+6, 550 m), 41 MP3s**, 4,987 s (~1h23m). Catalog **1256 → 1291 tours / 27 → 28 makers / 1596 → 1637 stops**. **Seventh consecutive complete drop, the cleanest yet** — MP3s already 44.1/128, images already 1200×900, zero processing. The walk arrived with 7 content stops and no intro; Flinders Station wired as manual stop 0 (Chicago Riverwalk precedent). **🐛 The open-every-image audit caught a Royal Arcade clock (Gog & Magog) filed under Flinders Street Station — dropped pre-upload**, 145/146 images shipped, 0 orphans. Ando (MPavilion) + Kurokawa (Shot Tower cone) used by name; Grounds/Reed/Pitt/Peebles/Askew/McGlashan/Powell/Thompson/Hudson & Wardrop absent → `Designed by a Master`. Four Pillars + TarraWarra ship `city: "Healesville"`. Validation **0/0 across 1291 tours** (mirror self-tested 45/45); uuid5 reverse-verified 8/8; all 186 asset URLs hash-verified against blobs; gh-pages pure plumbing (186 additions exactly, commit `a6cc859`); Tours.json diff **1,689 insertions / 0 deletions**. | 2026-08-11 |
| `HANDOFF-260809.md` | Session 87, 2026-08-09 (web, content): **Chicago launched as the 27th city/maker — and the audio-pending queue is EMPTY for the first time ever.** New maker **Atlas Studio ORD** 🇺🇸 (`f34cd76e-1e41-5c38-865d-d8eccd775cd3` = uuid5 `atlas-maker:ord`), **25 single-stop tours + 5 walks, 53 tracks**, 6,238 s (~1h44m). Catalog **1226 → 1256 tours / 26 → 27 makers / 1543 → 1596 stops**. The last queue drain: staged 2026-07-29/30, narration arrived and wired same day. **First non-MP3 delivery — 53 WAVs (48 kHz mono, 600 MB), transcoded to 44.1 kHz/128 kbps MP3**; matched staging 1:1 incl. the five documented numbering gaps (the master list says 18/19/22/26/27 WERE drafted — a future second batch). **🐛 The open-every-image audit caught two wrong images PR #475's reuse-only audit couldn't:** tour 02's hero showed the WRONG BRIDGE (LaSalle-area bascule, not the DuSable; gallery `_2` was the real one → promoted, both walk reuses corrected) and riverwalk stop 1 faced east at the St. Regis instead of northwest at Wolf Point (→ `merchandise-mart_hero`, shot from the stop's exact position). Two byte-identical cross-tour pairs deduped; **93 referenced = 97 uploaded − 4 documented orphans**. Willis hero cleared the crop43 check. Eastland sensitivity honored end-to-end; Federal Plaza Calder-free; 🔴 Pilsen stops 1+3 ship with the owner-directed OPEN mural rights. Validation **0/0 across 1256 tours** (mirror self-tested 39/39 fault classes); uuid5 reverse-verified (staging READMEs' id forms were wrong — live convention used); all 53 audio URLs hash-verified against blobs; 93 images 200. gh-pages pure plumbing (53 additions exactly); Tours.json diff **1,675 insertions / 0 deletions**. | 2026-08-09 |
| `HANDOFF-260808.md` | Session 86, 2026-08-08 (web, content): **Buenos Aires launched as the 26th city/maker — the catalog's first Argentine city.** New maker **Atlas Studio BUE** 🇦🇷 (`64f37bdd-7cb4-5727-b525-6a801162ff9a` = uuid5 `atlas-maker:bue`), **34 single-stop tours + 2 walks, 46 MP3s**, 6,156 s (~1h43m). Catalog **1190 → 1226 tours / 25 → 26 makers / 1497 → 1543 stops**. **Not from the audio-pending queue** — arrived complete (audio + scripts + images in one Dropbox `/scl/fo/` drop, 111 MB, first try) and wired the same day; **queue unchanged: Chicago alone, 30 tours / 53 MP3s**. **Fourth zero-image-work delivery** (all 120 images already 1200×900, byte-distinct) and **the first drop with pre-structured walks** — `Multi Stop <name>` folders with per-stop coordinate subfolders; intro folders carry no coordinate, wired at the first stop's. The walks: `bue-uba-walk` "Universidad de Buenos Aires" (intro+3, 1.0 km, architecture) · `bue-tresdefebrero-walk` "Parque Tres de Febrero" (intro+7, 4.8 km, natureAndParks). **Script headers are TWO lines** (title + location), both proven un-narrated against the `_tts-safe` twin and stripped; strip on the `— Atlas Audio Tour` suffix, not the first em-dash. ⚠️ **First-ever cross-city slug collision:** Rio owns `catedral-metropolitana`, so BUE ships `catedral-metropolitana-bue`. ⚠️ **Three tours outside the capital** ship with their own `city` (Estación de La Plata / Museo de Arte de Tigre / Alo's Bistro → San Isidro), per the Campinas/Niterói convention. ⚠️ **The Argentine canon (Testa, Palanti, Meano, Tamburini, Catalano) is absent from the tag vocabulary** — `Designed by a Master` fallback; Calatrava's Puente de la Mujer uses the named tag. Validation **0 errors / 0 warnings across all 1226 tours** (Python mirror, vocab cross-checked between `Tag.swift` and the Swift validator, **self-tested 36/36 fault classes**); uuid5 reverse-verified against 4 live makers + the São Paulo walk-stop pattern; `check-image-duplicates.py --maker BUE` clean, **120 uploaded = 120 referenced, 0 orphaned**; **all 166 asset URLs hash-verified against uploaded blobs**. gh-pages via pure plumbing (166 additions exactly, 0 deletions, 0 pre-existing paths of 5,885 checked); Tours.json diff **1,766 insertions / 0 deletions**. ✅ No tour ships hero-only. | 2026-08-08 |
| `HANDOFF-260807.md` | Session 85, 2026-08-07 (web, content): **Marrakech launched as the 25th city/maker — the catalog's first African city.** New maker **Atlas Studio RAK** 🇲🇦 (`c4e51efc-846e-5e78-b699-67e7f9d203e8` = uuid5 `atlas-maker:rak`), **26 single-stop tours, 26 MP3s**, 3,093 s (~51.5m). Catalog **1164 → 1190 tours / 24 → 25 makers / 1471 → 1497 stops**. **Not from the audio-pending queue** — arrived complete (audio + scripts + images in one Dropbox `/scl/fo/` drop, 66 MB, first try) and wired the same day, like Rio/São Paulo; **queue unchanged: Chicago alone, 30 tours / 53 MP3s**. **Third zero-image-work delivery** (all 109 images already 1200×900, byte-distinct, `01` = hero) and **the cleanest scripts of any city yet — 0 `[beat]` markers across all 26**. **First Arabic-script city:** bilingual `English \| العربية` titles on 18 of 26 (proper-noun venues single-name per SGN convention); two supplied Arabic names cleaned (Nomad spacing, YSL folder-truncated final letter). ⚠️ **Aït Benhaddou is ~180 km southeast** (near Ouarzazate), ships with `city: "Aït Benhaddou"` per the Campinas/Niterói convention. ⚠️ **Studio KO not in the tag vocabulary** — and Marrakech is *their* city (YSL Museum + Sahbi Sahbi both ship `Designed by a Master`), sharpening the Schinkel/Niemeyer/Bo Bardi case. Validation **0 errors / 0 warnings across all 1190 tours** (Python mirror, vocab cross-checked between `Tag.swift` and the Swift validator, **self-tested 34/34 fault classes**); uuid5 reverse-verified against 7 live makers + tour/stop pairs; `check-image-duplicates.py --maker RAK` clean, **109 uploaded = 109 referenced, 0 orphaned**; **all 135 asset URLs hash-verified against uploaded blobs**. gh-pages via pure plumbing (135 additions exactly, 0 deletions, no pre-existing paths); Tours.json diff **1,191 insertions / 0 deletions**. ✅ No tour ships hero-only. | 2026-08-07 |
| `HANDOFF-260806.md` | Session 83, 2026-08-06 (web, content): **Berlin launched as the 24th city/maker** — **Atlas Studio BER** 🇩🇪 (`a0717b10-a295-5ab5-a875-d5a9587d0274` = uuid5 `atlas-maker:ber`), **31 single-stop tours + 5 walks, 57 MP3s**, 7,489 s (~2h05m) — **the largest single narration drop to date**. Catalog **1128 → 1164 tours / 23 → 24 makers / 1414 → 1471 stops**. **The first drain from the audio-pending queue since Montreal** (Rio, São Paulo and Dubai all arrived complete and jumped it); **queue is now Chicago alone, 30 tours / 53 MP3s**. Scripts + images had been staged since 2026-07-21, so zero image work. Delivered via a Dropbox `/scl/fo/` shared-folder link, 116 MB, first try — the shape that keeps working, unlike `/t/` Transfer links. **Three README deviations applied**: singles set `stop0.imageURL` to the hero (README says null); walk galleries drop whichever stop image is also the walk hero (else the validator hard-errors) — **both are the exact Dubai-era errors the tracker predicted would recur, and they did, and they are still unfixed for Chicago**; and 🐛 **`ghostline_hero.webp` is BYTE-IDENTICAL to `ghostline_stop4.webp`** (sha256-verified on the live URLs), which would have shown the same photo as the Ghost Line's cover and again as gallery slide 4 — **a class `validate-tours.swift` structurally cannot catch, since the URLs differ and both 200**. 🐛 **Imperial Spine stop 4 (Lustgarten) was staged with the wrong photograph** — `museum-island_hero` is the **Bode Museum from the water**, 600 m north, while the script names the Altes Museum colonnade, the Dom and the palace facade; now `museum-island_3`. Same finding as the then-unmerged [PR #475](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/475) 94-image reuse audit, **verified independently by opening both files**, after which **all 21 Berlin walk-stop images were opened and checked against their scripts** (other 20 correct, including the sensitivity-critical Große Hamburger and Topography images). ⚠️ The single-stop `museum-island` hero is still that Bode shot — defensible as a cover, one-line swap if the owner disagrees. Validation **0 errors / 0 warnings across all 1164 tours** via the Python mirror of `validate-tours.swift`, which **cross-checks its parsed vocabulary against `Models/Tag.swift` and raises on disagreement or an empty parse**, **self-tested against 30 injected fault classes — 30/30 caught**. uuid5 reverse-verified against 16 live makers plus a São Paulo single and all 7 stops of its walk; 0 duplicate ids across 1164/1471/24. **127 images uploaded = 127 referenced, 0 orphaned**; all **184** asset URLs live-checked 200. gh-pages via pure plumbing with both guards — none of the 57 paths pre-existed, tree diff proven exactly 57 additions / 0 deletions / nothing outside `audio/`; Pages lagged 404 but the Actions API showed **`in_progress`, not `cancelled`**, and assets were confirmed by hashing downloaded bytes against uploaded blob SHAs. `transcriptText` = each script minus its 42 `[beat]` markers — **no headers to strip at all**, the cleanest script set any city has delivered; shortest caption **63**. ⚠️ **Karl Friedrich Schinkel is not in the tag vocabulary** despite the Neue Wache, Altes Museum and Konzerthaus (nor Scharoun, Endell, Henselmann), while Foster/Piano/Mies/Calatrava are and are used — with São Paulo's gap, one combined `Models/Tag.swift` PR is now clearly worth it. ⚠️ `east-side-gallery_hero` is Vrubel's in-copyright mural on **German freedom of panorama** (§59 UrhG) — not the same footing as the Chicago Pilsen murals. ⚠️ 2 tours hero-only (Mauerpark, Nollendorfplatz). ✅ The `crop43` portrait-decapitation scare was checked and cleared — the Wasserturm is intact. | 2026-08-06 |
| `HANDOFF-260804.md` | Session 82, 2026-08-04 (web, content): **São Paulo launched as the 23rd city/maker** — **Atlas Studio SAO** 🇧🇷 (`b366d042-881b-5226-aaa8-1dce36c7a2cb` = uuid5 `atlas-maker:sao`), **41 single-stop tours + 1 walk, 48 MP3s**, 5,279 s (~1h28m). Catalog **1086 → 1128 tours / 22 → 23 makers / 1366 → 1414 stops**; Brazil now has two bureaus. **Not from the audio-pending queue** — like Rio it arrived complete in one Dropbox `/scl/fo/` drop (106 MB, first try) and was wired the same day; **Berlin and Chicago untouched**. The walk is `sao-ibirapuera-walk` "Ibirapuera Park" (manual intro + 6 geofenced stops at 40 m, 3.0 km) — Auditório → Oca → Museu Afro Brasil → Planetário → Pavilhão Japonês → Mirante MAC USP; unlike Dubai/Montreal it reuses **no** single-stop hero, so the hero-in-gallery trap applied only within its own image set. **✅ Second delivery running that needed zero image work** — all 173 images arrived already 1200×900, byte-distinct, `01` = hero per folder; 48 MP3s + 48 clean/TTS pairs, 1:1, nothing spare or missing; coordinates from the `output <Name> <lat>, <long>` folder names, no geocoding. **Two cities in a row like this is a pattern, not luck — a drop shaped this way turns a multi-session city launch into one session.** Coverage splits between the modernist canon (MASP, Copan, SESC Pompéia, Casa de Vidro, Pinacoteca, Memorial da América Latina, the Ibirapuera ensemble) and the restaurant/bar/design scene (A Casa do Porco, Nelita, Jacó, KOTORI, Hirá, Varal, Dōmo, Misci, Monica Pondé, VERNIZ). **⚠️ The missing-architect-tag problem is far sharper than Rio's**: Lina Bo Bardi ×4, Paulo Mendes da Rocha ×3, Niemeyer ×3 plus the whole walk, Vilanova Artigas ×2, Ramos de Azevedo ×3, Rino Levi ×2, Burle Marx — all absent from the vocabulary and shipping as `Designed by a Master`, while `Kengo Kuma` (Japan House) and `Jean Nouvel` (Cidade Matarazzo) **are** in it and used by name. Adding the rest is a `Models/Tag.swift` **code** change needing owner OK + sim review, so again kept out of a content PR. **⚠️ Mercado Municipal de Campinas is in Campinas** (~90 km away), shipped with `city: "Campinas"` per the Niterói/Củ Chi convention. **⚠️ MAC USP appears twice deliberately** — single-stop tour about the museum, walk stop 6 about its public rooftop; different scripts, ~30 m apart, not a duplicate. **⚠️ 4 tours ship hero-only** (Estádio do Morumbi, Feira Benedito Calixto, Nubank Parque, Residências Vilanova Artigas). Validation **0 errors / 0 warnings across all 1128 tours** via the Python mirror of `validate-tours.swift`, which now **cross-checks its parsed vocabulary against `Models/Tag.swift` and raises if they disagree or either parse is empty** (closing the Dubai failure mode) and was **self-tested against 26 injected fault classes — 26/26 caught**. uuid5 reverse-verified against live RIO/DXB/YUL/ROM/MAD, a live tour/stop pair and a live walk's stops; 0 duplicate ids. gh-pages via **pure plumbing** (commit `2984f48`) with both guards repeated — **none of the 221 target paths pre-existed** and the **tree diff was proven exactly 221 additions, 0 deletions, nothing outside `audio/`+`images/`**. Pages lagged, serving 404; the Actions API showed **`in_progress`, not `cancelled`**, and assets were confirmed by hashing downloaded bytes against the uploaded blob SHAs. **Reusable finding: don't copy the first tour's key order** — the first single in `Tours.json` is unrepresentative; matching the dominant convention (explicit nulls + full-precision centroid) made the diff **1,989 insertions / 0 deletions**. `transcriptText` = each script minus its `— Atlas Audio Tour` header and every `[beat]`; **two header shapes** (singles `# Name — …`, walk stops bare `Name — …`). Captions span paragraphs; shortest shipped exactly **60**. **Queue unchanged: Berlin (36 / 57) + Chicago (30 / 53) = 66 tours / 110 MP3s** — the two Dubai-era staging-README errors remain unfixed and will recur on both. | 2026-08-04 |
| `HANDOFF-260801.md` | Session 81, 2026-08-01 (web, content): **Rio de Janeiro launched as the 22nd city/maker** — **Atlas Studio RIO** 🇧🇷 (`016bddbe-c759-56fd-8558-869df74b179b` = uuid5 `atlas-maker:rio`), **46 single-stop tours, 46 MP3s**, 5,102 s (~1h25m). Catalog **1040 → 1086 tours / 21 → 22 makers / 1320 → 1366 stops**. **Not from the audio-pending queue** — Rio arrived complete (audio + scripts + images in one Dropbox `/scl/fo/` drop, 106 MB, first try) and was wired the same day; Berlin and Chicago untouched. **✅ The cleanest delivery yet, and the reason is nameable: all 149 images arrived already 1200×900**, byte-distinct, numbered `01..NN` with `01` = hero — so the image pipeline, cropping, sourcing, owner picks and the Gemini gate were **all zero work**, the step that normally dominates a city launch. 46 MP3s + 46 clean/TTS script pairs, 1:1, nothing spare or missing; coordinates came out of the `output <Name> <lat>, <long>` folder names (the HCMC convention) so no geocoding. Coverage splits between landmarks (Christ the Redeemer, Sugarloaf, Maracanã, Theatro Municipal, Museu do Amanhã, Escadaria Selarón) and the boteco/restaurant scene (Jobi, Bip Bip, Guimas, Oseille, Balcão 201). **⚠️ Oscar Niemeyer is NOT in the controlled tag vocabulary** despite five Rio tours being his — Reidy, Portzamparc, Costa and Burle Marx likewise absent; those carry **`Designed by a Master`** as the honest fallback, while `Le Corbusier` and `Santiago Calatrava` **are** in the vocabulary and are used. Adding him means editing `Models/Tag.swift` — a **code** change needing owner OK + sim review — so it was deliberately kept out of a content PR. **⚠️ Two tours are in Niterói** (MAC Niterói, Teatro Popular Oscar Niemeyer — both Caminho Niemeyer), shipped under the Rio maker with `city: "Niterói"` per the Kyoto/HCMC convention. **⚠️ 4 tours ship hero-only** (Álef Antiguidades, Bip Bip, Hotel Nacional, Santa Teresa Tram); 7 more have hero + 1. Validation **0 errors / 0 warnings across all 1086 tours** via a Python mirror of `validate-tours.swift` that **parses the vocabulary out of the Swift source and raises on an empty parse** (the Dubai near-miss) and was **self-tested against 23 injected fault classes first — 23/23 caught**, with the baseline asserted clean so no fault could hide behind a pre-existing error. Its one real finding was **fixed, not suppressed**: Prainha had no Theme tag → `Maritime`. uuid5 reverse-verified against live DXB/YUL/ROM/MAD **and** a DXB tour/stop pair; 0 duplicate ids; `check-image-duplicates.py --maker RIO` clean over 149 images; all 46 MP3s byte-distinct. gh-pages via **pure plumbing** (commit `76d86e1`) with two guards worth repeating — **none of the 195 target paths pre-existed** (nothing could be silently overwritten) and the **tree diff was proven to be exactly 195 additions, 0 deletions, nothing outside `audio/`+`images/`**. Pages lagged ~5 min serving 404; checked the Actions API and found the run **`in_progress`, not `cancelled`**, then confirmed **all 195 assets by hashing the downloaded bytes against the uploaded blob SHAs** — not by a 200. `transcriptText` = each script minus its single `[beat]` (46 of them); captions span paragraphs, shortest shipped **73** chars. **Queue unchanged: Berlin (36 / 57) + Chicago (30 / 53) = 66 tours / 110 MP3s** — and the two Dubai-era staging-README errors are **still unfixed** and will recur on both. | 2026-08-01 |
| `HANDOFF-260731.md` | Historical (superseded by `-260801`). Session 80, 2026-07-31 (web, content): **Dubai launched as the 21st city/maker** — **Atlas Studio DXB** 🇦🇪 (`e94b8814-2c31-5113-963c-1743e6c86b4b` = uuid5 `atlas-maker:dxb`), **26 tours = 22 single-stop + 4 walks** (Creek Crossing, Old Quarter, Downtown Loop, Marina & JBR), **40 MP3s**, 5,012 s (~1h24m). Catalog **1014 → 1040 tours / 20 → 21 makers / 1280 → 1320 stops**. Fourth city wired straight from the audio-pending queue; the delivery matched the staging **1:1** — nothing spare, nothing missing. **✅ Transport settled: a Dropbox SHARED-FOLDER link (`/scl/fo/…`) downloads headlessly with `dl=1`** — 82 MB zip, first try, exactly as Ho Chi Minh City did. **Do not confuse it with a Dropbox *Transfer* link (`/t/…`), which cannot** — that was Montreal's four-attempt fight, along with SharePoint work-tenant links and Chromium being unable to reach the network at all. **Check the URL shape before declaring a link undownloadable.** **⚠️ Two staging-README errors found and corrected, both of which will recur on Berlin and Chicago:** (1) the batch README says singles take `stop0.imageURL: null`, but Montreal/Rome/Madrid set it on **100%** of their singles — Dubai now sets it to the tour hero; (2) each walk README lists `additionalImageURLs` as *every* stop image, but each walk's hero is picked from among those same images, so that spec **hard-errors** the validator's `heroImageURL also appears in additionalImageURLs` check and would show the same photo twice — Montreal already drops it (6 stops → 4 entries), Dubai now does too. **Fix those READMEs before the next city wires in.** **⚠️ The Python mirror validator nearly lied:** its first revision parsed **zero** tags because `load_vocab` matched `(facet:…, tags:…)` while `Tag.swift` writes `(.placeType, [ … ])` — it emitted 4,687 bogus errors, which is the only reason it got noticed; a subtler parse failure would have passed everything. **Assert a non-empty parse, don't just run it.** Final **0 errors / 0 warnings across 1040 tours**, self-tested against **18 injected fault classes (18/18 caught)**; 0 duplicate tour/stop/maker ids; uuid5 reverse-verified against live YUL/ROM/MAD/AMS/SGN; `check-image-duplicates.py --maker DXB` clean over 72 images. **⚠️ gh-pages needs PURE PLUMBING, not a worktree:** a full fetch times out, and after a blobless fetch every index operation (`checkout`, `add`, `diff --cached`, plain `write-tree`) **hangs** fetching missing blobs on demand. Use `read-tree` into a temp `GIT_INDEX_FILE` → `hash-object -w` → `update-index --cacheinfo` → **`write-tree --missing-ok`** → `commit-tree` → push the sha; then prove the tree diff is exactly the intended additions and **0 deletions**. Pages lagged ~9 min serving 404 — checked the Actions API and found the run **`in_progress`, not `cancelled`**, then confirmed all 40 MP3s by **hashing the downloaded bytes against the uploaded blob SHAs**. All **112** Dubai asset URLs 200. **✅ Caption rule now spans paragraphs** — several scripts open with a one-sentence bare instruction that the old first-paragraph-only splitter could not extend past (the Rome "Start with the holes." failure in a new guise); shortest shipped is exactly 60 chars. **Open for the owner:** two heroes ship flagged at their prior explicit direction — `al-shindagha_hero` (licence unverifiable, upscaled ~1.6× so visibly soft) and `difc-gate_hero` (garbled signage, likely AI-generated; **`difc-gate_2` is a verified photo and a one-line swap**). 11 credit-required images in `drafts/CREDITS.md`; ⚠️ `al-shindagha_2` is **FAL**, copyleft like BY-SA. **Queue after Dubai: Berlin (36 / 57) + Chicago (30 / 53) = 66 tours / 110 MP3s.** | 2026-07-31 |
| `HANDOFF-260728-3.md` | Historical (superseded by `-260731`). Session 79, 2026-07-28 (infra, no code): **paid tours Phase 2 went LIVE** — the merged backend is now running. Owner applied `backend/paid_tours.sql` ("Success. No rows returned."); verified live: `get_catalog` 200 with **26 makers / 1014 tours**, all carrying `priceTier`, **0 non-null** (catalog still entirely free), the five new objects readable-but-empty to anon and every anon **write** 401, `maker_net_cents(299)` → **203** ($2.99→$2.03 per the design doc). Created the App Store **In-App Purchase** key — ⚠️ a *different key type* from the TestFlight signing key, on its own ASC tab, with **no role selector**, so the Admin-role gotcha does NOT apply — **Key ID `95442B44CC`**, **Issuer `f34324bd-aa34-4de0-8acb-2537b0e9325e`**, `.p8` downloaded once and stored by the owner (ASC now marks it downloaded; lost = revoke + regenerate). Four secrets set (owner pasted `APPSTORE_IAP_KEY`; Claude handles no key material) and both Edge Functions deployed via the dashboard editor with the **owner pasting the code** — Monaco auto-inserts closing brackets, so typing 300+ lines of TS through automation corrupts it, unacceptable for payment code. JWT gates **proven by request, not by UI**: `record-purchase` ON → unauth POST `401 UNAUTHORIZED_NO_AUTH_HEADER`; `appstore-notifications` OFF → unauth POST `200 ok`. **Reusable proof:** POSTing the publishable key to `record-purchase` returns `"sign in required"` rather than `"App Store API secrets not configured"` — the secrets are checked *before* auth, so that response proves all three (incl. the pasted `.p8`) are present and non-empty. Apple's **Production + Sandbox** notification URLs both set to the `appstore-notifications` endpoint (ASC's dialog offers **no version selector** — confirm V2 at the Phase 6 rehearsal). **⚠️ Phase 3 test item:** the toggle says *"Verify JWT with **legacy secret**"* but this project signs users with **ES256** (JWKS returns an EC key; `SUPABASE_ANON_KEY` DEPRECATED) — untestable until a logged-in user exists. If purchases 401, verify the JWT against the JWKS **inside** `record-purchase` **then** turn the toggle off; **never off first**, since the function decodes `sub` unverified and leans on the gateway. Functionality risk, not a live hole. **NEXT: Phase 3 buyer UI.** | 2026-07-28 |
| `HANDOFF-260728-2.md` | Historical (superseded by `-260728-3`). Session 78, 2026-07-28 (web, content): **Montreal launched as the 20th city/maker** — **Atlas Studio YUL** 🇨🇦 (`4f7241f0-…`), **29 tours = 25 single-stop + 4 walks**, 46 MP3s, **90m13** of narration. Catalog **985 → 1014 tours / 19 → 20 makers / 1234 → 1280 stops**. Third city wired straight from the audio-pending queue; the drop matched the staged drafts **1:1**, and its numbering gaps (13, 18–20, 28, 29) are exactly the scripts never written — no surprise extras, unlike Rome. **⚠️ The session's real lesson is file transport, not content.** A **Dropbox Transfer link (`/t/`) cannot be downloaded headlessly** (JS-gated list; the download sits behind an undocumented internal API — every guessed endpoint 404s). **Chromium cannot reach the network through the agent proxy at all** — even `example.com` returns `ERR_CONNECTION_RESET` with **no** proxy-side failure recorded — so "drive it in a real browser" is not a fallback in this environment. And a **corporate SharePoint/OneDrive link is an identity boundary, not a link-format problem**: `tishman-my.sharepoint.com` issued **zero** cookies on redemption, Graph returned **401 "Access token is empty"**, `api.onedrive.com` returned **"User migrated"** — it works for the owner only because their browser is already signed in to the work tenant, and the `?e=` token is a tracking param, not a key. **Do not try to route around a work-tenant link; ask for a personal account or a chat attachment.** **What worked: attaching the MP3s in chat** → `/root/.claude/uploads/<session>/` with a hex prefix per file. **For any 20–60 file batch, lead with that** — it needs no link, no sharing settings, and cannot be blocked by the owner's IT. Verification: **0 errors / 0 warnings** across 1014 tours via a Python mirror of `validate-tours.swift` that was **self-tested against 8 injected fault classes first** (a validator that passes everything is worse than none); uuid5 reverse-verified against the live **MAD + ROM** maker ids; all 46 MP3s **byte-distinct** (the Thyssen bug applied to audio); `check-image-duplicates.py --maker YUL` clean over 74 images. **✅ The `St.` caption trap CLAUDE.md predicted for Montreal was real and handled** — the abbreviation-aware splitter kept "the St. Lawrence" whole instead of producing a 40-char fragment; **Berlin has the same hazard.** **⚠️ Both gh-pages pushes served 404 for ~10 minutes** while the Pages deploy ran `in_progress` — confirmed only by **hashing the live URL against the uploaded bytes**, never by the push succeeding. **Open for the owner:** the **Mount Royal walk hero** (Kondiaronk Belvedere vs the Cross — the staged README marked it owner-to-confirm; wired with the belvedere, its own stated default, one-line swap); **5 singles ship hero-only** (Mary Queen, Place Ville Marie, Square Saint-Louis, The Main, Plateau staircases); and **no PR is open** — merging publishes over the air to real users, so that is the owner's call. **Audio-pending queue after Montreal: Berlin (36 / 57) + Dubai (26 / 40) = 62 tours / 97 MP3s.** ⚠️ **I first told the owner "Berlin only" and was wrong** — Dubai was script- and image-staged on 2026-07-27 by a parallel session that never updated `drafts/AUDIO-PENDING-SURVEY.md`, so the tracker's own "last verified" stamp was misleading and I repeated it into four documents before the owner corrected me. **Re-derive the queue from `git ls-tree … drafts/` when a parallel content session may have run.** ⚠️ **Dubai has no master pick-map README** — its slug↔coord↔category↔image assignments are written down nowhere, so a wire-in session must reconstruct them or staging must write it first. | 2026-07-28 |
| `HANDOFF-260728.md` | Session 77c, 2026-07-28 (web, code + SQL): **lists on other people's maker pages, and LIKED on everyone's** ([PR #462](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/462)). Pass 2 of the maker-page work. **Owner has one thing to run: `backend/public_liked.sql`** — until then every profile's LIKED shows empty. (`backend/public_lists.sql` already run + verified live.) **The blocker was one missing field:** lists are filed under an **auth account id** and the creator profile the app holds carried none — so `get_catalog()` now emits `makers.user_id` → `Maker.userId: UUID?` → `TourListService.publicLists(ofUser:)`. **⚠️ That optional is load-bearing** — the gh-pages mirror and the bundled offline seed come from `Tours.json` and will never carry it. **Verified before shipping that `seed_from_toursjson.py` does not name `user_id`**; if it ever does, the next content merge nulls every user's maker link and silently breaks lists, follows and the profile. Owner decisions applied: **new lists visible by default** and **every existing list flipped visible** (one-way, explicitly requested — disclosed that it touched **two accounts, not one**), and the editor toggle now reads **"Only me"**. **LIKED now appears on every profile, Atlas studios included** — it is the one list everybody has, and a page without it read as broken rather than empty (that was the **"black square"** in the 1.1 (54) review). **⚠️ Reading someone else's Liked needed a server path, and its shape matters:** Liked is backed by `LibraryStore` so saving works signed out, and that store only holds *yours*; `liked_tour_ids(p_user)` is `SECURITY DEFINER`, so its body is the security boundary and is deliberately tiny — **downloads, playback progress and completion sit in the same `user_library` row and are not returned.** Also: a **key badge** on the cover of a list only its owner can see; the **MAP tab draws the world centred on the Atlantic when a maker has no tours** (`initialRegion(for:)` is non-optional now); and **LIKED's permanence is written into `LikedListView`** — it is not a `journeys` row with its controls hidden, and the moment it becomes one, an un-save stops being the only way to remove a save. **⚠️ Two corrections worth carrying:** I told the owner this needed no backend work and was **wrong** (I'd taken `userId` from a report describing `MakerRow`, the private Supabase DTO, not the rendered `Maker`); and the owner couldn't follow my explanation of it — *"i'm really not understanding what you're saying"* — because I led with "this needs a backend change" instead of the plain fact, **"the app doesn't hold their account ID."** **Still deferred:** saved tours on the map (treatment approved, colour + timing open) · Shared/Only-me **follower** visibility · **private-hides-everything** (its own project) · the rest of the owner's *"lots to do"* list, still undescribed. | 2026-07-28 |
| `HANDOFF-260727-4.md` | Session 77b, 2026-07-27 (web, code): **maker page gains a TOURS / LISTS / MAP tab strip, and the app's two segmented controls convert to the same strip** ([PR #461](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/461)). Code only — no `Tours.json`, no SQL, nothing for the owner to run. **Net −249 lines** (+293/−542). Driven by **four rounds of HTML mockup reviewed on device**; every decision was made by the owner looking at a thing, not reading an argument. **⚠️ The owner corrected me on the central design question and was right:** I argued a pannable map can't live in a `ScrollView`; they pointed out **tour detail has done exactly that for months** — 320pt, inset 24pt, square corners, and **the 24pt gutters either side are the trick** (that's where a drag scrolls the page instead of panning the map). *Check whether the codebase already solved it before reasoning about whether it can be.* Five new shared files, each replacing something duplicated or unreachable: `AtlasTabStrip`, `MapPins`, `MapClustering`, `TourListRows`, `MakerMapSection`. **The pin extraction fixed a live drift** — `TourDetailView` had hand-reimplemented `StopPin` + `UserLocationDot` with a comment saying it had to (they were `private` to `HomeMapSection`), and the copies had diverged to a **14pt dot against Home's 16pt**; the maker map would have been the third copy. **Deleting the segmented controls also deleted two global `UISegmentedControl.appearance()` mutations** that were restyling every segmented control the app could ever show, including in system sheets — plus **`TourDetailView.init(tour:)` entirely**, which existed only to run one and was duplicated from `LibraryView.init` to dodge a cold-launch ordering bug that no longer exists. `cellsAcross` is now a parameter (counts cells across the **region**, not the screen — a 320pt map needs ~12, not 20). **🐛 A bug CI could not catch, found by re-reading after the build went green:** the camera and the clustering region were seeded in **two different `.task` blocks**, so whichever ran first read `.automatic` (region `nil`) and `currentRegion` stayed nil — **a maker with 40 tours in one city would have opened to a pile of unclustered dots and stayed that way** until the first pan. Fixed in `6f31c7a`; *two tasks that must agree are a race*. **⚠️ Scope not delivered as asked:** the owner wanted all three tabs on **every** maker page, but **LISTS is own-profile only** — reading another creator's public lists needs their `auth.users` id and **`get_catalog()` never emits `makers.user_id`**, so the client `Maker` has no `userId`. I had earlier told the owner this needed no backend work; **that was wrong** (I'd taken `userId` from a report describing `MakerRow`, the private Supabase DTO, not the rendered `Maker`). **Owner's list-visibility model is decided and designed, not built:** a list is **Shared or Only me**, and the *account* setting decides who "Shared" reaches (public → anyone; private → accepted followers); "Only me" hides from followers too. **Two schema traps recorded:** `follows.followee_id` is a **MAKER** id while `journeys.owner_user_id` is an **AUTH USER** id — the bridge `makers.user_id` is unique only via a **partial** index and the 19 Atlas studios are all `NULL`, so a careless join **matches every seed studio at once**; and `makers.user_id` is `on delete set null` while `journeys.owner_user_id` is `on delete cascade`. **Device review, riskiest first: Library and tour detail** — shipped screens that were not broken. **Open:** saved-tours-on-the-map (treatment approved — solid dot vs hollow ring so it survives greyscale — only timing deferred); whether Library's Lists tab shrinks; the strip's full-bleed rule vs inset; and the rest of the owner's *"lots to do"* maker-page list, not yet described. | 2026-07-27 |
| `HANDOFF-260727-3.md` | Session 77, 2026-07-27 (web, code + docs): **closing out the saving consolidation — TestFlight 1.1 (51) verified, and `Journey` renamed to `List` throughout the app.** Two PRs: [#457](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/457) (`8f29626`, docs) and [#458](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/458) (`370b9f0`, the rename). **⚠️ The most transferable lesson has nothing to do with the rename: build 50 carried the saving branch's bottom-module fix, but the merge took `main`'s better one from [#443](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/443) — so the combination that shipped was never the combination that was tested.** Both branches were verified; their merge wasn't. A build from `main` (`dfc3a5d`) closed it and the owner verified the real thing: *"SHINSEGAE VIDEO WORKS, IS FINE. TAB BAR IS PRESENT FROM FIRST FRAME. DEAD TAB SEEMS FINE."* **I also burned two wrong theories on the tab bar** (orphaned window, then zero-size window) before the owner's clarification — *"only not there momentarily after launch… missing long enough to look like a mistake"* — identified it as a **late** install, not a failed one, which #443 had already solved by rendering the bars **inline in `ContentView`** when the secondary window is absent. All my bottom-module work was discarded; **check whether `main` already fixed it first**, and note I re-added `w.frame = scene.coordinateSpace.bounds` even though CLAUDE.md flags it as a known dead end. **The rename:** `Journey`→`TourList`, `JourneyItem`→`TourListItem`, `JourneyService`→`TourListService`, `Features/Journeys/`→`Features/Lists/`; identifiers + comments only, no behaviour change. **Not plain `List`** — SwiftUI's `List` is used in three views, so a same-named type makes every `List { }` ambiguous. **⚠️ The Supabase tables deliberately STAY `journeys` / `journey_items` / `journey_id`:** renaming needs a hand-run migration and **every build already on a tester's phone queries the old names** — they'd break the moment it ran, for a name no user sees; literal counts verified identical before/after. **⚠️ Rename trap worth carrying:** a word-boundary search **cannot see `_journeyService`**, the property-wrapper storage for an `@State` — underscore is a word character, so `\bjourneyService\b` never matches, and it fails to compile. Also landed **`docs/tag-taxonomy.md`** (unmerged since June) reframed as **shipped, not proposed** — it wasn't stale, but it read as a proposal for a system live since session 57; kept because it's the only written record that *"Designed by a Master" is implied by any Architect tag*, which no code enforces. **No build owed; 1.1 (51) is current.** **Owed:** delete `claude/journey-bookmarks-default-folder-c8ur85`, `claude/handoff-260726`, `claude/dreamy-wozniak-tags-260612`, `claude/rename-journey-to-list` in the GitHub UI. | 2026-07-27 |
| `HANDOFF-260727-2.md` | Historical (superseded by `-260727-3`). Session 76, 2026-07-27 (web, content + tooling): **a wrong hero shipped for a month — root-caused, fixed, and the whole catalog swept clean of duplicate images** ([PR #453](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/453) `7b5762c`, [PR #455](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/455) `7c1d48b`, + 3 gh-pages asset commits). Owner spotted Museo Thyssen-Bornemisza showing the Reina Sofía building. Cause, provable from git: gh-pages commits `41a03d5` and `103c90c` are **40 seconds apart and their hero blobs share the identical hash** — the Thyssen commit re-used the image decoded 40s earlier, so the owner's paste was never written. **⚠️ It was unrecoverable:** inline-pasted images live as base64 in the session transcript, but **a web session's container is fresh-cloned, so prior transcripts are gone**. Owner re-pasted; that paste *was* recovered from this session's `.jsonl`. **No `Tours.json` change was needed** — the URL was always right, only the bytes were wrong — and replacing the one file also fixed the Paseo del Arte walk, which was showing Reina Sofía twice. New **`scripts/check-image-duplicates.py`** hashes every referenced image and groups by SHA-256; this class is **invisible to `validate-tours.swift`** (distinct URLs, all 200, only bytes match). **Not in `ci.yml`** — network-bound; run `--maker <CODE>` when staging a city. **⚠️ Read a full run before trusting your rules:** v1 flagged 18 groups, **4 legitimate**, and Ponte Sant'Angelo exposed a real gap (a walk's **stop** image doesn't resolve to the parent walk's slug) — fixed in the tool, kept narrow so a walk's *gallery* matching a single-stop hero still errors. **A URL-level check in the Swift validator was planned and dropped — it would have caught neither Madrid bug.** All **14** other groups resolved, **no hero changed**; two were genuine mis-stages (a Mission mural in **Haight-Ashbury**, a Munttoren sunset in **De Wallen**), two dead swipes in Fifth Avenue Walk, the rest adjacent-landmark shares. **Also found: 2 dead 404 URLs** (Madison Square Garden, Riverside Church stop images) + a double-URL-encoded Ford Foundation reference. Madrid de los Austrias got its **own** hero (owner-picked CC0) instead of reusing Círculo de Bellas Artes. **Catalog now 0 errors across 3,615 images.** **⚠️ Gotchas:** `git push … | tail` inside an `if` reads **tail's** exit code — a rejected push reported "PUSH OK"; a gh-pages Pages deploy came back **`cancelled`** and served stale ~10 min, so **verify by hashing the live URL, not by the push succeeding**; `Tours.json` round-trips byte-identically as `json.dumps(indent=2, ensure_ascii=False)` with **no trailing newline**; `pull_request_read get_status` reads `total_count: 0` even when CI is green (those are check *runs*). **Open:** King of Kowloon dedupe is the most debatable (both tours are about the same artist) — one-line revert; Champs-Élysées hero is an aerial of the **Arc de Triomphe**, arguably the wrong subject. | 2026-07-27 |
| `HANDOFF-260727.md` | Historical (superseded by `-260727-3`). Session 75, 2026-07-27 (web/PM, content): **Rome launched — 30 tours + 19th maker Atlas Studio ROM** ([PR #450](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/450), squash `4598ed6`, auto-merged on green as content class). 25 single-stop (geofenced 30 m) + **5 walks** (Ancient Rome 1.5 km, The Baroque Heart 2.0 km, The Ghetto and Trastevere 2.5 km, The Vatican and the Borgo 1.25 km, The Aventine and Testaccio 2.5 km), 53 MP3s / 6,866 s. **Catalog 948 → 978 tours / 18 → 19 makers / 1174 → 1227 stops.** Second consecutive city wired straight from the audio-pending queue — all 116 images were already staged by slug, so narration was the only missing ingredient and the image work was zero. **⚠️ The delivery was 60 MP3s, not the 53 staged: 7 extra singles (master-list 25–31 — Quirinale, Monti, Santa Maria Maggiore, San Giovanni in Laterano, Trajan's Column, Porta San Sebastiano, Testaccio) arrived with NO scripts and no images for five of them** (the Dropbox drop carried only MP3s + handoff `.md` files, zero `.txt`). **Not wired** — a tour with no hero can't ship and one with no script would carry `transcriptText: null` + an invented caption — but **their audio is banked on gh-pages under its eventual slug**, so the follow-up needs scripts + owner image picks, not a re-upload. Judgement call open: Trajan's Column + Testaccio *do* have a hero and could ship as thin singles if the owner prefers. **⚠️ Two staging-README errors, corrected at wire-in and expected again in Montreal + Berlin (same author):** the READMEs say `kind: "singleStop"` but the catalog's real value is **`"single"`**, and their suggested tags (`ancient-site`, `square`, `fountain`, `castle`…) are **not in the controlled vocabulary** — **`Models/Tag.swift` is the vocabulary, not a batch README**. Walk intro audio is `{walkslug}_stop0.mp3`, not `_intro.mp3`. **Verification:** CI Swift validator 0/0 (locally a **Python mirror** stood in for the absent `swift`, and was **self-tested against 5 injected faults** so its pass meant something); **all 169 asset URLs 200**; uuid5 ids reverse-verified against live Madrid; diff additions-only. **Gotchas:** `while read` silently skips a final line with no trailing newline (first sweep checked 168 of 169 — assert the count, not just failures); in a `--no-checkout` gh-pages worktree **every file reads as an unstaged deletion**, so stage by explicit path and **never `git add -A`**; CI job-state API lags (one poll showed zero jobs in flight mid-build). Confirmed live on Supabase at 978 tours / Rome 30. **Owed:** delete merged branch `claude/rome-audio-stage-tours-m05vud` in the GitHub UI (proxy blocks it). **Queue after Rome: 65 tours / 103 MP3s** — Montreal 29, Berlin 36. | 2026-07-27 |
| `HANDOFF-260726.md` | Historical (superseded by `-260727`). Session 74 (1 of 2 — parallel session). Session 74, 2026-07-26 (code/web): Group Listen polish driven turn-by-turn off owner screenshots across TestFlight **1.1 (42)→(46)**, plus two real bugs it surfaced. [#441](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/441) `57095f0` **deleted the bottom session banner** — the tour page's group icon turns **green** while a session runs and opens the sheet (which carries Leave); glyph 17→16pt. [#443](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/443) `bf9f98e` carries the rest. **🐛 The "dead" tab bar was NOT group-specific:** taps registered (icon lit) but the tour-detail layer was never dismissed, so the new tab loaded *behind* it — `ContentView`'s `.onChange(of: selectedTab)` lives in the main window, which the modal covers, and **SwiftUI can stop updating a hierarchy hidden behind a modal**. The tab-bar binding now dismisses as part of the tap, from the secondary window. **🐛 Missing mini-player + tab bar, third attempt:** the bars existed ONLY in the secondary window, so a failed install left no bars and no fallback; `ContentView` now renders them **inline** whenever that window is absent (controller is `@Observable`). Also **reverted a self-inflicted `w.frame = scene.coordinateSpace.bounds`** that pins a geometry snapshot and, on an unconfigured scene, pins *zero* — the invisible window it was meant to prevent; readiness moved into `foregroundActiveScene()`, and `setInteractiveBottomInset` now clamps to ≥ `AtlasBottomModule.height()`. **Sheet:** caption type throughout, `.medium` detent, **overlap of the nav title fixed by a ScrollView** (a fixed frame let overflow render *upwards* through the nav bar), bottom inset so nothing hides behind the mini-player, and all three screens as two aligned columns (`LEAD A TOUR`/`JOIN A TOUR`; `SCAN TO JOIN` beside `OR ENTER CODE`). **⚠️ QR floor is 110pt** — owner-verified as still scannable; if that screen needs room, open a taller detent, don't shrink the code. **Owed: two-phone sync still never run end-to-end.** | 2026-07-26 |
| `HANDOFF-260726-2.md` | Historical (superseded by `-260727`). Session 74 (2 of 2 — parallel session). Session 74, 2026-07-26 (code/web): **saving consolidated — one save action, "Liked" is the default list** ([PR #447](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/447), open). There were **two** ways to keep a tour with **two stores and no knowledge of each other** — the bookmark (`LibraryStore.savedAt`) and "Add to a Journey" (`journey_items`); a tour in a list wasn't bookmarked, a bookmarked tour was in no list. Same redundancy killed for makers in #398. **The brief sharpened mid-session and that mattered:** the first plan only merged the *views* (rename the tab, pin a Liked row) while leaving two write paths — it would have shipped the exact redundancy the owner was removing. **"Add a default folder" and "consolidate saving" are different briefs.** Now: **saved = in at least one list**; **Liked is the default** (owner: *"if the user doesn't specify, there should always be a default 'liked' folder"*) — filing into a named list puts it *there*, not also in Liked, and nothing moves implicitly. **Bookmark tap:** nothing → add to Liked · exactly one list → remove it (second tap always undoes the first) · several → open the membership sheet, everything ticked, user unticks what they want out (the owner's own UX call, better than the blunt "remove from everything?" confirm first proposed). **⚠️ The constraint that shaped it all: bookmarking works SIGNED OUT, lists are cloud-only** — so **Liked stays on `LibraryStore`** and named lists stay in Supabase; one concept, two backends, no visible seam. **Making Liked a real server row would gate bookmarking behind an account — don't.** `LibraryStore`/`SyncService` untouched; **no backend change, no migration.** New `SaveState` (pure, unit-tested rules) + `TourSaveActions` (binds them to the stores so cards / detail / player can't drift); `AddToJourneySheet` → **`TourListMembershipSheet`** (removes as well as adds, leads with Liked). **Library is the single home** for kept things; the profile's Journeys row **removed**, `JourneysListView` deleted. **Perf:** `isSaved` is read by every card in every rail, so per-tour queries were never viable — `loadMyJourneys()`'s embed already returns every listed tour id, making `allListedTourIds` free. **Fixed in passing:** `JourneyService.clear()` existed but was **never called**, so a stale list survived account switches. **⚠️ Naming still open** — copy says "list", types/tables still `Journey`; "Journey" breaks on the default bucket ("Liked" isn't a journey) and collides with **Walk**. **⚠️ Device check owed:** the signed-out bookmark path is the whole design constraint and the sim has no session. | 2026-07-26 |
| `HANDOFF-260725-4.md` | Historical (superseded by `-260726`). Session 73, 2026-07-25 (content/PM, local Mac): **Madrid launched — 34 tours + 18th maker Atlas Studio MAD** ([PR #435](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/435)). The owner asked whether they could hand over audio for tours already **staged**; they could, and did (`~/Downloads/260630_MADRID ALL`). **First city wired in end-to-end from the audio-pending queue** — scripts + all 118 images had been staged since 2026-06-24/29 on `claude/dreamy-wozniak-nM6a4`, so the job was: match audio → read durations → upload → assemble. 30 single-stop + **4 walks** (Madrid de los Austrias 1.1 km, Paseo del Arte 1.3 km, El Retiro 1.8 km, Royal Madrid 1.4 km), 55 MP3s, 6,459 s. **Catalog 914 → 948 tours / 17 → 18 makers / 1119 → 1174 stops.** The drop matched the staged drafts **1:1**. **Two decisions:** singles ship geofenced at **30 m** (the catalog-wide default) not the "~40 m" the READMEs suggested, walk stops 40 m + `manual` intro; and the tracker's Madrid count was **one over** (31/56 → actual 30/55), now corrected. **Gotchas:** two files share the `madrid_04_` prefix (match full stems, never the NN index); `[beat]` markers must be stripped (validator hard-errors on bracketed stage directions); captions needed a two-sentence fallback because many scripts open with a bare instruction; on this Mac Python `urllib` fails SSL verification (use `curl`) and **`xargs -P` silently exited 0 after "command line cannot be assembled, too long"** — use `while read`. Validator 0/0; **all 173 asset URLs 200**; 0 duplicate ids. **Key-facts drift caught again** (read 871/16/1076, `main` was 914/17/1119 — Chiang Mai never folded in) — third resync in a row. **Queue after Madrid: 95 tours / 156 MP3s** — Montreal 29, Rome 30, Berlin 36. | 2026-07-25 |
| `HANDOFF-260725-3.md` | Historical (superseded by `-260725-4`). Session 72, 2026-07-25 (code/web): **Group Listen ("SharePlay") went from "does nothing" to device-verified working**, then tightened. [PR #423](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/423) (`3e9a6d9`) fixed it; [PR #428](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/428) (`18ba375`) added tighter sync, QR joining, a tappable banner + polish. It had shipped in 1.1 (8) **never device-verified** — the owner was its first tester. Root causes were all discovery/connection: **no `didNotStart…` delegates and no connection state**, so a denied **Local Network** permission was a silent dead-end (the headline); false "Leader left" during the handshake; **epoch poisoning** (a former leader joining as follower ignored every broadcast); intro-audio desync; a `peerParticipants` data race. **Sync:** the 1.25 s drift dead zone became two-tier — ignore <0.15 s, **trim speed ≤3%** to glide back 0.15–2 s, precise seek beyond; new `AudioPlayerService.syncTrim` kept separate from the user-facing `rate` (solo playback unchanged) + `seek(to:precise:)`; heartbeat 1.0→0.5 s. Owner: **"good enough to ship"** — don't tune further; genuinely tighter needs shared-clock architecture, not tuning. **QR join:** Core Image generates, AVFoundation scans, payload is a Universal Link (`…/g/?code=`) so the system Camera app works; `DeepLink.group(code)`; adds `NSCameraUsageDescription`. **Banner Leave was untappable** — the bottom-module window claimed a fixed 126 pt touch strip while the banner sits above it; it now reports its measured height. **Apple cert-cap permanently auto-fixed** in `testflight.yml` (no more manual revoking). **Device-verified:** QR join ✅, sync ✅, Leave ✅; **solo-playback regression check still owed**. Deferred: banner/content overlap (needs a bottom-inset sweep), anonymous followers, leader handoff. **`shareplay` SF Symbol is a SINGLE figure — no symbol matches Spotify's Jam icon; reverted to `person.2.wave.2.fill`.** **Addendum (session 73, [PR #441](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/441), squash `57095f0`, owner-verified on 1.1 (42)): the bottom banner is GONE** — `GroupBanner.swift` deleted; the tour page's group icon turns **green** while a session runs and opens the sheet (which carries Leave); glyph 17→16 pt. Retires the banner/content-overlap item. `BottomModuleRoot`'s measured-height fix stays — it's the general fix for anything above the mini-player. Trade-off: no app-wide active-session indicator any more. | 2026-07-25 |
| `HANDOFF-260725-2.md` | Historical (superseded by `-260725-3`). Session 71, 2026-07-25 (backend, no app code): **paid tours Phase 2 — the money backend is written, awaiting 3 owner dashboard steps.** New `backend/paid_tours.sql` (idempotent, one paste): `tours.price_tier` (NULL = free, CHECK-constrained to the 10 Phase-1 ASC tiers), **`purchases`** (source of truth for entitlements AND earnings; `apple_transaction_id` UNIQUE → replay-safe), `payouts`, `maker_payout_accounts`, `maker_net_cents()` (68% of sticker), `maker_sales` + `maker_earnings` views, and `get_catalog()` rebuilt to emit `priceTier`. New Edge Functions **`record-purchase`** (Verify JWT **ON**) and **`appstore-notifications`** (Verify JWT **OFF**). **Three deliberate deviations from the design doc:** the Stripe id lives in `maker_payout_accounts`, not `makers.stripe_account_id` (makers is public-read → a column there would leak it to every client), and that table is read-only to clients; neither function trusts its caller — each takes only a transaction id from the JWS then re-fetches the authoritative record from **Apple's App Store Server API** under our signed ES256 key, so forged POSTs can't mint entitlements or fake refunds (needs an ASC In-App Purchase `.p8` key); and makers never see buyer identities. **RLS: no client writes to `purchases`, and only the buyer (+admin) can read it.** **⚠️ Includes a hardening pass ([PR #433](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/433)) after an adversarial review caught a critical hole in #431 — `record-purchase` never compared the tier bought against the tour's price, so a genuine $0.99 receipt re-sent with any `tourId` would unlock a $19.99 tour and credit that maker 99¢. Apple can't catch that (tier products are reusable); the tier↔tour check is load-bearing — don't remove it.** Same pass: verify the buyer via GoTrue instead of decoding the JWT, 503 (not "not found") on Apple outages, `paid_out_cents` from `payouts.amount_cents`, UUID-validated `tourId`, 409 on a transaction redeemed by someone else. `price_tier` is deliberately absent from `seed_from_toursjson.py` (price is DB-side, not in Tours.json) — don't add it. SQL not executed (no Postgres in-session) — the owner's step 1 is its first run. **Owner steps + runbook: `backend/README.md` § "Paid tours".** NEXT: Phase 3 buyer UI (StoreKit 2). | 2026-07-25 |
| `HANDOFF-260725.md` | Historical (superseded by `-260726`). Session 70, 2026-07-25 (CI/docs): **build notes now land in TestFlight's "What to Test" field — owner-confirmed working.** Took **three PRs and three blank builds**: [#425](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/425) added the mechanism via fastlane `set_changelog`, which **cannot target a TestFlight build** (sets the App Store version's notes, rejects `build_number`) → build 36 blank; [#430](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/430) switched to **`upload_to_testflight` + `distribute_only: true`**, which then *prompted* for the platform and crashed in CI → builds 39/40 blank; [#437](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/437) added **`app_platform: "ios"`** (mandatory in CI though the docs call it optional) and it worked. **Durable lesson is the retry classifier, not fastlane** — both failures hid for 20–25 min because every error was treated as "Apple still processing" (second time via a bare-word `processing` grep matching fastlane's own option list); it now defaults to **permanent**, since a wrong permanent call costs nothing and a wrong retryable call hides the error. Also codified: **`workflow_dispatch` runs the workflow file from the branch you build FROM**, so build from `main` or merge `main` in first. A green run does **not** imply notes attached — read the `Done` line. Branch `claude/build-release-notes-f1samw` merged, needs UI deletion. | 2026-07-25 |
| `HANDOFF-260724.md` | Session 69, 2026-07-24 (infra, no code): **paid tours Phase 1 done — all 10 IAP tier products created in App Store Connect** (app 6771030927 → In-App Purchases, "Drafts (10)"). The V2 Step 6 design doc `docs/paid-tours-design.md` merged via [PR #422](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/422) (`c154807`). **Owner widened pricing from 3 to 10 tiers** — $0.99–$19.99 low-end-dense; product IDs `tour.tier.<price×100>` (`tour.tier.099`…`tour.tier.1999`, per-product Apple IDs in the handoff). Each: Non-Consumable, US base price + Apple auto-pricing 175 regions, en-US localization "Premium Audio Tour" / "Unlocks this audio tour". **Deliberately left in "Prepare for Submission"** (sandbox works; review screenshot + "Add for Review" pair with an app version at go-live). Claude drove the owner's Chrome through ASC (first time; `form_input` fails on ASC textboxes — click+type). **NEXT: Phase 2 backend** (purchases table/RLS, price_tier, Apple-JWS Edge Function, ASSN refunds endpoint, get_catalog tier). | 2026-07-24 |
| `HANDOFF-260722.md` | Historical (superseded by `-260724`). Session 68, 2026-07-22 (web/PM content): **Ho Chi Minh City launched — 43 tours + 16th maker Atlas Studio SGN** ([PR #419](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/419), `1ad9e38`, squash, owner-authorized merge on green CI). Vietnam's first city; new maker Atlas Studio SGN (`40a6f268-…`, 🇻🇳); 43 single-stop geofenced (30 m) tours; bilingual `English | Tiếng Việt` where a Vietnamese name exists. **Catalog 828 → 871 tours / 15 → 16 makers / 1033 → 1076 stops.** Delivered as a ~123 MB Dropbox folder (`dl=1`), 43 `output <Name> <lat>,<long>` folders with coords in the names (`#UXXXX`-escaped); parsed + durations via `mutagen`. Assets-first to gh-pages via a **blobless `--no-checkout` worktree** (178 new blobs only), all URLs 200. Deterministic uuid5 ids (`atlas-{maker,tour,stop}:sgn`). **`[beat]` stripped from every transcript** (validator hard-errors on bracketed stage directions); a **Python port of `validate-tours.swift`** stood in for the missing `swift` (0 errors). CI green (validator + sim build + unit tests); confirmed live on Supabase `get_catalog` + gh-pages mirror (assert on tour count — RPC over-reports makers via upsert). Outliers kept as supplied: Củ Chi Tunnels (~75 km NW) + Bửu Long Pagoda (far east). **Owed:** delete merged branch `claude/tours-upload-audio-photos-3pcty4` in GitHub UI (proxy blocks it). | 2026-07-22 |
| `HANDOFF-260721.md` | Historical (superseded by `-260722`). Session 66, 2026-07-21 (code, web): **maker tour list + grid now distinguish multi-stop walks from single stops** — [PR #413](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/413) (`ac6e421`, TestFlight 1.1 (29)) + [PR #414](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/414) (`ed4daa9`, TestFlight 1.1 (30)), both owner-authorized merges on green CI. Owner: the maker page didn't tell walks apart from single stops and any fitting icon was illegible (two same-named "The Jordaan" tours — a 2m single stop, a 12m multi-stop walk — looked identical). Chose words over glyphs via an HTML mock. **List:** `MakerView.tourRow` gains a brass `WALK` pill (new `walkPill`, reuses the `statusBadge` shape in `AtlasColors.accent` `#8B7535`) + a stop count leading the subtitle (`6 stops · 12m 39s · 1.2 mi away`); fixed-size pill so distance truncates first; single stops unchanged. **Grid:** the same pill as a top-leading tile badge (clear of the bottom-leading Draft/In-review badge, soft shadow over photos). One file (`Features/Maker/MakerView.swift`), no data-model/backend change (reads `tour.kind`/`stops.count`). `testflight.yml` built 1.1 (29)/(30) clean (no cert snag). **Owed:** device-eyeball grid-pill legibility over real photos (light + dark); delete merged branch `claude/tour-list-stop-distinction-kyk2xr` in GitHub UI (proxy blocks it) — branch was restarted off `main` after #413 for the #414 follow-up per the merged-branch rule. | 2026-07-21 |
| `HANDOFF-260720-3.md` | Historical (superseded by `-260721`). Session 64, 2026-07-20 (code, web): **Follow-request UX polish batch → TestFlight 1.1 (25)** ([PR #409](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/409) → `main`, squash `f0e77d1`; CI green, owner device-reviewed, authorized merge). Five owner-driven tweaks in the batch-D social layer, no backend change: (1) Follow Requests title → caption font (principal-toolbar `Text`); (2) Approve/Decline → green ✓ / red ✗ circular icon buttons; (3) **gold notification dot on the ME tab** when the user has pending follow requests — `FollowService.ownPendingRequests` (observed, cache-seeded) drives `AtlasTabBar.badgedTabs` via `BottomModuleRoot` (needed injecting `FollowService` + `MakerProfileService` into the bottom-module window); (4) **gold heart badge** over the own-profile followers count (replaces the "N follow requests" line); (5) **requests page merged into the Followers list** — a "PENDING REQUESTS" section pinned atop `FollowListView` (`showsPendingRequests`), approving moves the person into followers in place + refreshes header counts + Me-tab dot; **deleted `FollowRequestsView.swift`** (fs-synchronized target → no pbxproj edit). Login-gated → owner device-reviewed on 1.1 (25) with a private test account. **Owed:** delete merged branch `claude/caption-font-checkmark-styling-5x5w6g` in GitHub UI (proxy blocks it). | 2026-07-20 |
| `HANDOFF-260720-2.md` | Historical (superseded by `-260720-3`). Session 63, 2026-07-20 (code, web): **Library-tab jitter fixed → TestFlight 1.1 (21)** ([PR #404](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/404) → `main`, squash `98f2993`; +307/−32; owner device-confirmed fixed, authorized merge). Owner: "clicking home→library has a slight jitter when it loads or re-formats." Cause: the Library tab is switch-swapped (rebuilt each entry), so `followingMakers` `@State` reset to `[]` and the **Saved** tab drew tours-only, then re-formatted a `list_following` round-trip later (`TOURS` header popped in at top + `FOLLOWING` section below). Fix = the app's stale-while-revalidate pattern applied to the follow **list**: `FollowService` caches `following(of:)` in an observed, per-viewer map backed by a new **`Data/FollowingListStore.swift`** (per-account `UserDefaults` blob of `[Maker]`), hydrated **synchronously at init**; `LibraryView` reads `followingMakers` from the cache **synchronously in `body`** (+ `.task` refresh). Every Library open — warm re-entry or cold relaunch (after the first seed) — now renders the final layout on the first frame. Adds `FollowingListStoreTests`; behavior-preserving. CI green (sim build + unit tests + validator). **Build-cut gotcha codified:** two `testflight.yml` runs failed fast (~50s) at Archive with *"account has reached the maximum number of certificates"* — **Apple Development** cert cap (not code; CI was green); owner revoked the Apple **Development**-type certs (Distribution didn't help; ~5-min propagation) → rebuild signed clean. Recurs per cloud build (each mints a dev cert); durable fix = distribution-only archive signing (offered, deferred). **Owed:** delete merged branch `claude/library-tab-jitter-wha8o3` in GitHub UI (proxy blocks it, 403). | 2026-07-20 |
| `HANDOFF-260720.md` | Historical (superseded by `-260720-2`). Session 62, 2026-07-20 (code, web): **drawer rails re-anchored to the user's location → TestFlight 1.1 (16)** ([PR #405](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/405) → `main`, squash `18b96ea`, owner-authorized merge on green CI). Owner: "the primary sort for the rails in the drawer should still be based on distance to the user's location, especially if the user's location is within view." Only `HomeRailsViewModel.viewerLocation` changed — the anchor feeding `sortedByDistance` (curated shelves) + `filteredResults`. Now returns the **user's own location** whenever known and (no settled region yet **or** region contains the user, via `MKCoordinateRegion.contains`); falls back to the map viewport center only when panned away to a region that no longer shows the user (§1.5). The `Near you`↔`In view` rail *swap* (500m `isPannedFar`) is untouched — ordering *within* shelves only. +2 `HomeRailsViewModelTests` (user-in-view → user anchor; user-off-screen → viewport-center); CI #961 green. Build notes attached to 1.1 (16). **Owed:** device check (drawer over your location → nearest-to-you tours lead each shelf); delete merged branch `claude/rail-drawer-distance-sort-rx65l1` in GitHub UI (proxy blocks it, 403). | 2026-07-20 |
| `HANDOFF-260719-3.md` | Historical (superseded by `-260720`). Session 61, 2026-07-19 (code + docs, web): **maker bookmark removed — Follow is the single "keep a creator" concept** ([PR #398](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/398) → `main`, squash `8890cc7`; +57/−236). Owner: "just have one, follow not bookmark." `MakerView` `…` menu is now **Share · Follow · Report** (bookmark button + "Save" item gone); Library → Saved's maker list is now a live **"Following"** list via `FollowService.following(of: myMaker.id)` (owner chose this over tours-only); deleted `SavedMakersStore`/`SavedMakerEntry` + all `user_saved_makers` sync in `SyncService` + its unit test; App/ContentView no longer inject the store. **Tour bookmarking (`LibraryStore`) unchanged.** Built + owner-reviewed as **TestFlight 1.1 (9)** via `testflight.yml` `workflow_dispatch` on the branch (first web-session build cut purely for review); PR CI green incl. unit tests. **Owed:** delete merged branch in GitHub UI (proxy blocks it); `user_saved_makers` table unused-but-not-dropped. | 2026-07-19 |
| `HANDOFF-260719-2.md` | Historical (superseded by `-260719-3`). Session 60, 2026-07-19 (code + docs, web): **(1) home map/animation performance fix → TestFlight 1.1 (10)** ([PR #397](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/397) → `main`, squash `51c628a`, owner device-verified smoother). The home map + drawer did catalog-scaled work every pan/settle; fixed behavior-preservingly (pin shadow kept): `HomeMapSection.cluster` viewport-culls markers to the visible region + 1 viewport of margin before bucketing (was diffing an annotation for all ~969 stops); `HomeRailsViewModel` memoizes distances (decorate-sort, ~100k `CLLocation` allocs/settle → n); `HomeDrawerContent` computes the in-view count once. Scales with what's on screen now, not the catalog. **(2) catalog docs resynced** 509→**790** tours / 9→**14** makers / 561→**969** stops (undocumented bureaus: Bangkok, Paris, Seoul, LA, Naoshima; 760 single + 30 multi-stop; bilingual now `日本語`/`中文`/`한국어`/`ไทย`). Branch-delete owed via GitHub UI (proxy blocks it). | 2026-07-19 |
| `HANDOFF-260719.md` | Historical (superseded by `-260719-2`). Session 59, 2026-07-19 (code/infra, web session): **(1) on-demand signed-TestFlight CI** (`.github/workflows/testflight.yml`, PR #393) — a web/Linux session can now build device-testable TestFlight builds with no Mac (PR label `build` or Actions → Run workflow; runbook `docs/testflight-ci.md`). Owner set 3 Actions Secrets + a `build` label; cloud signing needs the API key at **Admin** role; build numbers use `github.run_number`, `MARKETING_VERSION` bumped to **1.1** (PR #394). **(2) Journeys — Phase 1 shipped** ([PR #395](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/395) → `main`, squash `9fe6149`; TestFlight **1.1 (7)**, owner device-verified): user-curated ordered collections of whole tours (never split). New `Models/Journey.swift`, `Data/JourneyService.swift` (Supabase CRUD), `Features/Journeys/` (list/detail/add-to-journey); "Journeys" row on own-profile + "Add to a Journey" in tour menu; `JourneyService` injected app-wide + into both UIKit slide-up layers. `backend/journeys.sql` applied to Supabase by owner (app build is independent of it). First feature built + shipped entirely from a web session via the new CI. **(3) Group Listen — Phase 1 shipped** ([PR #396](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/396) → `main`, squash `2ba2f58`; TestFlight **1.1 (8)**): synced group listening, leader-driven, free "Listen Together" over **MultipeerConnectivity** (offline). `Features/GroupListen/` (state + transport seam + `MultipeerTransport` + `GroupListenCoordinator` + sheet + banner); "Listen together" in the tour ⋯ menu; `Info.plist` local-network + Bonjour keys. Additive; **device-only sync (2+ phones) owner-owned, not yet verified**. Second feature shipped from a web session today. **⚡ Capability:** the "features need a local Mac session" rule is retired — web sessions build + ship via `testflight.yml` (proven twice today). **Backlogs:** Journeys polish (`docs/journeys-design.md` §14); Group Listen remaining = Hosted mode + Pro Guide + leader-handoff/QR (`docs/group-listen-design.md`). | 2026-07-19 |
| `HANDOFF-260705-5.md` | Historical (superseded by `-260719`). Session 58, 2026-07-05 (code): **Me-tab first-open lag fully killed → TestFlight 1.0 (74).** Build 73 cached only the follow counts, so the Me tab still flashed a placeholder profile + empty tour feed on the first tap after a cold launch (owner: "i dont EVER want to see the lag"). [PR #365](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/365) extends stale-while-revalidate to the **whole profile**: new `Data/ProfileSnapshotStore.swift` (generic per-signed-in-user `Codable` snapshot in `UserDefaults`); `MakerProfileService.myMaker` (name/bio/avatar/links) + `MakerTourService.myTours` **hydrate from it synchronously at init** (uid available because `AuthService` restores the session synchronously) and write through on load/save; `MakerTour` made `Codable`; the App pre-warms both at launch so the refresh is in flight before Me opens; `hydrateIfUserChanged()` swaps accounts without clobbering fresher data. First Me paint now shows the real profile + tours instantly (avatar frame-zero too, since `myMaker`/`avatarURL` is known). `test_sim` **193/193** (+8 `ProfileSnapshotStoreTests`). Bump **73 → 74** ([PR #367](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/367)); archived clean off `origin/main`, binary-verified (`1.0 (74)` / `Dozent` / no `TEMP_LOCAL_DEMO`); owner uploading via Organizer. **Device-verify owed:** build 74 (cold-launch → tap Me → instant, no flash) + build 73's #357 launch race / #358 counts+avatar. | 2026-07-05 |
| `HANDOFF-260705-4.md` | Historical (superseded by `-5`). Session 58, 2026-07-05 (code): **Build 73 cut — profile perf + create-tour tag picker + bottom-module fix.** Built the Me-tab perf/polish fix ([PR #358](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/358)) — stale-while-revalidate: `Data/FollowStateStore.swift` caches follow counts (in-memory + a bounded, **per-signed-in-user** `UserDefaults` blob; returns last-known on a failed refresh, never clobbers to 0) so the Me tab shows counts instantly; `ImageCache` gained an **avatar disk layer** (SHA-256 files in `Caches/AvatarCache`, memory-first so #326 is preserved) so the profile photo renders frame-zero across relaunches (no monogram flash). Then, on owner direction, **merged #358 + [#359](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/359) (create-tour tag picker) + [#357](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/357) (bottom-module fix)** and cut **TestFlight 1.0 (73)** (bump #361, normal squash on green CI; archived clean off `origin/main`, binary-verified `1.0 (73)` / `Dozent` / no `TEMP_LOCAL_DEMO`; owner uploading via Organizer). **Device-verify owed on 73:** #358 counts+avatar (login-gated) and #357 launch race (not sim-reproducible). Kyoto batch 2 (#360, +41) rode along as content — catalog **9 makers / 550 tours / 602 stops**. | 2026-07-05 |
| `HANDOFF-260705-3.md` | Historical (superseded by `-4`; **#357 merged into build 73**). Session 58, 2026-07-05 (code): **Bottom-module install race fixed — mini-player/tab bar could be missing on launch** ([PR #357](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/357), CI + `test_sim` green, **NOT merged — awaiting owner device verification**). Root cause: the mini-player + tab bar live in a **secondary `UIWindow`** (`BottomModuleWindowController.install()`) that required a `.foregroundActive` scene, returned silently if none, and was called **only once** from `.onAppear` — so on cold launches where `.onAppear` beat the scene to active, install bailed with no retry and the module was absent all session. Fix: also call install on **`scenePhase == .active`** (factored into `installBottomModule()` so both sites inject identical environments) + harden `install()` with a **one-shot `UIScene.didActivateNotification` retry** (iPad-multi-scene-correct); same `window == nil` guard → created exactly once; deferred install re-applies the cached color scheme. New `installOutcome(...)` pure decision + `BottomModuleWindowTests`. **`test_sim` 162/162** (159+3); normal launch still renders the module (sim-verified). NEXT: **owner launches the next build repeatedly (cold+warm) to confirm, then merge #357** — no build cut yet. | 2026-07-05 |
| `HANDOFF-260705-2.md` | Session 57, 2026-07-05 (code): **Tag Phase 2 (browsable tag UI) + Dozent identity → TestFlight 1.0 (72).** Phase 2 ([PR #352](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/352)) turns the Phase-1 tag vocabulary into browsable Home UI: `Models/Tag.swift` (curated shelves + filter chips + D6 matcher + D5 primary — the single editorial edit point); curated shelves replace category shelves; `TagFilterChipRow` multi-select filter chips + a "Walks" format chip; filtered results = a flat feed of **full-width 4:3 cards** (195pt = rail height, owner-reviewed), Continue-listening filter-scoped; location rails anchor to map-view center with a near/far swap (§1.5); `validate-tours.swift` enforces the vocab (15 backfill warnings). `test_sim` 159/159. Shipped alongside **#353** (brass app icon + rename to **Dozent**). Build 72 (bump #354) archived + binary-verified (`1.0 (72)`, `CFBundleDisplayName=Dozent`); owner uploading. NEXT: 15 tag-backfill tours (over-the-air); Tag Phase 3 (drop `primaryCategory`) is later. | 2026-07-05 |
| `HANDOFF-260705.md` | Session 56, 2026-07-05 (code): **Directed polish pass → TestFlight 1.0 (71).** Owner pivoted from features to polish after an honest app assessment. **Brand identity decided: dark gold (brass) `#8B7535`** — the color the app already wore, confirmed as THE accent, same value light+dark (no dark variant); terracotta `#B85042` fully removed; `AtlasColors.mapPin` now = `accent` ([PR #344](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/344)). Same PR: **haptics** (`AtlasHaptics` — save/follow/download + the geofence "you've arrived" bump; device-only) + **error toasts** (`AtlasToast` — a `ToastCenter`/`ToastHost` in the bottom-module window, above every modal; wired to follow + approve/decline failures). Also merged the owner's **home-lag fix** ([PR #347](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/347)): `HomeView` stays mounted across tab switches so the `MKMapView` isn't rebuilt on return. Dead `TourListCard.swift` deleted. `test_sim` 140/140; build 70→71 (#351) archived + binary-verified — **live on TestFlight 2026-07-05 (owner-confirmed).** NEXT: upload-progress, empty-states sweep, gold-led app icon (still placeholder). Memory: `project-brand-color-brass-gold`. | 2026-07-05 |
| `HANDOFF-260704-4.md` | Historical (restored 2026-07-26 — written in a parallel session and never merged; the CLAUDE.md/ROADMAP half of that sync landed, the handoff itself did not). 2026-07-04 (code): **perf — Home + its map kept alive across tab switches, fixing the return-to-Home lag the owner reported on device** ([PR #347](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/347), squash `b3441c6`, owner-reviewed on device → merged). `ContentView.tabContent` was a `switch` that unmounted `HomeView` on tab-away → returning rebuilt the SwiftUI `Map` (reload tiles + re-cluster ~509 annotations) and reset the `@State` camera to the NYC default. Profiling (temp timing, reverted) proved the Swift recompute negligible (`filteredTours` ≈0.02 ms, cluster ≈1–3 ms) — the cost is the map's UIKit reconstruction. Fix: `tabContent` → a `ZStack` keeping `HomeView` permanently mounted (visibility toggled via `.opacity`/`.allowsHitTesting`/`.accessibilityHidden`), Library/Me overlaid opaque; off-screen Home made inert via `allowsHitTesting(false)` + a new `isActive` guard on the camera callbacks. The map is SwiftUI's **native `Map`, not a `UIViewRepresentable MKMapView`** (correction to the task's premise). `test_sim` **140/140**; CI green; overlay composition sim-verified, tab-switch timing owner-device-verified (the modal-window tab bar isn't automatable). Tradeoffs: one persistent map (small memory cost); an open Search now persists across a tab-away. | 2026-07-04 |
| `HANDOFF-260704-3.md` | Session 55, 2026-07-04 (code): **Batch D COMPLETE — D2 follow-lists + D3 requests shipped in TestFlight 1.0 (70)** ([PR #334](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/334), one owner-authorized merge; no new backend). **D2:** follower/following counts on any maker page tap through to `FollowListView` (shared `MakerAvatarView` rows) via `list_followers`/`list_following`. **D3:** a private account's `pending` follows surface as a "N follow requests" link → `FollowRequestsView` with Approve (`status→accepted`) / Decline (delete), gated by the existing RLS. `MakerRow.userId` made optional (seed studios have null `user_id`). `test_sim` 140/140; count-tap nav code-verified, populated lists + approve/decline owner-device-verified. Build 70 (bump #338) archived + binary-verified; owner uploading. **Batch D done** (D1→D2→D3). NEXT: a "creators you follow" home feed (later add-on, no schema change) or Step 6 payments. | 2026-07-04 |
| `HANDOFF-260704-2.md` | Session 54, 2026-07-04 (code): **Batch D (social layer) begun — designed + D1 shipped in TestFlight 1.0 (68).** Owner chose the follow model (asymmetric, Instagram-style), new accounts default public, v1 = counts + lists + approve/decline (feed later). Backend (owner-applied, verified live): `makers.is_private` + a `follows` table + trigger + RLS + SECURITY-DEFINER RPCs (`follow_state`/`list_followers`/`list_following`/`list_follow_requests`); `get_catalog` returns `isPrivate`. **D1 ([PR #329](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/329)):** `Maker.isPrivate`; `Data/FollowService` (follow_state RPC + follow/unfollow); MakerView header follower/following counts + Follow/Following/Requested button; a Private-account toggle in the editor. `test_sim` 140/140; counts + toggle sim-verified, Follow button + writes device-verified. Build 68 (bump #330) archived + verified; owner uploading. Design in `docs/social-design.md` + `backend/social.sql`. NEXT: D2 (Followers/Following lists), D3 (private requests). | 2026-07-04 |
| `HANDOFF-260704.md` | Session 53, 2026-07-04 (code): **Device-testing polish batch (8 fixes) → TestFlight 1.0 (67) + a live moderation-email upgrade** ([PR #326](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/326)). From the owner running the authoring loop on build 66: (1) public maker page reflects a profile edit immediately; (2) **Play recording** to review a take; (3) no avatar flash in Library (shared `ImageCache`); (4) tighter profile editor + countdowns; (5) tighter New Tour form + countdowns; (6) Settings theme **D→L→D** fix; (7) signed-out redesign (icon 72→20pt, JOIN DOZENT mono, 44pt Sign-in — the app's universal control diameter); (8) shared self-hiding `JoinDozentPrompt` below all 3 Library empty states. **Server-side:** `notify-moderation` rewritten to a rich review email (title/maker name/description/transcript/▶ Listen/photos) with **one-click Approve & publish / Take down** — owner-deployed (Verify JWT off + `MODERATION_TOKEN`). `test_sim` 140/140. Build 67 (bump #327) archived + verified; owner uploading. NEXT: batch D — social layer (design-first). | 2026-07-04 |
| `HANDOFF-260703-4.md` | Session 53, 2026-07-03 (code): **Profile/maker polish + audio-write bug fix → TestFlight 1.0 (66)** ([PR #324](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/324)). Owner device-testing found five items: **(1)** audio/transcript writes failing with `"failed to parse order (eq.0)"` — the `stops.order` column collides with PostgREST's reserved sort param (`.eq("order",0)`→`?order=eq.0`→400); fixed by filtering single-stop drafts by `tour_id` alone (memory `reference-postgrest-order-column-collision`); **had shipped broken in 62/63** (login-only path). **(2)** editor Save ran under the bottom module → added the missing `AtlasBottomModule.height()` inset (also New Tour). **(3)** avatar **pinch-zoom+drag crop** (`AvatarCropSheet`/`ImageRenderer`). **(4)** discard-changes prompt (+`interactiveDismissDisabled`). **(5)** New Tour "Save draft & continue" + "Step 1 of 2" caption, and save now pushes the editor (step 2). `test_sim` 140/140; writes device-verified. Build 66 (bump #325, admin-merged) archived + verified; owner uploading. NEXT: batch D — social layer (design-first). | 2026-07-03 |
| `HANDOFF-260703-3.md` | Session 53, 2026-07-03 (code): **Profile/maker polish batch C → TestFlight 1.0 (65)** ([PR #322](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/322)). Two features on one owner-run Supabase migration (`link_2_url`/`link_3_url`/`avatar_initials`/`avatar_color` + `get_catalog` rebuild; RPC verified). **C1 — up to 3 links:** `Maker.link2URL`/`link3URL` + computed `links`; editor "LINKS (up to 3)"; maker page renders up to 3 inline blue links; `MakerRow` custom encoder writes cleared links as explicit null. **C2 — custom profile icon:** new shared `Components/MakerAvatarView` (photo → emoji → custom initials+colour → display-name monogram) reused across header/mini-player/Library/Search/lock-screen — closes the "no saved-list icon" report; editor avatar section (live preview + PhotosPicker square-crop upload to `tour-images/{maker_id}/avatar-*.jpg`, or initials + 10-colour grid). Seed studios have emoji → pages unchanged. `test_sim` 140/140; monogram + editor sim-verified. Build 65 (bump #323, admin-merged) archived + binary-verified; owner uploading. NEXT: batch D — social layer (follow/followers, public/private, friend requests; design-first, new `follows` table). | 2026-07-03 |
| `HANDOFF-260703-2.md` | Session 53, 2026-07-03 (code): **Profile/maker polish batches A + B → TestFlight 1.0 (64).** Owner pivoted from pricing to polishing the profile/maker experience; 11 notes grouped into 4 batches (A visual · B delete/recover/avatar · C links+custom-icon · D social). **Batch A ([PR #319](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/319)):** inline blue website `Link` under bio, display name keeps user casing + 40-char limit, public empty state = single grey box (own keeps "+"), Library Saved = tours-first. **Batch B ([PR #320](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/320)):** delete a tour (`MakerTourService.deleteTour` + destructive button/confirm in `TourAuthoringView`), Forgot-password (`AuthService.resetPassword` + sign-in link), saved-list avatar honors `avatarURL`. `test_sim` **140/140**. Build 64 (bump #321) archived clean + binary-verified; owner uploading. NEXT: batch C (3 links + custom profile icon — small `makers` schema add) · batch D (social layer — `follows` table, design-first). | 2026-07-03 |
| `HANDOFF-260703.md` | Session 51 cont'd, 2026-07-03 (code): **V2 Step 4 maker-authoring Phase-1 loop COMPLETE — LIVE in TestFlight 1.0 (63).** A signed-in user can now do the whole thing, all to Supabase: profile → create draft (metadata + map pin/radius) → editor → **record** (`AVAudioRecorder`, `NSMicrophoneUsageDescription`) **or import** audio → `tour-audio` → **photos** (PhotosPicker → crop 1200×900 → `tour-images`) → **transcript** → **Submit for review** (status→`in_review`). Increments: **#307** (create-a-tour draft form + My-Tours feed w/ status badges; `MakerTourService`, `TourStatus`, `ensureMaker`) → **#310** (full `TourAuthoringView` editor: `attachAudio`/`attachPhotos`/transcript/submit; `AudioRecordSheet`). Builds **62** then **63**, both owner-confirmed live; archived clean w/ `-allowProvisioningUpdates` (mic key present, no poison). `test_sim` **140/140**. **Every write is owner-device-verified** (sim holds no session). **Remaining (both small):** submit-EMAIL owner webhook (`public.tours` UPDATE → `notify-moderation`, reports-twin) + admin **Publish** (`publish_tour`). Phase 2 multi-stop needs no backend change. Gotchas: direct table selects snake_case (CodingKeys) vs camelCase `get_catalog`; recording works in-sim via host mic; once committed to local `main` by mistake → recovered via branch+reset+rebase. Catalog grew parallel: Toronto #306 + Kyoto #309 → ~509/9. | 2026-07-03 |
| `HANDOFF-260702-2.md` | Session 52, 2026-07-02 (web/PM — content): **Kyoto launched — 30 tours + 9th maker Atlas Studio KYO ([PR #309](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/309), `af02819`, auto-merged).** New maker Atlas Studio KYO (`50b53af5-…`, 🇯🇵) + 30 single-stop geofenced tours (30 m), bilingual `English \| 日本語`, owner-supplied audio + images (no pipeline), deterministic uuid5 (`atlas-*:kyo`, same scheme as Tokyo). **Catalog 479 → 509 tours / 8 → 9 makers / 561 stops; Kyoto = 30.** Validator PASS. Assets-first to gh-pages (30 mp3 + 111 webp, lowercase-hyphen slugs). **Confirmed live** on Supabase RPC (~1 min, 509/Kyoto 30) + gh-pages mirror (~7 min CDN lag, 9/509/30). **🚩 Flag:** La Collina Ōmi-Hachiman (`35.16193, 136.08880`) is in **Ōmi-Hachiman, Shiga Prefecture** (~40 km, different prefecture) — `city`=Ōmi-Hachiman, owner to confirm; other out-of-city tours set to locality (Byōdo-in→Uji, d-matcha→Wazuka, Asahi villa→Ōyamazaki, Kibune→Kibune). Supplied JP for 7 folders (brief said 6 — HARIO CAFE also lacked it). **Kyoto is the 9th city, not the 8th** — Toronto (#306, YYZ 🇨🇦, +10) landed as the 8th first; Kyoto rebased additively onto the moved main. New memory: `reference-live-poll-maker-count` (Supabase RPC reports more makers than the file — assert on tour counts). Also backfilled the Toronto content block + drifted CLAUDE.md counts (were 469/7). | 2026-07-02 |
| `HANDOFF-260702.md` | Session 51, 2026-07-02 (code): **V2 Step 4 (maker authoring) BEGUN — the profile-as-maker-page architecture + first Supabase content writes.** Owner's key idea: the **Me tab is a Profile = a maker page** (one `MakerView`, modes; "each maker is a user too"). Three increments merged/near: **#300** Me tab → `ProfileView` (`MakerViewMode` `.publicMaker`/`.ownProfile`; Settings behind a gear; "+" add affordance); **#302** public creator page is now **its own standalone screen** via `MakerPresenter` (bottom-layer slide-up + X, mode `.publicStandalone`; deep-link/Search/Library route through it; **replaced #297's `.sheet` placeholder**; "Go to creator" stays an in-stack push); **#304 (OPEN, owner review)** create/edit creator profile → real `makers` row (**first client→Supabase content write**; `MakerProfileService` + `ProfileEditorView` + "Edit Profile" pill). Owner decision: **one login = one profile**, starts empty; seed studios stay separate. `test_sim` **140/140**. Backend needed no setup (RLS/grants already applied). Also merged parallel: **#297** (Universal Link deep-linking, share) + **#301** (report emails include tour title). **TestFlight = 1.0 (61)** (`main` build 61, rode in with #297); Step-4 work may not be in the live 61 — **cut a fresh build after #304** to get maker authoring on device. **Next: increment 2b** — create-a-tour form (metadata + map pin/radius → `draft`), then audio/photos/transcript/submit. New memory: `reference-two-repo-clones-build-target`. | 2026-07-02 |
| `HANDOFF-260701-2.md` | Session 50, 2026-07-01 (infra, no code): **report-a-concern EMAIL notifications now LIVE — owner-confirmed end-to-end.** Closed the last open checkbox on the shipped "Report a concern" feature (the in-app insert already worked; only the server email was unset). Pure Supabase-dashboard + Resend hand-holding — the `notify-moderation` Edge Function code already existed in the repo. Set up on project "Dozent" (`apkcihljybvuyuzpbnqd`): Resend free account (`edward.yung@gmail.com`) → API key; Edge Function `notify-moderation` deployed (Verify-JWT ON); three secrets (`RESEND_API_KEY`, `MODERATION_EMAIL=edward.yung@gmail.com`, `FROM_EMAIL=Atlas <onboarding@resend.dev>`); Database Webhook `report-notify` on `public.reports` INSERT → the function (Webhooks moved out of the Database sub-menu → use `…/database/webhooks` URL). **Verified:** curl insert (anon key, `Prefer: return=minimal`) → HTTP 201 → owner confirmed the "Atlas: tour reported — …" email arrived. Caveats: Resend free tier + `onboarding@resend.dev` only emails the Resend account owner's own address (fine for the recipient; custom FROM domain = later upgrade); `MODERATION_EMAIL` is a temp recipient (swap the secret to change); recipient + key are server-side only. **Deferred:** optional `tours` UPDATE→in_review webhook (maker moderation) until Step 4. **Owner cleanup owed (SQL Editor):** delete the test report row + old throwaway `auth.users` test users. | 2026-07-01 |
| `HANDOFF-260701.md` | Session 49, 2026-07-01 (code): **fixed the owner's real-device sync bug — un-saving a tour then signing out/in no longer resurrects it in Saved — and archived TestFlight 1.0 (58).** Two PRs: **#291** (pre-sign-out flush; shipped in 57, *necessary but not sufficient*) added `SyncService.flushPendingWrites()` awaited via a new `AuthService.preSignOut` hook *before* `client.auth.signOut()` (uid still valid; `[weak self]` avoids a retain cycle). Owner retested 57 → still resurrected, because that flush's push was itself a no-op. **#294** (the real fix, build 58): `LibraryStore.toggleSaved` un-saves by nil-ing `savedAt` but **keeps** the entry, so the row is *upserted, not deleted* — and Swift's **synthesized `Encodable` omits nil optionals**, so `saved_at` was absent from the upsert body and PostgREST's `ON CONFLICT DO UPDATE` left the old (saved) value in place. Fix: custom `UserLibraryRow.encode(to:)` emits nullable columns as **explicit JSON `null`** so the upsert clears them (only `UserLibraryRow` affected). New test `test_libraryRow_encodesNilOptionalsAsExplicitNull`; `test_sim` **121/121**. Build 58 (bump #295) archived clean (`/tmp/Atlas-20260701-0023-b58.xcarchive`, `1.0 (58)`, binary greps clean). **Pending owner Organizer upload + device retest.** Gotcha logged: memory `reference-supabase-upsert-null-omission` (upserts that must NULL a column need explicit-null encoding). Not fixed (flagged): cross-device *offline* un-saves still won't propagate (additive-union merge → needs tombstones). Did all work in a `git worktree` off `origin/main` — shared checkout was mid-flight on `claude/share-universal-links`. | 2026-07-01 |
| `HANDOFF-260630-2.md` | Session 47, 2026-06-30 (code): **V2 Step 3 (accounts & auth) COMPLETE — shipped TestFlight 1.0 (56).** Took the auth foundation through the full sign-in trio + cross-device sync. Builds: 52 **Apple** (#274, `signInWithIdToken(.apple)` + nonce + `applesignin` entitlement; device-confirmed) · 53 **Google** (#277, supabase-swift OAuth web flow via `ASWebAuthenticationSession`, no SDK; `dozent://` scheme; owner set up a GCP **Web** OAuth client + Supabase Google provider) · 54 **library + saved-makers sync** (#279, new `Data/SyncService.swift`: sign-in pull→merge(union)→push + debounced write-through as upsert/delete-not-in via PostgREST; stores gained `onChange`+`applyMerged`) · 55 **logout-clear** (#283, sign-out wipes local synced data, no remote delete; anonymous unaffected) · 56 **Recents-recording fix** (#285 — progress recording moved from the full-screen-only `PlayerView` down to `AudioPlayerService.onProgressCheckpoint` so it fires from the mini-player too; bonus: `.ended` marks completed) + **recently-viewed sync** (#287, `RecentlyViewedStore` reshaped to `{tourId, viewedAt}` with same-key legacy migration; `user_recently_viewed`). `test_sim` **120/120**; merge logic unit-tested. **Every archive needs `-allowProvisioningUpdates`** (since the Apple capability). Owner device-confirmed sign in/out + wipe/restore; **build-56 Recents fix + recently-viewed still pending owner device retest.** Gotcha logged: a stacked-PR `--delete-branch` auto-closed the child PR — recovered via `rebase --onto main`. **Addendum (same session):** shipped **"Report a concern"** (#290) + **build 1.0 (57)** (bump #292; also carries sync-flush fix #291) — ••• menu → `ReportSheet` inserts into `reports` (`returning: .minimal`; email removed from client). Two bugs fixed live: RLS-blocked insert (read-back tripped admin-only SELECT → `.minimal`; owner re-applied the missing `reports` insert grant/policy) + a crash (`AuthService` env absent in the UIKit-hosted layers → made it optional). **Next:** report-email notifications (owner Resend + `notify-moderation` deploy + webhook — owner doing Resend next); optional "Sign in to sync" nudge; then **Step 4 maker authoring UI**. Cleanup: throwaway `auth.users` test rows. | 2026-06-30 |
| `HANDOFF-260630.md` | Session 48, 2026-06-30 (web/PM — content): **Tokyo launched — 63 tours + 7th maker Atlas Studio TYO ([PR #280](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/280), `4ab886a`, auto-merged).** New maker Atlas Studio TYO (`be5797bb-…`, 🇯🇵) + 63 single-stop geofenced tours (30 m), bilingual `English \| 日本語`, owner-supplied audio + images (no pipeline). **Catalog 406 → 469 tours / 6 → 7 makers / 521 stops; Tokyo = 63.** Validator PASS. Assets-first to gh-pages (63 mp3 + 215 webp, lowercase-hyphen slugs; Sumida Hokusai `.jpg.webp` cleaned); idempotent assembler off `origin/main` (uuid5 ids). **Confirmed live** on both Supabase RPC (~1 min) + gh-pages mirror (~6 min CDN lag): 7/469/63, assets 200. **3 geocoded data fixes flagged:** Hōrin-ji folder coord was in Kyoto → Waseda (`35.70729, 139.71889`); Edo-Tokyo Open Air Museum (no coord) → Koganei (`35.71637, 139.51274`); Nanago-Dori toilets (no coord) → Hatagaya (`35.67902, 139.67477`). Supplied/cleaned Japanese for 9 folders + garbled Shibuya Crossing → 渋谷スクランブル交差点. *6th city was SF (SFO, 35 tours), already on main — brief's 371/5 baseline was stale.* | 2026-06-30 |
| `HANDOFF-260627.md` | Session 46, 2026-06-27 (code): **V2 Step 2 app cutover — the app now reads its catalog from the live Supabase backend ([PR #255](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/255), `fa8ec2a`).** `RemoteCatalogLoader` now fetches the `get_catalog` RPC on project "Dozent" (`apkcihljybvuyuzpbnqd.supabase.co`) **first**, with gh-pages `Tours.json` as an automatic **fallback mirror** (then on-disk cache, then bundled seed). **No SDK** (owner decision) — a plain `URLSession` `SupabaseCatalogFetcher` (apikey/Bearer headers) covers the RPC POST; `supabase-swift` deferred to Step 3 (auth). New `Data/SupabaseConfig.swift` holds the project URL + **client-safe anon/publishable key** (RLS-gated, ships in binary; service_role NOT in repo) + `isConfigured` guard. New ordered `CatalogSource`s; `refresh()` tries Supabase→gh-pages; existing callers route through a backward-compatible convenience init. +3 fallback tests; **`test_sim` 113/113**, CI green. **Live sim-verified:** debug log (removed before commit) showed `CATALOG REFRESH OK from apkcihljybvuyuzpbnqd.supabase.co — 370 tours, 5 makers`; Home rendered the full catalog. RPC curl-checked: 200, 5/370/396. **⚠️ New rule:** Supabase is now primary, so content changes must reach the DB (`seed_from_toursjson.py`), not just gh-pages. **Shipped in TestFlight 1.0 (50)** (bump 49→50 via PR #257), live 2026-06-27 — also carries the geofence fix (#251). | 2026-06-27 |
| `HANDOFF-260626-2.md` | Session 45, 2026-06-26 (code): **geofence "already-inside" fix — AMNH stop 2 now triggers at tour start ([PR #251](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/251), `2eb89c0`).** `CLLocationManager` only fires `didEnterRegion` on a boundary **crossing**, never for a region the user is already inside at monitoring start — AMNH's manual intro and geofenced stop 2 share identical coords, so stop 2 never fired. `ProximityMonitor` now `requestState(for:)` + `didDetermineState`: on `.inside`, a pure unit-tested `decideInsideStopAction` plays now if the player's idle else **holds the stop until the intro ends** (`withObservationTracking`); de-dupe via `playedStopIds`; overlapping → first stop in tour order. `PlayerView` passes `startedStopId` + cancels the pending hold on the intro→stop-0 auto-advance so the two paths never double-play. **General fix** (4/5 multi-stop tours lead with a geofenced stop-0); **tour data unchanged.** +7 tests, `test_sim` **110/110**, CI green. Sim-verified (stop 2 fires untouched; stop 3 still crosses normally). **Owner authorized the merge but hasn't done the in-sim review yet; not in a TestFlight build — needs a 49→50 bump when wanted.** UX choice (revisitable): inside stop plays *after* the intro, not mid-intro. | 2026-06-26 |
| `HANDOFF-260626.md` | Session 44, 2026-06-26 (code): **catalog refresh hardened (PR #245) + shipped in TestFlight 1.0 (49).** Resolves the build-46 stuck-tester bug — the remote `refresh()` gave up after one 15s timeout, silently, only at cold launch. Four fixes in `RemoteCatalogLoader`/`DataService`/the App entry: **retry + ~1s/2s backoff** (3 attempts; transient-only — network/timeout/5xx/408/429, not a clean 4xx; local copy never clobbered), **30s/60s timeouts** (was 15s), **refresh-on-foreground** (`scenePhase → .active`, 60s debounce + in-flight guard → reopen picks up new content, **no force-quit**), and a **`CFBundleVersion`-stamped cache** discarded across app-version changes (47→48 case). +8 tests (**103/103** via `test_sim`). **Proven live in the sim:** ★ on one gh-pages title → background+reopen (same pid) → ★ appeared ~1s → reverted **byte-exact** (sha256, 0 ★). `CatalogFetching` seam preserved. Build bumped 48 → 49 via PR #249; build 49 also carries #244 (detail Nearby Tours), #246 (maker sort/view persist), #247 (home polish), #248 (search polish). Content unchanged (~370 tours / 5 makers, ships live via the remote catalog). | 2026-06-26 |
| `HANDOFF-260623.md` | Session 42, 2026-06-23 (web/PM — docs sync): refreshed `CLAUDE.md` + `ROADMAP.md` Current State to reality after the docs drifted to ~307/4 while the catalog grew. **Verified live: 362 tours / 5 makers / 381 stops / 4 multi-stop** — NYC 100 · London 97 · Lisbon 66 · Porto 54 · **Hong Kong 45** (5th maker, newest city, built 0→45 over a few days, **all 45 bilingual `English \| 中文`**). 4 multi-stop walks: AMNH Four Facades + Fifth Avenue Walk (NYC), After the Fire: Wren's City + Albertopolis (London); 3 more London walks drafted on `claude/london-batch3-scripts-260616`. **Remote-catalog era re-stated:** since build 46 content ships with no app build (PR #209 fetch + PR #212 auto-publish → live ~5 min after merge + relaunch); **TestFlight 1.0 (46) current, last content-driven build.** In flight: Paris (6th city, `claude/paris-scripts-260622`) + V2 creator-platform design branches. Docs only — no app/content/build change. Carried TODO: 2 dead gallery images (The Oculus, The Charging Bull). | 2026-06-23 |
| `HANDOFF-260621.md` | Session 41, 2026-06-21 (web/PM — V2 backend design): designed + merged the near-term V2 maker-platform backbone as docs + non-shipping SQL / one Edge Function (**nothing ships in the app yet**). **#218** catalog foundation (Supabase schema + `get_catalog()` RPC + seed, 5/307/316 parity), **#220** accounts/auth (self-serve makers + per-tour moderation, Apple+email+Google, consumer accounts now; profiles/RLS/reports/sync tables), **#222** maker dashboard P1 + media storage (public buckets, `{maker_id}/{tour_id}` path RLS), **#223** Phase 2 multi-stop (no backend change needed), **#224** minimal moderation ("email me" Edge Function + `publish_tour`/`takedown_tour` helpers), plus **#219** doc-sync (307/5/316) and **#221** tracked V2 roadmap. Owner decisions recorded. **TODO:** 2 dead gallery images (The Oculus, The Charging Bull — Wikimedia 404) deferred by owner. **Next (owner/Mac):** stand up Supabase + seed; supabase-swift + RPC swap + sign-in/sync + maker UI; Step 6 payments needs pricing/Stripe calls. #223/#224 green-pending at session end (repo auto-merge off). | 2026-06-21 |
| `HANDOFF-260618.md` | Fortieth session 2026-06-18 (verify/build): **PR #209 (catalog detach) verified + merged (`de8ff6a`) and TestFlight 1.0 (46) cut and shipped.** The app now fetches `Tours.json` from gh-pages at launch — local copy first (last-good network cache → bundled seed) for an instant, offline-capable first frame, then a background refresh republishes the `@Observable` `tours`/`makers` live; any network/decode failure keeps the local copy. New `Data/RemoteCatalogLoader.swift` (fetch behind a `CatalogFetching` protocol) + injectable loader/`autoRefresh` on `DataService`; bundled `Tours.json` kept as the offline seed; 95/95 tests (7 new `RemoteCatalogLoaderTests`). **Content can now ship by pushing one gh-pages file — no rebuild/App Store review.** **Caught a drift bug:** the published gh-pages file was stale at 272 while `main` was 300 (sessions 39's #205/#206 never re-published) — a fresh launch would have regressed 300 → 272; **republished gh-pages to 300** (verified live), proved the live update end-to-end (added ★ to one title on gh-pages → relaunch showed it with no rebuild → reverted clean), and caught the PR branch up to 300 before merging. Bump 45 → 46 via short-lived **PR #210** (`6418fba`, admin-merged); archive `/tmp/Atlas-20260618-2257-b46.xcarchive`; embedded `1.0 (46)`; no validation 90474. **Upload gotcha:** Organizer's *PLA Update available* + *No iOS Distribution certificate* = unaccepted Program License Agreement (cert error is downstream) — owner accepted at developer.apple.com/account, retried, **1.0 (46) live.** **Follow-up flagged: auto-publish `Tours.json` → gh-pages on merge-to-main** so the remote can't drift from the bundle again. | 2026-06-18 |
| `HANDOFF-260616.md` | Thirty-ninth session 2026-06-16 (local build cut): **TestFlight 1.0 (45) cut and shipped — catalog crosses 300 tours.** Build cut to ship everything on `main` since build 44 (`7f675a3`): **PR #205** (6 Lisbon tours — Conserveira, Dolls Hospital, Oceanário, Palace Fronteira, Ponte 25 de Abril, Vasco da Gama Bridge; LIS 60 → 66) + **PR #206** (22 London tours into Tours.json; LDN 58 → 80). Content + images only, no app-code change. **Catalog 272 → 300 tours / 4 makers** (100 NYC + 80 LDN + 66 LIS + 54 OPO). Bump 44 → 45 via short-lived **PR #207** (`acc05b4`); archive clean at `/tmp/Atlas-20260616-2045-b45.xcarchive` (~23s warm); embedded `1.0 (45)` verified; no validation 90474; owner uploaded via Organizer. Worktree pattern: `git worktree add -b claude/build-45-bump /tmp/build45 main` (plain `worktree add /tmp/build45 main` fails — `main` already checked out in the primary). **Recurring snag (3rd time): `gh pr merge --delete-branch` throws `'main' is already used by worktree` after the squash already landed server-side — recover by deleting the remote branch + ff-pull in the primary checkout, then archive from there.** | 2026-06-16 |
| `HANDOFF-260615.md` | Thirty-eighth session 2026-06-15 (web/PM): **Lisbon batch 3 — 14 tours (PR #200) + TestFlight 1.0 (44) cut and shipped.** LIS 46 → 60 (Belém — Monument to the Discoveries + National Coach Museum; the four Alfama/Bairro Alto miradouros; the Estrela basilica + garden; Casa dos Bicos; Avenida da Liberdade; Jazigo dos Duques de Palmela; Ribeira das Naus; Village Underground Lisboa); **catalog 258 → 272 tours / 4 makers / 281 stops** (100 NYC + 58 LDN + 54 OPO + 60 LIS). Audio `d4bd503` + images `b56899a` (38 webp) on gh-pages, all URLs 200; owner-supplied assets, no sourcing pipeline; append-only assembler (clean 640-insertion diff); squash-merged `cccbd9b`. Build also carries **PR #201** (`0ea562b`, parallel session) — home placecard polish (ALL-CAPS title, 2/3-screen width, secondary distance). Bump 43 → 44 via short-lived PR #202 (`7f675a3`); archive clean at `/tmp/Atlas-20260615-2246-b44.xcarchive`; embedded `1.0 (44)` verified; owner uploaded via Organizer. Category note: Ribeira das Naus = `culturalHeritage` (shipyard heritage, not green space). **Parallel-session win: all mutating work ran in isolated worktrees — no branch-flip this session.** Quirks: accented drop-folder name (`Graça`) broke shell globs (macOS NFC/NFD); `gh pr merge --delete-branch` threw a local `'main' is already used by worktree` error after the merge already landed server-side. | 2026-06-15 |
| `HANDOFF-260612-4.md` | Thirty-seventh session 2026-06-12 (web/PM): **Lisbon batch 2 — 15 tours (PR #198) + TestFlight 1.0 (43) cut and shipped.** LIS 31 → 46 (funiculars, the big museums, Pink Street); **catalog 243 → 258 tours / 4 makers / 267 stops** (100 NYC + 58 LDN + 54 OPO + 46 LIS). Audio `a7d532c` + images `4116b8e` (48 webp) on gh-pages, all 63 URLs 200; owner-supplied assets, no sourcing pipeline. Bump 42 → 43 via short-lived PR #199 (`30db8fa`); archive clean at `/tmp/Atlas-20260612-1923-b43.xcarchive`; embedded `1.0 (43)` verified; owner uploaded via Organizer. Quirks: São Roque drop folder named by coords only (identified by inner filenames); Pink Street ships hero-only; two transcripts needed CSV quote-artifact cleanup. **Parallel-session lesson: another session flipped this checkout back to `main` mid-task — re-verify the current branch immediately before committing.** | 2026-06-12 |
| `HANDOFF-260612-3.md` | Thirty-sixth session 2026-06-12 (implementation): **home drawer pivoted to category rails** (PR #194, owner-driven at the sim) + **TestFlight 1.0 (42) cut and shipped** (bump PR #196). Drawer renders `HomeRailsViewModel.rails()` — Continue-listening row (player's loaded tour first, last-listened fallback) → NEAR YOU → IN VIEW → whole-catalog category shelves, viewer-distance-sorted; chips jump-scroll. Rail cards 260pt / 4:3 uncropped heroes, one-line all-caps titles. Detent-persistence fix (drawer stays mounted under the tour-detail layer — `tourLayerCoversDrawer` + `BottomLayerController.dismiss` completion); compass relocated beside the recenter button (needs ⌥-drag hand check); 44pt pin hit areas. `TourListCard.swift` now unused. 88/88 tests. Classifier note: direct-to-main bump push was blocked — use the short-lived-PR pattern. | 2026-06-12 |
| `HANDOFF-260612-2.md` | Thirty-fifth session 2026-06-12 (local build cut): **TestFlight 1.0 (41) cut and shipped.** Build bumped **40 → 41 direct-to-main** (`7cf3590`, app-target `CURRENT_PROJECT_VERSION` lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). `xcodebuild archive` clean at `/tmp/Atlas-20260612-0832.xcarchive` (~1–2 min, warm DerivedData); embedded version verified `1.0 (41)`; **no validation 90474**. Owner uploaded via Organizer. **TestFlight 1.0 (41) is live.** Carries **PR #193** (London batch 2 — 18 Bloomsbury/South Bank tours) + #195 docs. **London 40 → 58 — now the #2 city ahead of Porto; catalog 225 → 243 tours / 4 makers.** Bump + archive ran in a `/tmp/build41` worktree — primary checkout was mid-flight on `claude/home-drawer-rails` (**PR #194 intentionally NOT included**; owner reviewing separately). | 2026-06-12 |
| `HANDOFF-260612.md` | Thirty-fourth session 2026-06-12 (web/PM, multi-day): **33 London tours shipped via the audio-pending staging workflow** — its first at-scale run. Tours text-drafted + image-staged on per-batch branches before audio existed (`drafts/pending-tours.json` + gh-pages images); MP3s uploaded 5-at-a-time when ready (durations via `mutagen`); per-tour captions + longDescriptions authored at assembly. **PR #192 (batch 1: 15 West End/Soho)** + **PR #193 (batch 2: 18 Bloomsbury/South Bank)** squash-merged, CI green. **Catalog 4 makers / 243 tours / 252 stops; London 25 → 58.** Batch 1 in TestFlight 1.0 (40); batch 2 awaits the next cut. Lessons: conflicted PRs never trigger CI; take main's Tours.json + re-run the idempotent assembler on conflict; remote-env git proxy blocks branch deletion (delete `claude/dreamy-wozniak-nM6a4` + `-batch2` via UI); racing gh-pages deploys 401 harmlessly. | 2026-06-12 |
| `HANDOFF-260611.md` | Thirty-third session 2026-06-11 (web/PM): **TestFlight 1.0 (40) cut and shipped.** Build bumped **39 → 40 direct-to-main** (`0cf79b3`, app-target `CURRENT_PROJECT_VERSION` lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). `xcodebuild archive` clean at `/tmp/Atlas-20260611-1900.xcarchive` (~3 min); embedded version verified `1.0 (40)`; **no validation 90474** — `UIRequiresFullScreen=YES` from build 34 held. Owner uploaded via Organizer. **TestFlight 1.0 (40) is live.** Carries **PR #192** (London batch 1 — 15 West End / Soho tours: theatre district, shopping, Soho landmarks). Content + images only, no app-code change. **London 25 → 40** (West End / Soho, on top of the City + Westminster/Whitehall shipped in build 39 `ea197f0`); **catalog 210 → 225 tours / 4 makers** (100 NYC + 54 OPO + 40 LDN + 31 LIS). | 2026-06-11 |
| `HANDOFF-260610.md` | Thirty-second session 2026-06-10 (web/PM): **TestFlight 1.0 (39) cut and shipped** (backfilled 2026-06-11 from git `ea197f0`). Build bumped **38 → 39 direct-to-main** (app-target lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). **TestFlight 1.0 (39) live.** Carries **PR #191** (`56f7a30`) — 26 Lisbon tours (Belém, Jerónimos, Castelo de São Jorge, Tram 28…), **Lisbon 5 → 31**, **catalog 184 → 210 tours / 4 makers** (100 NYC + 54 OPO + 25 LDN + 31 LIS). Content + images only. Caveat: the bump message said "26 Lisbon + 19 London", but the 19 London already shipped in build 38 — only the 26 Lisbon were new. Superseded by 1.0 (40). | 2026-06-10 |
| `HANDOFF-260609-2.md` | Thirtieth session 2026-06-09 (web/PM): **London expansion II** — 10 Westminster/Whitehall tours under Atlas Studio LDN (Westminster Abbey, Houses of Parliament & Big Ben, Westminster Hall, Trafalgar Square, The National Gallery, St Martin-in-the-Fields, Banqueting House, The Cenotaph, Churchill War Rooms, Parliament Square), London 15 → 25, catalog → 4 makers / 174 tours / 183 stops. Merged via PR #188 (one consolidated PR). Unsplash deep for famous landmarks; restricted-interior subjects (Westminster Hall roof, Banqueting House Rubens ceiling, Churchill War Rooms Map Room) used owner-pasted images (pulled from the session-transcript `.jsonl`). Verify-gate caught many look-alikes (V&A vs National Gallery, Budapest parliament vs Parliament Square, Women-of-WWII/Battle-of-Britain vs the Cenotaph). No build bump (TestFlight 1.0 (37); 19 newer London tours ship in the next cut, 37 → 38). Westminster Abbey hero swap to the west front in progress. | 2026-06-09 |
| `HANDOFF-260609.md` | Twenty-ninth session 2026-06-09 (web/PM): **London expansion** — 9 more London tours under Atlas Studio LDN (Lloyd's of London, Bank Junction, St Stephen Walbrook, St Bartholomew the Great, Smithfield Market, Postman's Park, The Barbican, Guildhall, Temple Church), London 6 → 15, catalog → 4 makers / 164 tours / 173 stops. Merged via PR #185 (Lloyd's) + PR #186 (consolidated 8). Lessons: interior-famous City churches have ~no usable CC0 modern photo (Unsplash = other churches, Wikimedia = CC BY-SA), so owner supplied images by pasting them in chat — **owner-pasted images are pulled from the session-transcript `.jsonl` (base64 image blocks)**, not the uploads dir; apply `exif_transpose` with no manual rotate; batch = one consolidated PR. No Swift/asset/project changes; no build bump (TestFlight stays 1.0 (37); these 9 ship in the next cut). | 2026-06-09 |
| `HANDOFF-260608.md` | Twenty-eighth session 2026-06-08 (web/PM): **London launch.** Added **Openverse** as a CC0-only image source to the pipeline (owner policy: public-domain `cc0,pdm` only — no attribution UI; verify gate mandatory since Openverse titles are unreliable; Wikimedia 429-throttles bursts). Formalized the "upload tours without images" protocol with **inline full-size** candidate presentation (grid too small). Created **fourth maker Atlas Studio LDN** (🇬🇧) and added the **first 6 London tours** (St Paul's Cathedral, The Monument, The Tower of London, Tower Bridge, Leadenhall Market, The Gherkin) — all single-stop, geofenced, Unsplash imagery (owner picked modern photos over CC0 historical art). **Tall-subject crop rule:** columns/spires need a top-biased crop (The Monument's urn) since the 1200×900 hero is landscape. Catalog 149 → **155 tours / 3 → 4 makers / 164 stops**. No Swift/asset/project changes; build bumped 36 → 37 (`49a81ac`), **TestFlight 1.0 (37) live** with the 6 tours. All merged to `main` (PR #181 + #182). | 2026-06-08 |
| `HANDOFF-260607-2.md` | Twenty-seventh session 2026-06-07 (web/PM): TestFlight 1.0 (36) cut and shipped. Build bumped 35 → 36 direct-to-main (`ca671c8`, app-target `CURRENT_PROJECT_VERSION` lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). `xcodebuild archive` clean at `/tmp/Atlas-20260607-2112.xcarchive` (fast — DerivedData warm from build 35; embedded version verified 1.0 (36)); **no validation 90474** — the `UIRequiresFullScreen=YES` iPad-orientation fix from build 34 (`62d00df`) held. Owner uploaded via Organizer. **TestFlight 1.0 (36) is live.** Carries PR #163 (full-edge module + no drawer leak on pushed details), #164 (place results limited to cities/landmarks, businesses dropped), #166 (Maker + Search background pinned to module shade), #167 (maker editorial typography + square thumbnails), #168 (new galleries: The Shed, Citi Field, LOVE Sculpture), #170 (save-maker bookmark + nav chrome + Library section). No Swift / asset / project changes beyond the pbxproj bump. Catalog unchanged at 149 tours / 3 makers. | 2026-06-07 |
| `HANDOFF-260607.md` | Twenty-sixth session 2026-06-07 (web/PM): TestFlight 1.0 (35) cut and shipped. Build bumped 34 → 35 direct-to-main (`ce32d88`, app-target `CURRENT_PROJECT_VERSION` lines only; test target stays 1; `MARKETING_VERSION` stays 1.0). `xcodebuild archive` clean at `/tmp/Atlas-20260607-0649.xcarchive` (~4 min, embedded version verified 1.0 (35)); **no validation 90474** — the `UIRequiresFullScreen=YES` iPad-orientation fix from build 34 (`62d00df`) held. Owner uploaded via Organizer. **TestFlight 1.0 (35) is live.** Carries PR #159 (Player presented from top window — gapless transition + floating island on retract), PR #160 (place-search perf via `MKLocalSearchCompleter` + tap spinner), and 7 tours' new photo galleries (Intrepid, Little Island, Manhattan Bridge, Chelsea Hotel, Four Freedoms Park, Unisphere, Cooper Union). No Swift / asset / project changes beyond the pbxproj bump. Catalog unchanged at 149 tours / 3 makers. | 2026-06-07 |
| `HANDOFF-260606-3.md` | Twenty-fifth session 2026-06-06 (implementation): Search polish + place-search performance, owner-directed at the simulator. [PR #154](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/154) — Search typography flattened to the `caption` token (result/maker titles stay `body` ALL CAPS); result rows single-line all-caps with maker-only subtitle + square thumbnails (removes the two-line category•maker wrap); new **Makers** section deep-links to `MakerView`. `SearchView.swift` only; shared `SearchBar` (Home) untouched. [PR #160](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/160) — place-search typing lag fixed by replacing per-keystroke `MKLocalSearch` with `MKLocalSearchCompleter` (instant type-ahead; heavy geocode deferred to a single on-tap resolve, spinner on the resolving row); supersedes session 23's 300ms-debounced `PlaceSearchService` internals (Search→map channel + `HomeView.flyTo` + no-tours overlay untouched). No build bump (34); 88/88 tests pass. | 2026-06-06 |
| `HANDOFF-260606-2.md` | Twenty-fourth session 2026-06-06 (implementation): full-screen Player polish, round 2 (continues session 22). Structural change — `PlayerView` is now presented as a `.fullScreenCover` from `BottomModuleRoot` (the top window) instead of `ContentView`, so the cover slides over the mini-player + tab bar in the same window; removed the hide/show-the-module-window dance (`BottomModuleWindowController.setHidden` deleted) that left a transition gap, and `PassThroughWindow.hitTest` now claims all touches while presenting a modal. Opening the player dismisses the underlying detail sheet so retracting returns to the tab root (Home floating island, not edge-to-edge). Also: single-line/marquee stop title, caption that always reserves 3 lines, system `MPVolumeView` bracketed by speaker icons with a 12pt thumb. No build bump (33); no `AudioPlayerService` API change; 88/88 tests pass. Drag-to-dismiss + volume/AirPlay device-only (sim HID can't drag). | 2026-06-06 |
| `HANDOFF-260606.md` | Twenty-third session 2026-06-06 (implementation): place/location search added to Search. A **Places** section (Apple `MKLocalSearch` — no new deps) sits above the Makers/Tours catalog results; tapping a place dismisses Search and glides the Home map camera to that region via a one-shot `HomeSharedState.pendingMapMove`. New `PlaceSearchService` (debounced 300ms, per-feature zoom from placemark radius) + `MapRegionGeometry.anyStop` (reuses the antimeridian-aware `MKCoordinateRegion.contains`). When the destination has no tours, a transient "No Atlas tours here yet" hint shows (attached via `.overlay` — a ZStack sibling of the `Map` didn't composite). Owner approved lifting the "Home map camera is settled" note for this additive change; recenter / pin-tap / startup paths untouched. Folds in the same-day Search-polish commit (caption typography, single-line result rows, maker results). No build bump (stays 33); 88/88 tests pass (4 new). Sim-only: the no-tours hint can paint a few seconds late under MKMapView tile streaming — verify prompt on device. One cosmetic `placemark` iOS-26 deprecation warning left as a follow-up. | 2026-06-06 |
| `HANDOFF-260605.md` | Twenty-second session 2026-06-05 (implementation): full-screen Player polish, owner-driven at the simulator. Two `PlayerView`-focused PRs — [#148](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/148): full-screen cover (bottom-module window hidden while the player is up via new `BottomModuleWindowController.setHidden`), caption typography, carousel parity with the detail sheet; [#150](https://github.com/ehky2882/TRAVEL-GUIDED-TOUR/pull/150): drag-to-dismiss grab-handle sheet, ••• overflow menu mirroring the detail sheet (player wrapped in its own `NavigationStack` so "Go to creator" pushes `MakerView`), five-equal-column transport with play screen-centered (`speed · −10 · play · +10 · next-track`; skip ±10s always live, next-track disabled on single-stop), thin gold knob-less scrubber, 0.5×/0.75× speeds, and a system `MPVolumeView` slider (device-only — blank in the sim by design). No build bump (stays 32); no `AudioPlayerService` API change; 84/84 tests pass each PR. Mini-player design untouched; native iOS menus kept (UIMenu font isn't customizable). Drag-to-dismiss feel + volume + AirPlay need a real-device check (sim can't drag; MPVolumeView is device-only). | 2026-06-05 |
| `HANDOFF-260604.md` | Twentieth session 2026-06-04 (web/PM): pure image-pipeline pass on existing NYC tours. No new tours, no Swift changes, no build bump. Catalog stays at 138 tours / 147 stops / 3 makers. Image pipeline codified as CLAUDE.md Rule #8 (+ full § Image Pipeline procedure). Fourteen NYC tours received new hero/gallery images via Unsplash + Gemini verify + owner pick → 1200×900 WebP q82 gh-pages: Empire State Building, Chrysler Building, Brooklyn Bridge, Met Museum, Bethesda Terrace, Grand Central (gallery only), High Line (gallery only), Rockefeller Center, One WTC, Guggenheim, Statue of Liberty, Washington Square Park (gallery only), Flatiron Building, Lincoln Center. Times Square skipped at owner request. ~25 NYC tours still need gallery images; 9/11 Memorial queued next (background fetch script running at session end). Branch `claude/session-012bd7xvvgfz8cpkucw3bqy8-0MeY7` open, not yet merged to main. TestFlight still at 1.0 (28). | 2026-06-04 |
| `HANDOFF-260603-2.md` | Nineteenth session 2026-06-03 (implementation, same-day continuation of session 18 `260603`): six small home-screen polish PRs driven turn-by-turn at the simulator, plus the build bumps that cut 27 (left unshipped by session 18) then 28. PR #128 muted standard map style (`emphasis: .muted`). PR #129 `StopPin` 14/18 → 16/20pt; cluster count text to 12pt SF Mono regular. PR #130 `HomeMapSection.tourPOI` `.including(...)` allowlist — cultural / civic / nature / transit kept; ATMs / gas stations / retail / nightlife / activity venues hidden. PR #131 drawer's `displayedTours` now filters to `visibleRegion` + sorts by map-center distance; header count collapses to `displayedTours.count`; strip-clipping helpers and unused `UIKit` import dropped; doc comment flags the future rails direction. PR #132 `BottomModuleRoot` + `BottomSheet` flipped from `.ignoresSafeArea(.container, edges: .bottom)` to `.ignoresSafeArea(.all, edges: .bottom)` — focusing a TextField (SearchView) now lets the keyboard overlay the bottom module + drawer instead of pushing them up. PR #133 `onPinTapped` animates `cameraPosition` to centre the tapped pin at the current visible span. PR #134 bumped 27 → 28 defensively (session 18's `89dd5df` direct-to-main bump to 27 left dangling). `xcodebuild archive` clean at `/tmp/Atlas-20260603-2123-b28.xcarchive`; owner uploaded via Organizer. **TestFlight 1.0 (28) is live.** Catalog grew to **138 tours / 147 stops / 3 makers** with session 18's PR #127 (7 central Porto classics — Cathedral, São Bento, Clérigos, Ribeira, São Francisco, Bolsa, Dom Luís I) absorbed into the same build. | 2026-06-03 |
| `HANDOFF-260603.md` | Eighteenth session 2026-06-03 (web/PM): 18 new NYC tours that had landed direct-to-main between sessions 17 and 18 (catalog **113 → 131 tours**, NYC-area **73 → 91**) bundled into a TestFlight cut. Notable adds: MoMA, Bryant Park, Federal Hall, Coney Island, Schomburg Center, Eldridge Street Synagogue, Grand Concourse, Strivers' Row, IAC Building (Gehry, 2007), The Strand Bookstore, Four Freedoms Park, Green-Wood Cemetery, African Burial Ground, Cooper Union, Tompkins Square Park, Columbus Park (Chinatown), Grand Army Plaza (Brooklyn), plus **Fifth Avenue Walk — second multi-stop tour ever** in the catalog (joining AMNH Four Facades from 2026-05-26). Two validator-caught typo fixes preceded the build: `triggerMode geofence → geofenced` across tours 114–131 (`3235d33`) and `TourKind multi → multiStop` on Fifth Avenue Walk (`7c11003`). Build bumped 25 → 26 in `17dba88` (direct-to-main per established pattern — single-line pbxproj edit, test-target lines stay at 1). `xcodebuild archive` clean at `/tmp/Atlas-20260603-1840.xcarchive` (~3 min); owner uploaded via Organizer. **TestFlight 1.0 (26) is live.** No Swift / asset / project structure changes this session beyond the pbxproj bump. | 2026-06-03 |
| `HANDOFF-260602-2.md` | Seventeenth session 2026-06-02 (implementation, same-day continuation of session 16 `260602`): owner sat at the sim and asked for one or two changes at a time across a long iterative polish pass. PR #113 bundles ~30 1-2-line tweaks plus the cluster smoothness fix that closes item #6 from the original 11-item home brief — typography overhaul (`AtlasTypography.caption` is now 13pt SF Mono regular; `body` is now 15pt SF Pro regular, fixed-size so Dynamic Type stops scaling that token — flagged); home-chrome dimensions tightened (search/chips 24→16pt horizontal inset, 46→44pt height; map control glyphs 16→20pt; tab bar icons 22→20pt; drawer top-corner 30→28pt); mini-player paddings completely retuned (avatar-left-to-bar-left and ring-outer-to-bar-right both 16pt; avatar→text 16pt; skip-glyph→ring outer 16pt via outer HStack spacing 0); ALL CAPS pass across home header, mini-player title line, tab bar labels (display site only — VoiceOver-safe); new `Maker.avatarEmoji` field with Atlas Studio NYC set to 🍎 (resolution order emoji → URL → bundled `AtlasStudioAvatar`); cluster smoothness via new `HomeMapSection.snappedSpan(_:)` static that rounds the visible region's span to two significant figures before deriving cluster cell pitch, killing MapKit's sub-percent settle drift on pure pans. PR #114 bumped 24 → 25 (admin-merged). `xcodebuild archive` clean at `/tmp/Atlas-20260602-2310.xcarchive`; owner uploaded via Organizer. **TestFlight 1.0 (25) is live.** Catalog unchanged at 113 tours / 3 makers. `xcodebuild test` succeeds throughout. | 2026-06-02 |
| `HANDOFF-260602.md` | Sixteenth session 2026-06-02 (web/PM): 11-tour Portugal batch via PR #110 — 8 Atlas Studio Porto (Batalha Centro de Cinema, Building in Senhora da Luz, Mosteiro Santo Agostinho da Serra do Pilar, Teatro Rivoli, Trindade Metro Station, Vodafone Headquarters, Municipal Library of Viana do Castelo, **Biblioteca Pública e Arquivo Regional Luís da Silva Ribeiro — first Azores tour**) + 3 Atlas Studio Lisbon (Adega Mayor / Campo Maior — first Alentejo tour, Óbidos, Capela do Monte / Lagos — first Algarve tour). Audio + images uploaded to `gh-pages` across four chunked commits (`f4e849d`, `259309d`, `7a67dc9`, `24c6e36`) after the combined push repeatedly hit HTTP 408 (no SSH key on machine, fell back to HTTPS chunks). Catalog 102 → 113 tours / 105 → 117 stops. Atlas Studio Porto 22 → 30; Atlas Studio Lisbon 2 → 5. New cities: Viana do Castelo, Angra do Heroísmo, Campo Maior, Óbidos, Lagos. PR #111 bumped build 23 → 24 (admin-merged). `xcodebuild archive` clean at `/tmp/Atlas-20260602-2146.xcarchive`; owner uploaded via Organizer. **TestFlight 1.0 (24) is live.** All 22 post-merge spot-checks (11 audio + 11 heroes) returned 200. No Swift / asset changes. | 2026-06-02 |
| `HANDOFF-260601-3.md` | Fifteenth session 2026-06-01 (implementation, same-day continuation of `260601-2`): eleven-item home-screen polish pass. PR #103 (default location at launch geolocates user with NYC fallback + Look Around button + `LookAroundView.swift` removed) and PR #104 (drawer `.large` detent capped below search/chips via new `BottomSheet.topReservedHeight` + `AtlasSpacing.searchAndChipsBlockHeight` token; placecard fill swapped from `.regularMaterial` to `AtlasColors.secondaryBackground`; bookmark Button replaces hero-corner category chip with `LibraryStore.toggleSaved` wiring; animated `. / .. / ...` ellipsis in tours-in-view header while panning via `TimelineView` + `HomeSharedState.isMapMoving`). Drawer-gap geometry bug caught mid-review via magenta diagnostic — `BottomSheet.heightForDetent(.large)` was double-counting top safe-area inset (`geo.safeAreaInsets.top` reports the device value even when the reader's bounds already start below it); fix removed `topInset` from the `.large` formula and both `drawerVisibleHeight` mirrors. Initial-launch zoom span widened to `0.1°` (~Manhattan length) after owner review. Items #2, #3, #8 reported as informational only — no code change. Item #6 (clustering smoothness) deferred pending visual verify. PR #105 bumped `CURRENT_PROJECT_VERSION` 21 → 22 (merged with `--admin`). `xcodebuild archive` clean at `/tmp/Atlas-20260601-2233.xcarchive`; owner uploaded via Organizer → TestFlight 1.0 (22). Owner then caught a real-device bug — drawer could be dragged past the `.large` cap during the gesture before snapping back — fixed in PR #107 (clamp drag-time ceiling to `heightForDetent(.large, ...)` instead of `geo.size.height - horizontalInset`). PR #108 bumped 22 → 23 (admin-merged); `xcodebuild archive` clean at `/tmp/Atlas-20260601-2302.xcarchive`. **TestFlight 1.0 (23) is live.** Catalog unchanged at 97 tours / 3 makers. 84/84 tests pass throughout. | 2026-06-01 |
| `HANDOFF-260601-2.md` | Fourteenth session 2026-06-01 (web/PM, same-day continuation of `260601`): 6 new Porto-area tours under Atlas Studio Porto via PR #100 — Porto Tram Museum, Igreja de Santa Maria de Marco de Canaveses (Siza, 1996), Fundação Livraria Lello (foundation HQ at the 14th-c Mosteiro de Leça do Bailio), Livraria Lello (Art Nouveau bookstore, 1906), Parque de São Roque (former Calém Port-wine Quinta da Lameira), and Pavilhão Multiusos de Gondomar (Siza, 2007). Both Lello entries shipped as distinct tours (owner-confirmed); briefing-tagged "Souto de Moura" for the Gondomar pavilion was a transcript-confirmed Siza correction. Audio + 28 webps to gh-pages (`332367f` + `2f3dc04`). New cities in catalog: Marco de Canaveses, Matosinhos (Leça do Bailio), Gondomar. Catalog 91 → 97 tours. Then PR #101 bumped build 20 → 21 (auto-mode classifier blocked direct-to-main bump per historical pattern; PR route used instead — see § 5 for follow-up). `xcodebuild archive` clean at `/tmp/Atlas-20260601-2057.xcarchive`; owner uploaded via Organizer. **TestFlight 1.0 (21) is live.** No Swift changes. | 2026-06-01 |
| `HANDOFF-260601.md` | Thirteenth session 2026-06-01 (web/PM): 10 new NYC tours added (Domino Park, Wave Hill, Queens Museum, Museum of the Moving Image, Snug Harbor, Yankee Stadium, Citi Field, Madison Square Garden, Riverside Church, One World Trade Center). Hero image audit — all 10 had broken Wikimedia URLs, all fixed. Coordinate corrections for The Cloisters + Beacon Theatre. Flatiron Building hero replaced with square-friendly portrait (3024×3903). No Swift changes. Catalog 81 → 91 tours; 73 NYC-area total. Build still 1.0 (19). Superseded by `260601-2`. | 2026-06-01 |
| `HANDOFF-260529-2.md` | Twelfth session 2026-05-29 (evening): build-bump-and-archive only — cut TestFlight build 1.0 (17). `CURRENT_PROJECT_VERSION` 16 → 17 in `f359f55` direct to `main`; `xcodebuild archive` clean at `/tmp/Atlas-20260529-1626.xcarchive`; owner uploaded via Organizer. Build 17 carries PR #91 (chrome seam, already in 16), PR #92 (Casa das Histórias Paula Rego — first Cascais tour), PR #93 (detail masthead + toolbar + overflow menu, part 1 of 2), PR #94 (5 Porto-area architecture tours — Burgo, Crasto 213, Leixões Cruise Terminal, Majestic, Marés), PR #95 (light-mode bars appearance fix). Catalog 53 → 59 tours, 3 makers unchanged. No Swift / Tours.json / asset changes this session. | 2026-05-29 |
| `HANDOFF-260529.md` | Same-day earlier session 2026-05-29: shipped PR #95 — bottom-module appearance tracks Settings picker correctly in both directions. The mini-player + tab bar live in a manually-created secondary `UIWindow`; SwiftUI's `.preferredColorScheme(...)` doesn't propagate into that window, so its `userInterfaceStyle` always tracked SYSTEM and the dynamic-provider colors from PR #91 resolved inverted whenever the picker disagreed with the system. Fix: new `BottomModuleWindowController.apply(preference:)` writes `overrideUserInterfaceStyle` directly; driven from App body via `.onAppear` + `.onChange(of: colorSchemePreference)`. Removed the install-closure `.preferredColorScheme` that was freezing the host VC's override at install time. Preserves PR #91's chrome seam and the single-form rule. 84/84 tests pass. PR #93 (detail masthead) also landed earlier the same date from a different session. TestFlight still at 1.0 (16). | 2026-05-29 |
| `HANDOFF-260528.md` | Eleventh session 2026-05-28: shipped PR #91 — bottom-module chrome-shade seam fix. Two coordinated changes: (1) `AtlasColors.secondaryBackground` swapped from `Color(uiColor: .secondarySystemBackground)` to a hardcoded `UIColor(dynamicProvider:)` block that keys only on `userInterfaceStyle` (no elevation variance) — light `#F2F2F7` / dark `#1C1C1E`. New `secondaryBackgroundUIColor` companion for UIKit consumers (`BottomLayerPresentation`'s detail hosting view). (2) `MiniPlayerBar` + `AtlasTabBar` gained `extendsToScreenEdges` parameter; bars grow edge-to-edge on Library / Me / detail-up, stay floating island on Home-no-detail. Button x positions identical across both forms via inner padding (design rule holds). Separate window-2 Rectangle removed. Diagnosed with bright contrasting test colors — workflow saved as `feedback-visual-debugging.md`. Build bumped 15 → 16; archive built; owner uploaded to TestFlight. Three polish suggestions parked: mini-player title type style, control glyph size alignment, avatar/play-ring balance. 84/84 tests pass. | 2026-05-28 |
| `HANDOFF-260527-3.md` | Tenth session 2026-05-27 (night): discovery-only kickoff of the tour-detail-sheet retool pass. Surveyed `TourDetailView.swift` post-PR #79 (X close + hero/carousel + title + chip row + maker row + meta row + description + stops + inline Start/bookmark/download row + bottom inset). Built + launched on iPhone 17 Pro sim (dark, NYC) and captured Braga gallery sheet at `/tmp/atlas-detail-DARK-gallery-*.jpg`. Asked Group A (share-button placement + shape); owner dismissed and called night. **No code or content changed.** `main` unchanged from session 9. To resume: re-pose Group A as a single question, then walk B→G per the brief. | 2026-05-27 |
| `HANDOFF-260527-2.md` | Ninth session 2026-05-27 (late): content-only PR train. Backfilled `additionalImageURLs` gallery for six existing Porto/Braga tours whose hero links were broken in PR #81 (Bouça, Chapel of Souls, Capela do Senhor da Pedra, Cantareira/Rua do Passeio Alegre 212, Casa de Chá da Boa Nova, Braga Municipal Stadium). Added 3 new single-stop Portugal tours: Expo'98 Portuguese National Pavilion (Lisbon, Siza), Piscina da Quinta da Conceição (Matosinhos, Siza), Porto School of Architecture / FAUP (Porto, Siza). New maker "Atlas Studio Lisbon" added (`B1A9EAF0-…`) for the Expo'98 tour. Catalog now: 53 tours, 3 makers, 57 stops. PRs #84–#90 all auto-merged. No `*.swift` changes; build stays at 13. | 2026-05-27 |
| `HANDOFF-260527.md` | Earlier handoff (session 8 — UIKit slide-up + chrome unification). Superseded by `260527-2`. Eighth session 2026-05-27: shipped UIKit-backed slide-up presentation for tour detail + chrome unification + action bar removal + Tours.json hero URLs to 1280px + TestFlight 1.0 (12). Replaces the SwiftUI `.offset` slide layer with a `UIPresentationController`-driven modal — mini-player + tab bar hoisted into a secondary higher-level `UIWindow` (Apple Music pattern). Fixes three regressions surfaced during the session: Library/Me tabs not switching (PassThroughWindow.hitTest was rejecting legitimate Button taps), tap-tab while detail open leaves user appearing stuck (added auto-dismiss on tab change), and the "bump" hairline at the bar's top edge (MiniPlayerBar.topGap → 0). **Known issue: subtle chrome shade mismatch in dark mode** — `.elevated` trait override on the detail's hosting controller narrows the gap but doesn't close it. See handoff for three follow-up leads. 84/84 tests pass. | 2026-05-27 |
| `HANDOFF-260526.md` | 2026-05-26 remote session: shipped PR #78 (merged — enter-slide mirror fix) + AMNH Four Facades multi-stop tour (39 tours, unblocks M-qa items 6+7). Superseded by `260527`. | 2026-05-26 |
| `HANDOFF-260525-7.md` | Seventh session 2026-05-25: shipped PR #78 — enter-slide mirror fix. Owner confirmed PR #77's exit-slide is "working perfectly" and asked for enter to be the "exact opposite." Remaining asymmetry was `AsyncImage`'s 250ms phase crossfade firing during the slide-up but not the slide-down (hero is pre-loaded by exit time). New `disableLoadAnimation: Bool` flag on `HeroImageView` lets `TourDetailView` opt out of the crossfade; other surfaces keep the gentle polish. 84/84 tests pass. Superseded by `260527`. | 2026-05-25 |
| `HANDOFF-260525-6.md` | Sixth session 2026-05-25: shipped PR #77 — tour-detail slide animation fix, closes the open issue from session 5. Two changes: (a) `displayedTour` moves to `TourPresenter`, updated synchronously inside `present(_:)` so the inner content lands in the view tree on the same SwiftUI tick the layer's offset starts animating — kills the one-tick gap SwiftUI was filling with a default opacity-fade transition; (b) drawer no longer animates opacity, instead swaps `.zIndex` based on `displayedTour` (z-4 above mini-player when no detail, z-1 below detail layer when a detail is up). Mini-player + tab bar stay at z-3 so their buttons remain tappable through the detail session. 84/84 tests pass. | 2026-05-25 |
| `HANDOFF-260525-5.md` | Fifth session 2026-05-25: shipped PR #76 — tour detail as a slide-up layer (NOT a sheet), drawer hoisted from `HomeView` into `ContentView` so it z-stacks above mini-player + tab bar, X close replaces back chevron, SearchBar + chips chrome unified to `secondaryBackground`. Three new files (`HomeSharedState.swift`, `HomeDrawerContent.swift`, `TourPresenter.swift`) + rewrites of `HomeView`, `ContentView`, `TourDetailView`, `TourListCard`, `RailCarousel`, `LibraryView`, `MakerView`, `SearchView`, `SearchBar`, `CategoryChipRow`, `BottomSheet`. Bumped to TestFlight 1.0 (10). Left one **open issue (slide animation reads as fade)** that PR #77 above closes. Superseded by `-6`. | 2026-05-25 |
| `HANDOFF-260525-4.md` | Fourth session 2026-05-25: shipped PR #70 (`643cbd7`) — buttons identical across every surface; only the edge-to-edge background fill behind the inset bar differs between Home (no fill, map shows through gaps) and the rest (filled, gaps blend into continuous strip). `AtlasTabBar` + `MiniPlayerBar` dropped their `extendsToScreenEdges` flags — single form everywhere. Bumped build to 1.0 (9); `xcodebuild archive` ran cleanly at `/tmp/Atlas-20260525-1746.xcarchive`; owner running Organizer upload. Verified via `snapshot_ui`: tab buttons at x=8/136.67/265.33 w=128.67 on every surface, matching OLD Home exactly. Owner approved this iteration. New feedback memory `feedback-atlas-module-design.md` captures the single-form rule. Superseded by `-5`. | 2026-05-25 |
| `HANDOFF-260525-3.md` | Third session 2026-05-25: shipped PR #69 (`8d928b3`), fixing two regressions PR #68 introduced on Home. (1) `AtlasTabBar` added the home-indicator safe-area inset to the view's *height* in full-edge mode, which physically pushed buttons + mini-player up by ~34pt. Fixed by making the view a constant 64pt in both modes; only what's painted in the 8pt outer strip changes (transparent on Home, opaque elsewhere). The safe-area zone is already covered by the painted button row because of `.ignoresSafeArea(.bottom)` on the parent ZStack. (2) PreferenceKey-driven `moduleGeometry` got stuck at `.fullEdge` after popping back from a detail. Replaced with `@Observable AtlasNavigationState` tracking `pushedDepth` via push/pop from each pushed view's onAppear/onDisappear — deterministic. `AtlasBottomModule.height` is a constant 126pt across modes. Verified via `snapshot_ui`: NEW Home buttons at y=807.33 (matches OLD Home exactly); NEW Library / Me also at y=807. 84/84 tests pass. Auto-merged on green. | 2026-05-25 |
| `HANDOFF-260525-2.md` | Second session 2026-05-25: shipped PR #68 (`fe11d99`), three connected follow-ups to PR #66 surfaced by the visual review. (1) `SearchBar` switched from `.sheet` to `NavigationLink` so the mini-player + tab bar stay visible while searching. (2) `AtlasTabBar` button row anchored at the same screen-y in both geometries — safe-area inset moved OUT of the painted button area into a separate background rectangle below. (3) Detail screens always render with the full-edge module now via a typed `AtlasModuleGeometry` preference (replaces the deleted `\.atlasIsHomeTab` env value). 84/84 tests pass. Auto-merged on green. **Introduced two regressions on Home that PR #69 (handoff `-3`) fixes**, then PR #70 (handoff `-4`) finished the design refinement. Superseded by `-4`. | 2026-05-25 |
| `HANDOFF-260525.md` | First session 2026-05-25: shipped PR #66 (`2452f52`), extending PR #60's bottom-module geometry to the rest of the app. New `AtlasBottomModule.height(extendsToScreenEdges:)` helper + `\.atlasIsHomeTab` env value + `extendsToScreenEdges` flag on `MiniPlayerBar` / `AtlasTabBar`. Non-Home tabs (Library / Settings / Manage downloads / Tour Detail / Maker) now show the module flush to the screen edges; their scrollable content reserves bottom space via `.safeAreaInset(.bottom)`. TourDetailView's `actionBarHeight` + trailing spacer rebuilt around the helper — also fixes a pre-existing too-small 72pt spacer. Auto-merge policy changed this session (commit `b06284b`): all PRs auto-merge on CI green now, code included. 84/84 tests pass. Superseded by the `-2` handoff above (which replaced the env value with a typed preference and added the cross-tab consistency fixes the visual review caught). | 2026-05-25 |
| `HANDOFF-260524-3.md` | Late-evening session 2026-05-24: shipped PR #60 (home polish bundle + player-state hardening, `e5b31da`). Visual-review pass against the running build surfaced three audio-state bugs the original polish scope didn't cover — added to the same PR: Open-player no longer disabled mid-load, `AudioPlayerService.seek(to:)` synthesizes `.ended` on scrub-to-end (AVPlayer doesn't fire `didPlayToEndTime` on manual seek), full-player `togglePlayPause` now replays on `.ended` via new `replayCurrent()`. 84/84 tests pass. TestFlight 1.0 (7) still queued for owner Archive/Upload. Superseded by the 260525 handoff above. | 2026-05-24 |
| `HANDOFF-260524-2.md` | Afternoon/evening Mac session 2026-05-24: shipped PR #61 (mini-player end-of-tour state fix — kills the post-tour "Loading…" flicker, adds in-place replay via new `AudioPlayerService.replayLast()`). Sibling session pushed `5db7aaa` to `claude/home-visual-polish-bundle` (drawer-stacking + sizing + tours-in-view iteration) — branch is on origin but not PR'd yet at session end. Superseded by the `-3` handoff above. | 2026-05-24 |
| `HANDOFF-260524.md` | Mac session 2026-05-23/24 morning: populated `heroImageURL` for all 38 tours via CC-licensed Wikimedia Commons photos (landscape-optimised batch), bumped build number to 7, opened Xcode for TestFlight upload. Superseded by the `-2` / `-3` handoffs above. | 2026-05-24 |
| `HANDOFF-260522.md` | Mac session 2026-05-21 evening → 2026-05-22: picked up remote PR #54, fixed CI, simulator UX-polish batch, 9 new tours (catalog 20), merged PR #54, uploaded **TestFlight build 1.0 (5)**. Historical. | 2026-05-22 |
| `HANDOFF-260521.md` | Short handoff from a parallel 2026-05-21 remote session, written just after PR #54 merged (build 5 bumped but not yet uploaded). Superseded by the 260522 handoff, which covers the same ground through the build 5 upload. Historical. | 2026-05-21 |
| `HANDOFF-260520.md` | Consolidated end-of-day snapshot for 2026-05-20, covering both that day's sessions: (1) morning remote session — Times Square images, P1/P2/P3 audit cleanup, download retry, Option A data layer (landed on `main` via PR #51); (2) evening Mac session — home-screen UX batch, location-button rework, PlayerView carousel, MakerView fixes, ESB GPS fix, and the **TestFlight build 1.0 (4) upload**. Historical — superseded by the 5/22 handoff. | 2026-05-20 |
| `HANDOFF-260519.md` | Consolidated end-of-day snapshot from 2026-05-19. Covers both that day's sessions: (1) morning remote session — TestFlight prep code work + 8 new real tours + ROADMAP expansions; (2) evening local Mac session — Xcode signing setup + first Archive + Upload to App Store Connect (build 1.0/1 uploaded at 9:38 PM, awaiting Apple processing). Its "Tomorrow's queue" — complete TestFlight setup → install on iPhone → run M-qa checklist — is still the live TestFlight plan; read alongside the 5/20 handoff. The morning-only version of this file is preserved in `git log -- archive/HANDOFF-260519.md`. | 2026-05-19 |
| `HANDOFF-260518.md` | End-of-day snapshot from the 2026-05-18 session. Historical — superseded by the 5/19 handoff. Kept for context on what shipped that day (AllTrails-style home, unit test target wiring, session-start ritual, P0 audit closure). | 2026-05-18 |
| `pre-qa-audit-260518.md` | Pre-QA code self-audit (22 findings, P0–P3). Produced under PR #21; most P0 + 2 P1 findings closed by PRs #22 / #23 / #24. Remaining findings tracked as live checklist in `ROADMAP.md` § M-qa. | 2026-05-18 |

When the next handoff is written, name it for the new date
(`HANDOFF-YYMMDD.md`) and update the entry above to point to the
latest one as the "Active handoff." Older handoffs stay as
historical entries, but only the latest is part of the session-start
ritual.
